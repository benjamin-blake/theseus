# Fast-tier subset for CI pr-validate job (ruff, mypy, pytest affected-set selection
# (Decision 135), prompt validators). Lint/type/test tool FLOORS (ruff, mypy, pytest*) must
# match requirements-dev.in; runtime-tool floors (pydantic, pyyaml, networkx, import-linter,
# mcp) must match requirements.in. This file stays a hand-maintained plain floor list with no
# matching .in input, so dependabot keeps editing it directly instead of recompiling it.
ruff==0.15.20
mypy>=1.5.0
pytest>=9.0.3
pytest-asyncio>=1.4.0
pytest-randomly>=4.1.0
pytest-socket>=0.8.0
pytest-xdist>=3.6.1
pytest-timeout>=2.3.1
pyyaml>=6.0.3
pydantic>=2.0.0
networkx>=3.6.1
import-linter>=2.0
mcp>=1.28.0,<2
