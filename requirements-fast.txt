# Fast-tier subset for CI pr-validate job (ruff, mypy, pytest affected-set selection
# (Decision 135), prompt validators). Lint/type/test tool constraints (ruff, mypy,
# pytest*) must match requirements-dev.txt; runtime-tool constraints (pydantic, pyyaml,
# networkx, import-linter, mcp) must match requirements.txt. Dependabot's pip group updates
# every requirements*.txt file in the same grouped PR, so both pairs stay in lockstep.
ruff==0.15.20
mypy>=1.5.0
pytest>=9.0.3
pytest-asyncio>=1.4.0
pytest-randomly>=4.1.0
pytest-socket>=0.8.1
pytest-xdist>=3.6.1
pytest-timeout>=2.3.1
pyyaml>=6.0.3
pydantic>=2.0.0
networkx>=3.6.1
import-linter>=2.0
mcp>=1.28.0,<2
