# Sanitised CloudTrail events for the platform-security-* metric filters (rec-4044, Decision 202).
#
# Each entry names, in must_match, the EXACT set of filters (keyed by alarm name, as in
# local.platform_security_filter_patterns in terraform/bootstrap/platform_security_alarms.tf) that
# must match its event. Read by tests/checks/iam_tf/test_platform_role_iam_change_alarms.py (shape,
# coverage, no account id) and by the plan's ADMIN logs:TestMetricFilter step (live evaluation of the
# events exactly as committed). The account id is the literal ACCOUNT -- never twelve digits.
events:
  - name: lowercase-platformadmin-update-role
    must_match: [platform-security-platform-role-iam-change, platform-security-iam-event-heartbeat]
    event:
      eventVersion: "1.09"
      userIdentity: {type: AssumedRole, arn: "arn:aws:sts::ACCOUNT:assumed-role/PlatformAdmin/session"}
      eventTime: "2026-09-25T12:00:00Z"
      eventSource: iam.amazonaws.com
      eventName: UpdateRole
      awsRegion: us-east-1
      requestParameters: {roleName: platformadmin, maxSessionDuration: 3600}
      readOnly: false
      eventID: fixture-event-01
      eventType: AwsApiCall
      recipientAccountId: ACCOUNT
  - name: platformdev-tag-role-denied
    must_match:
      - platform-security-platform-role-iam-change
      - platform-security-denied-iam-write
      - platform-security-iam-event-heartbeat
    event:
      eventVersion: "1.09"
      userIdentity: {type: AssumedRole, arn: "arn:aws:sts::ACCOUNT:assumed-role/PlatformDev/session"}
      eventTime: "2026-09-25T12:01:00Z"
      eventSource: iam.amazonaws.com
      eventName: TagRole
      awsRegion: us-east-1
      errorCode: AccessDenied
      errorMessage: "User is not authorized to perform: iam:TagRole"
      requestParameters: {roleName: PlatformDev, tags: [{key: probe, value: "1"}]}
      readOnly: false
      eventID: fixture-event-02
      eventType: AwsApiCall
      recipientAccountId: ACCOUNT
  - name: boundary-policy-create-policy-version
    must_match: [platform-security-platform-role-iam-change, platform-security-iam-event-heartbeat]
    event:
      eventVersion: "1.09"
      userIdentity: {type: AssumedRole, arn: "arn:aws:sts::ACCOUNT:assumed-role/PlatformAdmin/session"}
      eventTime: "2026-09-25T12:02:00Z"
      eventSource: iam.amazonaws.com
      eventName: CreatePolicyVersion
      awsRegion: us-east-1
      requestParameters:
        policyArn: "arn:aws:iam::ACCOUNT:policy/agent-platform-github-ci-apply-boundary"
        policyDocument: "{}"
        setAsDefault: true
      readOnly: false
      eventID: fixture-event-03
      eventType: AwsApiCall
      recipientAccountId: ACCOUNT
  - name: trail-stop-logging
    must_match: [platform-security-detector-tamper]
    event:
      eventVersion: "1.09"
      userIdentity: {type: AssumedRole, arn: "arn:aws:sts::ACCOUNT:assumed-role/PlatformAdmin/session"}
      eventTime: "2026-09-25T12:03:00Z"
      eventSource: cloudtrail.amazonaws.com
      eventName: StopLogging
      awsRegion: eu-west-2
      requestParameters: {name: "arn:aws:cloudtrail:eu-west-2:ACCOUNT:trail/platform-security-trail"}
      readOnly: false
      eventID: fixture-event-04
      eventType: AwsApiCall
      recipientAccountId: ACCOUNT
  - name: log-group-put-metric-filter
    must_match: [platform-security-detector-tamper]
    event:
      eventVersion: "1.09"
      userIdentity: {type: AssumedRole, arn: "arn:aws:sts::ACCOUNT:assumed-role/PlatformAdmin/session"}
      eventTime: "2026-09-25T12:04:00Z"
      eventSource: logs.amazonaws.com
      eventName: PutMetricFilter
      awsRegion: eu-west-2
      requestParameters:
        logGroupName: platform-security-cloudtrail
        filterName: platform-security-iam-event-heartbeat
        filterPattern: "{ $.eventSource = \"iam.amazonaws.com\" }"
      readOnly: false
      eventID: fixture-event-05
      eventType: AwsApiCall
      recipientAccountId: ACCOUNT
  - name: alerts-subscription-unsubscribe
    must_match: [platform-security-alerts-topic-change]
    event:
      eventVersion: "1.09"
      userIdentity: {type: AssumedRole, arn: "arn:aws:sts::ACCOUNT:assumed-role/PlatformDev/session"}
      eventTime: "2026-09-25T12:05:00Z"
      eventSource: sns.amazonaws.com
      eventName: Unsubscribe
      awsRegion: eu-west-2
      requestParameters:
        subscriptionArn: "arn:aws:sns:eu-west-2:ACCOUNT:agent-platform-alerts:fixture-subscription-id"
      readOnly: false
      eventID: fixture-event-06
      eventType: AwsApiCall
      recipientAccountId: ACCOUNT
  - name: iam-get-role-read
    must_match: [platform-security-iam-event-heartbeat]
    event:
      eventVersion: "1.09"
      userIdentity: {type: AssumedRole, arn: "arn:aws:sts::ACCOUNT:assumed-role/agent-platform-github-ci-planner/session"}
      eventTime: "2026-09-25T12:06:00Z"
      eventSource: iam.amazonaws.com
      eventName: GetRole
      awsRegion: us-east-1
      requestParameters: {roleName: PlatformAdmin}
      readOnly: true
      eventID: fixture-event-07
      eventType: AwsApiCall
      recipientAccountId: ACCOUNT
  - name: unrelated-agent-platform-role-update
    must_match: [platform-security-iam-event-heartbeat]
    event:
      eventVersion: "1.09"
      userIdentity: {type: AssumedRole, arn: "arn:aws:sts::ACCOUNT:assumed-role/agent-platform-github-ci-apply/session"}
      eventTime: "2026-09-25T12:07:00Z"
      eventSource: iam.amazonaws.com
      eventName: UpdateRole
      awsRegion: us-east-1
      requestParameters: {roleName: agent-platform-github-ci-branch, maxSessionDuration: 3600}
      readOnly: false
      eventID: fixture-event-08
      eventType: AwsApiCall
      recipientAccountId: ACCOUNT
  - name: disable-alarm-actions-unrelated-first
    must_match: [platform-security-detector-tamper]
    event:
      eventVersion: "1.09"
      userIdentity: {type: AssumedRole, arn: "arn:aws:sts::ACCOUNT:assumed-role/PlatformAdmin/session"}
      eventTime: "2026-09-25T12:08:00Z"
      eventSource: monitoring.amazonaws.com
      eventName: DisableAlarmActions
      awsRegion: eu-west-2
      requestParameters:
        alarmNames: [ducklake-maintenance-circuit-breaker, platform-security-platform-role-iam-change]
      readOnly: false
      eventID: fixture-event-09
      eventType: AwsApiCall
      recipientAccountId: ACCOUNT
  - name: delivery-role-create-log-stream
    must_match: []
    event:
      eventVersion: "1.09"
      userIdentity: {type: AssumedRole, arn: "arn:aws:sts::ACCOUNT:assumed-role/platform-security-cloudtrail-logs/session"}
      eventTime: "2026-09-25T12:09:00Z"
      eventSource: logs.amazonaws.com
      eventName: CreateLogStream
      awsRegion: eu-west-2
      requestParameters:
        logGroupName: platform-security-cloudtrail
        logStreamName: ACCOUNT_CloudTrail_eu-west-2
      readOnly: false
      eventID: fixture-event-10
      eventType: AwsApiCall
      recipientAccountId: ACCOUNT
