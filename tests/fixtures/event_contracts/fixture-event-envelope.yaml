---
# Minimal Class C fixture envelope for tests/test_field_semantics_event_projection.py.
# Not a real ratified contract -- test fixture only (tests/fixtures/event_contracts/, rec-4025 layout).
contract:
  id: fixture-event-envelope
  class: C
  contract_version: 1
  status: provisional_v0
  description: Minimal shared event envelope fixture for the event-projection generator tests.
fields:
  event_id:
    type: str
    iceberg_type: string
    nullable: true
    description: Row identity ULID.
    semantics: Fixture only.
    dq_intent:
      not_null:
        enforced: false
  parser_version:
    type: int
    iceberg_type: bigint
    nullable: true
    description: Monotonic parser version.
    semantics: Fixture only.
    dq_intent:
      not_null:
        enforced: false
  session_started_at:
    type: datetime
    iceberg_type: timestamptz
    nullable: true
    description: Root session start time, the partition basis.
    semantics: Fixture only.
    dq_intent:
      not_null:
        enforced: false
  event_timestamp:
    type: datetime
    iceberg_type: timestamptz
    nullable: true
    description: The originating event's own timestamp.
    semantics: Fixture only.
    dq_intent:
      not_null:
        enforced: false
  created_timestamp:
    type: datetime
    iceberg_type: timestamptz
    nullable: true
    description: Write-time timestamp.
    semantics: Fixture only.
    dq_intent:
      not_null:
        enforced: false
governance_notes: Test fixture; not a ratified Class C contract.
amendment_log: []
previous_versions: []
