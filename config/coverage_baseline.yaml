# Coverage baseline registry (Decision 130-style one-time grandfather roster, Decision 159).
# Each entry pins a repo-relative Python file to the minimum per-file line coverage percentage
# it must measure at or above. A changed file WITHOUT an entry keeps the 100% standard
# (Decision 48 V2 row, unchanged). Values are floored to 1 decimal.
#
# Ratchet UP only: raising an entry (or removing it entirely once it reaches 100%) is always
# unrestricted. Lowering an entry, or registering a NEW sub-100 entry, requires a manual,
# reviewable edit carrying an inline `# baseline-lowered: dec-NNN <reason>` marker naming a real
# `## Decision NNN:` header -- enforced by validate_coverage_baseline_edits in both presubmit
# tiers (scripts/checks/misc/coverage_baseline.py).
#
# This is a ONE-TIME grandfather of pre-existing debt at Decision 159's implement time (measured
# post-exclude_also, post-rec-943-parent-directory-spec) -- not a precedent for future debt.
# Paydown is tracked by rec-2914 (sibling: rec-2902).
entries:
  scripts/checks/ops_governance/validate_outbox_staleness.py: 95.8  # grandfathered dec-159 (one-time roster)
  scripts/checks/ci_guards/validate_actions_evidence.py: 98.6  # grandfathered dec-159 (one-time roster)
  scripts/roadmap/plan_audit.py: 88.1  # grandfathered dec-159 (one-time roster)
  scripts/session/postflight.py: 84.4  # grandfathered dec-159 (one-time roster)
  scripts/validate.py: 73.0  # baseline-lowered: dec-169 Decision 169 (amends Decision 104) deletes the 187-SLOC check-facade import block from scripts/validate.py -- those deleted lines were fully-covered imports, so the remaining file's measured coverage drops below the prior dec-159 grandfather pin.
  # Known-unknowns sweep (grep -rl spec_from_file_location tests/, rec-943 makes these newly
  # measurable): pre-existing debt discovered incidentally, not caused by this plan's diff.
  scripts/prompt_compliance.py: 63.7  # grandfathered dec-159 (one-time roster)
  scripts/session/metrics.py: 69.9  # grandfathered dec-159 (one-time roster)
  scripts/sync/recommendations.py: 78.0  # raised from 61.0 (dec-159 grandfather) -- concern-split recovery, rec-2914 owns residual
  scripts/agent_telemetry_writer.py: 79.3  # grandfathered dec-159 (one-time roster)
  scripts/dependency_graph.py: 79.9  # grandfathered dec-159 (one-time roster)
