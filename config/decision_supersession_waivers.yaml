# Supersession-annotation waiver allowlist (DPI-01, PLAN-decisions-supersession-guard).
#
# scripts/checks/decisions/validate_supersession_annotations.py's forward-pointer sub-check FAILs
# any (superseder, victim) edge extracted from docs/DECISIONS.md / docs/DECISIONS_ARCHIVE.md whose
# victim block carries no annotation pointing back at the superseder -- UNLESS the edge is listed
# here. Waiving an edge is a deliberate operator ruling that the victim's core still stands
# (clause-level amend, partial/scoped supersession, or a citation false-positive), made explicit
# and auditable rather than baked into extractor regex cleverness. A waived edge that later
# becomes annotated is a WARN (stale waiver), not a FAIL -- prune it here when convenient, but it
# never blocks the build.
#
# Every entry MUST name a real (superseder, victim) edge the extractor currently produces; run
# the extractor before editing this file if unsure. Do not add a waiver for a genuinely-new,
# unannotated FULL supersession -- annotate the victim instead (Wave 1 DPI-02 pattern).
waivers:
  - superseder: 148
    victim: 104
    reason: >-
      VF-01 hermetic VP-replay: replay at implement-time, not plan-time; hermetic-default
      restored. Extends Decision 104's _common.py sole-home discipline with five new
      plan-resolution helpers (mirrors the Decision 131/132 precedent for the same victim) --
      does not replace the check-registry decomposition pattern itself.
  - superseder: 145
    victim: 134
    reason: >-
      Arbitrary stopgap raise of the DECISIONS.md live byte ceiling (400,000 -> 500,000); amends
      Decision 134 clause 2 (the byte-ceiling value) only -- Decision 134's structural
      size-governance framework (header count ceiling, combined ceiling, authoring grammar)
      still stands.
  - superseder: 160
    victim: 134
    reason: >-
      Retires the live-byte ceiling (Decision 134 clause 2, as amended by Decision 145) for
      bounded decision-scout retrieval; amends clause 2 only -- Decision 134's surviving
      structural size-governance framework (header count ceiling, combined ceiling, authoring
      grammar, and clause 5's portal end-state) still stands. Same forward-direction convention
      as the 145->134 waiver above: no edit to Decision 134's body text itself.
  - superseder: 160
    victim: 145
    reason: >-
      Retires the constant Decision 145's stopgap raise set (_DECISIONS_LIVE_MAX_BYTES) once its
      own reversal conditions are satisfied in purpose (the decision-scout gate no longer reads
      the live file wholesale); Decision 145's rationale and its "why a raise, not a structural
      fix, at that time" reasoning remain historically accurate and are not edited.
  - superseder: 144
    victim: 98
    reason: >-
      Allow-list inversion (broad-but-bounded deployer under the mandatory permissions boundary
      as the target Terraform deploy/IAM model); amends Decision 98 point 1 only
      (forward-direction amendment, not a full supersession) -- Decision 98's other points stand.
  - superseder: 144
    victim: 92
    reason: >-
      Same Decision 144 forward-direction amendment; amends Decision 92 point 5 only (the
      mandatory-permissions-boundary model), not a full supersession -- Decision 92's other
      points stand.
  - superseder: 143
    victim: 81
    reason: >-
      Privileged-verb Lambda decomposition (scope identities by worst reachable verb; enforce the
      boundary in a primitive outside the agent merge loop); amends Decision 81 clause 1 only --
      Decision 81's core boundary-primitive stands, per Decision 143's own body ("Decision 98
      points 2-4 are unchanged").
  - superseder: 135
    victim: 73
    reason: >-
      Live, cacheless, strictly-additive affected-set selection replacing the --pre edited-set
      tier gate; amends Decision 73 (2nd amendment) only -- Decision 73's core clause
      (validate.py is the single authoritative gate) is unchanged and is the very clause this
      guard's own FULL-tier placement relies on.
  - superseder: 132
    victim: 104
    reason: >-
      Verification graduation as an enforced pre-merge obligation (successor to
      T3.18/VF-05/VF-06); amends Decision 104 by extending the owner-tagged check registry with a
      graduation obligation, it does not replace the registry pattern itself.
  - superseder: 131
    victim: 104
    reason: >-
      Test-colocation mapping inverted to a mirror rule + retiring grandfather-table, plus the
      no-cross-test-import guard and per-package conftest hierarchy; amends Decision 104's test
      colocation clause only -- the check-registry pattern (register/full_sequence/facade
      re-export) this guard itself follows is unchanged.
  - superseder: 126
    victim: 125
    reason: >-
      Two-verb deployment model + heal button + operator-only admin tier; amends Decision 125
      point 5 only (realizes the four DuckLake Lambdas' deploy-verb as the governed code-deploy
      channel) -- Decision 125's other points, including the channel point 2 names, stand.
  - superseder: 104
    victim: 80
    reason: >-
      scripts/validate.py decomposed into an owner-tagged check registry; partially supersedes
      Decision 80 point 3 only (the check-registry decomposition) -- Decision 80's other points
      (e.g. the import-contracts oracle) stand.
  - superseder: 85
    victim: 76
    reason: >-
      Ratify CD.22 (PLAN-*.yaml planning artefacts with the PlanDocument schema); amends Decision
      76 clause 3 only (the planning-artefact format) -- Decision 76's broader
      Claude-Code-on-the-Web workflow migration stands.
  - superseder: 76
    victim: 89
    reason: >-
      Claude-Code-on-the-Web Workflow Migration; amends Decision 89 clause 4 only (squash-merge
      transport moves from `gh pr merge --squash` to the GitHub MCP `merge_pull_request`, policy
      preserved) -- Decision 89's branch-protection core stands.
  - superseder: 54
    victim: 52
    reason: >-
      Lambda Scheduled Agents Revert to Copilot SDK; supersedes Decision 52 for Lambda scope only
      per its own header -- a scoped, archived supersession, not a full one.
  - superseder: 53
    victim: 52
    reason: >-
      Gemini CLI as Executor Inference Provider; partially supersedes Decision 52 per its own
      header -- an archived partial supersession, not a full one.
  - superseder: 128
    victim: 102
    reason: >-
      clause-level SLOC-policy-default amend; Decision 102's waiver-ratchet enforcement
      (validate_sloc_budget_raises) still stands
  - superseder: 85
    victim: 58
    reason: >-
      false positive: the phrase 'Decision 76 supersedes Decision 58' is cited inside Decision
      85's rationale; Decision 85 does not supersede Decision 58, and the real D76->D58 edge is
      separately annotated
  - superseder: 153
    victim: 73
    reason: >-
      Fast-tier budget assertion waives a full-suite-forced --pre run (warn, not hard-fail)
      bounded by a derived forced-run ceiling; amends Decision 73's unconditional "fails non-zero
      on breach" enforcement (point 1 / "Enforced budgets") for exactly the full_suite_forced
      case -- Decision 73's core clause (validate.py is the single authoritative gate; the
      two-tier --pre/default split) is unchanged.
  - superseder: 153
    victim: 135
    reason: >-
      Wires Decision 135/VTS-03's full_suite_forced manifest flag into the fast-tier budget gate
      as a load-bearing input (previously observability-only) and reconciles Decision 135's
      internal KG.13-trigger inconsistency (AND-gate boundary note vs. reversal-condition (b)
      OR-phrasing) in favour of the AND-gate reading -- Decision 135's affected-set-selection
      mechanism itself is unchanged.
  - superseder: 182
    victim: 153
    reason: >-
      Fast-tier budget split (Decision 182): the single 300s aggregate becomes an unwaivable
      240s non-test budget plus a breadth-derived test-execution allowance bounded by the
      existing derived ceiling; amends Decision 153 point 3's "exactly ONE design budget"
      clause only -- Decision 153's forced-full-suite waiver, its derived forced-run ceiling
      and its fail-closed full_suite_forced read all still stand, subordinated to the new
      unwaivable arm rather than replaced.
  - superseder: 179
    victim: 134
    reason: >-
      Retires the decision-corpus stock ceilings (amends Decision 134 clause 2's live-header-count
      guard only) -- Decision 134's other clauses stand: clause 1's prose-monolith authoring
      retention, clause 3's authoring-contract direction, clause 4's ETL-parity direction, and
      clause 5's portal end-state (T1.5) are all untouched.
  - superseder: 179
    victim: 160
    reason: >-
      Amends Decision 160 point 4 (which retained the header and combined ceilings as the
      decision-scout read-cost backstop -- that premise is gone under bounded retrieval) and
      resolves point 9's index-byte-pin reassessment by retiring the pin outright -- Decision
      160's other points stand: point 2's adjudication of the Decision 145/152 reversal triggers,
      point 6's Decision 114 splitting-vs-bounding analogy, point 7's widened SPIRIT gate, and
      points 1/3/5/8/10-14 are all unaffected.
  - superseder: 179
    victim: 166
    reason: >-
      Amends Decision 166 point 9 (the re-derived docs/decisions-index.json byte pin, retired
      outright here) and elects reversal condition (g) ahead of its literal trigger -- Decision
      166's core structural-size-governance engine (the classified non-Python file taxonomy, the
      twelve-class table, the shared raise-marker guard, and every class row other than the
      decisions-index pin) stands unchanged; point 6's own registry-ceiling shape was already
      amended separately by Decision 176.
  - superseder: 180
    victim: 144
    reason: >-
      Metadata-write scope class (rec-3327): amends Decision 144 clause 2 only, adding PlatformDev
      to IAMRoleMetadataWrite's Resource alongside role/agent-platform-* -- clause 3's
      mandatory-boundary framing is retained and still constrains the widened grant. No
      forward-pointer annotation lands on Decision 144's body: this plan's own verification
      (docs/plans/PLAN-ci-convergence-fixes.yaml VP step 8) asserts Decision 144's body stays
      byte-intact per the append-only dialect (Decision 177) -- amendment is recorded here and in
      Decision 180's own body, not by splicing Decision 144's block.
live_archive_pair_allowlist: []
