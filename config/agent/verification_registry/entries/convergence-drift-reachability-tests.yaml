check_id: convergence-drift-reachability-tests
primitive_slot: command_exit_zero
guard_target: scripts/convergence_health/code_drift.py
guard_symbol: _is_fresh
plan_slug: convergence-health-prod-drift-red
graduated_at: '2026-08-18'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'from scripts.convergence_health.code_drift import _is_fresh; SRC = ''d024e63f''; DISPATCH = ''149c36b''; reach = lambda argv: SRC if argv[4] == DISPATCH else ''''; assert _is_fresh({''source_git_sha'':
    DISPATCH}, SRC, reach, (''x'',)) is True, ''incident case: a dispatch deploy postdating the latest source commit must read FRESH''; assert _is_fresh({''source_git_sha'': None}, SRC, reach, (''x'',))
    is False, ''null sha fails closed''; assert _is_fresh({''source_git_sha'': ''deadbeef''}, SRC, lambda a: '''', (''x'',)) is False, ''unresolvable sha fails closed''; assert _is_fresh(None, SRC, reach,
    (''x'',)) is False, ''absent record fails closed''; assert _is_fresh({''source_git_sha'': DISPATCH}, '''', reach, (''x'',)) is False, ''empty latest_sha fails closed''; print(''REACHABILITY_ORACLE ok'')'
