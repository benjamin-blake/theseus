check_id: exit-criteria-ledger-is-class-a
primitive_slot: command_exit_zero
guard_target: docs/contracts/exit-criteria-ledger.yaml
guard_symbol: null
plan_slug: cfg-migration-closeout
graduated_at: '2026-09-01'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - "import pathlib; from scripts.contracts import load_contract, resolve_refs; from scripts.contracts_enforcement import check_required_inline_fields; p=pathlib.Path('docs/contracts/exit-criteria-ledger.yaml'); d=load_contract(p); resolve_refs(d, p.parent); e=check_required_inline_fields(d); assert d.contract.class_.value=='A', d.contract.class_; assert not e, e; assert d.contract.ratified_via and 'dec-136' in d.contract.ratified_via, d.contract.ratified_via; print('CLASS A OK', d.contract.id, sorted(d.fields))"
