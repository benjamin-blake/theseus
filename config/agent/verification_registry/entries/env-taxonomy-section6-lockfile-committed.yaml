check_id: env-taxonomy-section6-lockfile-committed
primitive_slot: command_exit_zero
guard_target: docs/contracts/environment-taxonomy.yaml
guard_symbol: null
plan_slug: terraform-t2-42-path-hardening
graduated_at: '2026-07-21'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'import yaml

    d = yaml.safe_load(open("docs/contracts/environment-taxonomy.yaml", encoding="utf-8"))

    sec = str(d.get("lockfile", ""))

    assert "committed" in sec.lower(), "lockfile section missing ''committed''"

    assert ".terraform.lock.hcl" in sec, "lockfile section missing ''.terraform.lock.hcl''"

    assert "RECOMMENDED (non-blocking, follow-on): un-ignore and commit" not in sec

    '
