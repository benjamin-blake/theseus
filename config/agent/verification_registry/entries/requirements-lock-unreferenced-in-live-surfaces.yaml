check_id: requirements-lock-unreferenced-in-live-surfaces
primitive_slot: command_exit_zero
guard_target: requirements.txt
guard_symbol: null
plan_slug: dependabot-automation-root-fix
graduated_at: '2026-09-13'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - set -u; paths=''; for p in bin scripts src tests config .github docs/contracts
    docs/ROADMAP-PLATFORM.yaml requirements.in requirements-dev.in requirements.txt
    requirements-dev.txt requirements-fast.txt .pre-commit-config.yaml pyproject.toml;
    do [ -e "$p" ] && paths="$paths $p"; done; ! grep -rIn "requirements\.lock" $paths
