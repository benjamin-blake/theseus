check_id: registry-other-lambda-writes-asserted
primitive_slot: command_exit_zero
guard_target: config/agent/verification_registry/entries/
guard_symbol: null
plan_slug: terraform-decompose-oidc-rename
graduated_at: '2026-08-14'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'import sys

    from scripts.verification_graduation import load_entries

    entries=load_entries()

    cmds=[(e[''check_id''],'' ''.join(map(str,(e.get(''check_spec'') or {}).get(''command'') or []))) for e in entries]

    bad=[cid for cid,c in cmds if ''other_lambda_writes'' in c and ''$other'' not in '';''.join(p for p in c.split('';'') if ''echo'' not in p)]

    print(''computed_but_ungated='',bad)

    sys.exit(1 if bad else 0)

    '
