check_id: materialise-tfvars-single-implementation
primitive_slot: command_exit_zero
guard_target: .github/actions/materialise-tfvars/materialise_tfvars.sh
guard_symbol: null
plan_slug: materialise-tfvars-delegate-consolidation
graduated_at: '2026-09-21'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - bin/venv-python -c "import subprocess; out=sorted(subprocess.run(['grep','-rl','secretsmanager
    get-secret-value','.github/'],capture_output=True,text=True).stdout.split());
    print('CARRIERS', len(out), out); assert out==['.github/actions/materialise-tfvars/materialise_tfvars.sh'],
    out"
