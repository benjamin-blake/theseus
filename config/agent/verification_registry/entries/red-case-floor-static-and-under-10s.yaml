check_id: red-case-floor-static-and-under-10s
primitive_slot: command_exit_zero
guard_target: scripts/checks/verification/validate_red_case_floor.py
plan_slug: red-case-floor
graduated_at: '2026-09-11'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - "\nimport ast, pathlib, time\nfrom scripts.checks.verification import validate_red_case_floor\
    \ as f\nsrc = pathlib.Path('scripts/checks/verification/validate_red_case_floor.py').read_text()\n\
    banned = {'subprocess', 'requests', 'urllib', 'pytest', 'boto3', 'socket'}\nmods\
    \ = set()\nfor n in ast.walk(ast.parse(src)):\n    if isinstance(n, ast.Import):\n\
    \        mods.update(a.name.split('.')[0] for a in n.names)\n    elif isinstance(n,\
    \ ast.ImportFrom) and n.module:\n        mods.add(n.module.split('.')[0])\nassert\
    \ not (mods & banned), ('the floor must stay static -- banned imports', sorted(mods\
    \ & banned))\nfailed: list[str] = []\nt0 = time.perf_counter()\nf.validate_red_case_floor(failed)\n\
    elapsed = time.perf_counter() - t0\nassert elapsed < 10.0, ('the floor must be\
    \ cheap; took', elapsed)\nprint('FLOOR_IS_CHEAP_AND_STATIC', round(elapsed, 3))\n"
