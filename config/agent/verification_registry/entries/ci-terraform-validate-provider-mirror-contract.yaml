check_id: ci-terraform-validate-provider-mirror-contract
primitive_slot: command_exit_zero
guard_target: .github/workflows/ci.yml
guard_symbol: null
plan_slug: ci-provider-mirror-terraform-init-hardening
graduated_at: '2026-07-26'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - 'grep -q ''id: mirror_gate'' .github/workflows/ci.yml && grep -q ''Provider-mirror fail-closed consistency invariant'' .github/workflows/ci.yml && grep -q "github.event_name == ''pull_request'' && ''agent-platform-github-ci-pr''"
    .github/workflows/ci.yml && grep -q ''terraform-provider-neon_${NEON_VERSION}_'' .github/workflows/ci.yml

    '
