check_id: roadmap-regrowth-growth-line-report-only
primitive_slot: command_output_matches
guard_target: scripts/checks/roadmap/check_roadmap_regrowth.py
guard_symbol: check_roadmap_regrowth
plan_slug: roadmap-touched-item-spans-and-regrowth-guard
graduated_at: '2026-09-05'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import re; from scripts.checks import registry; from scripts.checks.roadmap.check_roadmap_regrowth import check_roadmap_regrowth;
    f=[]; check_roadmap_regrowth(f); d=registry.pop_declaration(); assert f == [], f; assert d is not None and d.kind == 'examined'
    and d.unit == 'terminal_records'; print('REGROWTH_REPORT_ONLY')
  expected: GROWTH lines=[0-9]+ baseline=[0-9]+ growth=-?[0-9]+ headroom=-?[0-9]+ escalate=(true|false) threshold=[0-9]+
  use_regex: true
