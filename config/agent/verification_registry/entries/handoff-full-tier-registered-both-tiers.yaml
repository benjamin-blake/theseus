check_id: handoff-full-tier-registered-both-tiers
primitive_slot: command_exit_zero
guard_target: scripts/checks/registry.py
guard_symbol: null
plan_slug: coverage-gate-handoff-integrity
graduated_at: '2026-08-03'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import sys;sys.path.insert(0,'.');from scripts.checks import registry;n='validate_handoff_full_tier';pre={s.name for s in registry.pre_sequence() if s.kind=='check'};full={s.name for s in registry.full_sequence()
    if s.kind=='check'};resolved=bool(registry.resolve(n)) if n in registry._ALL_ENTRIES else False;print('pre:',n in pre,'full:',n in full,'resolves:',resolved);sys.exit(0 if (n in pre and n in full and
    resolved) else 1)
