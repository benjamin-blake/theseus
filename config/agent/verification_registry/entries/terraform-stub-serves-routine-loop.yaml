check_id: terraform-stub-serves-routine-loop
primitive_slot: command_exit_zero
guard_target: terraform/CLAUDE.md
guard_symbol: null
plan_slug: ambient-prose-contract-relocation
graduated_at: '2026-09-01'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - grep -q "^## Hard rules" terraform/CLAUDE.md && grep -q "^## AWS context" terraform/CLAUDE.md
    && grep -q "^## Lambda interaction" terraform/CLAUDE.md && grep -q "Agents never
    run terraform apply" terraform/CLAUDE.md && grep -q "tf-gated-apply" terraform/CLAUDE.md
    && grep -q "terraform-apply-sandbox" terraform/CLAUDE.md && grep -q "docs/contracts/terraform-cc-web-operations.yaml"
    terraform/CLAUDE.md && grep -qiE "read .{0,60}terraform-cc-web-operations" terraform/CLAUDE.md
