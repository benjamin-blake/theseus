check_id: ci-rca-generator-consumes-validation-result
primitive_slot: command_exit_zero
guard_target: .github/workflows/ci-rca.yml
guard_symbol: null
plan_slug: ci-rca-evidence-fidelity
graduated_at: '2026-08-04'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - bin/venv-python -c "import yaml; d=yaml.safe_load(open('.github/workflows/ci-rca.yml')); steps=d['jobs']['rca']['steps']; gen=next(s for s in steps if s.get('name')=='Generate evidence bundle')['run'];
    dl=next(s for s in steps if 'validation-result' in str(s.get('run','')))['run']; assert '--validation-result' in gen, gen; assert '/tmp/ci-rca-validation-result' in gen and '/tmp/ci-rca-validation-result'
    in dl"
