check_id: cc-web-signing-hook-registered
primitive_slot: command_exit_zero
guard_target: .claude/settings.json
guard_symbol: null
plan_slug: commit-signing-false-positive
graduated_at: '2026-08-03'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - bin/venv-python -c "import json,sys;d=json.load(open('.claude/settings.json'));g=[k.get('command','') for b in d['hooks']['SessionStart'] for k in b['hooks']];sys.exit(0 if any('session_start_commit_signing'
    in c for c in g) else 1)" && echo REGISTERED
