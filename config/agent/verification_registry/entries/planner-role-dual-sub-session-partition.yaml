check_id: planner-role-dual-sub-session-partition
primitive_slot: command_exit_zero
guard_target: terraform/personal/oidc.tf
guard_symbol: github_ci_planner
plan_slug: ci-deploy-role-consolidation
graduated_at: '2026-07-23'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - 'f="terraform/personal/oidc*.tf"; def=$(cat $f | grep -c ''resource "aws_iam_role" "github_ci_planner"''); prsub=$(cat $f | grep -c ''pull_request''); mainsub=$(cat $f | grep -c ''refs/heads/main'');
    sess=$(cat $f | grep -c ''sts:RoleSessionName''); echo "planner=$def pr_sub=$prsub main_sub=$mainsub session_cond=$sess"; [ "$def" -ge 1 ] && [ "$sess" -ge 2 ] && [ "$prsub" -ge 1 ] && [ "$mainsub"
    -ge 1 ]

    '
