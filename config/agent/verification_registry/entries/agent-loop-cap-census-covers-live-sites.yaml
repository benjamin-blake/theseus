check_id: agent-loop-cap-census-covers-live-sites
primitive_slot: command_exit_zero
guard_target: scripts/checks/ci_guards/_agent_loop_caps.py
guard_symbol: scan_cap_literals
plan_slug: declared-caps
graduated_at: '2026-09-11'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - from scripts.checks import registry; from scripts.checks._common import ROOT;
    from scripts.checks.ci_guards._agent_loop_caps import scan_cap_literals; f=[];
    [registry.resolve(n)(f) for n in ('validate_ci_rca_taxonomy','validate_ci_rca_adjudication','validate_composite_action_shape_rosters','validate_ci_rca_lifecycle_projection','validate_check_accounting')];
    d,e=scan_cap_literals(ROOT); assert not f, f; assert not e, e; assert d.get('.github/workflows/ci-rca.yml',{}).get('max_turns')==30,
    d; assert d.get('.github/actions/subagent-plan-review/review.sh',{}).get('max_turns')==5,
    d; print('CENSUS_OK', len(d), sorted(d))
