check_id: decisions-index-pin-112000-fresh-and-under
primitive_slot: command_exit_zero
guard_target: docs/decisions-index.json
guard_symbol: null
plan_slug: migration-step-5-index-skeleton
graduated_at: '2026-08-18'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import json,pathlib,re; from scripts.decisions_index import build_index; pin=int(re.search(r'_COMMITTED_INDEX_MAX_BYTES = (\S+)', pathlib.Path('tests/test_decisions_index.py').read_text()).group(1).replace('_',''));
    assert pin==112000, pin; p=pathlib.Path('docs/decisions-index.json'); n=len(p.read_bytes()); assert n<=pin, (n,pin); assert json.loads(p.read_text())==build_index(), 'committed index is stale'; print('PIN
    ok', n, 'of', pin)
