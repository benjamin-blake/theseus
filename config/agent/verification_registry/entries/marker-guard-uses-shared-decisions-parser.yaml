check_id: marker-guard-uses-shared-decisions-parser
primitive_slot: command_exit_zero
guard_target: scripts/checks/_marker_guard.py
guard_symbol: load_decision_bodies
plan_slug: size-gov-marker-guard
graduated_at: '2026-08-04'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - grep -q "from scripts.decisions_md import" scripts/checks/_marker_guard.py && ! grep -q "## Decision" scripts/checks/_marker_guard.py && grep -q "DECISIONS_ARCHIVE" scripts/checks/_marker_guard.py &&
    echo "shared parser OK"
