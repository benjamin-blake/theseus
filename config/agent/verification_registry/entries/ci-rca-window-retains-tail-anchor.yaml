check_id: ci-rca-window-retains-tail-anchor
primitive_slot: command_exit_zero
guard_target: scripts/ci_rca/log_evidence.py
guard_symbol: null
plan_slug: ci-rca-evidence-fidelity
graduated_at: '2026-08-04'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - bin/venv-python -c "from tests.fixtures.ci_rca.oversized_validate_log import build_log; from scripts.ci_rca.log_evidence import bound_text_windowed, DEFAULT_MAX_BYTES, DEFAULT_MAX_LINES; body,lim=bound_text_windowed(build_log(),
    DEFAULT_MAX_BYTES, DEFAULT_MAX_LINES); assert 'Failed checks:' in body and 'Coverage below 100%' in body; assert lim['included_bytes'] <= DEFAULT_MAX_BYTES and lim['included_lines'] <= DEFAULT_MAX_LINES;
    assert lim['truncation_reason'] == 'head_tail_window'; assert lim['observed_bytes'] == lim['included_bytes'] + lim['omitted_bytes']"
