check_id: decomposed-sources-route-to-test-packages
primitive_slot: command_exit_zero
guard_target: scripts/test_coverage_checker.py
guard_symbol: _CONCERN_SPLIT_TEST_PACKAGES
plan_slug: verification-registry-sharding
graduated_at: '2026-08-24'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - from pathlib import Path; from scripts.test_coverage_checker import map_source_to_test; from scripts.checks.misc.validate_test_coverage import validate_test_coverage; srcs=['scripts/verification_graduation.py','scripts/checks/verification/validate_graduation_completeness.py'];
    [print(' ',s,'->',map_source_to_test(Path(s))) for s in srcs]; assert all(map_source_to_test(Path(s)).is_dir() for s in srcs), 'still routing to a file'; f=[]; validate_test_coverage(f); assert not
    f, f; print('both sources route to packages; coverage gate green')
