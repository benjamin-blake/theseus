check_id: differential-canary-globs-graduation-module
primitive_slot: command_exit_zero
guard_target: scripts/checks/verification/_manifest.py
guard_symbol: null
plan_slug: verification-graduation-gate-fix
graduated_at: '2026-08-31'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - from scripts.checks.verification._manifest import ENTRIES; e=[x for x in ENTRIES if x.name=='validate_differential_gate_baseline'][0];
    assert any('verification_graduation' in g for g in e.pre_globs), e.pre_globs; print('OK')
