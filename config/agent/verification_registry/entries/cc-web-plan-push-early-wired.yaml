check_id: cc-web-plan-push-early-wired
primitive_slot: command_exit_zero
guard_target: .claude/commands/plan.md
guard_symbol: null
plan_slug: commit-signing-false-positive
graduated_at: '2026-08-03'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - grep -q -- "git push --force-with-lease" .claude/commands/plan.md && grep -q "git push -u origin HEAD" .claude/commands/plan.md && test "$(grep -c "git push" .claude/commands/plan.md)" -ge 3 && echo
    PUSH_EARLY_WIRED
