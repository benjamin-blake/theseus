check_id: roster-twelve-to-ten
primitive_slot: command_exit_zero
guard_target: config/coverage_baseline.yaml
plan_slug: coverage-paydown-ops-writer-sync-ops
graduated_at: '2026-07-31'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'import pathlib, yaml; e=yaml.safe_load(open(''config/coverage_baseline.yaml''))[''entries'']; stale=[p for p in e if not pathlib.Path(p).exists()]; assert not stale, f''roster pins paths that no
    longer exist: {stale}''; assert ''scripts/sync/ops.py'' not in e, ''sync/ops still rostered''; r=e.get(''scripts/sync/recommendations.py''); assert r is not None and r>=78.0, f''sync/recommendations
    not raised: {r}''; print(f''roster now {len(e)} entries; sync/recommendations raised to {r}'')'
