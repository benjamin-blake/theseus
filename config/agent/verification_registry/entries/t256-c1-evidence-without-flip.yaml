check_id: t256-c1-evidence-without-flip
primitive_slot: command_exit_zero
guard_target: docs/ROADMAP-PLATFORM.yaml
guard_symbol: T2.56.c1
plan_slug: ambient-prose-contract-relocation
graduated_at: '2026-09-01'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - bin/venv-python -c "import yaml; d=yaml.safe_load(open('docs/ROADMAP-PLATFORM.yaml'));
    i=[x for x in d['tier_items'] if x['id']=='T2.56'][0]; c1=[c for c in i['exit_criteria']
    if c['id']=='c1'][0]; n=i.get('progress_note') or ''; print(c1['status'], i['status'],
    'rec-3378' in n); raise SystemExit(0 if c1['status']=='open' and i['status']=='in_progress'
    and 'rec-3378' in n else 1)"
