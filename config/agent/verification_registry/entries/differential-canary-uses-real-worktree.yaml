check_id: differential-canary-uses-real-worktree
primitive_slot: command_exit_zero
guard_target: scripts/checks/verification/validate_differential_gate_baseline.py
guard_symbol: validate_differential_gate_baseline
plan_slug: verification-graduation-gate-fix
graduated_at: '2026-08-31'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import pathlib; s=pathlib.Path('scripts/checks/verification/validate_differential_gate_baseline.py').read_text(encoding='utf-8');
    assert 'simulated pre-change' not in s, 'hardcoded FAIL revert_runner still present'; assert 'run_differential' in s,
    'canary does not call run_differential'; print('OK')
