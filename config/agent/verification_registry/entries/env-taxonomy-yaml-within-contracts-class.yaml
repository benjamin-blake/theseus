check_id: env-taxonomy-yaml-within-contracts-class
primitive_slot: command_exit_zero
guard_target: docs/contracts/environment-taxonomy.yaml
guard_symbol: null
plan_slug: cfg-11-environment-taxonomy-conversion
graduated_at: '2026-08-13'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'from scripts.checks.structural import _classify

    import pathlib

    p=''docs/contracts/environment-taxonomy.yaml''

    t=pathlib.Path(p).read_text(encoding=''utf-8'')

    e=_classify.effective_lines(t)

    L=_classify.longest_line(t)

    c=_classify.classify_path(p, text=t)

    reg=pathlib.Path(''config/structural_size_budgets.yaml'').read_text(encoding=''utf-8'')

    print(c, e, L)

    assert c == ''contracts'', c

    assert e <= 500, e

    assert L <= 2000, L

    assert p not in reg, ''a budgets entry was added -- use the defer-detail relief valve instead''

    '
