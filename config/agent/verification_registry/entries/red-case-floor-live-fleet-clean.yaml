check_id: red-case-floor-live-fleet-clean
primitive_slot: command_exit_zero
guard_target: scripts/checks/verification/validate_red_case_floor.py
plan_slug: red-case-floor
graduated_at: '2026-09-11'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - "\nfrom scripts.checks import registry\nfrom scripts.checks.verification import\
    \ validate_red_case_floor as f\nfailed: list[str] = []\nwith registry.outcome_scope('validate_red_case_floor'):\n\
    \    f.validate_red_case_floor(failed)\n    d = registry.pop_declaration()\nassert\
    \ failed == [], ('live fleet must be clean on day one, no grandfather hook; got',\
    \ failed)\nassert d is not None and d.kind == 'examined', ('Decision 170: must\
    \ declare examined, got', d)\nassert d.unit == 'registered_checks', ('wrong unit',\
    \ d.unit)\nassert d.count == len(registry._ALL_ENTRIES), ('count must equal the\
    \ LIVE registry', d.count)\nassert d.count >= 123, ('an empty or truncated population\
    \ must red, never all-clear', d.count)\nassert registry.derive_status(d, bool(failed))\
    \ == 'enforced'\nprint('LIVE_FLEET_CLEAN', d.count)\n"
