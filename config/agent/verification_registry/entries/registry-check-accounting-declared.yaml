check_id: registry-check-accounting-declared
primitive_slot: command_exit_zero
guard_target: scripts/checks/verification/validate_verification_registry.py
plan_slug: verification-registry-sharding
graduated_at: '2026-08-24'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import pathlib; from scripts.checks.hygiene.validate_check_accounting import validate_check_accounting; src=pathlib.Path('scripts/checks/verification/validate_verification_registry.py').read_text();
    assert 'examined(' in src or 'skipped(' in src, 'no declaration'; base=pathlib.Path('config/check_accounting_baseline.yaml').read_text(); assert 'validate_verification_registry' not in base, 'still
    baselined'; f=[]; validate_check_accounting(f); assert not f, f; print('declaration adopted; baseline entry removed')
