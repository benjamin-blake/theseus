check_id: drift-pending-gated-narrative
primitive_slot: command_exit_zero
guard_target: .github/workflows/terraform-drift.yml
guard_symbol: null
plan_slug: terraform-recovery-semantics
graduated_at: '2026-07-22'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - 'f=.github/workflows/terraform-drift.yml; pg=$(grep -c ''pending_gated'' "$f"); mark=$(grep -c ''PENDING_GATED_DRIFT'' "$f"); tok=$(grep -c ''GH_TOKEN'' "$f"); echo "pending_gated=$pg marker=$mark gh_token=$tok";
    [ "$pg" -ge 1 ] && [ "$mark" -ge 1 ] && [ "$tok" -ge 1 ]

    '
