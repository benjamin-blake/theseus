check_id: reconcile-pending-gate-registered-both-tiers
primitive_slot: command_exit_zero
guard_target: scripts/checks/registry.py
guard_symbol: null
plan_slug: ducklake-applied-schema-gate
graduated_at: '2026-07-25'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'from scripts.checks.registry import pre_sequence, full_sequence, resolve

    assert resolve("validate_reconcile_pending_gate")

    assert any(s.name == "validate_reconcile_pending_gate" for s in pre_sequence())

    assert any(s.name == "validate_reconcile_pending_gate" for s in full_sequence())

    '
