check_id: agents-md-retains-enforced-norms-and-pointer
primitive_slot: command_exit_zero
guard_target: AGENTS.md
guard_symbol: null
plan_slug: ambient-prose-contract-relocation
graduated_at: '2026-09-01'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - bin/venv-python -c "from scripts.checks.prose.prose_limits import validate_prose_limits
    as v; f=[]; v(f); print(f); raise SystemExit(1 if f else 0)" && grep -q 'never
    edit or commit on `main`' AGENTS.md && grep -q 'claude/' AGENTS.md && grep -q
    'ci-rca' AGENTS.md && grep -q 'docs/contracts/git-ops.yaml' AGENTS.md && grep
    -q 'docs/contracts/ci-rca-lifecycle.yaml' AGENTS.md && grep -q 'docs/contracts/decision-entry.yaml'
    AGENTS.md && grep -q -- '--pre' AGENTS.md && grep -qi 'squash' AGENTS.md && grep
    -q 'subscribe_pr_activity' AGENTS.md && grep -q 'Resolves:' AGENTS.md && grep
    -q 'docs/contracts/data-modeling-standard.yaml' AGENTS.md && grep -qi 'grain'
    AGENTS.md && grep -qiE 'read .{0,60}git-ops' AGENTS.md
