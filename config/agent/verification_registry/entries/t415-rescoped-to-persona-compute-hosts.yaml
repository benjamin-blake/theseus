check_id: t415-rescoped-to-persona-compute-hosts
primitive_slot: command_exit_zero
guard_target: docs/ROADMAP-PLATFORM.yaml:T4.15
guard_symbol: null
plan_slug: step-functions-executor-host
graduated_at: '2026-09-07'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import yaml,re; d=yaml.safe_load(open('docs/ROADMAP-PLATFORM.yaml')); ti={t['id']:t for t in d['tier_items']}; t=ti['T4.15'];
    it=t['intent']; hits=[m.start() for m in re.finditer('hosted_cli_runner', it)]; assert hits and all(re.search(r'eliminat|removed',
    it[max(0,h-240):h+240]) for h in hits), 'T4.15 still lists hosted_cli_runner as an arm'; assert 'T4.19' in t['depends_on']
    and 'T4.20' in t['depends_on'], t['depends_on']; ec={c['id']:c['text'] for c in t['exit_criteria']}; assert 'persona compute'
    in ec['c10'] and 'verdict plane' in ec['c10'] and 'reselling' in ec['c10'], ec['c10']; assert 'DISTINCT WORKFLOW RUNS'
    in ec['c9'], 'c9 formulation lost'; assert 'eliminated' in ec['c1'], 'c1 still reads the five-member set'; assert 'mocked
    personas' in it.lower() and 'arm-zero' in it.lower(), 'arm-zero on the oracle not stated'; print('OK')
