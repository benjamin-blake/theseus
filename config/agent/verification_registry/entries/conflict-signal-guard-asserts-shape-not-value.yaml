check_id: conflict-signal-guard-asserts-shape-not-value
primitive_slot: command_exit_zero
guard_target: scripts/checks/ci_guards/validate_pr_conflict_signal.py
guard_symbol: validate_pr_conflict_signal
plan_slug: conflict-wake-prefix-realignment
graduated_at: '2026-09-15'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'from pathlib import Path

    import scripts.checks.ci_guards.validate_pr_conflict_signal as m

    failed = []

    m.validate_pr_conflict_signal(failed)

    assert not failed, failed

    src = Path(m.__file__).read_text(encoding=''utf-8'')

    assert ''agent/'' not in src and (''claude/'' not in src or src.count(''claude/'')
    == src.count(''.claude/'')),     ''guard hardcodes a prefix literal''

    print(''GUARD_VALUE_AGNOSTIC_OK'')

    '
