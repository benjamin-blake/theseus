check_id: vp-red-before-decision-amends-148-live
primitive_slot: command_exit_zero
guard_target: docs/DECISIONS.md
guard_symbol: null
plan_slug: vp-red-before-gate
graduated_at: '2026-09-13'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import re; from pathlib import Path; from scripts.checks.decisions.validate_decision_entry_conformance import validate_decision_entry_conformance as f; failed=[]; f(failed); assert not failed, failed; t=Path('docs/DECISIONS.md').read_text(); hs=[m.start() for m in re.finditer(r'^## Decision \d+:', t, re.M)] + [len(t)]; blks=[t[hs[i]:hs[i+1]] for i in range(len(hs)-1)]; hit=[b for b in blks if re.search(r'amends:\s*\[148\]', b)]; assert len(hit)==1, len(hit); assert 'significance:' in hit[0], 'no significance envelope in the amending entry'; print('DECISION_AMENDS_148_CONFORMS')
