check_id: no-retired-ci-role-overview-comments
primitive_slot: command_exit_zero
guard_target: terraform/personal/oidc.tf
guard_symbol: null
plan_slug: terraform-decompose-oidc-rename
graduated_at: '2026-08-14'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - 'n=$(grep -REn "^[[:space:]]*#.*github_ci_(plan|drift)\b" terraform/personal/ terraform/bootstrap/ | grep -vE "[Ff]rom github_ci_(plan|drift)" | grep -vE "github_ci_plan \(PR-sub" | wc -l | tr -d "
    "); echo "stale_retired_role_comments=$n"; [ "$n" -eq 0 ]

    '
