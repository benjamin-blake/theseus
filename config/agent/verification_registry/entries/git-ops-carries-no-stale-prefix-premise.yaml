check_id: git-ops-carries-no-stale-prefix-premise
primitive_slot: command_exit_zero
guard_target: docs/contracts/git-ops.yaml
guard_symbol: null
plan_slug: conflict-wake-prefix-realignment
graduated_at: '2026-09-15'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - "import yaml\nfrom pathlib import Path\nd = yaml.safe_load(Path('docs/contracts/git-ops.yaml').read_text(encoding='utf-8'))\
    \ or {}\nbt = d.get('branching_topology') or {}\nassert bt.get('agent_branch_prefixes'),\
    \ 'branching_topology.agent_branch_prefixes not declared'\n\n\ndef walk(n, p):\n\
    \    if isinstance(n, dict):\n        return [x for k, v in n.items() for x in\
    \ walk(v, p + [str(k)])]\n    if isinstance(n, list):\n        return [x for v\
    \ in n for x in walk(v, p)]\n    if isinstance(n, str):\n        return [(p, n)]\n\
    \    return []\n\n\nbad = [\n    (p, s)\n    for p, s in walk(d, [])\n    if 'claude/'\
    \ in s.replace('.claude/', '') and not any(k.startswith('agent_branch_prefixes')\
    \ for k in p)\n]\nassert not bad, bad\nprint('GIT_OPS_PREFIX_STRUCTURAL_OK')\n"
