check_id: maintenance-policy-matrix-reaches-lambda-asset
primitive_slot: command_exit_zero
guard_target: config/lambda/ducklake/field_semantics.yaml
plan_slug: compaction-scope-policy-matrix
graduated_at: '2026-09-14'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - bin/venv-python -m scripts.schema_to_field_semantics --check && bin/venv-python
    -c "import yaml,io; d=yaml.safe_load(io.open('config/lambda/ducklake/field_semantics.yaml',encoding='utf-8'));
    assert 'maintenance_policy' in d, 'matrix absent from the bundled asset'; print('matrix
    present')" && bin/venv-python -m pytest tests/test_schema_to_field_semantics.py::test_maintenance_policy_passthrough
    -q
