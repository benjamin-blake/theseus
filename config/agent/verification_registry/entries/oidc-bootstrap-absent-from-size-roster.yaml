check_id: oidc-bootstrap-absent-from-size-roster
primitive_slot: command_exit_zero
guard_target: config/structural_size_budgets.yaml
guard_symbol: null
plan_slug: terraform-decompose-oidc-rename
graduated_at: '2026-08-14'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'import sys,yaml; d=yaml.safe_load(open(''config/structural_size_budgets.yaml'')); b=d.get(''budgets'',{}); tf=[k for k in b if str(k).endswith(''.tf'')]; gone=[k for k in (''terraform/personal/oidc.tf'',''terraform/bootstrap/github_ci_apply.tf'')
    if k in b]; print(''remaining_tf_keys='',sorted(tf)); print(''should_be_absent_but_present='',gone); sys.exit(1 if gone or len(tf)!=3 else 0)

    '
