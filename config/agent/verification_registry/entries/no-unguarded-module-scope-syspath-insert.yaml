check_id: no-unguarded-module-scope-syspath-insert
primitive_slot: command_exit_zero
guard_target: tests/
guard_symbol: null
plan_slug: syspath-hygiene-advisory-read-tolerance
graduated_at: '2026-07-27'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - out=$(grep -rEn "^sys\.path\.(insert|append|extend)" tests/ scripts/ src/ --include=*.py); rc=$?; test "$rc" -eq 1 && test -z "$out"
