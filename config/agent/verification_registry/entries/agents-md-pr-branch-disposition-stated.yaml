check_id: agents-md-pr-branch-disposition-stated
primitive_slot: command_exit_zero
guard_target: AGENTS.md
guard_symbol: null
plan_slug: pr-ci-red-ownership
graduated_at: '2026-08-31'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - grep -q "PR-branch" AGENTS.md && grep -q "no rec is filed" AGENTS.md && grep -q "trigger_scope" AGENTS.md
