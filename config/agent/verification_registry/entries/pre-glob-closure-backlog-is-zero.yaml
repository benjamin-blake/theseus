check_id: pre-glob-closure-backlog-is-zero
primitive_slot: command_exit_zero
guard_target: scripts/checks/deps/validate_pre_glob_closure.py
guard_symbol: null
plan_slug: loop-spec-audit-tail
graduated_at: '2026-09-13'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'import ast,collections; import scripts.checks.deps.validate_pre_glob_closure as m; from scripts.checks import _common;
    root=_common.ROOT; v=m._closure_view(root); per=collections.Counter(); [per.update({e.module.split(''.'')[2]: len(m._unmatched_paths(e,v,root))})
    for e in m._gated_entries()]; print(''per-domain residual:'', dict(per)); assert not any(per.values()), dict(per); f=[];
    m.validate_pre_glob_closure(f); assert not f, f; print(''BACKLOG ZERO per domain, gate green'')'
