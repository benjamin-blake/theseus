check_id: coverage-verifier-planner-repointed
primitive_slot: command_exit_zero
guard_target: scripts/checks/iam_tf/_read_coverage.py
guard_symbol: null
plan_slug: ci-deploy-role-consolidation
graduated_at: '2026-07-23'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - 'f=scripts/checks/iam_tf/_read_coverage.py; planner=$(grep -c ''github_ci_planner'' "$f"); stale=$(grep -Ec ''_(PLAN|DRIFT)_ROLE_POLICY_NAME *= *"github_ci_(plan|drift)"'' "$f"); echo "planner_ref=$planner
    stale_constants=$stale"; [ "$planner" -ge 1 ] && [ "$stale" -eq 0 ]

    '
