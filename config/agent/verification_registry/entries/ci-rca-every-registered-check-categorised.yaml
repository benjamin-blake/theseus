check_id: ci-rca-every-registered-check-categorised
primitive_slot: command_exit_zero
guard_target: scripts/ci_rca/taxonomy.py
guard_symbol: null
plan_slug: ci-rca-evidence-fidelity
graduated_at: '2026-08-04'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - bin/venv-python -c "from scripts.checks.registry import all_checks; from scripts.ci_rca.taxonomy import load_taxonomy; missing=sorted(set(all_checks())-set(load_taxonomy().get('function_to_category')
    or {})); print(len(missing), missing[:5]); assert not missing, missing"
