check_id: validate-import-closure-excludes-check-modules
primitive_slot: command_exit_zero
guard_target: scripts/validate.py
guard_symbol: null
plan_slug: validate-facade-manifest-dispatch
graduated_at: '2026-08-11'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'import importlib,sys

    importlib.import_module(''scripts.validate'')

    leaked={m for m in sys.modules if m.startswith(''scripts.checks.'') and m.count(''.'')>=3 and not m.rsplit(''.'',1)[1].startswith(''_'')}

    allowed={''scripts.checks.iam_tf.validate_terraform_try''}

    assert leaked==allowed, (''leak set != declared exception set'', sorted(leaked^allowed))

    print(''OK'')

    '
