check_id: registry-self-referential-records-green
primitive_slot: command_exit_zero
guard_target: config/agent/verification_registry/entries/
plan_slug: verification-registry-sharding
graduated_at: '2026-08-24'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'from scripts.verification_graduation import load_entries, materialize_check

    SELF = ''registry-self-referential-records-green''

    rows=[e for e in load_entries() if e[''check_id''] != SELF and (''verification_registry'' in str(e.get(''check_spec'')) or str(e.get(''guard_target'','''')).startswith(''config/agent/verification_registry''))]

    bad=[e[''check_id''] for e in rows if materialize_check(e).run().status.name!=''PASS'']

    print(''live self-referential records:'', len(rows), ''failing:'', bad)

    assert not bad, bad

    '
