check_id: registry-size-raises-removed
primitive_slot: command_exit_zero
guard_target: config/structural_size_budgets.yaml
plan_slug: verification-registry-sharding
graduated_at: '2026-08-24'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import yaml,pathlib; from scripts.checks.structural.size_limits import validate_structural_size_limits; from scripts.checks.structural.budget_raises import validate_structural_size_budget_raises; f=[];
    validate_structural_size_limits(f); validate_structural_size_budget_raises(f); assert not f, f; reg=yaml.safe_load(pathlib.Path('config/structural_size_budgets.yaml').read_text()); stale=[k for k in
    list(reg.get('budgets') or {})+list(reg.get('long_line_budgets') or {}) if 'verification_registry' in k]; assert not stale, stale; print('size gates green; both registry raise entries removed')
