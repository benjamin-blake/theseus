check_id: decision-83-reversal-stanza-armed
primitive_slot: command_exit_zero
guard_target: docs/DECISIONS.md
guard_symbol: null
plan_slug: ulf-01-evidence-closure
graduated_at: '2026-08-11'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'from scripts.preflight.decision_conditions import evaluate

    r={x.decision_id: x for x in evaluate()}

    s=r.get(83)

    assert s is not None, ''Decision 83 carries no reversal-conditions stanza''

    assert s.state != ''MALFORMED'', s.error

    ids={c.id for c in s.conditions}

    assert {''probe-token-expiry'',''probe-monitor-blind''} <= ids, ids

    print(''stanza armed:'', sorted(ids))

    '
