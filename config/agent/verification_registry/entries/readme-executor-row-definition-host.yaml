check_id: readme-executor-row-definition-host
primitive_slot: command_exit_zero
guard_target: README.md
guard_symbol: null
plan_slug: step-functions-executor-host
graduated_at: '2026-09-07'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import pathlib,re; t=pathlib.Path('README.md').read_text(encoding='utf-8'); assert 'loop definition' in t, 'README executor
    row not reworded'; assert 'Step Functions state machine per rec' not in t and 'Step Functions + Lambda recommendation-queue
    consumer' not in t, 'definition-shaped README wording survives'; assert len(re.findall(r'Decision \d+ clause 2', t))>=2,
    'README NS.3/NS.5 rows lack the cloud-tier reading'; print('OK')
