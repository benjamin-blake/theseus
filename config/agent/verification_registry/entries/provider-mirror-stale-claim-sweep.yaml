check_id: provider-mirror-stale-claim-sweep
primitive_slot: command_exit_zero
guard_target: .github/workflows/ci.yml
guard_symbol: null
plan_slug: ci-provider-mirror-terraform-init-hardening
graduated_at: '2026-07-26'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - '! grep -rn ''no OIDC step is added'' .github/workflows/ci.yml && ! grep -rn ''the three grep -qE sites in terraform-apply-sandbox'' .github/workflows/terraform-drift.yml && ! grep -n ''bounded retry
    loop in$'' scripts/checks/_terraform.py

    '
