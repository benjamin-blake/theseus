check_id: convergence-classify-stdlib-only
primitive_slot: command_exit_zero
guard_target: scripts/ci/convergence_classify.py
guard_symbol: null
plan_slug: convergence-drift-classification
graduated_at: '2026-09-14'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import sys; sys.path.insert(0,'.'); import scripts.ci.convergence_classify; extra={n.split('.')[0] for n in sys.modules} - set(sys.stdlib_module_names) - {'scripts','__main__','sitecustomize','_distutils_hack'}; assert not extra, f'non-stdlib {sorted(extra)}'
