check_id: t41-rescoped-to-step-functions-host
primitive_slot: command_exit_zero
guard_target: docs/ROADMAP-PLATFORM.yaml:T4.1
guard_symbol: null
plan_slug: step-functions-executor-host
graduated_at: '2026-09-07'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import yaml; d=yaml.safe_load(open('docs/ROADMAP-PLATFORM.yaml')); ti={t['id']:t for t in d['tier_items']}; t=ti['T4.1'];
    assert 'render' in t['intent'] and 'committed' in t['intent'], 'T4.1 intent not re-scoped to render-and-deploy'; nodes=[l
    for l in t['intent'].splitlines() if '[' in l and 'T4.2]' in l]; assert len(nodes)==4, nodes; fs=' '.join(map(str,t['files_in_scope']));
    assert 'src/executor_loop/rendered/executor_loop.asl.json' in fs, 'rendered ASL not in T4.1 files_in_scope'; assert {'T4.19','T4.20','T4.21'}<=set(t['depends_on']),
    t['depends_on']; ec=t['exit_criteria']; assert all(isinstance(c,str) for c in ec), 'T4.1 criteria must stay bare strings';
    assert 'T4.20' in ec[0] and 'trace' in ec[0], ec[0]; cred=[c for c in ec if 'GitHub credential model' in c]; assert len(cred)==1
    and all(w in cred[0] for w in ('GitHub App','bypass_actors','rec-2827','T4.21 c3')), 'credential criterion not pinned
    to a non-bypass App owned by T4.21 c3'; print('OK')
