check_id: ci-workflow-guards-accounting-declaration-adopted
primitive_slot: command_exit_zero
guard_target: scripts/verify_ci_workflow
guard_symbol: null
plan_slug: verify-ci-workflow-decomposition
graduated_at: '2026-09-15'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - from pathlib import Path; from scripts.checks.hygiene import validate_check_accounting as vca; import scripts.checks.ci_guards.validate_ci_workflow_guards
    as agg; assert vca._module_declares(Path(agg.__file__).read_text(encoding='utf-8')), 'validate_ci_workflow_guards still
    declares no examined()/skipped() outcome'; entries=set(vca._load_baseline_entries(Path('config/check_accounting_baseline.yaml').read_text(encoding='utf-8')));
    assert 'validate_ci_workflow_guards' not in entries, 'still baselined'; failed=[]; vca.validate_check_accounting(failed);
    assert not failed, failed; print('ACCOUNTING_RATCHET_OK')
