check_id: scope-boundary-mirror-maps-to-package
primitive_slot: command_exit_zero
guard_target: scripts/test_coverage_checker.py
guard_symbol: map_source_to_test
plan_slug: secrets-baseline-sanction-row
graduated_at: '2026-09-04'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - |
    from pathlib import Path

    from scripts.test_coverage_checker import map_source_to_test

    t = map_source_to_test(Path('scripts/checks/verification/validate_scope_boundary.py'))
    assert t.is_dir(), t
    assert t.name == 'validate_scope_boundary', t
    print('OK')
