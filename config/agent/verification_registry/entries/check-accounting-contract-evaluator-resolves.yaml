check_id: check-accounting-contract-evaluator-resolves
primitive_slot: command_exit_zero
guard_target: docs/contracts/check-accounting.yaml
guard_symbol: validate_check_accounting
plan_slug: validate-vacuous-pass-accounting
graduated_at: '2026-08-13'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - from scripts.checks import registry; failed=[]; registry.resolve('validate_contract_drift')(failed); assert not failed, failed; import pathlib; src=pathlib.Path('scripts/checks/hygiene/validate_check_accounting.py').read_text(encoding='utf-8');
    assert 'check-accounting.yaml' in src, 'contract basename absent as a literal'; print('contract evaluator resolves')
