check_id: ci-rca-workflows-entry-shape
primitive_slot: command_exit_zero
guard_target: config/ci_rca_taxonomy.yaml
guard_symbol: null
plan_slug: ci-rca-adjudication-guard
graduated_at: '2026-08-11'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'from scripts.ci_rca.taxonomy import load_taxonomy; w=load_taxonomy()[''workflows'']; bad=[k for k,v in w.items() if not isinstance(v,dict) or v.get(''ci_rca'') not in (''watched'',''excluded'') or
    v.get(''owner'')!=''platform'' or not str(v.get(''rationale'','''')).strip() or not str(v.get(''tier'','''')).strip()]; assert not bad, bad; print(''entry shape OK'', len(w))

    '
