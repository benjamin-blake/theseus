check_id: ci-guards-pre-globs-cover-verify-ci-workflow-package
primitive_slot: command_exit_zero
guard_target: scripts/verify_ci_workflow
guard_symbol: null
plan_slug: verify-ci-workflow-decomposition
graduated_at: '2026-09-15'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'from fnmatch import fnmatch; from scripts.checks.ci_guards import _manifest; g={e.name: (e.pre_globs or ()) for e in
    _manifest.ENTRIES}; targets=[''validate_ci_rca_trigger'', ''validate_dependabot_automation'', ''validate_branch_cleanup'',
    ''validate_dispatch_gated_apply_topology'']; probe=''scripts/verify_ci_workflow/__init__.py''; miss=[n for n in targets
    if not any(fnmatch(probe, x) for x in g[n])]; assert not miss, miss; noise=[n for n in targets if any(fnmatch(''README.md'',
    x) for x in g[n])]; assert not noise, noise; print(''PRE_GLOBS_COVER_PACKAGE_OK'')'
