check_id: env-taxonomy-md-deleted-and-delisted
primitive_slot: command_exit_zero
guard_target: docs/contracts/file-router.yaml
guard_symbol: null
plan_slug: cfg-11-environment-taxonomy-conversion
graduated_at: '2026-08-13'
graduated_by: implement-agent
check_spec:
  command:
  - sh
  - -c
  - 'name="environment-taxonomy"; ext=".md"

    test ! -f "docs/contracts/${name}${ext}" && ! grep -q "${name}${ext}" docs/contracts/file-router.yaml && echo CLEAN

    '
