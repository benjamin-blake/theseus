check_id: ci-rca-filter-equals-watched
primitive_slot: command_exit_zero
guard_target: .github/workflows/ci-rca.yml
guard_symbol: null
plan_slug: ci-rca-adjudication-guard
graduated_at: '2026-08-11'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'import yaml; from scripts.ci_rca.taxonomy import load_taxonomy; d=yaml.safe_load(open(''.github/workflows/ci-rca.yml'')); f=set(d.get(True, d.get(''on'',{}))[''workflow_run''][''workflows'']); w={k
    for k,v in load_taxonomy()[''workflows''].items() if v[''ci_rca'']==''watched''}; assert f==w, (sorted(f-w), sorted(w-f)); assert ''deploy-prod-lambdas'' in f; print(''filter==watched OK'', sorted(f))

    '
