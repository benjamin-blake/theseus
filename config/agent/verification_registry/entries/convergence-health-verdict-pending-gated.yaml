check_id: convergence-health-verdict-pending-gated
primitive_slot: command_exit_zero
guard_target: scripts/convergence_health
guard_symbol: HealthVerdict.pending_gated
plan_slug: terraform-recovery-semantics
graduated_at: '2026-07-22'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - 'a=scripts/convergence_health/assess.py; m=scripts/convergence_health/__main__.py; av=$(grep -c ''pending_gated'' "$a"); mv=$(grep -c ''pending_gated'' "$m"); echo "assess=$av main=$mv"; [ "$av" -ge
    1 ] && [ "$mv" -ge 1 ]

    '
