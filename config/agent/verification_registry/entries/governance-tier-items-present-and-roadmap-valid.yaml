check_id: governance-tier-items-present-and-roadmap-valid
primitive_slot: command_exit_zero
guard_target: docs/ROADMAP-PLATFORM.yaml
guard_symbol: null
plan_slug: coverage-gate-handoff-integrity
graduated_at: '2026-08-03'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import sys,yaml;sys.path.insert(0,'.');from pathlib import Path;d=yaml.safe_load(Path('docs/ROADMAP-PLATFORM.yaml').read_text(encoding='utf-8'));ids={i['id'] for i in (d.get('tier_items') or [])};want={'T2.54','T2.55','T2.56'};print('missing:',sorted(want-ids));from
    scripts.checks.roadmap.validate_platform_roadmap import validate_platform_roadmap as v;f=[];v(f);print('FAILED:',f);sys.exit(0 if not (want-ids) and not f else 1)
