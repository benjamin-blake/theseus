check_id: dec-167-reversal-stanza-monitored
primitive_slot: command_output_matches
guard_target: docs/DECISIONS.md
guard_symbol: null
plan_slug: migration-step-4-cfg-audit
graduated_at: '2026-08-17'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - from scripts.preflight.decision_conditions import preflight_bucket; import sys; r=preflight_bucket(); bad=[m for m in (r.get('malformed') or []) if str(m).find('167')>=0]; sys.exit(f'167 not monitored
    or malformed -- {r}') if (167 not in (r.get('monitored') or []) or bad) else print('OK 167 monitored')
  expected: OK 167 monitored
  use_regex: false
