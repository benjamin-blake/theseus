check_id: apply-role-hazard4-delete-verbs
primitive_slot: command_exit_zero
guard_target: terraform/bootstrap/github_ci_apply.tf
guard_symbol: null
plan_slug: terraform-iam-allowlist-inversion
graduated_at: '2026-07-22'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - for a in 'iam:DeleteRole' 'iam:DetachRolePolicy' 'iam:DeleteRolePolicy'; do grep -q "$a" terraform/bootstrap/github_ci_apply*.tf || { echo "MISSING $a"; exit 1; }; done && grep -q 'role/agent-platform-\*'
    terraform/bootstrap/github_ci_apply*.tf
