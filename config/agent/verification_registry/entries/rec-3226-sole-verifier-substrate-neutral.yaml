check_id: rec-3226-sole-verifier-substrate-neutral
primitive_slot: command_exit_zero
guard_target: docs/ROADMAP-PLATFORM.yaml:T4.9,T4.9a
guard_symbol: null
plan_slug: step-functions-executor-host
graduated_at: '2026-09-07'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - bin/venv-python -c "import re,pathlib; raw=pathlib.Path('docs/ROADMAP-PLATFORM.yaml').read_text(encoding='utf-8'); assert
    'the executor runs neither in AWS' not in raw, 'incumbent-shaped sole-verifier wording survives'; assert raw.count('only
    verifier / validation runner')>=2, 'sole-verifier rule dropped rather than reworded'; assert len(re.findall(r'distinct
    workflow runs?|T4\.15 c9', raw, re.I))>=3, 'substrate-neutral formulation not adopted at 3+ sites'; print('OK')" && !
    grep -q "the executor runs neither in AWS" docs/ROADMAP-PLATFORM.yaml && test "$(grep -c "only verifier / validation runner"
    docs/ROADMAP-PLATFORM.yaml)" -ge 2 && test "$(grep -ciE "distinct workflow runs?|T4\.15 c9" docs/ROADMAP-PLATFORM.yaml)"
    -ge 3 && echo OK-rec-3226-literal
