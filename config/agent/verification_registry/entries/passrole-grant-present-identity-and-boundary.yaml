check_id: passrole-grant-present-identity-and-boundary
primitive_slot: command_exit_zero
guard_target: terraform/bootstrap/github_ci_apply.tf
guard_symbol: null
plan_slug: t248-passrole-liveproof
graduated_at: '2026-07-24'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - 'p=$(grep -vE ''^[[:space:]]*#'' terraform/bootstrap/github_ci_apply_policy.tf | grep -cF ''"iam:PassRole"''); b=$(grep -vE ''^[[:space:]]*#'' terraform/bootstrap/github_ci_apply_boundary.tf | grep
    -cF ''"iam:PassRole"''); echo "policy=$p boundary=$b"; [ "$p" -ge 1 ] && [ "$b" -ge 1 ]

    '
