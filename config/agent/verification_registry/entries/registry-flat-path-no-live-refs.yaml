check_id: registry-flat-path-no-live-refs
primitive_slot: command_exit_zero
guard_target: config/agent/verification_registry/entries/
plan_slug: verification-registry-sharding
graduated_at: '2026-08-24'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import subprocess,sys; pat=r'config/agent/verification_registry/registry[.]yaml|"verification_registry" / "registry[.]yaml"'; r=subprocess.run(['git','grep','-n','-E',pat,'--','scripts/','config/','tests/','docs/contracts/','docs/ROADMAP-PLATFORM.yaml','.claude/',':!config/agent/verification_registry/entries/'],capture_output=True,text=True);
    hits=[l for l in r.stdout.splitlines() if l.strip()]; print('live_flat_path_refs=',len(hits)); [print('  ',h[:140]) for h in hits[:10]]; sys.exit(1 if hits else 0)
