check_id: t08-regrounded-off-library-framing
primitive_slot: command_exit_zero
guard_target: docs/ROADMAP-PLATFORM.yaml
guard_symbol: null
plan_slug: agent-verb-tool-surface
graduated_at: '2026-07-26'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import yaml,pathlib; d=yaml.safe_load(pathlib.Path('docs/ROADMAP-PLATFORM.yaml').read_text()); t=[i for i in d['tier_items'] if i['id']=='T0.8'][0]; blob=yaml.dump(t); assert 'client.py' not in blob,
    'stale client.py still in T0.8'; assert 'imports verbs' not in blob, 'stale library framing still in T0.8'; assert 'defer' in blob.lower(), 'write-leg deferral not recorded'; print('T0.8_REGROUNDED')
