check_id: plan-digest-emitted-as-commit-status
primitive_slot: command_exit_zero
guard_target: .github/workflows/terraform-apply-sandbox.yml
guard_symbol: null
plan_slug: terraform-apply-chain-consolidation
graduated_at: '2026-07-22'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - 'a=.github/workflows/terraform-apply-sandbox.yml; emit=$(grep -Ec ''terraform-plan-digest'' "$a"); sha=$(grep -Ec ''sha256sum|plan_digest'' "$a"); stat=$(grep -c ''statuses: write'' "$a"); echo "digest_status=$emit
    sha=$sha statuses_write=$stat"; [ "$emit" -ge 1 ] && [ "$sha" -ge 1 ] && [ "$stat" -ge 1 ]

    '
