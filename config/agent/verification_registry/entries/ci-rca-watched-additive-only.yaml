check_id: ci-rca-watched-additive-only
primitive_slot: command_exit_zero
guard_target: config/ci_rca_taxonomy.yaml
guard_symbol: null
plan_slug: ci-rca-adjudication-guard
graduated_at: '2026-08-11'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'from scripts.ci_rca.taxonomy import load_taxonomy; w=load_taxonomy() [''workflows'']; prior=[''CI'',''Main Canary'',''terraform-apply-sandbox'',''rec-autoclose'', ''deploy-ducklake-lambdas'']; bad=[p
    for p in prior if w[p][''ci_rca'']!=''watched'']; assert not bad, bad; print(''additive-only OK'')

    '
