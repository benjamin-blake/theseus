check_id: ci-workflow-guards-accounting-outcome-enforced
primitive_slot: command_exit_zero
guard_target: scripts/verify_ci_workflow
guard_symbol: null
plan_slug: verify-ci-workflow-decomposition
graduated_at: '2026-09-15'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - from scripts.checks import registry; import scripts.checks.ci_guards.validate_ci_workflow_guards as agg; failed=[]; ctx=registry.outcome_scope('validate_ci_workflow_guards');
    ctx.__enter__(); agg.validate_ci_workflow_guards(failed); ctx.__exit__(None, None, None); o=registry.build_outcome('validate_ci_workflow_guards',
    'check', registry.pop_declaration(), bool(failed)); assert o.status == 'enforced', (o, failed); assert o.examined_count
    == len(agg._GUARDS) > 0, (o, len(agg._GUARDS)); print('ACCOUNTING_OUTCOME_OK', o)
