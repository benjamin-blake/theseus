check_id: reconcile-stale-plan-fresh-replan-present
primitive_slot: command_exit_zero
guard_target: .github/workflows/reconcile.yml
guard_symbol: null
plan_slug: terraform-recovery-semantics
graduated_at: '2026-07-22'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - 'f=.github/workflows/reconcile.yml; sig=$(grep -Eic ''saved plan is stale|plan file can no longer be applied|state was changed'' "$f"); mark=$(grep -c ''STALE_PLAN_FRESH_REPLAN'' "$f"); rep=$(grep -Ec
    ''terraform plan .*-out=plan.bin'' "$f"); echo "sig=$sig marker=$mark replan=$rep"; [ "$sig" -ge 1 ] && [ "$mark" -ge 1 ] && [ "$rep" -ge 1 ]

    '
