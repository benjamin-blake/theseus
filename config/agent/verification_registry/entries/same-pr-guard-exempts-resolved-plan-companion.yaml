check_id: same-pr-guard-exempts-resolved-plan-companion
primitive_slot: command_exit_zero
guard_target: scripts/checks/verification/validate_verifier_same_pr_guard.py
plan_slug: same-pr-guard-plan-companion-exemption
graduated_at: '2026-09-05'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - from unittest.mock import MagicMock, patch;from scripts.checks.verification.validate_verifier_same_pr_guard import validate_verifier_same_pr_guard as g;c=['scripts/verifiers/harness.py','docs/plans/PLAN-same-pr-guard-plan-companion-exemption.yaml'];f=[];patch('scripts.checks._common.get_changed_files',return_value=c).start();patch('scripts.checks._common.run',return_value=MagicMock(returncode=0,stdout='')).start();g(f);assert
    not f,f;print('OK')
