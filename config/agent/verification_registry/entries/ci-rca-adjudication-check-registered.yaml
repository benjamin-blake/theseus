check_id: ci-rca-adjudication-check-registered
primitive_slot: command_exit_zero
guard_target: scripts/checks/ci_guards/_manifest.py
guard_symbol: null
plan_slug: ci-rca-adjudication-guard
graduated_at: '2026-08-11'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'from scripts.checks import registry; n=''validate_ci_rca_adjudication''; assert n in registry.all_checks(), ''not registered''; assert callable(registry.resolve(n)); assert n in [s.name for s in registry.pre_sequence()
    if s.kind==''check''], ''absent from --pre''; assert n in [s.name for s in registry.full_sequence() if s.kind==''check''], ''absent from full''; print(''registered OK'')

    '
