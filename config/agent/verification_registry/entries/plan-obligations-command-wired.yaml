check_id: plan-obligations-command-wired
primitive_slot: command_exit_zero
guard_target: .claude/commands/plan.md
guard_symbol: null
plan_slug: plan-obligation-closure
graduated_at: '2026-08-11'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'import pathlib


    t = pathlib.Path(".claude/commands/plan.md").read_text()

    assert "scripts.roadmap.plan_obligations" in t, "Steps 4/6b do not invoke the obligation query"

    assert "split the plan" in t.lower(), "convergence escalation lacks the split option"

    print("command wired ok")

    '
