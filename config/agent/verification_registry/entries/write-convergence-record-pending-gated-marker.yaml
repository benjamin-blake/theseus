check_id: write-convergence-record-pending-gated-marker
primitive_slot: command_exit_zero
guard_target: .github/actions/write-convergence-record/action.yml
guard_symbol: null
plan_slug: terraform-recovery-semantics
graduated_at: '2026-07-22'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - 'f=.github/actions/write-convergence-record/action.yml; pg=$(grep -c ''pending_gated'' "$f"); routed=$(grep -c ''ROUTED'' "$f"); echo "pending_gated=$pg routed_refs=$routed"; [ "$pg" -ge 1 ] && [ "$routed"
    -ge 1 ]

    '
