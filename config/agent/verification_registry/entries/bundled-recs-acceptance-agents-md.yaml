check_id: bundled-recs-acceptance-agents-md
primitive_slot: command_exit_zero
guard_target: AGENTS.md
guard_symbol: null
plan_slug: ambient-prose-contract-relocation
graduated_at: '2026-09-01'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - (grep -q "^## Operational runbooks" AGENTS.md && exit 1 || exit 0) && sed -n "/##
    Shell invocations/,/## Safety/p" AGENTS.md | grep -q "gh CLI" && sed -n "/## Shell
    invocations/,/## Safety/p" AGENTS.md | grep -q "mcp__github" && ! grep -qE "(^|[^-])python
    -m scripts\.sync\.ops" AGENTS.md && grep -q "bin/venv-python -m scripts.sync.ops"
    AGENTS.md
