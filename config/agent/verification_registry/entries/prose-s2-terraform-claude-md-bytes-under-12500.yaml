check_id: prose-s2-terraform-claude-md-bytes-under-12500
primitive_slot: command_exit_zero
guard_target: terraform/CLAUDE.md
guard_symbol: null
plan_slug: ambient-prose-contract-relocation
graduated_at: '2026-09-01'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - bin/venv-python -c "from scripts.preflight.prose_context import measure_prose_context
    as m; f={x['path']:x['prose_bytes'] for x in m()['S2']['files']}; b=f['terraform/CLAUDE.md'];
    print(b); raise SystemExit(0 if b <= 12500 else 1)"
