check_id: sole-home-relocation-sweep
primitive_slot: command_output_matches
guard_target: scripts/roadmap/plan_document.py
guard_symbol: _partition_command
plan_slug: vp-expected-literal-carrier
graduated_at: '2026-09-18'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import re,pathlib; pat=re.compile(r"^def _partition_command|^_BACKTICK_LITERAL_RE|^def
    _extract_literals", re.M); n=sum(len(pat.findall(pathlib.Path(f).read_text()))
    for f in ["scripts/roadmap/plan_document.py","scripts/checks/verification/validate_vp_replay.py"]);
    assert n==0, n; print("SOLE_HOME_CLEAN")
  expected: SOLE_HOME_CLEAN
