check_id: fetch-saved-plan-digest-verify
primitive_slot: command_exit_zero
guard_target: .github/actions/fetch-saved-plan/action.yml
guard_symbol: null
plan_slug: terraform-apply-chain-consolidation
graduated_at: '2026-07-22'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - 'f=.github/actions/fetch-saved-plan/action.yml; ver=$(grep -Ec ''plan_digest|sha256sum'' "$f"); fc=$(grep -Eic ''mismatch|fail.?closed|DIGEST'' "$f"); echo "verify=$ver failclosed=$fc"; [ "$ver" -ge
    1 ] && [ "$fc" -ge 1 ]

    '
