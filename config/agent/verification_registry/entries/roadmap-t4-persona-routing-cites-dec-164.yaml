check_id: roadmap-t4-persona-routing-cites-dec-164
primitive_slot: command_output_matches
guard_target: docs/ROADMAP-PLATFORM.yaml
guard_symbol: null
plan_slug: esb-routing-rationale
graduated_at: '2026-08-03'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import pathlib, yaml; from scripts.decisions_md import decision_header_numbers; d=yaml.safe_load(open('docs/ROADMAP-PLATFORM.yaml')); it={i['id']:i for i in d['tier_items']}; t42=it['T4.2']; t412=it['T4.12'];
    assert 'Decision 164' in t42['intent'], 'T4.2 intent does not cite Decision 164'; r42=t42.get('related_decisions') or []; r412=t412.get('related_decisions') or []; assert 164 in r42 and 164 in r412,
    (r42, r412); assert 116 in r42 and 122 in r42 and 116 in r412, (r42, r412); live=decision_header_numbers(paths=[pathlib.Path('docs/DECISIONS.md')]); dangling=sorted(n for n in set(r42)|set(r412) if
    n not in live); assert not dangling, dangling; print('ROADMAP_CITES_164 ok', sorted(r42), sorted(r412))
  expected: ROADMAP_CITES_164 ok
  use_regex: true
