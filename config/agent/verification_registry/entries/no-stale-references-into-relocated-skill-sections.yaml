check_id: no-stale-references-into-relocated-skill-sections
primitive_slot: command_exit_zero
guard_target: .claude/skills/orient/SKILL.md
guard_symbol: null
plan_slug: skills-layer-prose-relocation
graduated_at: '2026-09-12'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -lc
  - test $(wc -c < .claude/skills/orient/SKILL.md) -le 23278 && grep -q 'tier-item-lifecycle.yaml'
    .claude/skills/orient/SKILL.md && grep -q 'data-modeling-standard.yaml' .claude/commands/plan.md
    && grep -q 'recommendation-relevance.yaml' .claude/commands/orient.md && grep
    -q 'data-modeling-standard.yaml' AGENTS.md && ! grep -q 'Assessment walks the
    full checklist' AGENTS.md && grep -q 'skills-layer-prose-relocation' docs/DECISIONS.md
