check_id: ontheloop-publish-workflow-shape
primitive_slot: command_exit_zero
guard_target: .github/workflows/publish-ontheloop.yml
guard_symbol: null
plan_slug: ontheloop-placeholder-packages
graduated_at: '2026-09-22'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import yaml;d=yaml.safe_load(open('.github/workflows/publish-ontheloop.yml'));on=d.get('on') or d.get(True);j=d['jobs']['publish'];print('WF_TRIGGERS='+','.join(sorted(on)));print('WF_ENV='+str(j['environment']));print('WF_JOB_IDTOKEN='+str(j['permissions'].get('id-token')));print('WF_TOP_IDTOKEN='+str((d.get('permissions')
    or {}).get('id-token')))
