check_id: ci-rca-lifecycle-points-at-taxonomy
primitive_slot: command_exit_zero
guard_target: docs/contracts/ci-rca-lifecycle.yaml
guard_symbol: null
plan_slug: ci-rca-adjudication-guard
graduated_at: '2026-08-11'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'import yaml; d=yaml.safe_load(open(''docs/contracts/ci-rca-lifecycle.yaml'')); s=d.get(''watched_workflow_set'',{}); assert ''watched'' not in s and ''excluded'' not in s, ''second registry survives'';
    raw=open(''docs/contracts/ci-rca-lifecycle.yaml'').read(); assert ''config/ci_rca_taxonomy.yaml'' in raw and ''validate_ci_rca_adjudication'' in raw; assert ''watched_workflow_set above'' not in raw,
    ''starved_review_observability still back-references the deleted maps''; print(''contract points at SoT + evaluator'')

    '
