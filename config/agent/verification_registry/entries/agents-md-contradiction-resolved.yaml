check_id: agents-md-contradiction-resolved
primitive_slot: command_exit_zero
guard_target: AGENTS.md
guard_symbol: null
plan_slug: pr-ci-red-ownership
graduated_at: '2026-08-31'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - '! grep -q "On CI failure" AGENTS.md && grep -q "forward-fix, never auto-revert" AGENTS.md && grep -q "source=ci_rca" AGENTS.md && grep -q "priority=critical" AGENTS.md && grep -q "Pre-handoff (local) + post-merge on .main." AGENTS.md'
