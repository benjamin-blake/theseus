check_id: gc-c2-criterion-rewritten
primitive_slot: command_exit_zero
guard_target: docs/ROADMAP-PLATFORM.yaml
guard_symbol: null
plan_slug: production-gc-and-storage-stability
graduated_at: '2026-09-15'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import yaml,io,json; d=yaml.safe_load(io.open('docs/ROADMAP-PLATFORM.yaml',encoding='utf-8'));
    assert 'S3 storage confirmed stable after N maintenance cycles' not in json.dumps(d),
    'c2 still carries the unadjudicable text'; print('c2 rewritten')
