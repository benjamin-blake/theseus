check_id: all-three-policy-docs-carry-size-precondition
primitive_slot: command_exit_zero
guard_target: terraform/bootstrap/github_ci_apply.tf
guard_symbol: null
plan_slug: iam-write-surface-completion
graduated_at: '2026-07-30'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - f="terraform/bootstrap/github_ci_apply*.tf"; p=$(cat $f | grep -c 'precondition {'); l1=$(cat $f | grep -c 10240); l2=$(cat $f | grep -c 6144); echo "precondition_blocks=$p limit10240=$l1 limit6144=$l2";
    [ "$p" -ge 3 ] && [ "$l1" -ge 1 ] && [ "$l2" -ge 2 ]
