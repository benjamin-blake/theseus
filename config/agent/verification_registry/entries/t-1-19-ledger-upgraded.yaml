check_id: t-1-19-ledger-upgraded
primitive_slot: command_exit_zero
guard_target: docs/ROADMAP-PLATFORM.yaml
guard_symbol: null
plan_slug: cfg-11-environment-taxonomy-conversion
graduated_at: '2026-08-13'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'import yaml

    from scripts.checks.roadmap.validate_platform_roadmap import validate_platform_roadmap

    f=[]

    validate_platform_roadmap(f)

    assert not f, f

    d=yaml.safe_load(open(''docs/ROADMAP-PLATFORM.yaml''))

    t=[i for i in d[''tier_items''] if i[''id'']==''T-1.19''][0]

    assert t[''status'']==''deferred_post_mvp'', t[''status'']

    assert all(isinstance(c, dict) and ''id'' in c for c in t[''exit_criteria'']), t[''exit_criteria'']

    assert not any(''docs/REPORT-'' in p for p in t[''files_in_scope'']), t[''files_in_scope'']

    print(''T-1.19 OK'')

    '
