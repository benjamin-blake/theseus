check_id: verify-ci-workflow-flat-module-fully-retired
primitive_slot: command_exit_zero
guard_target: scripts/verify_ci_workflow
guard_symbol: null
plan_slug: verify-ci-workflow-decomposition
graduated_at: '2026-09-15'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import subprocess; from pathlib import Path; assert Path('scripts/verify_ci_workflow/__init__.py').is_file(), 'facade
    package missing'; assert not Path('scripts/verify_ci_workflow.py').exists(), 'flat module still present'; r=subprocess.run(['git',
    'grep', '-lI', '\"scripts/verify_ci_workflow[.]py\"', '--', 'scripts', 'tests', '.github'], capture_output=True, text=True);
    assert r.returncode == 1, (r.returncode, r.stdout, r.stderr); print('NO_STALE_PATH_LITERALS')
