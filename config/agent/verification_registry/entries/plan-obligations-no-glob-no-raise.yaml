check_id: plan-obligations-no-glob-no-raise
primitive_slot: command_exit_zero
guard_target: scripts/checks/roadmap/validate_plan_scope_closure.py
guard_symbol: validate_plan_scope_closure
plan_slug: plan-obligation-closure
graduated_at: '2026-08-11'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'import time, tempfile, pathlib

    from scripts.checks.roadmap.validate_plan_scope_closure import validate_plan_scope_closure as C


    t = time.monotonic()

    f = []

    C(f, plan_paths=[])

    assert not f, f"empty plan_paths produced findings - it globbed: {f}"

    e = time.monotonic() - t

    assert e < 1.0, f"took {e:.2f}s on an empty path list - it globbed the corpus"

    g = []

    C(g, plan_paths=[pathlib.Path(tempfile.mkdtemp()) / "PLAN-absent.yaml"])

    print(f"no-glob ok ({e:.3f}s), missing file handled without raising")

    '
