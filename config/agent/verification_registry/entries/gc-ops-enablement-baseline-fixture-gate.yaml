check_id: gc-ops-enablement-baseline-fixture-gate
primitive_slot: command_exit_zero
guard_target: tests/fixtures/gc_ops_dryrun_baseline.json
guard_symbol: null
plan_slug: gc-ops-baseline-gate-and-schedule-enable
graduated_at: '2026-09-17'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -m
  - pytest
  - tests/checks/iam_tf/test_ducklake_maintenance_tf_invariants.py::TestGcOpsEnablementBaseline::test_committed_dry_run_baseline_licenses_the_enablement
  - -q
  # -s: the test PRINTS the measured ladder and the implied drain-cycle count, which is the evidence
  # that re-grounds rec-3870's N. Under bare -q pytest captures and discards that output on a green
  # run, so the standing check would emit nothing and the evidence requirement would be inert.
  - -s
