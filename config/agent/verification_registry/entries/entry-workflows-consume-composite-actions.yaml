check_id: entry-workflows-consume-composite-actions
primitive_slot: command_exit_zero
guard_target: .github/workflows/terraform-apply-sandbox.yml
guard_symbol: null
plan_slug: terraform-apply-chain-consolidation
graduated_at: '2026-07-22'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - 'for wf in .github/workflows/terraform-apply-sandbox.yml .github/workflows/reconcile.yml; do n=$(grep -c ''uses: ./.github/actions/'' "$wf"); echo "$wf uses=$n"; [ "$n" -ge 5 ] || { echo "too few action
    refs in $wf"; exit 1; }; done; for a in write-convergence-record deterministic-guard subagent-plan-review fetch-saved-plan terraform-init-retry build-ducklake-artifacts; do grep -q "uses: ./.github/actions/$a"
    .github/workflows/terraform-apply-sandbox.yml || { echo "sandbox missing $a"; exit 1; }; done

    '
