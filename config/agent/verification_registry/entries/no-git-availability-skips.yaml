check_id: no-git-availability-skips
primitive_slot: command_exit_zero
guard_target: tests/
plan_slug: ci-full-tier-history-parity
graduated_at: '2026-09-19'
graduated_by: implement-agent
check_spec:
  command:
    - bash
    - "-c"
    - 'grep -rn "pytest.skip" tests/ | grep -Eic "shallow|cat-file|absent from this checkout" | grep -qx 0'
