check_id: cd44-gov2-compute-plane-container-arm
primitive_slot: command_exit_zero
guard_target: docs/ROADMAP-PLATFORM.yaml:CD.44
guard_symbol: null
plan_slug: step-functions-executor-host
graduated_at: '2026-09-07'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import yaml,re; d=yaml.safe_load(open('docs/ROADMAP-PLATFORM.yaml')); cd=[c for c in d['candidate_decisions'] if c['id']=='CD.44'][0];
    det=cd['detail']; m=re.search(r'GOV-2.*?\[Amendment[^\]]*\]', det, re.S); assert m and 'long_running_container' in m.group(0)
    and 'hosted_cli_runner' in m.group(0) and 'cost_projection' in m.group(0) and 'lapse_condition' in m.group(0), 'CD.44
    GOV-2 carries no bracketed amendment re-mapping the compute plane onto the container arm and naming the residual cost_projection
    sites'; assert 'The candidate selects nothing' in str(cd['discipline_points']), 'selects-nothing discipline lost'; print('OK')
