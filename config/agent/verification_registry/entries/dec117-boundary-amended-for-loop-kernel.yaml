check_id: dec117-boundary-amended-for-loop-kernel
primitive_slot: command_exit_zero
guard_target: docs/DECISIONS.md
guard_symbol: null
plan_slug: step-functions-executor-host
graduated_at: '2026-09-07'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import re,pathlib; t=pathlib.Path('docs/DECISIONS.md').read_text(encoding='utf-8'); m=re.search(r'^## Decision 117:.*?(?=^##
    Decision |\Z)', t, re.M|re.S); assert m, 'Decision 117 missing'; b=m.group(0); a=[l for l in b.splitlines() if re.match(r'>
    \*\*Amended by Decision \d+ \(\d{4}-\d{2}-\d{2}\):\*\*', l)]; assert a, 'Decision 117 carries no dated Amended-by annotation';
    l=a[-1]; miss=[w for w in ('bypass_actors','src/executor_loop/','docs/contracts/personas/','.github/CODEOWNERS','scripts/checks/executor/','rec-3218','GitHub
    App') if w not in l]; assert not miss, f'Decision 117 annotation missing {miss}'; assert 'is unchanged from Decision 44'
    in b, 'original enforcement line edited rather than annotated'; print('OK')
