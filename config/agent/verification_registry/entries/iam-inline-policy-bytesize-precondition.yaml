check_id: iam-inline-policy-bytesize-precondition
primitive_slot: command_exit_zero
guard_target: terraform/personal/oidc.tf
guard_symbol: null
plan_slug: ci-deploy-role-consolidation
graduated_at: '2026-07-23'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - 'pre=$(grep -REc ''precondition'' terraform/personal/ terraform/bootstrap/ | awk -F: ''{s+=$2} END{print s+0}''); lim=$(grep -REc ''10240'' terraform/personal/ terraform/bootstrap/ | awk -F: ''{s+=$2}
    END{print s+0}''); echo "preconditions=$pre limit_10240=$lim"; [ "$pre" -ge 3 ] && [ "$lim" -ge 3 ]

    '
