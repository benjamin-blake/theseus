check_id: roadmap-wording-sweep-definition-host
primitive_slot: command_exit_zero
guard_target: docs/ROADMAP-PLATFORM.yaml:T4.16,T2.11b,T4.12
guard_symbol: null
plan_slug: step-functions-executor-host
graduated_at: '2026-09-07'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import yaml,re; d=yaml.safe_load(open('docs/ROADMAP-PLATFORM.yaml')); ti={t['id']:t for t in d['tier_items']}; ec={c['id']:c['text']
    for c in ti['T4.16']['exit_criteria']}; assert 'T4.21' in ec['c11'] and 'check run' in ec['c11'], ec['c11']; assert 'T4.21'
    in ti['T4.16']['depends_on'], ti['T4.16']['depends_on']; txt=' '.join((c['text'] if isinstance(c,dict) else str(c)) for
    c in ti['T2.11b']['exit_criteria']); assert 'loop definition' in txt and 'Lambda Durable Functions executor' not in txt,
    'T2.11b c5 still labels the executor mechanism-first'; ns={n['id']:n for n in d['north_star']['principles']}; assert all(('[Amendment'
    in ns[k]['detail'] and 'free tier' in ns[k]['detail']) for k in ('NS.3','NS.5')), 'north_star NS.3/NS.5 carry no cloud-tier
    reading'; cd43=[c for c in d['candidate_decisions'] if c['id']=='CD.43'][0]; assert any((isinstance(p,str) and 'hosted_cli_runner'
    in p and 'eliminated' in p) for p in cd43['discipline_points']), 'CD.43 carries no elimination note'; assert re.search(r'Decision
    \d+ clause 3', str(ti['T4.12'].get('progress_note',''))), 'T4.12 carries no pointer to the Actions workload-class ruling';
    print('OK')
