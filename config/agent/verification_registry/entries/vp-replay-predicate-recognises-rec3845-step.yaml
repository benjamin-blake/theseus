check_id: vp-replay-predicate-recognises-rec3845-step
primitive_slot: command_exit_zero
guard_target: scripts/checks/verification/validate_vp_replay.py
guard_symbol: _is_pr_relative
plan_slug: vp-replay-postmerge-partition-and-coverage-diagnostics
graduated_at: '2026-09-14'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - bin/venv-python -c "import yaml; from scripts.checks.verification.validate_vp_replay
    import _is_pr_relative; d=yaml.safe_load(open('docs/plans/PLAN-vp-red-before-gate.yaml'));
    s=[x for x in d['verification_plan'] if x['step']==16][0]; assert _is_pr_relative(s['command']),
    'the predicate does not recognise the step that produced rec-3845'; print('REC3845_STEP_RECOGNISED')"
