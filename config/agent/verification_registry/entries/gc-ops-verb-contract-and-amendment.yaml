check_id: gc-ops-verb-contract-and-amendment
primitive_slot: command_exit_zero
guard_target: docs/contracts/ducklake_maintenance.yaml
guard_symbol: null
plan_slug: production-gc-and-storage-stability
graduated_at: '2026-09-15'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import yaml,io; d=yaml.safe_load(io.open('docs/contracts/ducklake_maintenance.yaml',encoding='utf-8'));
    assert 'gc_ops' in d['verbs'], 'gc_ops verb undocumented'; assert 'gc' in d['verbs'],
    'the smoke gc verb must survive the addition'; assert 'dry_run' in d['verbs']['gc_ops']['payload_schema_ref'],
    'dry_run absent from the gc_ops payload'; a=d['amendment_log']; assert len(a)>=3,
    'no amendment entry appended (2 exist today)'; assert a[-1]['change_class']=='field_add'
    and a[-1]['semantic_break'] is True, 'a new verb is field_add with semantic_break
    true'; print('contract + amendment ok')
