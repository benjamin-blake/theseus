check_id: ci-rca-workflows-map-bidirectional
primitive_slot: command_exit_zero
guard_target: config/ci_rca_taxonomy.yaml
guard_symbol: null
plan_slug: ci-rca-adjudication-guard
graduated_at: '2026-08-11'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'from scripts.ci_rca.taxonomy import enumerate_workflow_names, load_taxonomy; t=load_taxonomy(); assert ''workflow_to_tier'' not in t, ''retired key survives''; w=t[''workflows'']; a=set(enumerate_workflow_names());
    assert set(w)==a, (sorted(set(w)-a), sorted(a-set(w))); print(''bidirectional coverage OK'', len(a))

    '
