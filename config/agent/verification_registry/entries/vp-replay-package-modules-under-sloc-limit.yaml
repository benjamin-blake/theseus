check_id: vp-replay-package-modules-under-sloc-limit
primitive_slot: command_exit_zero
guard_target: tests/checks/verification/validate_vp_replay/
guard_symbol: null
plan_slug: vp-replay-mirror-decomposition
graduated_at: '2026-09-13'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - from pathlib import Path; mods=list(Path('tests/checks/verification/validate_vp_replay').glob('*.py'));
    assert mods, 'package absent -- glob on a missing directory returns [] and would
    pass vacuously'; bad=[(str(p), n) for p in mods for n in [sum(1 for l in p.read_text().splitlines()
    if l.strip() and not l.strip().startswith('#'))] if n >= 500]; assert not bad,
    bad; print('ALL_MODULES_UNDER_LIMIT')
