check_id: vp-replay-flat-mirror-fully-retired
primitive_slot: command_exit_zero
guard_target: tests/checks/verification/validate_vp_replay/
guard_symbol: null
plan_slug: vp-replay-mirror-decomposition
graduated_at: '2026-09-13'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import subprocess; from pathlib import Path; assert Path('tests/checks/verification/validate_vp_replay').is_dir(),
    'package missing'; r=subprocess.run(['git','grep','-lI', r'tests/checks/verification/test_validate_vp_replay\.py','--','.',':!docs/plans'],
    capture_output=True, text=True); assert r.returncode == 1, (r.returncode, r.stdout,
    r.stderr); print('NO_STALE_REFERENCES')
