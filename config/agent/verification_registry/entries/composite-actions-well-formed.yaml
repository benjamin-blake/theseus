check_id: composite-actions-well-formed
primitive_slot: command_exit_zero
guard_target: .github/actions
guard_symbol: null
plan_slug: terraform-apply-chain-consolidation
graduated_at: '2026-07-22'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - 'bad=0; for f in .github/actions/*/action.yml; do grep -q ''using: *"\?composite'' "$f" || { echo "not composite: $f"; bad=1; }; done; n=$(ls .github/actions/*/action.yml 2>/dev/null | wc -l | tr -d
    '' ''); echo "actions=$n malformed=$bad"; [ "$n" -ge 6 ] && [ "$bad" -eq 0 ]

    '
