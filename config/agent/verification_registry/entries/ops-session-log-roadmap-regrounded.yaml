check_id: ops-session-log-roadmap-regrounded
primitive_slot: command_exit_zero
guard_target: docs/ROADMAP-PLATFORM.yaml,docs/DECISIONS.md
guard_symbol: null
plan_slug: t2-26-retire-ops-session-log
graduated_at: '2026-09-22'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import yaml; d=yaml.safe_load(open('docs/ROADMAP-PLATFORM.yaml')); ti={t['id']:t for t in d['tier_items']}; c7=[c for
    c in ti['T3.20']['exit_criteria'] if c['id']=='c7'][0]; assert 'ops_session_log' not in c7['text'], c7; c3=[c for c in
    ti['T2.26']['exit_criteria'] if c['id']=='c3'][0]; assert 'Section 5' not in c3['text'], c3; assert not any('ops_session_log'
    in str(c) for c in ti['T2.11b']['exit_criteria']), ti['T2.11b']['exit_criteria']; assert not any('ops_session_log' in
    str(c) for c in ti['T2.15']['exit_criteria']), ti['T2.15']['exit_criteria']; assert 'loop definition' in ' '.join(str(x)
    for x in ti['T2.11b']['exit_criteria']); assert 'scripts/s3_log_store.py' in ti['T2.26']['files_in_scope'], ti['T2.26']['files_in_scope'];
    cd40=[c for c in d['candidate_decisions'] if c['id']=='CD.40'][0]; assert any('clause 1 realized' in str(x) for x in cd40['discipline_points']),
    cd40['discipline_points']; dec=open('docs/DECISIONS.md', encoding='utf-8').read(); assert 'RETIRED, not migrated' in dec
    and 'git history' in dec, 'Decision 78 clause-2 annotation missing'; print('ROADMAP_REGROUNDED')
