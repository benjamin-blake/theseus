check_id: masked-errexit-zero-violations-over-asserted-file-set
primitive_slot: command_exit_zero
guard_target: scripts/checks/ci_guards/validate_masked_errexit_steps.py
guard_symbol: scanned_workflow_files
plan_slug: pr-conflict-signal-exit-status
graduated_at: '2026-08-10'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import pathlib; from scripts.checks.ci_guards.validate_masked_errexit_steps import validate_masked_errexit_steps, scanned_workflow_files as s; f=[]; validate_masked_errexit_steps(f); assert f==[], f;
    got={pathlib.Path(p).name for p in s()}; want={p.name for p in pathlib.Path('.github/workflows').glob('*.y*ml')}; assert got==want, sorted(want^got); print('zero violations over', len(got), 'workflow
    files')
