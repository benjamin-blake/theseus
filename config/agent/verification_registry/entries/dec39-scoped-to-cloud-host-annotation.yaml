check_id: dec39-scoped-to-cloud-host-annotation
primitive_slot: command_exit_zero
guard_target: docs/DECISIONS.md
guard_symbol: null
plan_slug: step-functions-executor-host
graduated_at: '2026-09-07'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import re,pathlib; t=pathlib.Path('docs/DECISIONS.md').read_text(encoding='utf-8'); m=re.search(r'^## Decision 39:.*?(?=^##
    Decision |\Z)', t, re.M|re.S); assert m, 'Decision 39 missing'; b=m.group(0); a=[l for l in b.splitlines() if re.match(r'>
    \*\*Amended by Decision \d+ \(\d{4}-\d{2}-\d{2}\):\*\*', l)]; assert a, 'Decision 39 carries no dated Amended-by annotation';
    l=a[-1]; miss=[w for w in ('scoped','cloud','free-tier','Custom DAG engine','sole cloud-tier') if w not in l]; assert
    not miss, f'Decision 39 annotation missing {miss}'; assert 'Rejected. Step Functions IS a managed DAG engine' in b, 'the
    original rejected-alternative text was edited, not annotated (Decision 177 append-only)'; print('OK')
