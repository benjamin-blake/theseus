check_id: plan-obligations-independent-evidence
primitive_slot: command_exit_zero
guard_target: scripts/checks/roadmap/validate_plan_scope_closure.py
guard_symbol: validate_plan_scope_closure
plan_slug: plan-obligation-closure
graduated_at: '2026-08-11'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - "import yaml, tempfile, pathlib\nfrom scripts.checks.roadmap.validate_plan_scope_closure import validate_plan_scope_closure as C\n\nd = pathlib.Path(tempfile.mkdtemp())\nbase = {\n    \"schema_version\"\
    : 4,\n    \"plan_type\": \"IMPLEMENTATION\",\n    \"slug\": \"fixture-probe\",\n    \"plan_path\": \"docs/plans/PLAN-fixture-probe.yaml\",\n    \"scope\": [\n        {\"file\": \"scripts/checks/roadmap/validate_x.py\"\
    , \"action\": \"Create\", \"purpose\": \"p\"},\n        {\"file\": \"scripts/checks/roadmap/_manifest.py\", \"action\": \"Modify\", \"purpose\": \"p\"},\n        {\"file\": \"config/ci_rca_taxonomy.yaml\"\
    , \"action\": \"Modify\", \"purpose\": \"p\"},\n        {\"file\": \"tests/checks/roadmap/test_validate_x.py\", \"action\": \"Create\", \"purpose\": \"p\"},\n    ],\n}\ngood = d / \"PLAN-fixture-probe.yaml\"\
    \ngood.write_text(yaml.safe_dump(base))\nf = []\nC(f, plan_paths=[good])\nassert not f, f\"positive control failed: {f}\"\nmiss = 0\nfor drop in (\"scripts/checks/roadmap/_manifest.py\", \"config/ci_rca_taxonomy.yaml\"\
    , \"tests/checks/roadmap/test_validate_x.py\"):\n    bad = dict(base)\n    bad[\"scope\"] = [r for r in base[\"scope\"] if r[\"file\"] != drop]\n    p = d / \"PLAN-fixture-probe.yaml\"\n    p.write_text(yaml.safe_dump(bad))\n\
    \    g = []\n    C(g, plan_paths=[p])\n    assert g and any(drop in str(x) for x in g), f\"not flagged: {drop} -> {g}\"\n    miss += 1\nprint(f\"independent evidence ok: positive control clean, {miss}/3\
    \ omissions named\")\n"
