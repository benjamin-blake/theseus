check_id: new-contracts-within-structural-size-class
primitive_slot: command_exit_zero
guard_target: docs/contracts/git-ops.yaml
guard_symbol: null
plan_slug: ambient-prose-contract-relocation
graduated_at: '2026-09-01'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - bin/venv-python -c "import pathlib; bad=[]; [bad.append((str(f),n,m)) for f in
    (pathlib.Path('docs/contracts/git-ops.yaml'),pathlib.Path('docs/contracts/terraform-cc-web-operations.yaml'))
    for n,m in [(sum(1 for l in f.read_text().splitlines() if l.strip() and not l.lstrip().startswith('#')),
    max((len(l) for l in f.read_text().splitlines()), default=0))] if n > 500 or m
    > 2000]; print(bad); raise SystemExit(1 if bad else 0)"
