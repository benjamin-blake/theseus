check_id: terraform-drift-materialise-step-retains-oidc-gate
primitive_slot: command_exit_zero
guard_target: .github/workflows/terraform-drift.yml
guard_symbol: null
plan_slug: materialise-tfvars-delegate-consolidation
graduated_at: '2026-09-21'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - bin/venv-python -c "import yaml; j=yaml.safe_load(open('.github/workflows/terraform-drift.yml'))['jobs']['drift-detect'];
    st=[s for s in j['steps'] if isinstance(s,dict) and 'materialise-tfvars' in str(s.get('uses',''))];
    print('USES_STEPS', len(st)); assert len(st)==1, len(st); cond=str(st[0].get('if',''));
    print('GATED', int('oidc_drift.outcome' in cond and 'success' in cond)); assert
    'oidc_drift.outcome' in cond and 'success' in cond, cond"
