check_id: prose-budgets-reseeded-to-measured
primitive_slot: command_exit_zero
guard_target: config/prose_budgets.yaml
guard_symbol: null
plan_slug: ambient-prose-contract-relocation
graduated_at: '2026-09-01'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - bin/venv-python -c "import yaml; from scripts.preflight.prose_context import measure_prose_context
    as m; r=m(); b=yaml.safe_load(open('config/prose_budgets.yaml')); s1=r['S1']['prose_bytes'];
    tf=[x for x in r['S2']['files'] if x['path']=='terraform/CLAUDE.md'][0]['prose_bytes'];
    got=(b['S1']['root_ambient_load_set'], b['S2']['terraform/CLAUDE.md']); want=(s1,
    tf); print('budget', got, 'measured', want); raise SystemExit(0 if got==want else
    1)"
