check_id: roadmap-platform-check-fully-declared
primitive_slot: command_exit_zero
guard_target: scripts/checks/roadmap/validate_platform_roadmap.py
guard_symbol: validate_platform_roadmap
plan_slug: roadmap-touched-item-spans-and-regrowth-guard
graduated_at: '2026-09-05'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import yaml; from pathlib import Path; from scripts.checks import registry; from scripts.checks.hygiene._declaring_coverage
    import is_fully_declared, measure_check; from scripts.checks.hygiene.validate_check_accounting import _BASELINE_SEED;
    row=measure_check('validate_platform_roadmap', registry.resolve('validate_platform_roadmap')); assert row.undeclared == 0
    and is_fully_declared(row), row; roster=set(yaml.safe_load(Path('config/check_accounting_baseline.yaml').read_text(encoding='utf-8'))['entries']);
    assert 'validate_platform_roadmap' not in roster; assert 'check_roadmap_regrowth' not in roster and 'check_roadmap_regrowth'
    not in _BASELINE_SEED; print('PLATFORM_CHECK_FULLY_DECLARED')
