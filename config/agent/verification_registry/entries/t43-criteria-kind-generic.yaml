check_id: t43-criteria-kind-generic
primitive_slot: command_exit_zero
guard_target: docs/ROADMAP-PLATFORM.yaml
guard_symbol: T4.3.exit_criteria
plan_slug: governance-and-work-item-boundary
graduated_at: '2026-09-20'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import sys,yaml; d=yaml.safe_load(open('docs/ROADMAP-PLATFORM.yaml')); t=[i for i in d['tier_items'] if i['id']=='T4.3'][0];
    cs={c['id']:c['text'] for c in t['exit_criteria']}; c2='work item of kind rec' in cs.get('c2',''); kg=sorted(k for k,v
    in cs.items() if 'kind-generic' in v and 'Decision 197' in v); print('c2_workitem:', c2, 'kind_generic_criteria:', kg);
    sys.exit(0 if c2 and kg else 1)
