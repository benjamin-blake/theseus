check_id: agents-md-full-tier-pre-handoff-stated
primitive_slot: command_exit_zero
guard_target: AGENTS.md
guard_symbol: null
plan_slug: coverage-gate-handoff-integrity
graduated_at: '2026-08-03'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import sys,re;from pathlib import Path;t=Path('AGENTS.md').read_text(encoding='utf-8');rows=[l for l in t.splitlines() if l.startswith('| Full ')];print(rows);sys.exit(0 if rows and re.search(r'pre-handoff|before
    handoff',rows[0],re.I) else 1)
