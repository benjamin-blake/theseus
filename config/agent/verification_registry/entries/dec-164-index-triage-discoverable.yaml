check_id: dec-164-index-triage-discoverable
primitive_slot: command_output_matches
guard_target: docs/decisions-index.json
guard_symbol: null
plan_slug: esb-routing-rationale
graduated_at: '2026-08-03'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import json; d=json.load(open('docs/decisions-index.json')); e=[x for x in d['decisions'] if x['number']==164][0]; assert e['live'] is True, e['live']; assert e['amends']==[116,122], e['amends']; assert
    e['triage_excerpt_source']=='Intent', e['triage_excerpt_source']; assert len(e['triage_excerpt'])<=320, len(e['triage_excerpt']); assert 'CLI-hosting class' in e['triage_excerpt'], 'the reopen consequence
    is outside the 320-char triage window'; assert 'lambda' in e['category_tags'], e['category_tags']; print('DEC164_INDEX ok', e['amends'], e['triage_excerpt_source'], len(e['triage_excerpt']))
  expected: DEC164_INDEX ok
  use_regex: true
