check_id: ontheloop-publish-workflow-body-registered
primitive_slot: command_exit_zero
guard_target: config/composite_action_body_baseline.yaml
guard_symbol: null
plan_slug: ontheloop-placeholder-packages
graduated_at: '2026-09-22'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import sys,yaml;from scripts.checks.ci_guards import _workflow_shell_bodies as w;b=yaml.safe_load(open('config/composite_action_body_baseline.yaml'))['r3_workflows'];k=[x
    for x in w.scan_workflow_bodies() if 'publish-ontheloop' in x];u=[x for x in k if x not in b];print('WF_BODIES='+str(len(k)));print('WF_BODIES_UNREGISTERED='+str(len(u)));sys.exit(0
    if len(k)==1 and not u else 1)
