check_id: graduation-completeness-check-registered-both-tiers
primitive_slot: command_exit_zero
guard_target: scripts/checks/registry.py
guard_symbol: pre_sequence
plan_slug: verification-graduation-enforcement
graduated_at: '2026-07-16'
graduated_by: implement-agent
check_spec:
  command:
  - python
  - -c
  - 'from scripts.checks.registry import pre_sequence, full_sequence, resolve

    assert any(s.name==''validate_graduation_completeness'' for s in pre_sequence())

    assert any(s.name==''validate_graduation_completeness'' for s in full_sequence())

    assert resolve(''validate_graduation_completeness'')

    '
