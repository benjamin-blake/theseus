check_id: preflight-never-runs-acceptance-probes
primitive_slot: command_exit_zero
guard_target: scripts/preflight
guard_symbol: null
plan_slug: ci-rca-deferral-instrumentation
graduated_at: '2026-09-03'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import pathlib; files=sorted(list(pathlib.Path('scripts/preflight').rglob('*.py'))+list(pathlib.Path('scripts/session').rglob('*.py')));
    hits=[str(p) for p in files if any(tok in p.read_text(encoding='utf-8') for tok
    in ('rec_relevance','run_acceptance_probe'))]; landed='annotate_prior_deferrals'
    in pathlib.Path('scripts/preflight/ci_rca_signals.py').read_text(encoding='utf-8');
    raise SystemExit(0 if not hits and landed else 1)
