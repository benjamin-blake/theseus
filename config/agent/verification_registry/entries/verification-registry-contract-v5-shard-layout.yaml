check_id: verification-registry-contract-v5-shard-layout
primitive_slot: command_exit_zero
guard_target: docs/contracts/verification-registry.yaml
plan_slug: verification-registry-sharding
graduated_at: '2026-08-24'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import yaml,pathlib; d=yaml.safe_load(pathlib.Path('docs/contracts/verification-registry.yaml').read_text()); assert d['contract']['contract_version']==5, d['contract']['contract_version']; e=d['amendment_log'][0];
    assert (e['change_class']=='prose_improvement')!=bool(e['semantic_break']), e; assert 'registry.yaml' not in d['contract']['description'], 'stale data-file path'; print('contract v5; amendment consistent;
    path re-grounded')
