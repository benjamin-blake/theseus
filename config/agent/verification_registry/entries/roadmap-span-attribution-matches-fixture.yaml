check_id: roadmap-span-attribution-matches-fixture
primitive_slot: command_exit_zero
guard_target: scripts/checks/roadmap/_roadmap_spans.py
guard_symbol: touched_item_ids
plan_slug: roadmap-touched-item-spans-and-regrowth-guard
graduated_at: '2026-09-05'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - from tests.fixtures.roadmap_touched_items import load_fixture; from scripts.checks.roadmap._roadmap_spans import touched_item_ids;
    rows=load_fixture(); bad=[r.commit for r in rows if sorted(touched_item_ids(r.pre_spans, r.post_spans, r.changed)) != r.expected];
    assert rows and not bad, bad; print('SPAN_MATCHES_FIXTURE', len(rows))
