check_id: prose-budgets-s4-entries-equal-measured-and-marker-free
primitive_slot: command_exit_zero
guard_target: config/prose_budgets.yaml
guard_symbol: null
plan_slug: skills-layer-prose-relocation
graduated_at: '2026-09-12'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'import pathlib,re,sys

    raw=pathlib.Path(''config/prose_budgets.yaml'').read_text()

    S=[''.claude/skills/planning/SKILL.md'',''.claude/skills/implement/SKILL.md'']

    rows={s: re.search(''^ +''+re.escape(s)+r'': *(\d+)(.*)$'', raw, re.M) for s in
    S}

    bad=[s for s in S if rows[s] is None or int(rows[s].group(1)) != len(pathlib.Path(s).read_bytes())
    or ''raise-approved'' in rows[s].group(2)]

    sys.exit(1 if bad else 0)'
