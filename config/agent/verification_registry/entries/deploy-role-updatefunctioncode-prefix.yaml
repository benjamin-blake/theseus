check_id: deploy-role-updatefunctioncode-prefix
primitive_slot: command_exit_zero
guard_target: terraform/personal/oidc.tf
guard_symbol: github_ci_deploy
plan_slug: ci-deploy-role-consolidation
graduated_at: '2026-07-23'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - 'f="terraform/personal/oidc*.tf"; def=$(cat $f | grep -c ''resource "aws_iam_role" "github_ci_deploy"''); pfx=$(cat $f | grep -c ''function:agent-platform-\*''); other=$(cat $f | grep -oE ''lambda:[A-Za-z]+''
    | grep -v ''lambda:UpdateFunctionCode'' | grep -vE ''lambda:(Invoke|Get|List)'' | sort -u | wc -l | tr -d '' ''); echo "deploy=$def prefix=$pfx other_lambda_writes=$other"; [ "$def" -ge 1 ] && [ "$pfx"
    -ge 1 ] && [ "$other" -eq 0 ]

    '
