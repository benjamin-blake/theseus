check_id: decision-37-compacted-stub-sole-superseder
primitive_slot: command_exit_zero
guard_target: docs/DECISIONS.md
guard_symbol: null
plan_slug: decision-37-migrate-then-rehome
graduated_at: '2026-08-24'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - 'from pathlib import Path; from scripts.decisions_md import parse_decisions_md, is_compacted_stub, extract_superseded_by; rows = parse_decisions_md([Path(''docs/DECISIONS.md'')]); r = next(x for x in
    rows if x[''decision_id''] == 37); b = r[''raw_block'']; assert is_compacted_stub(b), ''not a stub''; assert extract_superseded_by(b) == ''dec-116'', extract_superseded_by(b); body = [l for l in b.splitlines()
    if l.strip()][1:]; assert all(l.startswith((''**Status:**'', ''**Date:**'', ''**Decision:**'', ''**Superseded by:'', ''---'')) for l in body), ''residual pre-compaction line: %r'' % [l for l in body
    if not l.startswith((''**Status:**'', ''**Date:**'', ''**Decision:**'', ''**Superseded by:'', ''---''))]; assert ''**Superseded by: Decision 116**'' in b, ''wrong marker spelling''; print(''OK stub,
    sole superseder dec-116, no residual body'')'
