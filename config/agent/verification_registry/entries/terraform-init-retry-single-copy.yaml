check_id: terraform-init-retry-single-copy
primitive_slot: command_exit_zero
guard_target: .github/actions/terraform-init-retry/action.yml
guard_symbol: null
plan_slug: terraform-apply-chain-consolidation
graduated_at: '2026-07-22'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - 'inline=$(grep -Rh ''terraform init transient error'' .github/workflows/terraform-apply-sandbox.yml .github/workflows/reconcile.yml | wc -l | tr -d '' ''); act=$(grep -c ''terraform init transient error''
    .github/actions/terraform-init-retry/action.yml 2>/dev/null || echo 0); echo "inline=$inline action=$act"; [ "$inline" -eq 0 ] && [ "$act" -ge 1 ]

    '
