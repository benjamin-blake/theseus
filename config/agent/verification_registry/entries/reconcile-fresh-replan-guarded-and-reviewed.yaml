check_id: reconcile-fresh-replan-guarded-and-reviewed
primitive_slot: command_exit_zero
guard_target: .github/workflows/reconcile.yml
guard_symbol: null
plan_slug: terraform-recovery-semantics
graduated_at: '2026-07-22'
graduated_by: implement-agent
check_spec:
  command:
  - bash
  - -c
  - 'f=.github/workflows/reconcile.yml; g=$(grep -c ''uses: ./.github/actions/deterministic-guard'' "$f"); r=$(grep -c ''uses: ./.github/actions/subagent-plan-review'' "$f"); echo "guard=$g review=$r";
    [ "$g" -ge 2 ] && [ "$r" -ge 2 ]

    '
