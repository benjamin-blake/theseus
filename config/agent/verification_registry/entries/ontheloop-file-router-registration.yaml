check_id: ontheloop-file-router-registration
primitive_slot: command_exit_zero
guard_target: docs/contracts/file-router.yaml
guard_symbol: null
plan_slug: ontheloop-placeholder-packages
graduated_at: '2026-09-22'
graduated_by: implement-agent
check_spec:
  command:
  - bin/venv-python
  - -c
  - import sys,yaml;from pathlib import Path;from scripts.checks.structural._classify import effective_lines;p=Path('docs/contracts/file-router.yaml');txt=p.read_text();d=yaml.safe_load(txt);e=effective_lines(txt);hr=any(r['topic']=='distribution-packages'
    for r in d['routes']);print('ROUTER_EFF='+str(e));print('ROUTER_HEADROOM_OK='+str(e<=490));print('ROUTER_HAS_ROUTE='+str(hr));sys.exit(0
    if e<=490 and hr else 1)
