# Hard-deleted record IDs. The DQ runner checks this list on each run and emits
# HARD_GATE if any tombstoned ID reappears in ops_recommendations_current.
# Remove a tombstone entry once the record is physically deleted from the warehouse.
# Physical deletion is a stronger guarantee than
# the tombstone check, which only detects resurrection of still-extant rows.
tombstones: []
