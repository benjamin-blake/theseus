# Executor boundary patterns and maturity ceiling (Decision 44).
# Single source of truth -- scripts/validate.py and scripts/migrate_dq_ops_recs.py both load from here.
# Recommendations targeting boundary files must have automatable: false.
boundary_patterns:
  - execute_recommendation.py
  - scripts/executor/
  - config/agent/executor/prompts/
  - executor-supervisor
  - executor-implement
  - executor-critique
  - executor-planning
  - executor-review
  - develop-executor
  - executor-rca
  - executor/capabilities.yaml
  - scripts/llm/client.py
  - scripts/llm/utils.py
  - tool_runtime.py
  - tests/test_execute
  - tests/test_executor_
  - tests/test_llm_client
  - tests/test_llm_utils
  - tests/test_tool_runtime
  - tests/conftest.py

  # Infrastructure boundary -- executor must not self-modify these categories
  - terraform/
  - .tf
  - .github/workflows/
  - .github/actions/
  - config/lambda/
  - .claude/agents/
  - .github/prompts/
  - .github/agents/
  - CLAUDE.md
  - PLAN-
  - DECISIONS.md
  - build_lambda.py
  - run_scheduled_agent.py

# Current maturity ceiling for automatable derivation.
# Raise only via /plan -> /implement with DECISIONS.md entry.
maturity_ceiling: 1.0

# History of ceiling raises (newest first).
ceiling_history: []
