# docs/ROADMAP-PRODUCT.yaml
#
# Canonical product roadmap for the recursively-self-improving trading system.
# Sibling to docs/ROADMAP-PLATFORM.yaml. Organised around the four-layer
# trading model (Alpha / Portfolio Construction / Execution / Operations-
# Telemetry) and environment-as-config-bundle.
#
# Supersedes docs/ROADMAP-PRODUCT.md (banner added; markdown retained for one
# transition cycle as a recovery artefact).
#
# Validation is deferred to scripts/roadmap/product_roadmap.py (sibling to
# platform_roadmap.py) authored in a follow-up IMPLEMENTATION plan per the
# Section 10 gate #5 user decision. Until then this file is structurally
# defined but not Pydantic-validated; `bin/venv-python -m scripts.validate`
# does not exercise it.

document:
  id: ROADMAP-PRODUCT
  version: 1
  status: draft
  filed_via: pending_log_decision_lambda
  description: |
    Canonical product roadmap for the recursively-self-improving trading
    system. Sibling to docs/ROADMAP-PLATFORM.yaml. Organised around the
    four-layer trading model (Alpha / Portfolio Construction / Execution
    / Operations-Telemetry) and environment-as-config (research /
    backtest-canonical / paper / live-small / live-full). Supersedes
    docs/ROADMAP-PRODUCT.md.
  agent_instructions: |
    - Work is discoverable via tier_items[]. An item is eligible when status
      == "not_started" AND all depends_on entries have status == "complete"
      AND all cross_roadmap_depends_on PLATFORM entries are complete (or
      flagged GAP, in which case the PLATFORM gap must be filed before the
      product item can start).
    - depends_on entries are intra-PRODUCT only; cross_roadmap_depends_on
      carries PLATFORM:<id> or PLATFORM:GAP-<slug> references. Always
      reference items by id (e.g. L1.alpha.3); titles change, ids do not.
    - The four layers (Alpha, Portfolio Construction, Execution,
      Operations/Telemetry) are the organising principle (per PIVOT
      transcript Part C). Risk gates do NOT live at L4 - they are L2
      constraints (hedging, sizing, exposure caps in construct_portfolio's
      constraint argument) AND L3 pre-trade hooks (margin / position-cache
      / circuit-breaker checks in plan_trades' early-exit path).
      L4 = Operations/Telemetry/Feedback: LayerInvocation emission,
      Brinson attribution, deflated Sharpe, PBO, recursive-improvement
      loop. NOT Risk Gates.
    - Storage is three-tier per PIVOT Part A: broker = OLTP source of
      truth (orders, positions, cash, fills); DynamoDB = fast mutable
      tier (sub-ms cache, conditional writes for state-machine guards);
      Iceberg/DuckDB = append-only lakehouse for audit/research/history.
      Never write to Iceberg synchronously from decision hot path
      (buffered writer or SQS+Lambda batch).
    - Environments are config bundles (PIVOT Part G). Five envs: research,
      backtest_canonical, paper, live_small, live_full. Same code path
      across all; ONLY the broker_adapter and capital_allocation differ.
      Diverging backtest/live code paths is the #1 source of backtest-
      to-live performance degradation.
    - All product schemas persisted to Iceberg are Class A per the CD.25
      contract ratification ritual (PLATFORM:CD.25).
      Every persisted row carries strategy_version_id lineage AND
      _contract_version stamp.
    - Lambda-touching items carry the AGENTS.md canonical deferred_note
      string: "DEFERRED: build_lambda.py --deploy + run_scheduled_agent.py
      --smoke-test (pending Decision 67 reversal)". Deploys gate on
      PLATFORM CD.16 ratification, not CD.17.
    - STRATEGIC plan-artefact freeze (PLATFORM CD.17) applies: every L*
      item is strategic: false. L/XL effort items decompose into atomic
      IMPLEMENTATION plans during /plan; no STRATEGIC plans are filed
      while CD.17 holds.
    - The MVP v1 numeric parameters live INSIDE a versioned Class A
      contract at docs/contracts/minimum-viable-v1.yaml. NEVER inline as
      YAML scalars in this roadmap. Same discipline applies to
      promotion criteria (docs/contracts/promotion-criteria.yaml).
      Inline scalars defeat the five-property optimisability test.
    - Five-property test (parameterised / versioned / composable /
      observable / evaluable) is REQUIRED on every L1-L4 tier_item.
      Each attestation cites a path:line or a forward-pointing tier_item
      id. Bare prose attestations FAIL the audit definition-of-done.
    - Pre-codegen contract ratification (PLATFORM CD.25) gates every
      docs/contracts/<slug>.yaml. New product contracts are authored as
      follow-up REPORT-ONLY plans, NOT inline in this audit.
    - Options trading is PIVOT-DEFERRED until the stock system is live
      and stable. See known_gaps[].KG.options for the hypothesis trigger.
    - Per AGENTS.md: no emojis, no em dashes; plain ASCII hyphens.
      Single Portal Invariant applies to ops_recommendations + ops_decisions
      writes - extended use for trading drift events (PIVOT Part A
      tables/components table), not a new write portal.
    - Contract-class taxonomy (per docs/INTENT-pre-codegen-contract-
      ratification.md, summarised in-file per Checkpoint 4 F8 fix):
        Class A: data schemas that become persistent durable storage
          (Iceberg DDL, DynamoDB table shape). Every Class A row stamps
          its _contract_version column. Examples: signals, target_portfolios,
          fills, alpha_versions, pc_versions, exec_versions, strategy_runs,
          tca_events, promotion_events, orders, positions_cache. Amendments
          default forward-compatible; semantic breaks require explicit
          opt-in via ratified amendment plan.
        Class B: public agent surfaces (Lambda Function URL verb contracts).
          Carry typed payload shapes + typed error codes + auth model.
          Examples: promote_strategy, get_cost_curve, reconciliation
          Lambda. Class B contracts ratify post-PLATFORM-CD.16 with
          provisional_v0 + staggered re-ratification triggers.
        Class C: cross-system invariants + in-process Protocols /
          Pydantic-only configs that span multiple processes but don't
          persist to a DDL. Examples: BrokerAdapter Protocol, alpha()/
          construct_portfolio()/plan_trades() Protocols, MarketState,
          AlphaParams, PCParams, ExecParams, Constraint set,
          EnvironmentConfig, PromotionCriteria.
    - PIVOT TRANSCRIPT reference convention: tier_items, candidate_decisions,
      and cross_tier_gates cite "PIVOT Part A/B/C/D/E/F/G" pointing at
      docs/AUDIT-PROMPT-product-roadmap-yaml.md Section 11. Part A =
      data architecture; Part C = four-layer model; Part E = identifiability
      + execution + funnel + repo; Part G = hedging + options + envs +
      separation. Parts B/D/F are user questions only.
    - Tier_items are grouped by layer prefix (L0/L1/L2/L3/L4/D.fast/D.lake/
      E.env/MVP) rather than sorted lexicographically. Within each prefix
      items appear in the order they are introduced.

  gate_helpers:
    # PLATFORM helpers inherited via gate_helpers grammar:
    #   tier_complete/1, all_in_tier_with_status/2, grace_period_elapsed/2,
    #   item_field_eq/3 (see docs/ROADMAP-PLATFORM.yaml document.gate_helpers).
    # PRODUCT-local helpers below. All scope=product_local per Subagent G.
    - name: deflated_sharpe
      arity: 3
      params:
        - name: strategy_id
          type: str
        - name: env
          type: str
        - name: window_days
          type: int
      returns: float
      semantics: |
        Lopez de Prado deflated Sharpe ratio over window for strategy in
        env. Corrects for selection bias on multiple testing. Evaluator
        queries strategy_runs Iceberg table; returns float. Per PIVOT
        Part E statistical-hygiene discipline.
      scope: product_local
    - name: max_drawdown
      arity: 3
      params:
        - name: strategy_id
          type: str
        - name: env
          type: str
        - name: window_days
          type: int
      returns: float
      semantics: |
        Maximum peak-to-trough drawdown over window for strategy in env.
        Evaluator queries strategy_runs Iceberg table. Returns positive
        float (e.g. 0.12 = 12% drawdown).
      scope: product_local
    - name: consecutive_losing_weeks
      arity: 2
      params:
        - name: strategy_id
          type: str
        - name: env
          type: str
      returns: int
      semantics: |
        Current consecutive losing weeks for strategy in env. Per PIVOT
        Part E promotion criterion ("no more than 3 consecutive losing
        weeks"). Evaluator queries weekly Sharpe deltas from strategy_runs.
      scope: product_local
    - name: implementation_shortfall_within_budget
      arity: 3
      params:
        - name: strategy_id
          type: str
        - name: env
          type: str
        - name: budget_bps
          type: float
      returns: bool
      semantics: |
        True iff mean per-fill implementation shortfall in bps over the
        evaluation window is <= budget_bps. Reads tca_events Iceberg
        table. Per PIVOT Part E live_small -> live_full gate.
      scope: product_local
    - name: ic_oos
      arity: 2
      params:
        - name: alpha_version_id
          type: str
        - name: window_days
          type: int
      returns: float
      semantics: |
        Out-of-sample Information Coefficient for alpha_version over
        window. Per-day rank-correlation between predicted and realised
        returns, averaged. Sizing-agnostic L1 metric per PIVOT Part E
        identifiability fix.
      scope: product_local
    - name: ir_oos
      arity: 2
      params:
        - name: alpha_version_id
          type: str
        - name: window_days
          type: int
      returns: float
      semantics: |
        Out-of-sample Information Ratio of the signal (IC mean / IC std)
        for alpha_version over window. Signal stability over time. Per
        PIVOT Part E.
      scope: product_local
    - name: code_path_verification_present_for
      arity: 1
      params:
        - name: strategy_version_id
          type: str
      returns: bool
      semantics: |
        True iff a runtime-recorded code-path verification exists for
        the given strategy_version_id, attesting that the backtest and
        live invocations used the same code path (only broker_adapter
        differed). Demoted from code_path_hash because the hash mechanism
        is its own tier_item (E.env.10 backtest engine integration).
      scope: product_local
    - name: live_full_uptime_days
      arity: 1
      params:
        - name: strategy_id
          type: str
      returns: int
      semantics: |
        Continuous uptime days for strategy in live_full environment.
        Gates downstream items that need production stability evidence.
      scope: product_local
    - name: consecutive_quarters_positive_sharpe
      arity: 1
      params:
        - name: strategy_id
          type: str
      returns: int
      semantics: |
        Number of consecutive quarters with positive realised Sharpe in
        live_full. Long-horizon stability metric.
      scope: product_local
    - name: all_contract_gates_ratified
      arity: 1
      params:
        - name: item_id
          type: str
      returns: bool
      semantics: |
        True iff every docs/contracts/<slug>.yaml entry in this
        tier_item's contract_gates[] has status == "ratified". In-Python
        evaluator over docs/contracts/ git tree (NOT warehouse).
      scope: product_local
    - name: reconciliation_active_for_env
      arity: 1
      params:
        - name: env
          type: str
      returns: bool
      semantics: |
        True iff the reconciliation loop (E.env.8) is running on schedule
        for env, with last successful reconciliation event within the
        last hour. Reads ops_reconciliations Iceberg table.
      scope: product_local
    - name: survivorship_inclusive_stats_verified
      arity: 1
      params:
        - name: strategy_id
          type: str
      returns: bool
      semantics: |
        True iff promotion evaluation for strategy_id includes statistics
        from every pipeline that ever existed for the strategy family
        (dead or alive). Per PIVOT Part E survivorship-bias defence.
        In-Python evaluator over strategies_versions + promotion_events.
      scope: product_local

north_star:
  principles:
    - id: NSP.1
      principle: Discipline first, optimisation second.
      detail: |
        The frontier is reached by rigorous structure that admits clever
        algorithms when statistical power justifies them, NOT by clever
        algorithms applied to broken structure. (PIVOT transcript Part E
        TL;DR verbatim.)
    - id: NSP.2
      principle: Broker is the OLTP source of truth.
      detail: |
        Orders, positions, cash, and fills are the broker's authoritative
        responsibility. Our system is a client of the broker's OLTP, not
        its source of truth. ACID is the broker's problem.
    - id: NSP.3
      principle: Same code path across environments.
      detail: |
        Backtest, paper, live_small, live_full all use identical code;
        only the BrokerAdapter Protocol implementation and capital
        allocation differ. Diverging code paths is the #1 source of
        backtest-to-live degradation.
    - id: NSP.4
      principle: Pipeline is the unit of optimisation.
      detail: |
        Alpha + Portfolio Construction + Execution + risk gates are
        backtested together. Alpha-only optimisation is not promotable -
        joint identifiability between alpha quality and sizing quality
        is the killer bias.
    - id: NSP.5
      principle: Layer 2 and Layer 3 quality dominate Layer 1 quality at retail scale.
      detail: |
        At small-to-medium size, execution quality and risk discipline
        matter more than signal quality. Renaissance Medallion's edge is
        as much in execution and capacity management as in signal.
    - id: NSP.6
      principle: Promotion is config, never override.
      detail: |
        Pre-registered promotion criteria are ratified Class A contracts
        (docs/contracts/promotion-criteria.yaml). Promotion is a typed
        verb with criteria-check baked in. You cannot override your own
        past self when recent data feels good.
    - id: NSP.7
      principle: Recursive improvement is the loop, not a feature.
      detail: |
        Every layer invocation emits LayerInvocation telemetry.
        Underperforming components become recommendations via the
        ops portal. The autonomous-improvement substrate extends from
        platform to trading without architectural change.
    - id: NSP.8
      principle: Every component is parameterised, versioned, composable, observable, evaluable.
      detail: |
        The five-property optimisability test. Hardcoded magic numbers
        fail #1; anonymous functions fail #2; tight coupling fails #3;
        hidden state fails #4; "I'll know it when I see it" fails #5.
        Every L1-L4 tier_item attests to all five with path:line cites.
    - id: NSP.9
      principle: Append-only journaling; broker is OLTP, lakehouse is history.
      detail: |
        Iceberg never serves as a position store. DynamoDB carries
        sub-ms mutable state. Hot-path writes batch to Iceberg via
        buffered writer or SQS+Lambda. Reconciliation between broker and
        DynamoDB cache is non-negotiable.
    - id: NSP.10
      principle: Hedging is L2 constraint, not a new layer.
      detail: |
        For sole-quant scale, default to size-down over hedging. Hedging
        costs money; sizing-down is free. Revisit when factor model is
        trustworthy and alpha Sharpe survives halving from hedging costs.

current_state:
  # Grounded in Subagent E's code-surface audit (docs/audit-reports/wave-1-outputs/E.md).
  description: |
    Data plane is production-grade; trading plane is mostly scaffold or
    absent. The four-layer model surfaces are absent or partial.
  working:
    - "Market-data pipeline (src/data/pipeline.py): Step Functions orchestration; FTSE 100 daily; ~18 native feature columns + delta columns"
    - "Iceberg write path (src/data/writer.py): awswrangler MERGE upsert; CD.9 partitioned"
    - "Formula discovery (src/lab/pysr_factory.py): PySR wired to Iceberg; safe sympy evaluation; max_drawdown metric"
    - "Pydantic Annotated schema substrate (src/schemas/{rec,decision,annotations}.py): RecPayload + DecisionPayload with DqXxx markers per PLATFORM:T0.12"
    - "Agent telemetry (src/data/handlers/agent_telemetry.py): LayerInvocation-style instrumentation for scheduled agents"
  partial_or_scaffold:
    - path: src/live/rat_ensemble.py
      issue: "RATModel.predict raises NotImplementedError (src/live/rat_ensemble.py:90); VectorMemory.retrieve_similar always returns [] (src/live/rat_ensemble.py:78); RATEnsemble.predict uses np.mean instead of AdaptiveEnsemble"
      classification: L1 scaffold
    - path: src/execution/async_engine.py
      issue: "L3 surface is a latency-penalty sizer that print()s Trade dataclasses (src/execution/async_engine.py:292); no broker, no order_id, no submission"
      classification: L3 scaffold
    - path: src/meta_learner/gating_network.py
      issue: "AdaptiveEnsemble fully implemented but unwired; RATEnsemble.predict bypasses it"
      classification: L1 unwired
    - path: src/main.py
      issue: "Live mode wires mock_signal_generator and mock_market_data_source (src/main.py:129, src/main.py:139)"
      classification: L1+L3 mocked
  missing_surfaces:
    # Verified absent via grep (Subagent E)
    - EnvironmentConfig Pydantic
    - BrokerAdapter Protocol
    - Portfolio-construction methods (cvxpy / Optuna / risk_parity / equal_weight / black_litterman)
    - Pre-trade gate / kill-switch / circuit-breaker (trading-specific; not the executor-killswitch which exists)
    - TCA / cost-curve aggregation
    - Per-env table-prefix logic (research_*, paper_*, live_*)
    - DynamoDB orders/positions tables (only ops counter exists)
    - client_order_id mechanism
    - Reconciliation loop
    - Backtest engine using same code path as live
    - Three-place state machine (broker / DynamoDB conditional / app)
    - alpha_versions / pc_versions / exec_versions / strategies_versions Iceberg tables
    - LayerInvocation event schema and emission infrastructure
    - Brinson attribution / deflated Sharpe / PBO computation
    - promote_strategy() typed verb
  layer_rollup:
    L1_alpha: scaffold (RATEnsemble + PySR present but Protocol-less; mock generator in main.py)
    L2_portfolio_construction: absent (entirely)
    L3_execution: latency-penalty sizer (no broker; no order lifecycle)
    L4_operations_telemetry: agent_telemetry only (no Brinson, deflated Sharpe, PBO)
    risk_sublayer: absent (no pre-trade gates, no circuit breaker, no hedging constraints)

four_layer_model:
  # Per PIVOT TRANSCRIPT Part C. L4 = Operations/Telemetry/Feedback - NOT
  # Risk Gates. Risk gates live as (a) L2 constraints and (b) L3 pre-trade hooks.
  - layer: alpha
    name: Alpha / Signal Generation
    responsibility: |
      Produces a score per asset: expected return, direction+confidence,
      ranking, or some scalar that says "I think this is hot." User's
      domain - formulas, models, PySR, ML, factor combinations.
    interface: "alpha(market_state: MarketState, params: AlphaParams) -> Signal"
    inputs: MarketState (per-symbol feature vectors at decision_time)
    outputs: Signal (per-symbol score)
    contract_refs:
      - {path: docs/contracts/alpha-protocol.yaml, class: C, contract_version: 1}
      - {path: docs/contracts/signal-payload.yaml, class: A, contract_version: 1}
      - {path: docs/contracts/market-state.yaml, class: C, contract_version: 1}
      - {path: docs/contracts/alpha-params.yaml, class: C, contract_version: 1}
      - {path: docs/contracts/alpha-versions.yaml, class: A, contract_version: 1}
  - layer: portfolio_construction
    name: Portfolio Construction
    responsibility: |
      Translates signals + constraints + risk model into target positions.
      "Given these scores, how much of each should I hold?" Outputs target
      portfolio (vector of weights or share counts). High-leverage middle
      layer that retail-quant content ignores.
    interface: "construct_portfolio(signal: Signal, current_portfolio: Portfolio, params: PCParams) -> TargetPortfolio"
    inputs: Signal, Portfolio (current state), Constraint set, CostCurve (consumed from PLATFORM TCA)
    outputs: TargetPortfolio (weight vector or share-count vector)
    contract_refs:
      - {path: docs/contracts/pc-protocol.yaml, class: C, contract_version: 1}
      - {path: docs/contracts/target-portfolio.yaml, class: A, contract_version: 1}
      - {path: docs/contracts/pc-versions.yaml, class: A, contract_version: 1}
      - {path: docs/contracts/constraint-set.yaml, class: C, contract_version: 1}
      - {path: docs/contracts/cost-curve.yaml, class: A, contract_version: 1}
    contains_risk_constraints: true
    notes: |
      Hedging is a Layer-2 CONSTRAINT, not a new layer (PIVOT Part G).
      Beta/dollar/sector/factor/currency/pairs hedges are linear
      constraints in the optimiser. Tail hedging is a separate
      insurance-sleeve concern outside L2. For sole-quant scale, default
      to size-down rather than hedging. Hard drawdown circuit breaker is
      also an L2 constraint (NOT an L4 surface).
  - layer: execution
    name: Execution
    responsibility: |
      Translates (target portfolio - current portfolio) into orders sent
      to broker. "How do I get from where I am to where I want to be,
      given transaction costs, slippage, microstructure?" At retail scale,
      boring-correct dominates clever.
    interface: "plan_trades(target: TargetPortfolio, current: Portfolio, params: ExecParams) -> list[Order]"
    inputs: TargetPortfolio, Portfolio (current), ExecParams, live market data via BrokerAdapter
    outputs: list[Order] submitted via BrokerAdapter.submit_order, Fills observed via BrokerAdapter.get_fills
    contract_refs:
      - {path: docs/contracts/exec-protocol.yaml, class: C, contract_version: 1}
      - {path: docs/contracts/broker-adapter.yaml, class: C, contract_version: 1}
      - {path: docs/contracts/order.yaml, class: A, contract_version: 1}
      - {path: docs/contracts/fill.yaml, class: A, contract_version: 1}
      - {path: docs/contracts/exec-versions.yaml, class: A, contract_version: 1}
      - {path: docs/contracts/exec-params.yaml, class: C, contract_version: 1}
      - {path: docs/contracts/implementation-shortfall.yaml, class: A, contract_version: 1}
    contains_pretrade_gates: true
    notes: |
      Pre-trade gates (margin check, position-cache check, circuit-
      breaker query) are L3 hooks that consult L2 constraint state.
      Execution mechanics are boring (marketable-limit at 10:00 ET);
      execution POLICY thresholds (liquidity gating, spread gating,
      vol-conditional timing) are tuned from live TCA telemetry, NOT
      backtests. No neural networks at L3.
  - layer: operations_telemetry
    name: Operations / Telemetry / Feedback
    responsibility: |
      Captures every layer invocation as a structured event, computes
      attribution (selection vs sizing vs execution), computes
      statistical-hygiene metrics (deflated Sharpe, PBO), and feeds the
      recursive improvement loop. The diagnostic layer that answers
      "strategy v17 underperforms v12 - is alpha worse? PC changed?
      Execution costlier? Platform behind a stable contract?"
    interface: "LayerInvocation event emission per layer per invocation"
    inputs: LayerInvocation events from L1/L2/L3; fills; signals; target_portfolios
    outputs: Brinson attribution rows, deflated Sharpe / PBO records, drift-detector recs filed via ops portal
    contract_refs:
      - {path: docs/contracts/layer-invocation.yaml, class: A, contract_version: 1}
      - {path: docs/contracts/brinson-attribution.yaml, class: A, contract_version: 1}
      - {path: docs/contracts/deflated-sharpe.yaml, class: A, contract_version: 1}
      - {path: docs/contracts/pbo.yaml, class: A, contract_version: 1}
      - {path: docs/contracts/promotion-events.yaml, class: A, contract_version: 1}
    notes: |
      L4 is OPERATIONS / TELEMETRY / FEEDBACK per PIVOT Part C diagram,
      NOT Risk Gates. The diagnostic question ("did the alpha-version
      change? did the PC-version change? did the exec-version change?")
      assumes L4 = telemetry that observes versioned L1-L3 components.

three_tier_data_architecture:
  description: |
    Per PIVOT TRANSCRIPT Part A. Three tiers with one-way data flow
    between them. Lakehouse is never a position store.
  diagram: |
    +----------------------------------------------------+
    | Broker (IBKR / Alpaca / etc.)                       |  OLTP source of truth
    | - canonical positions, cash, order state machine    |  for orders, positions, cash
    | - native client_order_id idempotency                |
    | - their problem to keep ACID, not yours             |
    +-------------------+---------------------------------+
                        | API + WebSocket
                        v
    +----------------------------------------------------+
    | Fast tier (DynamoDB)                                |  small, mutable, sub-ms
    | - idempotency keys for in-flight submissions        |
    | - in-flight orders cache (submitted, not yet acked) |
    | - position cache (refreshed from broker, drift-mon) |
    | - precomputed features keyed by symbol              |
    | - risk-limit current exposures                      |
    +-------------------+---------------------------------+
                        | append events
                        v
    +----------------------------------------------------+
    | Lakehouse (S3 + Iceberg + DuckDB)                   |  append-only history
    | - immutable event journal: decisions, submissions,  |
    |   acks, fills, reconciliations                      |
    | - market data history (batch-appended)              |
    | - backtest features, model artefacts                |
    | - reconciliation snapshots                          |
    +----------------------------------------------------+
  tiers:
    - tier: broker_oltp
      name: Broker (OLTP source of truth)
      description: |
        The broker (IBKR / Alpaca / etc.) is the authoritative system of
        record for orders, positions, cash, fills. Our system is a
        client, not the source of truth. Native client_order_id
        idempotency. ACID is the broker's problem.
      examples: [alpaca, interactive_brokers]
      write_authority: external
      contract_refs:
        - {path: docs/contracts/broker-adapter.yaml, class: C, contract_version: 1}
    - tier: fast_tier
      name: DynamoDB fast tier (sub-ms mutable cache)
      description: |
        Small mutable state outside the Single Portal Invariant because
        it is a derivative projection of the warehouse, not a write
        source. Conditional writes provide the OLTP primitives we
        actually need.
      rationale_for_dynamodb_not_postgres: |
        Single-writer eliminates MVCC wins; AWS-serverless-native;
        already in stack via ops portal ID allocation; key-value query
        patterns; <$1/mo on-demand; conditional writes (attribute_not_exists,
        condition: current_status IN ('submitted')) cover 95% of Postgres'
        transactional semantics for this workload.
      revisit_dynamodb_decision_when:
        - "Multiple concurrent decision agents write to the same positions (multi-strategy with shared book)"
        - "Pre-trade filters require complex relational predicates (e.g., reject any order that would create position correlated >0.7 with existing top-3 holdings)"
        - "Multi-region resilience required (Aurora Global / Yugabyte enters conversation)"
      tables:
        - name: orders
          pk: client_order_id
          write_pattern: PutItem with attribute_not_exists; UpdateItem with conditional state-machine guards
          owning_layer: execution
          contract_ref: {path: docs/contracts/dynamodb-orders.yaml, class: A, contract_version: 1}
        - name: positions_cache
          pk: symbol
          write_pattern: Refresh from broker on schedule + after fills via cross-item transactions
          owning_layer: execution
          contract_ref: {path: docs/contracts/dynamodb-positions-cache.yaml, class: A, contract_version: 1}
        - name: features_serving
          pk: symbol
          write_pattern: ONE-WAY projection from Iceberg via nightly batch (DuckDB over Iceberg). Live loop is read-only.
          owning_layer: alpha
          contract_ref: {path: docs/contracts/dynamodb-features-serving.yaml, class: A, contract_version: 1}
    - tier: lakehouse
      name: Lakehouse (S3 + Iceberg + DuckDB, append-only)
      description: |
        Append-only journal for audit, research, market-data history.
        Single Portal Invariant applies. Hot path writes via buffered
        writer or SQS -> Lambda batch-append; NEVER synchronous from
        decision loop.
      write_hot_path_rule: NEVER synchronous; buffered writer or SQS+Lambda batching
      write_market_data_rule: WebSocket -> in-memory ring buffer for decisions; batch-write every ~1 minute for history
      tables:
        - {name: strategies_versions, owning_layer: operations_telemetry}
        - {name: alpha_versions, owning_layer: operations_telemetry}
        - {name: pc_versions, owning_layer: operations_telemetry}
        - {name: exec_versions, owning_layer: operations_telemetry}
        - {name: strategy_runs, owning_layer: operations_telemetry}
        - {name: signals, owning_layer: alpha}
        - {name: target_portfolios, owning_layer: portfolio_construction}
        - {name: fills, owning_layer: execution}
        - {name: ops_trade_events, owning_layer: execution}
        - {name: ops_reconciliations, owning_layer: execution}
        - {name: tca_events, owning_layer: execution}
        - {name: pnl_attribution, owning_layer: operations_telemetry, kind: view}
        - {name: promotion_events, owning_layer: operations_telemetry}
        - {name: ops_recommendations, owning_layer: operations_telemetry, kind: extended_use, note: "PIVOT Part A tables/components table extends ops_recommendations for trading drift events; reconciliation drift fires a critical rec via existing portal flow, not a new table"}
  what_not_to_do:
    - "Don't write to Iceberg synchronously from the hot path - commits are seconds-scale; use buffered writer or SQS+Lambda batching"
    - "Don't try to make the lakehouse the live position store - snapshot isolation at second-scale freshness is wrong for trading"
    - "Don't hand-roll concurrency primitives - DynamoDB cross-item transactions exist; use them for order+position-cache pair"
    - "Don't skip reconciliation - even with two-layer idempotency, network partitions can leave local view divergent from broker"
    - "Don't put live market data in Iceberg in real-time - stream to in-memory ring buffer; batch-write to Iceberg every ~1 minute"
    - "Don't introduce Kafka / Kinesis prematurely - at single-agent volume, SQS + Lambda is sufficient"

environments:
  description: |
    Per PIVOT TRANSCRIPT Part G. Five-stage taxonomy. Environment is a
    config bundle, not a deployment - same code, different broker_adapter
    and capital_allocation. Industry-leading firms call this deploy-once,
    configure-per-environment.
  envs:
    - env: research
      code: pre_merge_exploratory
      data: production_historical_readonly
      broker_adapter: none
      capital_allocation: none
      telemetry_destination: research_sandbox
      table_prefix: research_
    - env: backtest_canonical
      code: merged_versioned
      data: frozen_canonical_dataset
      broker_adapter: HistoricalSimulationBroker
      capital_allocation: notional
      telemetry_destination: backtest_results_table
      table_prefix: backtest_
    - env: paper
      code: merged_versioned
      data: realtime_market
      broker_adapter: PaperBroker
      capital_allocation: notional
      telemetry_destination: production_telemetry_tagged_paper
      table_prefix: paper_
    - env: live_small
      code: merged_versioned
      data: realtime_market
      broker_adapter: AlpacaBroker
      capital_allocation: small_allocation
      telemetry_destination: production_telemetry
      table_prefix: live_
    - env: live_full
      code: merged_versioned
      data: realtime_market
      broker_adapter: AlpacaBroker
      capital_allocation: full_allocation
      telemetry_destination: production_telemetry
      table_prefix: live_
  invariants:
    - "Same code path across all environments. Only broker_adapter and config differ."
    - "One-way data flow: production telemetry feeds research; research never writes to production tables."
    - "Capital allocation is the production gate - promotion is a config change, not a deploy."
    - "Backtest data is frozen, versioned, survivor-bias-corrected. 'I re-ran the backtest and got different numbers' is a bug."
    - "Paper trading uses production infrastructure - same Function URL, IAM, telemetry as live."
    - "Promotion is a typed action with audit trail via promote_strategy() verb + promotion_events Iceberg table."
  isolation_pattern: pattern_c
  isolation_pattern_rationale: |
    Single AWS account, IAM + table-prefix isolation. Per PIVOT Part G
    + PLATFORM CD.6. Multi-account (Pattern A) is industrial overkill
    for sole dev. Pattern C invariants enforced:
    - Iceberg tables prefixed (research_strategy_runs, paper_strategy_runs, live_strategy_runs).
    - DynamoDB tables prefixed similarly.
    - Separate IAM roles per env with policies enforcing prefix boundaries.
    - Broker creds in separate Secrets Manager entries per env.
    - Asymmetric: research role can READ live telemetry; CANNOT WRITE to live tables.
  continuous_vs_on_demand:
    research: on_demand
    backtest_canonical: on_demand_triggered_by_strategy_versions_or_ci
    paper: continuous_mirrors_live_trading_hours
    live_small: continuous_market_hours_only
    live_full: continuous_market_hours_only

evaluation_metrics:
  layer_1_alpha:
    # Sizing-agnostic per PIVOT Part E identifiability fix
    - name: ic
      description: Information Coefficient - per-day rank correlation between predicted and realised returns; sizing-agnostic
      sizing_dependent: false
    - name: ir
      description: Information Ratio of the signal - IC mean / IC std; signal stability over time
      sizing_dependent: false
    - name: quantile_spread
      description: Top-decile minus bottom-decile realised return
      sizing_dependent: false
    - name: hit_rate_magnitude_weighted
      description: Fraction of correct direction predictions weighted by realised magnitude
      sizing_dependent: false
    - name: signal_decay_half_life
      description: Time horizon over which the alpha's IC decays to half its initial value
      sizing_dependent: false
    - name: alpha_pairwise_ic_correlation
      description: Pairwise IC correlation between alpha versions; L1 orthogonality target (frontier = 100+ orthogonal alphas)
      sizing_dependent: false
  layer_2_portfolio_construction:
    - name: brinson_attribution_sizing
      description: P&L attributed to sizing decisions holding alpha fixed
      sizing_dependent: true
    - name: turnover_adjusted_sharpe
      description: Sharpe net of explicit transaction costs
      sizing_dependent: true
    - name: realised_constraint_violation_rate
      description: Frequency of constraint hits (max position, sector cap, drawdown breach) per evaluation window
      sizing_dependent: true
  layer_3_execution:
    - name: implementation_shortfall
      description: (fill_price - decision_price) * sign(side) in bps; bucketed by (time-of-day, spread quintile, ADV quintile, vol regime, side)
      sizing_dependent: true
    - name: slippage_vs_expected
      description: Realised minus modelled slippage per bucket
      sizing_dependent: true
    - name: execution_fill_rate
      description: Filled / submitted ratio; gates retry-then-rec policy effectiveness
      sizing_dependent: false
  layer_4_operations_telemetry:
    # `role` distinguishes attribution / observability / drift_detector
    - name: layer_invocation_coverage
      description: Fraction of L1/L2/L3 invocations that successfully emit a LayerInvocation event; gates the recursive improvement loop
      sizing_dependent: false
      role: observability
    - name: brinson_full_attribution
      description: Per-strategy-version P&L decomposed across selection (L1) / sizing (L2) / execution (L3) / residual; the diagnostic surface
      sizing_dependent: true
      role: attribution
    - name: paper_vs_live_divergence
      description: For the same strategy_version_id in both paper and live, gap in fill prices / Sharpe / drawdown; execution-quality drift detector
      sizing_dependent: true
      role: drift_detector
    - name: reconciliation_drift_rate
      description: Frequency and magnitude of broker-vs-DynamoDB position-cache divergence events
      sizing_dependent: false
      role: drift_detector
    - name: code_path_hash_divergence
      description: Hash-mismatch events between backtest and live code paths for the same strategy_version_id; structural integrity check
      sizing_dependent: false
      role: drift_detector
  portfolio_level:
    - name: deflated_sharpe
      description: Lopez de Prado deflated Sharpe correcting for selection bias on multiple testing
    - name: probability_of_backtest_overfitting
      description: PBO check; gates promotion under multiple-hypothesis-testing discipline
    - name: max_drawdown
      description: Maximum peak-to-trough drawdown over evaluation window
    - name: profit_factor
      description: Sum of gains / absolute sum of losses
    - name: consecutive_losing_weeks
      description: Current run of weekly negative-Sharpe periods

minimum_viable_v1:
  # Per PIVOT TRANSCRIPT Part C "Minimum viable trading system, opinionated."
  # CRITICAL: numeric parameters live INSIDE a versioned Class A contract
  # at docs/contracts/minimum-viable-v1.yaml. NEVER as YAML scalars in this
  # roadmap. Inline numbers would fail the five-property optimisability
  # test (property #1: Parameterised - explicit, typed inputs).
  description: |
    Names the v1 composition. Numeric parameters (top_n, max_position_weight,
    rebalance_threshold_epsilon, drawdown_circuit_breaker.{lookback_days,
    drawdown_threshold, cash_days}, cash_buffer_min, spread_aggression_bps,
    submit_time, retry_policy, reconciliation_cadence_minutes) live inside
    the versioned PortfolioConstructionVersion / ExecutionPolicyVersion
    artefacts named below.
  composition_ref:
    strategy_version_id: "mvp_v1@<hash_pending_contract_ratification>"
    alpha_version_ref: "formula_score_top_n@<hash>"
    pc_version_ref: "equal_weight_top_n_v1@<hash>"
    exec_version_ref: "marketable_limit_v1@<hash>"
    risk_constraints_ref: "L2_v1_constraints@<hash>"
    environment_config_ref: "EnvironmentConfig.live_small[mvp_v1]"
  contract_refs:
    - path: docs/contracts/minimum-viable-v1.yaml
      class: A
      contract_version: 1
      contains: |
        - alpha.top_n (PIVOT Part C: "Top-N ranking, N=20" as DEFAULT, tunable under Bayesian-opt)
        - portfolio_construction.method (equal_weight_top_n OR risk_parity_top_n)
        - portfolio_construction.max_position_weight
        - portfolio_construction.gross_exposure_max
        - portfolio_construction.rebalance.{policy: threshold, epsilon}
        - portfolio_construction.drawdown_circuit_breaker.{lookback_days, drawdown_threshold, cash_days}
        - portfolio_construction.cash_buffer_min
        - execution.order_type (marketable_limit)
        - execution.spread_aggression_bps
        - execution.submit_time (10:00 ET daily)
        - execution.retry_policy (single_retry_then_file_rec)
        - operations.reconciliation_cadence_minutes
  pivot_quote_reference: |
    PIVOT TRANSCRIPT Section 11 Part C: "Minimum viable trading system,
    opinionated" block. Auditor cites verbatim in docs/contracts/
    minimum-viable-v1.yaml when authoring that contract in a follow-up plan.

promotion_funnel:
  # Per PIVOT TRANSCRIPT Part E four-stage funnel. Pre-registered promotion
  # criteria are themselves a ratified Class A contract; numeric thresholds
  # live inside the contract, NEVER as YAML scalars here.
  description: |
    Four-stage funnel with pre-registered promotion criteria. Skipping
    stages is the #1 retail-quant failure mode; each stage discovers a
    different class of bug. Stage criteria themselves are versioned
    Class A contracts.
  criteria_ref:
    path: docs/contracts/promotion-criteria.yaml
    class: A
    contract_version: 1
    contains: |
      Per-stage promotion criteria (numeric thresholds), drawn verbatim
      from PIVOT Part E "Promotion criteria, pre-registered" block:
        - research_backtest -> out_of_sample_paper:
            walk_forward_cv_passed; deflated_sharpe filter threshold;
            probability_of_backtest_overfitting check
        - out_of_sample_paper -> live_small:
            min_duration_days; deflated_sharpe_threshold;
            max_drawdown_threshold; consecutive_losing_weeks_cap;
            same_code_path_verified; survivorship_inclusive_stats_required
        - live_small -> live_full:
            min_duration_days; deflated_sharpe_threshold;
            max_drawdown_threshold;
            implementation_shortfall_within_budget;
            survivorship_inclusive_stats_required
        - production_ensemble:
            weighting (vol_scaled_equal_weight); rebalance_cadence (monthly)
      Threshold numbers authored verbatim inside the contract YAML, NOT here.
  pivot_quote_reference: |
    PIVOT TRANSCRIPT Section 11 Part E "Promotion criteria, pre-registered"
    + "Production ensemble weighting" blocks.
  stages:
    - stage: research_backtest
      population_size_target: hundreds
      data: frozen_canonical
      broker_adapter: HistoricalSimulationBroker
      purpose: cheap_exploration_filter_obviously_broken
      criteria_contract: docs/contracts/promotion-criteria.yaml#research_backtest_to_paper
    - stage: out_of_sample_paper
      population_size_target: 5_to_10
      data: realtime_market
      broker_adapter: PaperBroker
      purpose: data_pipeline_bugs_realtime_edge_cases_lookahead_bias
      criteria_contract: docs/contracts/promotion-criteria.yaml#paper_to_live_small
    - stage: live_small
      population_size_target: 3_to_5
      data: realtime_market
      broker_adapter: AlpacaBroker
      capital_allocation: small
      purpose: true_cost_discovery_via_TCA
      criteria_contract: docs/contracts/promotion-criteria.yaml#live_small_to_live_full
    - stage: live_full
      population_size_target: 3_to_5
      data: realtime_market
      broker_adapter: AlpacaBroker
      capital_allocation: full
      purpose: production_ensemble_member
      criteria_contract: docs/contracts/promotion-criteria.yaml#ensemble_weighting
  concurrent_paper_alongside_live: true
  champion_challenger: true
  ensemble_rule: |
    Vol-scaled equal-weight: each live pipeline gets weight 1/N *
    (target_vol / realised_vol_of_pipeline). Monthly rebalance. RESIST
    dynamic / performance-weighted ensembles initially - recent performance
    is mean-reverting noise; dynamic weighting introduces correlation that
    defeats diversification.

candidate_decisions:
  # CDP.1, CDP.2 ratified at audit close (subject to log-decision Lambda).
  # All others drawn from PIVOT TRANSCRIPT Parts A-G with verbatim/tight-
  # paraphrase quotes in `detail`. The candidate_decisions_research_pool
  # block lists candidates surfaced but not lifted to CDPs.
  - id: CDP.1
    title: Adopt docs/ROADMAP-PRODUCT.yaml as canonical product roadmap
    detail: |
      ROADMAP-PRODUCT.yaml is the source of truth for product sequencing.
      Supersedes docs/ROADMAP-PRODUCT.md (banner added; legacy markdown
      retained for one transition cycle as recovery artefact, deletion in
      follow-up commit per Section 10 gate #4 user decision).
    gates: [L0, L1, L2, L3, L4, D, E, MVP]
    state: pending
  - id: CDP.2
    title: Four-layer trading model is the organising principle
    detail: |
      Per PIVOT Part C diagram. L1 = Alpha (signal generation), L2 =
      Portfolio Construction (sizing + constraints + hedging), L3 =
      Execution (broker-facing), L4 = Operations / Telemetry / Feedback.
      Risk gates are NOT a fourth layer - they live as L2 constraints
      (hedging-as-constraint, max position, sector caps, gross/net
      exposure, drawdown circuit breaker) AND L3 pre-trade hooks (margin
      check, position-cache check, circuit-breaker query).
    gates: [L1, L2, L3, L4]
    state: pending
  - id: CDP.3
    title: Broker is the OLTP source of truth
    detail: |
      Per PIVOT Part A. The broker (IBKR / Alpaca / etc.) is the
      authoritative system of record for orders, positions, cash, fills.
      Our system is a client of the broker's OLTP, not its source of
      truth. ACID is the broker's problem.
    gates: [L3, D.fast, E.env]
    state: pending
  - id: CDP.4
    title: Three-tier data architecture (Broker / DynamoDB / Iceberg)
    detail: |
      Per PIVOT Part A. Fast tier = DynamoDB (sub-ms cache, conditional
      writes); Lakehouse = Iceberg/DuckDB (append-only history). Iceberg
      is NEVER a position store. DynamoDB is NOT a substitute for the
      broker's OLTP. Three tiers, three responsibilities, one-way data
      flow downstream.
    gates: [D.fast, D.lake]
    state: pending
  - id: CDP.5
    title: Same code path across environments
    detail: |
      Per PIVOT Part E + Part G. Backtest, paper, live_small, live_full
      all use identical code; only the BrokerAdapter Protocol
      implementation and capital_allocation differ. Diverging code paths
      is the #1 source of backtest-to-live degradation.
    gates: [L3, E.env, MVP]
    state: pending
  - id: CDP.6
    title: Environments are config bundles, not deployments
    detail: |
      Per PIVOT Part G "deploy-once, configure-per-environment." Same
      binary, different EnvironmentConfig. Promotion is "change the env,
      run again."
    gates: [E.env]
    state: pending
  - id: CDP.7
    title: Single AWS account, Pattern C isolation (IAM + table-prefix)
    detail: |
      Per PIVOT Part G + PLATFORM CD.6. Multi-account is industrial
      overkill for sole dev. Pattern C invariants: Iceberg + DynamoDB
      tables prefixed per env; separate IAM roles enforcing prefix
      boundaries; broker creds in separate Secrets Manager entries;
      asymmetric research-can-READ-live-but-not-WRITE.
    gates: [E.env]
    state: pending
  - id: CDP.8
    title: Layer 2 is Bayesian-opt + structured priors, NOT formula discovery
    detail: |
      Per PIVOT Part E identifiability discussion. Statistical power
      collapses ~2000x from L1 (per-symbol-per-day) to L2 (portfolio-
      rebalance). PySR or any symbolic regressor at L2 finds noise.
      L2 hyperparameter tuning uses Bayesian optimisation (Optuna,
      BoTorch) over a small structured search space of 5-10
      hyperparameters with strong theoretical priors.
    gates: [L2]
    state: pending
  - id: CDP.9
    title: Hedging is L2 constraint; default size-down at retail scale
    detail: |
      Per PIVOT Part G. Beta / dollar / sector / factor / currency /
      pairs hedges are linear constraints in the optimiser. Tail hedging
      is a separate insurance-sleeve concern outside L2 (behavioural
      instrument, not edge instrument). For sole-quant scale: DEFAULT
      to size-down rather than hedging. Revisit when factor model is
      trustworthy and alpha Sharpe survives halving from hedging costs.
    gates: [L2]
    state: pending
  - id: CDP.10
    title: Options trading deferred to post-MVP
    detail: |
      Per PIVOT Part G. Options is a separate discipline (Greeks change
      L2 from "aggregate weights" to "aggregate Greek exposures";
      multi-leg execution; expiry state machine). Adding options before
      the stock system is live and stable is scope creep that delays the
      harder thing (proving the stock system makes money live). See
      known_gaps.KG.options for hypothesis trigger.
    gates: []
    state: pending
  - id: CDP.11
    title: Full Kelly forbidden; only fractional Kelly
    detail: |
      Per PIVOT Part C L2 sub-problem (a). Kelly criterion is
      mathematically optimal for log-wealth growth but pathologically
      sensitive to estimation error in expected return. Practical
      version is fractional Kelly (e.g. 1/4 Kelly) - trades growth-
      optimality for robustness to bad estimates.
    gates: [L2]
    state: pending
  - id: CDP.12
    title: Hard drawdown circuit breaker non-negotiable (L2 constraint)
    detail: |
      Per PIVOT Part C L2 sub-problem (e). For a sole developer with real
      money at risk, the hard circuit breaker is non-negotiable. Tune
      generously so it rarely triggers, but it must exist. Lives as L2
      constraint, NOT L4 surface (L4 is Operations/Telemetry).
    gates: [L2, MVP]
    state: pending
  - id: CDP.13
    title: Execution policy tuned from live TCA telemetry, NOT backtests
    detail: |
      Per PIVOT Part E. Backtest-derived execution rules systematically
      underestimate live execution costs (market impact and microstructure
      aren't in historical data). Implementation shortfall is the metric;
      bucketed cost curves feed back into L2 cost-aware optimisation +
      L3 policy thresholds. Mechanics stay boring; POLICY tunes from live.
    gates: [L3]
    state: pending
  - id: CDP.14
    title: No neural networks at Layer 3
    detail: |
      Per PIVOT Part E. A neural network executing trades is exactly the
      wrong place to put ML for a retail-scale system. Complexity-to-edge
      ratio is awful. L3 policies are rule-based with tuned thresholds
      (liquidity gating, spread gating, vol-conditional timing) - NOT
      learned formulas.
    gates: [L3]
    state: pending
  - id: CDP.15
    title: Pipeline is the unit of optimisation
    detail: |
      Per PIVOT Part C. Alpha + portfolio construction + cost model + risk
      gates are backtested TOGETHER on out-of-sample data. Strategy
      parameters cannot be tuned by alpha-only backtests - leaves edge
      on the table AND admits the alpha-quality-vs-sizing-quality
      identifiability failure.
    gates: [L1, L2, L3, MVP]
    state: pending
  - id: CDP.16
    title: Trade-on-open-vs-close - backtest mirrors live timing
    detail: |
      Per PIVOT Part C. Most academic backtests assume execution at close;
      live trades execute at open or mid-morning. That gap is where
      backtest-to-live performance degradation lives. Mirror in the
      backtest framework whatever is done live.
    gates: [L3, E.env, MVP]
    state: pending
  - id: CDP.17
    title: Four-stage promotion funnel; skipping stages forbidden
    detail: |
      Per PIVOT Part E. research_backtest -> out_of_sample_paper ->
      live_small -> live_full. Each stage corrects a specific bias and
      discovers a different class of bug. Skipping stages is the #1
      retail-quant failure mode. No promotion bypasses the funnel.
    gates: [MVP]
    state: pending
  - id: CDP.18
    title: Promotion criteria are themselves a ratified Class A contract
    detail: |
      Per PIVOT Part E. Numeric thresholds (deflated_sharpe, max_drawdown,
      consecutive_losing_weeks, min_duration_days) live in
      docs/contracts/promotion-criteria.yaml Class A; NEVER as YAML
      scalars in this roadmap. "You cannot override your own past self
      when recent data feels good."
    gates: [MVP]
    state: pending
  - id: CDP.19
    title: Statistical hygiene - deflated Sharpe + PBO + survivorship-inclusive
    detail: |
      Per PIVOT Part E. Two killer biases: selection bias on multiple
      testing (remedy = Lopez de Prado deflated Sharpe + probability of
      backtest overfitting) AND survivorship bias (every pipeline that
      ever existed must be in evaluation, dead or alive).
    gates: [L4, MVP]
    state: pending
  - id: CDP.20
    title: Vol-scaled equal-weight ensemble; resist dynamic weighting
    detail: |
      Per PIVOT Part E. Each live pipeline gets 1/N * (target_vol /
      realised_vol_of_pipeline) weight. Monthly rebalance. Recent
      performance is mean-reverting noise at measurable timescales;
      dynamic weighting introduces correlation that defeats diversification.
    gates: [E.env, MVP]
    state: pending
  - id: CDP.21
    title: Five-property optimisability test on every L1-L4 tier_item
    detail: |
      Per PIVOT Part E. For any component to participate in the recursive
      improvement loop it must be (1) parameterised - explicit typed
      inputs; (2) versioned - stable identifier; (3) composable - clean
      I/O contracts; (4) observable - structured telemetry per invocation;
      (5) evaluable - defined success metric computable from telemetry.
      Hardcoded magic numbers fail #1; anonymous functions fail #2.
    gates: [L1, L2, L3, L4]
    state: pending
  - id: CDP.22
    title: Component-as-versioned-artifact (SCD2 Iceberg + Annotated Pydantic)
    detail: |
      Per PIVOT Part E. AlphaModelVersion / PortfolioConstructionVersion /
      ExecutionPolicyVersion / StrategyVersion are SCD2 Iceberg tables
      with Annotated-Pydantic models (extending PLATFORM CD.12). A
      strategy is a composition of versioned components, not a monolith.
    gates: [L1, L2, L3, D.lake]
    state: pending
  - id: CDP.23
    title: All product-domain Iceberg rows carry strategy_version_id lineage
    detail: |
      Per PIVOT Part E. Every signal, target_portfolio, order, fill,
      tca_event, attribution row, telemetry event carries
      strategy_version_id (and run_id where applicable). Any fill can be
      traced back to the exact alpha/pc/exec versions that produced it.
    gates: [D.lake, L4]
    state: pending
  - id: CDP.24
    title: Telemetry-as-contract (LayerInvocation per layer per invocation)
    detail: |
      Per PIVOT Part E. Every L1/L2/L3 invocation emits a structured
      LayerInvocation event with run_id, strategy_version_id, layer,
      component_version_id, inputs_hash, outputs_hash, metrics,
      duration_ms. Reproducible from logged inputs. Feeds the recursive
      optimisation loop. Class A persisted contract.
    gates: [L4]
    state: pending
  - id: CDP.25
    title: Reconciliation every N minutes during session is non-negotiable
    detail: |
      Per PIVOT Part A 7-step write-path step 7 + MVP v1 spec. Pull
      positions from broker every N minutes (MVP v1 N=30), diff against
      DynamoDB cache, append reconciliation event. Drift > tolerance
      files a critical rec via ops_recommendations (extended use).
    gates: [D.fast, E.env, MVP]
    state: pending
  - id: CDP.26
    title: No synchronous Iceberg writes from decision hot path
    detail: |
      Per PIVOT Part A "what not to do" #1. Iceberg commits are
      seconds-scale. Append via buffered writer that batches every 5-30s
      OR write event to SQS queue and let Lambda batch-append. Reuses
      PLATFORM outbox mental model.
    gates: [D.lake]
    state: pending
  - id: CDP.27
    title: No premature Kafka / Kinesis; SQS+Lambda suffices
    detail: |
      Per PIVOT Part A "what not to do" #6. At single-agent volume,
      SQS + Lambda is sufficient. Kinesis costs more than it saves until
      multiple consumers of the same stream exist.
    gates: [D.lake]
    state: pending
  - id: CDP.28
    title: Promotion is a typed verb with criteria-check baked in
    detail: |
      Per PIVOT Part E. promote_strategy(strategy_id, from_stage, to_stage,
      evaluation_window, capital_allocation) -> PromotionEventId checks
      pre-registered criteria inline. Failure returns typed rejection;
      overrides require amending ratified criteria first.
    gates: [E.env, MVP]
    state: pending
  - id: CDP.29
    title: Marketable-limit orders only at L3; market orders forbidden on illiquids
    detail: |
      Per PIVOT Part C. Marketable limit orders (limit price slightly
      through the spread) avoid slippage on jumpy stocks while filling
      near-instantly on liquid names. Never pure market orders for
      anything illiquid.
    gates: [L3]
    state: pending
  - id: CDP.30
    title: Asymmetric research-to-prod IAM (read but never write)
    detail: |
      Per PIVOT Part G Pattern C. The research role can READ live
      telemetry; CANNOT WRITE to live tables. Enforced at IAM. Invariant
      of multi-account isolation without multi-account overhead.
    gates: [E.env]
    state: pending
  - id: CDP.31
    title: Layer 2 + Layer 3 quality dominate Layer 1 quality at retail scale
    detail: |
      Per PIVOT Part C. Renaissance Medallion's edge is famously as much
      in execution and capacity management as in signal. Execution
      quality and risk discipline matter more than signal quality at
      sole-dev to medium scale. Drives investment allocation.
    gates: [L1, L2, L3]
    state: pending
  - id: CDP.32
    title: DynamoDB cross-item transactions for order+position-cache pair
    detail: |
      Per PIVOT Part A "what not to do" #3. Don't hand-roll concurrency
      primitives. DynamoDB cross-item transactions exist; use them for
      the order-update + position-update pair on fill events.
    gates: [D.fast]
    state: pending
  - id: CDP.33
    title: ops_recommendations is extended (not replaced) for trading drift events
    detail: |
      Per PIVOT Part A tables/components table. Reconciliation drift,
      TCA anomaly, paper-vs-live divergence all file critical recs via
      existing ops portal (Single Portal Invariant intact). No new
      "trading_drift" table - the existing recommendation workflow
      handles trading drift identically to platform drift.
    gates: [D.fast, D.lake, L4]
    state: pending
  - id: CDP.34
    title: L1 evaluation is sizing-agnostic
    detail: |
      Per PIVOT Part E identifiability fix. Evaluate L1 with IC, IR,
      quantile spread, hit-rate (magnitude-weighted), signal decay
      half-life, alpha pairwise IC correlation. NEVER use full-portfolio
      Sharpe to evaluate alpha quality (admits alpha-vs-sizing identifiability
      failure).
    gates: [L1, L4]
    state: pending
  - id: CDP.35
    title: Three-place order state machine - broker, DynamoDB, app code
    detail: |
      Per PIVOT Part A. Order lifecycle is a DAG (submitted -> acked ->
      filled | partial | cancelled | rejected) enforced in three places
      in order of authority: broker (canonical lifecycle); DynamoDB
      conditional UpdateItem (local races); application code (trading
      loop checks state before transitioning).
    gates: [D.fast, L3]
    state: pending
  - id: CDP.36
    title: Tail-hedge sleeve is behavioural instrument, not edge instrument
    detail: |
      Per PIVOT Part G. A small tail-hedge sleeve (1-3% of capital in long
      OTM puts) has behavioural value (lets the operator size alpha
      larger) but expected return is negative. Not a profit centre. Treat
      as behavioural overlay if used at all.
    gates: [L2]
    state: pending

candidate_decisions_research_pool:
  # Surfaced from PIVOT but NOT lifted to ratified CDPs in this audit -
  # either because they need further user input or because they're
  # forward-looking commitments tied to product evolution.
  - id: CDPR.1
    title: 100+ orthogonal alphas via meta-model (frontier)
    pivot_reference: PIVOT Part C "What the frontier looks like"
    pivot_quote: |
      Layer 1 (alpha): 100+ orthogonal alpha signals combined via a
      meta-model. Not one formula - a portfolio of formulas with negative
      or low correlation. Your PySR loop should aim at the orthogonality,
      not just predictive power.
    why_not_ratified_now: |
      L1.alpha.7 captures this as a tier_item but the meta-model design
      is itself a forward-pointing question; ratify after L1.alpha.8
      proves the SCD2 alpha catalogue works.
    proposed_resolution: ratify_in_followup
    affected_tier_items: [L1.alpha.7, L1.alpha.8]
  - id: CDPR.2
    title: Cost-aware MVO as L2 frontier
    pivot_reference: PIVOT Part C + Part E
    pivot_quote: |
      Layer 2 (portfolio construction): convex optimization with
      explicit cost model, factor-risk constraints, turnover penalty,
      position-decay terms. Industry standard since ~2010.
    why_not_ratified_now: |
      L2.pc.10 captures this; ratify after PLATFORM:T3.5
      delivers CostCurve substrate.
    proposed_resolution: ratify_in_followup
    affected_tier_items: [L2.pc.10]
  - id: CDPR.3
    title: Garleanu-Pedersen dynamic optimisation for turnover control
    pivot_reference: PIVOT Part C L2 sub-problem (d)
    pivot_quote: |
      Cost-aware optimization - penalize the trade-list size inside the
      portfolio construction step (Garleanu-Pedersen dynamic optimization
      is the academic version).
    why_not_ratified_now: |
      Threshold rebalance (L2.pc.6) is the v1 default; G-P is the
      escalation path if threshold rebalance plateaus.
    proposed_resolution: ratify_in_followup
    affected_tier_items: [L2.pc.6, L2.pc.10]
  - id: CDPR.4
    title: Factor model with Ledoit-Wolf shrinkage
    pivot_reference: PIVOT Part C L2 sub-problem (b)
    pivot_quote: |
      Middle path: factor-based risk model (Fama-French style - exposures
      to market, size, value, momentum factors). Bloomberg PORT, Barra,
      Axioma all sell this. For your scale, Ledoit-Wolf shrinkage on a
      covariance estimate is sufficient.
    why_not_ratified_now: |
      Risk-parity at L2.pc.4 implies covariance estimate; factor-model
      decomposition is the escalation. Ratify when L2.pc.4 proves
      insufficient.
    proposed_resolution: ratify_in_followup
    affected_tier_items: [L2.pc.4, L2.pc.10]
  - id: CDPR.5
    title: Cloud-analysis agent extension to trading domain
    pivot_reference: PIVOT Part E "How this composes with existing system"
    pivot_quote: |
      Your recommendation system reads from this table, identifies
      underperforming components, and files recommendations for improved
      versions.
    why_not_ratified_now: |
      L4.ops.5 captures the surface; ratify after PLATFORM T3.3 (cloud
      analysis agent) is in production.
    proposed_resolution: ratify_in_followup
    affected_tier_items: [L4.ops.5]
tier_items:
  - id: L0.1
    tier: L0
    layer: platform_infra
    name: Market-data pipeline (Iceberg + features)
    intent: |
      EXISTING-COMPLETE per Subagent C catalogue Phase 1. Step Functions
      orchestration; FTSE 100 daily ingestion via yfinance; feature engine
      (~18 indicators); awswrangler MERGE upsert. Tier item captures the
      shipped state for PRODUCT-YAML current_state grounding.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.13
      - PLATFORM:CD.9
    contract_gates:
      - path: docs/contracts/market-data.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: complete
    validation_lens: structural
    deferred: false
  - id: L0.2
    tier: L0
    layer: platform_infra
    name: Schema flattening (~18 features as native Iceberg columns)
    intent: |
      EXISTING-COMPLETE. tech_rsi_14, tech_macd, tech_volatility_20d etc.
      promoted from features map<string,double>. Subagent C Phase 2.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.13
      - PLATFORM:CD.9
    contract_gates:
      - path: docs/contracts/market-data.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: complete
    validation_lens: structural
    deferred: false
  - id: L0.3
    tier: L0
    layer: platform_infra
    name: Pre-calculated delta columns (price/vol/zscore/sentiment)
    intent: |
      EXISTING-COMPLETE. delta_price_1d/5d/20d, delta_volatility_10d,
      zscore_close/volume/rsi_30d, delta_sentiment_1d.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.13
      - PLATFORM:CD.9
    contract_gates:
      - path: docs/contracts/market-data.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: complete
    validation_lens: structural
    deferred: false
  - id: L0.4
    tier: L0
    layer: platform_infra
    name: Historical backfill (1-month hourly + 20-year daily)
    intent: |
      EXISTING-IN-PROGRESS. Required for meaningful formula discovery and
      walk-forward CV. ~13,398 hourly rows + ~435,000 daily rows for FTSE 100.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.13
    contract_gates:
      - path: docs/contracts/market-data.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: in_progress
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: Step Functions orchestration in-progress; attestations follow shipping (status -> complete).
      will_attest_when: before status == complete
  - id: L0.5
    tier: L0
    layer: lab_offline
    name: PySR formula discovery wired to Iceberg market data
    intent: |
      EXISTING-COMPLETE. src/lab/pysr_factory.py runs formula discovery
      against Iceberg-backed feature columns. Safe sympy evaluation (no eval).
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T2.6
    contract_gates: []
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: complete
    validation_lens: structural
    deferred: false
  - id: L0.6
    tier: L0
    layer: platform_infra
    name: Pydantic Annotated schema substrate
    intent: |
      EXISTING-COMPLETE via PLATFORM:T0.12. src/schemas/{rec,decision,
      annotations}.py with DqXxx markers. PRODUCT schemas extend this
      substrate; PLATFORM owns the marker vocabulary.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:CD.12
    contract_gates: []
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: complete
    validation_lens: structural
    deferred: false
  - id: L1.alpha.1
    tier: L1
    layer: alpha
    name: Define alpha() Protocol + Signal payload contract
    intent: |
      Author docs/contracts/alpha-interface.yaml (Class C) and docs/contracts/
      signal-payload.yaml (Class A); land src/trading/alpha/protocol.py with
      typing.Protocol matching the PIVOT Part E signature `alpha(market_state,
      params) -> Signal`. Gates downstream L1 work.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:CD.25
      - PLATFORM:T-1.12
    contract_gates:
      - path: docs/contracts/alpha-protocol.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/signal-payload.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/market-state.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/alpha-params.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L1.alpha.2 (AlphaModelVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_1_alpha.{ic, ir, quantile_spread, signal_decay_half_life}
    deferred: false
  - id: L1.alpha.2
    tier: L1
    layer: alpha
    name: AlphaModelVersion SCD2 Iceberg catalogue
    intent: |
      Land alpha_versions Iceberg table per PIVOT Part E verbatim BaseModel;
      contract docs/contracts/alpha-versions.yaml Class A. Every alpha model
      instance gets a stable version_hash + Iceberg row.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T0.13
      - PLATFORM:T2.4
      - PLATFORM:CD.9
      - PLATFORM:CD.25
    contract_gates:
      - path: docs/contracts/alpha-versions.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L1.alpha.2 (AlphaModelVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_1_alpha.{ic, ir, quantile_spread, signal_decay_half_life}
    deferred: false
  - id: L1.alpha.3
    tier: L1
    layer: alpha
    name: Wire RAT ensemble through four-layer alpha() interface
    intent: |
      Refactor src/live/rat_ensemble.py to expose alpha(market_state, params)
      -> Signal. Subagent E confirms RATModel.predict currently raises
      NotImplementedError; this item implements it against AlphaModelVersion-
      keyed RAT formulas.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/alpha-protocol.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/alpha-versions.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: M
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L1.alpha.2 (AlphaModelVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_1_alpha.{ic, ir, quantile_spread, signal_decay_half_life}
    deferred: false
  - id: L1.alpha.4
    tier: L1
    layer: alpha
    name: Replace mock_signal_generator with formula-score top-N
    intent: |
      src/main.py:129/139 currently wires mock_signal_generator and
      mock_market_data_source. Replace with real alpha() call producing
      score-per-symbol-per-day, top-N=20 per PIVOT Part C MVP v1.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/alpha-protocol.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/signal-payload.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L1.alpha.2 (AlphaModelVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_1_alpha.{ic, ir, quantile_spread, signal_decay_half_life}
    deferred: false
  - id: L1.alpha.5
    tier: L1
    layer: alpha
    name: Sizing-agnostic L1 evaluation metrics (IC, IR, quantile, decay, orthogonality)
    intent: |
      Land src/trading/evaluation/alpha_metrics.py. Per PIVOT Part E
      identifiability fix: evaluate L1 with metrics that DON'T depend on
      sizing (Information Coefficient daily rank-correlation, IR=IC mean /
      IC std, quantile spread, hit-rate magnitude-weighted, signal decay
      half-life, alpha pairwise IC correlation for L1 orthogonality).
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/alpha-metrics.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L1.alpha.2 (AlphaModelVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_1_alpha.{ic, ir, quantile_spread, signal_decay_half_life}
    deferred: false
  - id: L1.alpha.6
    tier: L1
    layer: alpha
    name: Walk-forward CV harness (no i.i.d. splits in time series)
    intent: |
      Per PIVOT Part E: "walk-forward CV ALWAYS, never i.i.d. splits."
      Harness consumed by AlphaModelVersion evaluation; rejects i.i.d.
      splits with a typed error.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/walk-forward-cv.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L1.alpha.2 (AlphaModelVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_1_alpha.{ic, ir, quantile_spread, signal_decay_half_life}
    deferred: false
  - id: L1.alpha.7
    tier: L1
    layer: alpha
    name: Multi-alpha orthogonality target (100+ orthogonal alphas)
    intent: |
      PIVOT Part C frontier: "100+ orthogonal alpha signals combined via
      meta-model." Subagent C Phase 7 retired (Sharpe-decay weighting
      superseded); this item replaces it with vol-scaled equal-weight at
      production ensemble per PIVOT Part E.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/alpha-orthogonality.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L1.alpha.2 (AlphaModelVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_1_alpha.{ic, ir, quantile_spread, signal_decay_half_life}
    deferred: false
  - id: L1.alpha.8
    tier: L1
    layer: alpha
    name: PySR registry to AlphaModelVersion bridge
    intent: |
      Auto-register discovered formulas as AlphaModelVersion instances.
      Existing PySR runs at L0.5; this item adds the SCD2 versioning
      bridge per PIVOT Part E "component-as-versioned-artifact."
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/alpha-versions.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L1.alpha.2 (AlphaModelVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_1_alpha.{ic, ir, quantile_spread, signal_decay_half_life}
    deferred: false
  - id: L2.pc.1
    tier: L2
    layer: portfolio_construction
    name: Define construct_portfolio() Protocol + TargetPortfolio + Constraint contracts
    intent: |
      Author docs/contracts/pc-interface.yaml (Class C), docs/contracts/
      target-portfolio.yaml (Class A), docs/contracts/constraint-set.yaml
      (Class C). PIVOT Part E signature: `construct_portfolio(signal,
      current_portfolio, params) -> TargetPortfolio`.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:CD.25
      - PLATFORM:T-1.12
    contract_gates:
      - path: docs/contracts/construct-portfolio-protocol.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/target-portfolios.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/pc-constraints.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/pc-params.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/portfolio-state.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L2.pc.2 (PortfolioConstructionVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_2_portfolio_construction.{brinson_attribution_sizing, turnover_adjusted_sharpe}
    deferred: false
  - id: L2.pc.10
    tier: L2
    layer: portfolio_construction
    name: Cost-aware portfolio construction (consumes CostCurve from PLATFORM TCA)
    intent: |
      Per PIVOT Part E + Part G concrete two-phase pattern: PRODUCT
      Phase 2 of cost-aware PC consumes PLATFORM TCA aggregation. New
      pc_version that uses get_cost_curve typed verb as turnover penalty
      in optimisation. Depends_on PLATFORM:T3.5.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T3.5
    contract_gates:
      - path: docs/contracts/cost-curves.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/pc-versions.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L2.pc.2 (PortfolioConstructionVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_2_portfolio_construction.{brinson_attribution_sizing, turnover_adjusted_sharpe}
    deferred: false
  - id: L2.pc.11
    tier: L2
    layer: portfolio_construction
    name: Regime-aware allocator (vol-conditional method switching)
    intent: |
      Per PIVOT Part E: "switch regime based on observed conditions
      (vol-regime-aware allocator)." Optional v2 feature; out of MVP v1
      scope.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/regime-allocator.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L2.pc.2 (PortfolioConstructionVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_2_portfolio_construction.{brinson_attribution_sizing, turnover_adjusted_sharpe}
    deferred: false
  - id: L2.pc.12
    tier: L2
    layer: portfolio_construction
    name: Black-Litterman shrinkage method (post-MVP)
    intent: |
      Per PIVOT Part C L2 sub-problem (a): grown-up version of mean-variance
      that shrinks views toward market-equilibrium prior. Deferred until
      L2.pc.3 / L2.pc.4 prove insufficient.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/construct-portfolio-protocol.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/pc-versions.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L2.pc.2 (PortfolioConstructionVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_2_portfolio_construction.{brinson_attribution_sizing, turnover_adjusted_sharpe}
    deferred: false
  - id: L2.pc.2
    tier: L2
    layer: portfolio_construction
    name: PortfolioConstructionVersion SCD2 catalogue
    intent: |
      Land pc_versions Iceberg table per PIVOT Part E verbatim BaseModel.
      Contract docs/contracts/pc-versions.yaml Class A.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.13
      - PLATFORM:T2.4
      - PLATFORM:CD.9
      - PLATFORM:CD.25
    contract_gates:
      - path: docs/contracts/pc-versions.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L2.pc.2 (PortfolioConstructionVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_2_portfolio_construction.{brinson_attribution_sizing, turnover_adjusted_sharpe}
    deferred: false
  - id: L2.pc.3
    tier: L2
    layer: portfolio_construction
    name: Equal-weight top-N method (v1 default)
    intent: |
      Per PIVOT Part C MVP v1: equal-weight over top-N=20. Robust baseline
      that PIVOT explicitly notes is "almost embarrassingly hard to beat."
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/construct-portfolio-protocol.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/pc-versions.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L2.pc.2 (PortfolioConstructionVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_2_portfolio_construction.{brinson_attribution_sizing, turnover_adjusted_sharpe}
    deferred: false
  - id: L2.pc.4
    tier: L2
    layer: portfolio_construction
    name: Risk-parity top-N method (v1 alternative)
    intent: |
      Per PIVOT Part C MVP v1: risk-parity over top-N as alternative.
      Ledoit-Wolf shrinkage on covariance estimate per PIVOT Part C "risk
      model" sub-problem.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/construct-portfolio-protocol.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/pc-versions.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/risk-model.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L2.pc.2 (PortfolioConstructionVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_2_portfolio_construction.{brinson_attribution_sizing, turnover_adjusted_sharpe}
    deferred: false
  - id: L2.pc.5
    tier: L2
    layer: portfolio_construction
    name: Constraint set (max position, sector caps, gross exposure, cash buffer)
    intent: |
      Per PIVOT Part C L2 sub-problem (c) + MVP v1: max position 7%, gross
      <=100% long-only, cash buffer 5%. Constraints are pre-trade gates;
      violation files a rec.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/pc-constraints.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L2.pc.2 (PortfolioConstructionVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_2_portfolio_construction.{brinson_attribution_sizing, turnover_adjusted_sharpe}
    deferred: false
  - id: L2.pc.6
    tier: L2
    layer: portfolio_construction
    name: Threshold rebalance (epsilon=2%)
    intent: |
      Per PIVOT Part C L2 sub-problem (d) + MVP v1: trade only when target
      weight differs from current by >2% absolute. Highest-leverage
      turnover-control choice at retail scale.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/pc-constraints.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/pc-params.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L2.pc.2 (PortfolioConstructionVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_2_portfolio_construction.{brinson_attribution_sizing, turnover_adjusted_sharpe}
    deferred: false
  - id: L2.pc.7
    tier: L2
    layer: portfolio_construction
    name: Hard drawdown circuit breaker (L2 constraint, NOT L4)
    intent: |
      Per PIVOT Part C: "Hard circuit breaker. Non-negotiable." MVP v1
      spec: 20-day return < -10% -> cash for 10 trading days. Risk gate
      lives as L2 constraint, NOT L4 (which is Operations/Telemetry).
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/pc-constraints.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L2.pc.2 (PortfolioConstructionVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_2_portfolio_construction.{brinson_attribution_sizing, turnover_adjusted_sharpe}
    deferred: false
  - id: L2.pc.8
    tier: L2
    layer: portfolio_construction
    name: Hedging-as-constraint surface (default size-down at retail scale)
    intent: |
      Per PIVOT Part G: hedging is NOT a new layer; beta/dollar/sector/
      factor/currency hedges are L2 constraints (linear constraints in the
      optimiser). Tail hedging is a separate insurance-sleeve concern.
      For sole-quant scale: DEFAULT to size-down, not hedging. Revisit
      with factor model that Sharpe survives halving from hedging costs.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/pc-constraints.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L2.pc.2 (PortfolioConstructionVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_2_portfolio_construction.{brinson_attribution_sizing, turnover_adjusted_sharpe}
    deferred: false
  - id: L2.pc.9
    tier: L2
    layer: portfolio_construction
    name: Bayesian hyperparameter tuner (Optuna/BoTorch)
    intent: |
      Per PIVOT Part E: L2 is NOT formula discovery. Bayesian optimisation
      with strong priors over 5-10 hyperparameters (shrinkage, lookback,
      threshold epsilon, drawdown trigger, vol target). PySR or symbolic
      regression at L2 finds noise (2000x statistical power collapse from
      L1 per-symbol-per-day to L2 portfolio-rebalance).
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/pc-tuner.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L2.pc.2 (PortfolioConstructionVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_2_portfolio_construction.{brinson_attribution_sizing, turnover_adjusted_sharpe}
    deferred: false
  - id: L3.exec.1
    tier: L3
    layer: execution
    name: BrokerAdapter typing.Protocol
    intent: |
      Author docs/contracts/broker-adapter.yaml (Class C). PIVOT Part E
      verbatim Protocol: get_positions / submit_order / get_fills. The
      SINGLE point of difference between backtest/paper/live per the
      same-code-path commitment.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:CD.25
      - PLATFORM:T-1.12
    contract_gates:
      - path: docs/contracts/broker-adapter.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L3.exec.7 (ExecutionPolicyVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_3_execution.{implementation_shortfall, slippage_vs_expected, execution_fill_rate}
    deferred: false
  - id: L3.exec.10
    tier: L3
    layer: execution
    name: Implementation shortfall metric + bucketed aggregation
    intent: |
      Per PIVOT Part E verbatim: `(fill_price - decision_price) * sign(side)`
      in bps, bucketed by (time-of-day, spread quintile, ADV quintile,
      vol regime, side). Class A docs/contracts/implementation-shortfall.yaml.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.13
      - PLATFORM:T2.4
      - PLATFORM:CD.9
    contract_gates:
      - path: docs/contracts/tca-events.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L3.exec.7 (ExecutionPolicyVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_3_execution.{implementation_shortfall, slippage_vs_expected, execution_fill_rate}
    deferred: false
  - id: L3.exec.11
    tier: L3
    layer: execution
    name: TCA aggregation pipeline + cost-curve typed verb
    intent: |
      Per PIVOT Part E TCA loop: live execution -> fill telemetry -> TCA
      aggregation -> cost-curve estimates -> L2 cost model + L3 thresholds.
      Cross-roadmap: depends on PLATFORM:T3.5 tier_item (stale "does not
      exist in PLATFORM yet" parenthetical dropped 2026-06-09 -- T3.5 exists,
      and its dependency chain was shortened by the platform audit
      integration, F-011).
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T3.5
    contract_gates:
      - path: docs/contracts/tca-events.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/cost-curves.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L3.exec.7 (ExecutionPolicyVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_3_execution.{implementation_shortfall, slippage_vs_expected, execution_fill_rate}
    deferred: false
  - id: L3.exec.12
    tier: L3
    layer: execution
    name: Liquidity / spread / vol gating execution policy tunables
    intent: |
      Per PIVOT Part E: rule-based with tuned thresholds, NOT learned
      formulas. Skip trade if spread > Y bps; avoid first/last 15 min;
      vol-conditional news avoidance. Tuned from live TCA telemetry, not
      backtests.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/exec-versions.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/exec-params.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L3.exec.7 (ExecutionPolicyVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_3_execution.{implementation_shortfall, slippage_vs_expected, execution_fill_rate}
    deferred: false
  - id: L3.exec.13
    tier: L3
    layer: execution
    name: Single-retry-then-file-rec policy
    intent: |
      Per PIVOT Part C MVP v1: single retry on rejection then file rec
      via ops_recommendations (extended use, NOT new table). Matches
      existing Decision 55 RCA-first executor philosophy.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.7a
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/exec-versions.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L3.exec.7 (ExecutionPolicyVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_3_execution.{implementation_shortfall, slippage_vs_expected, execution_fill_rate}
    deferred: false
  - id: L3.exec.2
    tier: L3
    layer: execution
    name: HistoricalSimulationBroker adapter (for backtest)
    intent: |
      Per PIVOT Part E. Replays historical OHLCV; simulates fills with
      execution-policy-aware slippage (trade-on-open vs close per
      backtest mirrors live, PIVOT Part C).
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/broker-adapter.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L3.exec.7 (ExecutionPolicyVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_3_execution.{implementation_shortfall, slippage_vs_expected, execution_fill_rate}
    deferred: false
  - id: L3.exec.3
    tier: L3
    layer: execution
    name: PaperBroker adapter (real prices, simulated fills)
    intent: |
      Production-infrastructure paper trading per PIVOT Part E + Part G.
      Same Function URL + IAM + telemetry as live; only the broker call
      differs.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T2.1
      - PLATFORM:T2.14
    contract_gates:
      - path: docs/contracts/broker-adapter.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/credential-routing.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L3.exec.7 (ExecutionPolicyVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_3_execution.{implementation_shortfall, slippage_vs_expected, execution_fill_rate}
    deferred: false
  - id: L3.exec.4
    tier: L3
    layer: execution
    name: AlpacaBroker adapter (live-small + live-full first)
    intent: |
      PIVOT Part C: "Alpaca is friendlier for paper-trading first."
      AlpacaBroker for both live-small and initial live-full deployments;
      later swap to IBKR for capacity if needed.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T2.1
      - PLATFORM:T2.14
    contract_gates:
      - path: docs/contracts/broker-adapter.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/credential-routing.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L3.exec.7 (ExecutionPolicyVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_3_execution.{implementation_shortfall, slippage_vs_expected, execution_fill_rate}
    deferred: false
  - id: L3.exec.5
    tier: L3
    layer: execution
    name: IBKRBroker adapter (capacity scale-out)
    intent: |
      Optional capacity scale-out per PIVOT Part C reference. Out of MVP v1
      scope; deferred until Alpaca proves insufficient.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T2.1
      - PLATFORM:T2.14
    contract_gates:
      - path: docs/contracts/broker-adapter.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/credential-routing.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L3.exec.7 (ExecutionPolicyVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_3_execution.{implementation_shortfall, slippage_vs_expected, execution_fill_rate}
    deferred: false
  - id: L3.exec.6
    tier: L3
    layer: execution
    name: plan_trades() Protocol + Order/Fill contracts
    intent: |
      Author docs/contracts/plan-trades-interface.yaml (Class C),
      docs/contracts/order.yaml (Class A), docs/contracts/fill.yaml
      (Class A). PIVOT Part E: `plan_trades(target, current, params) ->
      [Order]`.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:CD.25
      - PLATFORM:T-1.12
    contract_gates:
      - path: docs/contracts/plan-trades-protocol.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/orders.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/fills.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/exec-params.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L3.exec.7 (ExecutionPolicyVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_3_execution.{implementation_shortfall, slippage_vs_expected, execution_fill_rate}
    deferred: false
  - id: L3.exec.7
    tier: L3
    layer: execution
    name: ExecutionPolicyVersion SCD2 catalogue
    intent: |
      Land exec_versions Iceberg table per PIVOT Part E verbatim BaseModel.
      Contract docs/contracts/exec-versions.yaml Class A.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.13
      - PLATFORM:T2.4
      - PLATFORM:CD.9
      - PLATFORM:CD.25
    contract_gates:
      - path: docs/contracts/exec-versions.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L3.exec.7 (ExecutionPolicyVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_3_execution.{implementation_shortfall, slippage_vs_expected, execution_fill_rate}
    deferred: false
  - id: L3.exec.8
    tier: L3
    layer: execution
    name: Marketable-limit v1 execution policy
    intent: |
      Per PIVOT Part C MVP v1: marketable limit orders 5bps through spread,
      submit 10:00 ET daily, single retry on rejection then file rec.
      Boring-correct execution; the only thing that matters at retail scale.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/plan-trades-protocol.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/exec-versions.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L3.exec.7 (ExecutionPolicyVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_3_execution.{implementation_shortfall, slippage_vs_expected, execution_fill_rate}
    deferred: false
  - id: L3.exec.9
    tier: L3
    layer: execution
    name: Pre-trade gate (margin / position / circuit breaker check)
    intent: |
      Per PIVOT Part A 7-step write-path step 2 ("pre-trade risk gate")
      + PIVOT Part C MVP v1 drawdown circuit breaker. Gate is L3 pre-trade
      hook reading L2 constraint state + current portfolio from DynamoDB
      position cache.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/plan-trades-protocol.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/pc-constraints.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/dynamodb-positions-cache.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:L3.exec.7 (ExecutionPolicyVersion SCD2 catalogue)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_3_execution.{implementation_shortfall, slippage_vs_expected, execution_fill_rate}
    deferred: false
  - id: L4.ops.1
    tier: L4
    layer: operations_telemetry
    name: LayerInvocation event schema + emission infrastructure
    intent: |
      Per PIVOT Part E verbatim BaseModel. Every L1/L2/L3 invocation emits
      a LayerInvocation event with run_id, strategy_version_id, layer,
      component_version_id, inputs_hash, outputs_hash, metrics, duration_ms.
      Class A contract; feeds the recursive improvement loop.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T0.13
      - PLATFORM:T2.4
      - PLATFORM:CD.9
      - PLATFORM:CD.25
    contract_gates:
      - path: docs/contracts/layer-invocation.yaml
        class: A
        contract_version: 1
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:D.lake.7 (promotion_events + strategy_runs)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_4_operations_telemetry.{layer_invocation_coverage, brinson_full_attribution, paper_vs_live_divergence}
    deferred: false
  - id: L4.ops.2
    tier: L4
    layer: operations_telemetry
    name: Brinson attribution (selection/sizing/execution decomposition)
    intent: |
      Per PIVOT Part E. Decompose portfolio P&L into selection (L1) vs
      sizing (L2) vs execution (L3) contributions. Industry-standard
      attribution. Class A pnl_attribution view over fills + signals +
      target_portfolios.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.13
      - PLATFORM:T2.4
      - PLATFORM:CD.9
      - PLATFORM:T1.2
    contract_gates:
      - path: docs/contracts/pnl-attribution.yaml
        class: A
        contract_version: 1
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:D.lake.7 (promotion_events + strategy_runs)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_4_operations_telemetry.{layer_invocation_coverage, brinson_full_attribution, paper_vs_live_divergence}
    deferred: false
  - id: L4.ops.3
    tier: L4
    layer: operations_telemetry
    name: Deflated Sharpe + PBO computation (statistical hygiene)
    intent: |
      Per PIVOT Part E: Lopez de Prado deflated Sharpe corrects for
      selection bias on multiple testing; probability of backtest
      overfitting (PBO) check. Required for promotion criteria gating.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T2.5
    contract_gates:
      - path: docs/contracts/deflated-sharpe-pbo.yaml
        class: A
        contract_version: 1
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:D.lake.7 (promotion_events + strategy_runs)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_4_operations_telemetry.{layer_invocation_coverage, brinson_full_attribution, paper_vs_live_divergence}
    deferred: false
  - id: L4.ops.4
    tier: L4
    layer: operations_telemetry
    name: Survivorship-inclusive evaluation statistics
    intent: |
      Per PIVOT Part E: "every pipeline that ever existed must be in
      evaluation, dead or alive." Telemetry assertion blocking the
      survivorship_inclusive_stats_verified gate helper.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T1.2
    contract_gates:
      - path: docs/contracts/survivorship-stats.yaml
        class: C
        contract_version: 1
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:D.lake.7 (promotion_events + strategy_runs)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_4_operations_telemetry.{layer_invocation_coverage, brinson_full_attribution, paper_vs_live_divergence}
    deferred: false
  - id: L4.ops.5
    tier: L4
    layer: operations_telemetry
    name: Recursive improvement loop extension to trading domain
    intent: |
      Per PIVOT Part E "How this composes with existing system." Anomaly
      detected in LayerInvocation telemetry -> file_rec via ops portal ->
      /plan -> implement -> telemetry delta proves fix. Reuses PLATFORM's
      autonomous-improvement substrate.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.7a
      - PLATFORM:T1.1
      - PLATFORM:T3.3
      - PLATFORM:T3.2
    contract_gates: []
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: both
    five_property_test:
      parameterised:
        attestation: |
          Typed Pydantic schema + Annotated[T, DqXxx] markers; no module-level constants
        cites: |
          tier_item:L0.6 (Pydantic substrate via PLATFORM:T0.12)
      versioned:
        attestation: |
          SCD2 Iceberg row with version_hash; selection via EnvironmentConfig refs
        cites: |
          tier_item:D.lake.7 (promotion_events + strategy_runs)
      composable:
        attestation: |
          Protocol-bounded interface; no other coupling
        cites: |
          tier_item:L4.ops.1 (LayerInvocation telemetry contract)
      observable:
        attestation: |
          Every invocation emits a LayerInvocation event
        cites: |
          tier_item:L4.ops.1; evaluation_metrics.layer_4_operations_telemetry.layer_invocation_coverage
      evaluable:
        attestation: |
          Layer-specific evaluation metric per evaluation_metrics block
        cites: |
          evaluation_metrics.layer_4_operations_telemetry.{layer_invocation_coverage, brinson_full_attribution, paper_vs_live_divergence}
    deferred: false
  - id: D.fast.1
    tier: D.fast
    layer: platform_infra
    owning_layer: execution
    name: DynamoDB orders table (PK=client_order_id, conditional writes)
    intent: |
      Per PIVOT Part A. PutItem with attribute_not_exists(client_order_id);
      UpdateItem with conditional state-machine guards. Contract Class A
      docs/contracts/dynamodb-orders.yaml. owning_layer=execution.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T2.1
      - PLATFORM:CD.25
    contract_gates:
      - path: docs/contracts/dynamodb-orders.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/orders.yaml
        class: A
        contract_version: 1
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: DynamoDB schema/keys/GSI design crystallises during E.env.2 (EnvironmentConfig Pydantic); attestations would be speculative before that.
      will_attest_when: before status == in_progress
  - id: D.fast.2
    tier: D.fast
    layer: platform_infra
    owning_layer: execution
    name: DynamoDB positions_cache table (PK=symbol)
    intent: |
      Per PIVOT Part A. Refresh from broker on schedule + after fills via
      cross-item transactions. Contract Class A docs/contracts/dynamodb-
      positions-cache.yaml. owning_layer=execution.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T2.1
      - PLATFORM:CD.25
    contract_gates:
      - path: docs/contracts/dynamodb-positions-cache.yaml
        class: A
        contract_version: 1
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: DynamoDB schema/keys/GSI design crystallises during E.env.2 (EnvironmentConfig Pydantic); attestations would be speculative before that.
      will_attest_when: before status == in_progress
  - id: D.fast.3
    tier: D.fast
    layer: platform_infra
    owning_layer: alpha
    name: DynamoDB features_serving table (one-way projection from Iceberg)
    intent: |
      Per PIVOT Part A feature-store pattern. Nightly DuckDB batch job over
      Iceberg writes features keyed by symbol. Live decision loop reads
      sub-ms. owning_layer=alpha (alpha consumes features at decision time).
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T2.1
      - PLATFORM:T2.5
      - PLATFORM:CD.25
    contract_gates:
      - path: docs/contracts/dynamodb-features-serving.yaml
        class: A
        contract_version: 1
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: DynamoDB schema/keys/GSI design crystallises during E.env.2 (EnvironmentConfig Pydantic); attestations would be speculative before that.
      will_attest_when: before status == in_progress
  - id: D.fast.4
    tier: D.fast
    layer: platform_infra
    owning_layer: execution
    name: Conditional-write state machine for orders (submitted -> acked -> filled|cancelled|rejected)
    intent: |
      Per PIVOT Part A order-lifecycle DAG. Three-place enforcement:
      broker (authoritative), DynamoDB conditional writes (local races),
      app code. Defines the OrderState enum + transition guards.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/dynamodb-orders.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/order-state-machine.yaml
        class: C
        contract_version: 1
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: DynamoDB schema/keys/GSI design crystallises during E.env.2 (EnvironmentConfig Pydantic); attestations would be speculative before that.
      will_attest_when: before status == in_progress
  - id: D.fast.5
    tier: D.fast
    layer: platform_infra
    owning_layer: execution
    name: DynamoDB cross-item transactions for order+position-cache pair
    intent: |
      Per PIVOT Part A "what not to do" #3: don't hand-roll concurrency.
      Use DynamoDB cross-item transactions for the order-update + position-
      update pair on fill events.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T2.1
    contract_gates:
      - path: docs/contracts/dynamodb-orders.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/dynamodb-positions-cache.yaml
        class: A
        contract_version: 1
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: DynamoDB schema/keys/GSI design crystallises during E.env.2 (EnvironmentConfig Pydantic); attestations would be speculative before that.
      will_attest_when: before status == in_progress
  - id: D.lake.1
    tier: D.lake
    layer: platform_infra
    owning_layer: operations_telemetry
    name: strategies_versions Iceberg table
    intent: |
      SCD2 catalogue of strategy compositions per PIVOT Part E. A strategy
      = (alpha_ref, pc_ref, exec_ref, capital_allocation, stage). Class A.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.13
      - PLATFORM:T2.4
      - PLATFORM:CD.9
      - PLATFORM:CD.25
    contract_gates:
      - path: docs/contracts/strategies-versions.yaml
        class: A
        contract_version: 1
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: Iceberg table contracts ratify in CD.25 follow-up (PLATFORM:CD.25); the parameterised/versioned attestations specifically depend on the SCD2 row stamp design.
      will_attest_when: before status == in_progress
  - id: D.lake.2
    tier: D.lake
    layer: platform_infra
    owning_layer: alpha
    name: signals + target_portfolios Iceberg tables
    intent: |
      Per PIVOT Part E tables-to-add. signals append-only day-partitioned
      L1 outputs; target_portfolios append-only L2 outputs. Both carry
      strategy_version_id lineage. owning_layer per layer that owns writes.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.13
      - PLATFORM:T2.4
      - PLATFORM:CD.9
      - PLATFORM:CD.25
    contract_gates:
      - path: docs/contracts/signals.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/target-portfolios.yaml
        class: A
        contract_version: 1
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: Iceberg table contracts ratify in CD.25 follow-up (PLATFORM:CD.25); the parameterised/versioned attestations specifically depend on the SCD2 row stamp design.
      will_attest_when: before status == in_progress
  - id: D.lake.3
    tier: D.lake
    layer: platform_infra
    owning_layer: execution
    name: fills Iceberg table
    intent: |
      Per PIVOT Part E. Append-only broker fills. Class A. Carries
      strategy_version_id + run_id lineage. owning_layer=execution.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.13
      - PLATFORM:T2.4
      - PLATFORM:CD.9
      - PLATFORM:CD.25
    contract_gates:
      - path: docs/contracts/fills.yaml
        class: A
        contract_version: 1
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: Iceberg table contracts ratify in CD.25 follow-up (PLATFORM:CD.25); the parameterised/versioned attestations specifically depend on the SCD2 row stamp design.
      will_attest_when: before status == in_progress
  - id: D.lake.4
    tier: D.lake
    layer: platform_infra
    owning_layer: execution
    name: ops_trade_events + ops_reconciliations Iceberg tables
    intent: |
      Per PIVOT Part A tables-to-add. ops_trade_events = append-only
      journal of every state change. ops_reconciliations = every
      broker-vs-cache snapshot. Both Class A.
      Platform dependency: PLATFORM:T1.14 (DuckLake append-only write mode) is the
      prerequisite kernel -- ops_trade_events adopts append_only write_mode; the
      product schema lands after the platform kernel is proven via ops_smoke_events.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.13
      - PLATFORM:T1.14
      - PLATFORM:T2.4
      - PLATFORM:CD.9
      - PLATFORM:CD.25
    contract_gates:
      - path: docs/contracts/ops-trade-events.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/ops-reconciliations.yaml
        class: A
        contract_version: 1
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: true
    deferred_note: "DEFERRED: build_lambda.py --deploy + run_scheduled_agent.py --smoke-test (pending Decision 67 reversal)"
    five_property_test_waiver:
      reason: Iceberg table contracts ratify in CD.25 follow-up (PLATFORM:CD.25); the parameterised/versioned attestations specifically depend on the SCD2 row stamp design.
      will_attest_when: before status == in_progress
  - id: D.lake.5
    tier: D.lake
    layer: platform_infra
    owning_layer: operations_telemetry
    name: tca_events + pnl_attribution Iceberg tables
    intent: |
      Per PIVOT Part E. tca_events = per-fill implementation shortfall.
      pnl_attribution = Brinson-decomposed daily attribution. Class A.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.13
      - PLATFORM:T2.4
      - PLATFORM:CD.9
      - PLATFORM:CD.25
    contract_gates:
      - path: docs/contracts/tca-events.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/pnl-attribution.yaml
        class: A
        contract_version: 1
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: Iceberg table contracts ratify in CD.25 follow-up (PLATFORM:CD.25); the parameterised/versioned attestations specifically depend on the SCD2 row stamp design.
      will_attest_when: before status == in_progress
  - id: D.lake.6
    tier: D.lake
    layer: platform_infra
    owning_layer: operations_telemetry
    name: alpha_versions + pc_versions + exec_versions Iceberg tables
    intent: |
      Per PIVOT Part E "component-as-versioned-artifact." Three SCD2
      catalogues of L1/L2/L3 component versions. Class A. Cross-reference
      keys for strategy_runs.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.13
      - PLATFORM:T2.4
      - PLATFORM:CD.9
      - PLATFORM:CD.25
    contract_gates:
      - path: docs/contracts/alpha-versions.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/pc-versions.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/exec-versions.yaml
        class: A
        contract_version: 1
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: Iceberg table contracts ratify in CD.25 follow-up (PLATFORM:CD.25); the parameterised/versioned attestations specifically depend on the SCD2 row stamp design.
      will_attest_when: before status == in_progress
  - id: D.lake.7
    tier: D.lake
    layer: platform_infra
    owning_layer: operations_telemetry
    name: strategy_runs + promotion_events Iceberg tables
    intent: |
      Per PIVOT Part E. strategy_runs = each (strategy_version_id,
      date_range, environment) backtest or live invocation.
      promotion_events = stage transitions with criteria-check results.
      Class A. promotion_events is the append-only audit trail.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.13
      - PLATFORM:T2.4
      - PLATFORM:CD.9
      - PLATFORM:CD.25
    contract_gates:
      - path: docs/contracts/strategy-runs.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/promotion-events.yaml
        class: A
        contract_version: 1
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: Iceberg table contracts ratify in CD.25 follow-up (PLATFORM:CD.25); the parameterised/versioned attestations specifically depend on the SCD2 row stamp design.
      will_attest_when: before status == in_progress
  - id: D.lake.8
    tier: D.lake
    layer: platform_infra
    owning_layer: operations_telemetry
    name: Buffered/SQS Iceberg writer (no synchronous hot-path writes)
    intent: |
      Per PIVOT Part A "what not to do" #1: "Don't write to Iceberg from
      the hot path synchronously." Buffered writer that batches every
      5-30s OR SQS+Lambda batch-append. Reuses PLATFORM outbox pattern.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T2.1
    contract_gates:
      - path: docs/contracts/iceberg-buffered-writer.yaml
        class: C
        contract_version: 1
    environment_scope: ['paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: Iceberg table contracts ratify in CD.25 follow-up (PLATFORM:CD.25); the parameterised/versioned attestations specifically depend on the SCD2 row stamp design.
      will_attest_when: before status == in_progress
  - id: E.env.1
    tier: E.env
    layer: env_infra
    name: EnvironmentConfig Pydantic schema
    intent: |
      Per PIVOT Part G verbatim Pydantic block: environment (research |
      backtest | paper | live_small | live_full), broker_adapter,
      data_source, capital_allocation, table_prefix, iam_role_arn,
      risk_gate_overrides, promotion_criteria. Class C local Pydantic.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/environment-config.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: EnvironmentConfig Pydantic schema is itself the precursor item; attestations follow once the schema is locked and downstream items validate it.
      will_attest_when: before status == in_progress
  - id: E.env.10
    tier: E.env
    layer: env_infra
    name: Backtest engine integrated in trading system (same-code-path commitment)
    intent: |
      Per PIVOT Part E: "backtest engine is part of the trading system,
      not a separate tool." Same alpha() / construct_portfolio() /
      plan_trades() code path with HistoricalSimulationBroker adapter.
      Diverging backtest/live code paths is the #1 source of backtest-to-
      live degradation - this item is structural prevention.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/broker-adapter.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/same-code-path.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: M
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: Environment-as-config invariants crystallise during E.env.1 (EnvironmentConfig Pydantic authoring); per-env attestations follow.
      will_attest_when: before status == in_progress
  - id: E.env.11
    tier: E.env
    layer: env_infra
    name: Champion-challenger paper alongside live
    intent: |
      Per PIVOT Part E "Concurrent paper trading." 5-10 paper pipelines
      run as challengers + sanity checks + warmed-up replacement
      candidates. Architectural cost low (same code path); discipline cost
      real.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/champion-challenger.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: Environment-as-config invariants crystallise during E.env.1 (EnvironmentConfig Pydantic authoring); per-env attestations follow.
      will_attest_when: before status == in_progress
  - id: E.env.2
    tier: E.env
    layer: env_infra
    name: Pattern-C IAM + table-prefix isolation
    intent: |
      Per PIVOT Part G three-isolation-patterns. Single AWS account,
      naming conventions (research_*, paper_*, live_*) + IAM enforcing
      access boundaries. Asymmetric: research can READ live but NEVER
      WRITE. Class C terraform/iam_trading.tf.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T2.1
      - PLATFORM:CD.6
    contract_gates:
      - path: docs/contracts/iam-isolation.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: Environment-as-config invariants crystallise during E.env.1 (EnvironmentConfig Pydantic authoring); per-env attestations follow.
      will_attest_when: before status == in_progress
  - id: E.env.3
    tier: E.env
    layer: env_infra
    name: Per-env Secrets Manager broker credentials
    intent: |
      Per PIVOT Part G Pattern C: broker creds in separate Secrets Manager
      entries per env, accessed only by matching env's IAM role.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T2.1
      - PLATFORM:T2.9
    contract_gates:
      - path: docs/contracts/broker-credentials.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: Environment-as-config invariants crystallise during E.env.1 (EnvironmentConfig Pydantic authoring); per-env attestations follow.
      will_attest_when: before status == in_progress
  - id: E.env.4
    tier: E.env
    layer: env_infra
    name: promote_strategy() typed verb with criteria-check
    intent: |
      Per PIVOT Part E verbatim signature: `promote_strategy(strategy_id,
      from_stage, to_stage, evaluation_window, capital_allocation) ->
      PromotionEventId`. Criteria check baked in. Failure returns typed
      rejection. Class B Lambda? Or in-process? Default: in-process Class C
      verb consuming the criteria contract.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T0.7b
      - PLATFORM:T1.12
    contract_gates:
      - path: docs/contracts/promote-strategy.yaml
        class: B
        contract_version: 1
      - path: docs/contracts/promotion-events.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/promotion-criteria.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: Environment-as-config invariants crystallise during E.env.1 (EnvironmentConfig Pydantic authoring); per-env attestations follow.
      will_attest_when: before status == in_progress
  - id: E.env.5
    tier: E.env
    layer: env_infra
    name: Pre-registered promotion criteria as ratified Class A contract
    intent: |
      Per PIVOT Part E + audit prompt Section 5.1 minimum_viable_v1 +
      promotion_funnel discipline: criteria thresholds (deflated_sharpe,
      max_drawdown, consecutive_losing_weeks, min_duration_days) live in
      docs/contracts/promotion-criteria.yaml Class A, NEVER as inline
      YAML scalars. "You cannot override your own past self."
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.7b
      - PLATFORM:CD.25
    contract_gates:
      - path: docs/contracts/promotion-criteria.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: Environment-as-config invariants crystallise during E.env.1 (EnvironmentConfig Pydantic authoring); per-env attestations follow.
      will_attest_when: before status == in_progress
  - id: E.env.6
    tier: E.env
    layer: env_infra
    name: Paper-trading orchestration (continuous, mirrors live)
    intent: |
      Per PIVOT Part G "What runs continuously vs on-demand": paper =
      continuous, mirrors live trading hours. Same scheduler as live.
      Production infrastructure (Function URL, IAM, telemetry).
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T2.1
      - PLATFORM:T0.6
      - PLATFORM:T1.12
    contract_gates:
      - path: docs/contracts/scheduled-orchestration.yaml
        class: B
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: Environment-as-config invariants crystallise during E.env.1 (EnvironmentConfig Pydantic authoring); per-env attestations follow.
      will_attest_when: before status == in_progress
  - id: E.env.7
    tier: E.env
    layer: env_infra
    name: Production ensemble runner (vol-scaled equal-weight, monthly rebalance)
    intent: |
      Per PIVOT Part E: 1/N * (target_vol / realised_vol_of_pipeline)
      weight per live pipeline. Monthly rebalance. RESIST dynamic /
      performance-weighted ensembles initially.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/production-ensemble.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: M
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: Environment-as-config invariants crystallise during E.env.1 (EnvironmentConfig Pydantic authoring); per-env attestations follow.
      will_attest_when: before status == in_progress
  - id: E.env.8
    tier: E.env
    layer: env_infra
    name: Reconciliation Lambda (every 30 min during session)
    intent: |
      Per PIVOT Part A 7-step write-path step 7 + MVP v1 spec.
      Reconciliation is non-negotiable. Drift > tolerance -> critical rec
      via ops portal.
      DEFERRED: build_lambda.py --deploy + run_scheduled_agent.py --smoke-test
      (pending Decision 67 reversal)
      Platform dependency: PLATFORM:T1.14 (DuckLake append-only write mode) is the
      prerequisite kernel; the reconciliation Lambda writes append-only event records
      and must wait for the platform kernel to be proven and deployed (direction:
      product depends on platform, not vice versa -- Decision 93).
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.6
      - PLATFORM:T2.1
      - PLATFORM:CD.16
      - PLATFORM:T-1.6
      - PLATFORM:T1.14
      - PLATFORM:T1.12
    contract_gates:
      - path: docs/contracts/reconciliation-lambda.yaml
        class: B
        contract_version: 1
      - path: docs/contracts/ops-reconciliations.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: M
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: true
    deferred_note: "DEFERRED: build_lambda.py --deploy + run_scheduled_agent.py --smoke-test (pending Decision 67 reversal)"
    five_property_test_waiver:
      reason: Environment-as-config invariants crystallise during E.env.1 (EnvironmentConfig Pydantic authoring); per-env attestations follow.
      will_attest_when: before status == in_progress
  - id: E.env.9
    tier: E.env
    layer: env_infra
    name: Feature precomputation batch job (Iceberg -> DynamoDB features_serving)
    intent: |
      Per PIVOT Part A "legitimate OLAP feeds OLTP pattern." Nightly DuckDB
      job over Iceberg writes features to DynamoDB features_serving keyed
      by symbol. One-way projection; live loop reads only.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T2.1
      - PLATFORM:T2.5
      - PLATFORM:T0.12
    contract_gates:
      - path: docs/contracts/dynamodb-features-serving.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/feature-projection-job.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: structural
    deferred: false
    five_property_test_waiver:
      reason: Environment-as-config invariants crystallise during E.env.1 (EnvironmentConfig Pydantic authoring); per-env attestations follow.
      will_attest_when: before status == in_progress
  - id: MVP.1
    tier: MVP
    layer: env_infra
    name: Wire MVP v1 composition (alpha + PC + exec) end-to-end
    intent: |
      Compose AlphaModelVersion + PortfolioConstructionVersion +
      ExecutionPolicyVersion into a single StrategyVersion per PIVOT
      Part C MVP v1 prescription. The composition_ref + contract_refs in
      docs/contracts/minimum-viable-v1.yaml; auditor populates exact refs.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:CD.25
    contract_gates:
      - path: docs/contracts/minimum-viable-v1.yaml
        class: A
        contract_version: 1
      - path: docs/contracts/strategies-versions.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: S
    strategic: false
    status: not_started
    validation_lens: pnl
    deferred: false
    five_property_test_waiver:
      reason: MVP composition emerges from L1-L4 landing; five-property attestations are inherited from the upstream layer items once those land.
      will_attest_when: before status == in_progress
  - id: MVP.2
    tier: MVP
    layer: env_infra
    name: Research-backtest stage gate (walk-forward CV + deflated Sharpe + PBO)
    intent: |
      Per PIVOT Part E four-stage funnel stage 1. Research backtest of
      MVP.1 against frozen canonical dataset. Promote to paper if criteria
      contract gates pass.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T2.5
    contract_gates:
      - path: docs/contracts/promotion-criteria.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/walk-forward-cv.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/deflated-sharpe-pbo.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: M
    strategic: false
    status: not_started
    validation_lens: pnl
    deferred: false
    five_property_test_waiver:
      reason: MVP composition emerges from L1-L4 landing; five-property attestations are inherited from the upstream layer items once those land.
      will_attest_when: before status == in_progress
  - id: MVP.3
    tier: MVP
    layer: env_infra
    name: Paper stage gate (3 months + same-code-path verified)
    intent: |
      Per PIVOT Part E stage 2. Out-of-sample paper trading 3-10 strategies
      for >=3 months. Criteria: deflated_sharpe + max_drawdown + same-
      code-path-verified + survivorship-inclusive.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T0.7b
    contract_gates:
      - path: docs/contracts/promotion-criteria.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/same-code-path.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/survivorship-stats.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: M
    strategic: false
    status: not_started
    validation_lens: pnl
    deferred: false
    five_property_test_waiver:
      reason: MVP composition emerges from L1-L4 landing; five-property attestations are inherited from the upstream layer items once those land.
      will_attest_when: before status == in_progress
  - id: MVP.4
    tier: MVP
    layer: env_infra
    name: Live-small stage gate (real money, small allocation, 3 months)
    intent: |
      Per PIVOT Part E stage 3. Real money fixed small allocation. True
      cost discovery via TCA. Criteria + implementation_shortfall_within_
      budget gate.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T0.7b
      - PLATFORM:T3.5
    contract_gates:
      - path: docs/contracts/promotion-criteria.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/tca-events.yaml
        class: A
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: M
    strategic: false
    status: not_started
    validation_lens: pnl
    deferred: false
    five_property_test_waiver:
      reason: MVP composition emerges from L1-L4 landing; five-property attestations are inherited from the upstream layer items once those land.
      will_attest_when: before status == in_progress
  - id: MVP.5
    tier: MVP
    layer: env_infra
    name: Live-full + production ensemble stage gate
    intent: |
      Per PIVOT Part E stage 4 + production ensemble. 3-5 live pipelines
      vol-scaled equal-weighted, monthly rebalance.
    depends_on: []
    cross_roadmap_depends_on:
      - PLATFORM:T0.12
      - PLATFORM:T0.7b
    contract_gates:
      - path: docs/contracts/promotion-criteria.yaml
        class: C
        contract_version: 1
      - path: docs/contracts/production-ensemble.yaml
        class: C
        contract_version: 1
    environment_scope: ['research', 'backtest_canonical', 'paper', 'live_small', 'live_full']
    effort: M
    strategic: false
    status: not_started
    validation_lens: pnl
    deferred: false
    five_property_test_waiver:
      reason: MVP composition emerges from L1-L4 landing; five-property attestations are inherited from the upstream layer items once those land.
      will_attest_when: before status == in_progress


cross_tier_gates:
  # PRODUCT-specific gates. Rules use the gate_helpers grammar (PLATFORM
  # helpers tier_complete/all_in_tier_with_status/grace_period_elapsed/
  # item_field_eq inherited; PRODUCT-local helpers defined in
  # document.gate_helpers above). Rule strings are illustrative; the
  # validator (scripts/roadmap/product_roadmap.py) will reject undeclared helpers
  # at load time.
  - id: G.research_backtest_before_paper
    name: Research backtest stage must complete before any paper-trading promotion
    rule: 'tier_complete("MVP") == false or item_field_eq(MVP.2, "status", "complete")'
    rationale: |
      Per PIVOT Part E four-stage funnel + CDP.17. No paper trading
      promotion until research backtest stage gate (MVP.2) has passed.
      Skipping stages is the #1 retail-quant failure mode.
    helpers_required: [tier_complete, item_field_eq]
  - id: G.paper_before_live_small
    name: Paper stage must complete before any live-small promotion
    rule: 'item_field_eq(MVP.3, "status", "complete") and grace_period_elapsed(MVP.3, 90)'
    rationale: |
      Per PIVOT Part E + CDP.17. Paper stage must be complete AND have
      a minimum 3-month grace period demonstrating realtime-market
      stability before live-small promotion. Pre-registered criteria in
      docs/contracts/promotion-criteria.yaml gate the specific
      deflated_sharpe / max_drawdown / consecutive_losing_weeks
      thresholds.
    helpers_required: [item_field_eq, grace_period_elapsed]
  - id: G.live_small_before_live_full
    name: Live-small must demonstrate cost discovery before live-full promotion
    rule: 'item_field_eq(MVP.4, "status", "complete") and grace_period_elapsed(MVP.4, 90) and implementation_shortfall_within_budget("mvp_v1", "live_small", 25.0)'
    rationale: |
      Per PIVOT Part E + CDP.17 + CDP.13. Live-small must be complete,
      90-day grace, AND implementation shortfall within budget. TCA
      cost discovery is the entire purpose of the live-small stage; no
      promotion without verified cost data.
    helpers_required: [item_field_eq, grace_period_elapsed, implementation_shortfall_within_budget]
  - id: G.alpha_validation_before_layer2_consumption
    name: Alpha must pass sizing-agnostic OOS metrics before any L2 strategy promotes
    rule: 'ic_oos("formula_score_top_n", 90) > 0.02 and ir_oos("formula_score_top_n", 90) > 0.5'
    rationale: |
      Per PIVOT Part E identifiability fix + CDP.34. L1 evaluation must
      pass sizing-agnostic metrics (IC, IR) before any L2 strategy can
      claim alpha quality. Prevents the aggressive-sizing-on-weak-signal
      blowup pattern. Numeric thresholds are placeholders pending
      docs/contracts/promotion-criteria.yaml ratification.
    helpers_required: [ic_oos, ir_oos]
  - id: G.same_code_path_verified_before_paper
    name: Backtest and live code paths must match before paper promotion
    rule: 'code_path_verification_present_for("mvp_v1") == true'
    rationale: |
      Per PIVOT Part E + CDP.5 same-code-path commitment. The verification
      must exist as recorded runtime evidence (E.env.10 backtest engine
      integration + the runtime-recording mechanism). Demoted from
      code_path_hash because the hash mechanism is its own tier_item.
    helpers_required: [code_path_verification_present_for]
  - id: G.no_options_until_stock_system_live_and_stable
    name: Options trading deferred until stock system is live and stable
    rule: 'live_full_uptime_days("mvp_v1") >= 180 and consecutive_quarters_positive_sharpe("mvp_v1") >= 2'
    rationale: |
      Per PIVOT Part G + CDP.10. Options is a separate discipline that
      reshapes L2 from weights to Greeks; adding it before stock system
      proves profitable is scope creep. Threshold is 6 months live_full
      uptime + 2 consecutive positive-Sharpe quarters as the
      hypothesis-trigger gate.
    helpers_required: [live_full_uptime_days, consecutive_quarters_positive_sharpe]
  - id: G.no_recursive_optimiser_before_mvp_complete
    name: Recursive optimiser construction blocked until MVP funnel complete
    rule: 'tier_complete("MVP")'
    rationale: |
      Per PIVOT Part E "What you DON'T do in v1." Build one strategy
      through the funnel first; THEN add a second strategy; THEN add
      the optimiser. Recursive optimiser-first is the architecture-
      before-substance anti-pattern.
    helpers_required: [tier_complete]
  - id: G.reconciliation_active_before_any_live_trading
    name: Reconciliation loop must be active before any live broker submission
    rule: 'reconciliation_active_for_env("live_small")'
    rationale: |
      Per PIVOT Part A + CDP.25. Reconciliation is non-negotiable.
      Without active reconciliation, broker-vs-cache drift is invisible
      and silent failures compound. Gate fires per env separately.
    helpers_required: [reconciliation_active_for_env]
  - id: G.contract_ratification_before_any_iceberg_write
    name: All Class A contract gates must be ratified before any new Iceberg write
    rule: 'all_contract_gates_ratified(D.lake.1) and all_contract_gates_ratified(D.lake.2) and all_contract_gates_ratified(D.lake.3) and all_contract_gates_ratified(D.lake.4) and all_contract_gates_ratified(D.lake.5) and all_contract_gates_ratified(D.lake.6) and all_contract_gates_ratified(D.lake.7)'
    rationale: |
      Per PLATFORM CD.25 ratification ritual + INTENT-pre-codegen-
      contract-ratification.md. Every Iceberg DDL writer must be gated
      on contract ratification. Currently every Class A contract here
      is in PLATFORM:CD.25 state; this gate becomes
      enforceable once the ritual lands.
    helpers_required: [all_contract_gates_ratified]
  - id: G.survivorship_inclusive_stats_required_for_promotion
    name: Promotion evaluation must include statistics from every pipeline (dead or alive)
    rule: 'survivorship_inclusive_stats_verified("mvp_v1")'
    rationale: |
      Per PIVOT Part E + CDP.19. Every pipeline that ever existed must
      be in the evaluation statistics, dead or alive. Survivorship-bias
      defence applied at every stage transition.
    helpers_required: [survivorship_inclusive_stats_verified]
  - id: G.no_synchronous_iceberg_write_from_decision_loop
    name: Decision-loop hot path must not write to Iceberg synchronously
    rule: 'item_field_eq(D.lake.8, "status", "complete")'
    rationale: |
      Per PIVOT Part A "what not to do" #1 + CDP.26. The buffered / SQS+
      Lambda Iceberg writer (D.lake.8) must be in place. Synchronous
      hot-path writes block the decision loop at Iceberg-commit
      timescales.
    helpers_required: [item_field_eq]
  - id: G.backtest_execution_timing_mirrors_live
    name: Backtest execution timing must mirror live (CDP.16)
    rule: 'item_field_eq(E.env.10, "status", "complete")'
    rationale: |
      Per PIVOT Part C trade-on-open-vs-close + CDP.16. Backtest engine
      must use the same execution-timing model as live (not the
      academic-backtest at-close assumption). E.env.10 is the backtest
      engine integration item.
    helpers_required: [item_field_eq]
  - id: G.dynamodb_cross_item_transaction_for_order_position_pair
    name: Order+position updates must use DynamoDB cross-item transactions
    rule: 'item_field_eq(D.fast.5, "status", "complete")'
    rationale: |
      Per PIVOT Part A + CDP.32. Hand-rolled concurrency is forbidden;
      DynamoDB cross-item transactions handle the order-update + position-
      update pair on fill events atomically.
    helpers_required: [item_field_eq]
  - id: G.no_neural_network_at_l3
    name: No neural-network execution at Layer 3 (CDP.14)
    rule: 'item_field_eq(L3.exec.8, "status", "complete")'
    rationale: |
      Per PIVOT Part E + CDP.14. L3 policies are rule-based with tuned
      thresholds. Marketable-limit v1 (L3.exec.8) is the production
      policy. Any ML at L3 must be filed as a separate plan with
      explicit critique gate.
    helpers_required: [item_field_eq]
  - id: G.asymmetric_research_to_prod_iam
    name: Research IAM must be READ-only against live tables (CDP.30)
    rule: 'item_field_eq(E.env.2, "status", "complete")'
    rationale: |
      Per PIVOT Part G Pattern C + CDP.30. Asymmetric IAM is the
      single-account discipline that gives Pattern A invariants without
      Pattern A overhead. E.env.2 lands the IAM + table-prefix isolation.
    helpers_required: [item_field_eq]

retired_items:
  # Per Subagent C catalogue PIVOT-SUPERSEDED classifications. Each entry
  # was a PRODUCT.md deliverable that the four-layer model or three-tier
  # data architecture supersedes. source_bullet_ids[] preserve per-bullet
  # traceability to docs/audit-reports/wave-1-outputs/C.yaml (added in
  # Checkpoint 3 completeness F1 fix). Single source_section can map to
  # many source_bullet_ids when conjunction-expanded.
  - source_section: "Phase 3 / Formula Integration (PostgreSQL pgvector schema)"
    source_bullet_ids: [73, 74, 75, 76, 78, 79]
    reason: |
      The Phase 3 formula_models / formula_outcomes PostgreSQL pgvector
      schema is PIVOT-SUPERSEDED. Per PIVOT Part A, broker is OLTP and
      DynamoDB is the fast tier - no PostgreSQL. Per PIVOT Part E,
      alpha versioning lives in alpha_versions Iceberg SCD2 table.
      Per CDP.22, component-as-versioned-artifact is the discipline.
      PostgreSQL pgvector was the pre-pivot strategy A; the four-layer
      model replaces it with strategy-as-composition.
  - source_section: "Phase 4 / A/B Testing Framework (50/50 traffic split, t-test, GitHub Actions promotion)"
    source_bullet_ids: [80, 81, 82, 83, 84, 85, 87]
    reason: |
      Phase 4 A/B testing is PIVOT-SUPERSEDED by the four-stage promotion
      funnel (research_backtest -> out_of_sample_paper -> live_small ->
      live_full) per PIVOT Part E + CDP.17. The funnel discovers a
      different class of bug at each stage; the simple A/B with t-test
      conflates statistical power across stages. Promotion criteria are
      pre-registered as ratified Class A contracts per CDP.18, not
      computed inline.
  - source_section: "Phase 5 / Circuit Breakers (Per-formula failure detection)"
    source_bullet_ids: [90, 92]
    reason: |
      The Phase 5 per-formula circuit breaker concept is PIVOT-RECLASSIFIED.
      Per PIVOT Part C + CDP.12, drawdown circuit breaker is an L2
      constraint (in construct_portfolio's constraints argument). Per
      PIVOT Part E + CDP.13, execution-side gating (spread, liquidity,
      vol-conditional) is an L3 policy tuned from live TCA, not a
      per-formula heuristic. The Phase 5 mechanism is split across
      L2.pc.7 (drawdown breaker) and L3.exec.9 (pre-trade gate) +
      L3.exec.12 (policy thresholds). Sharpe-decay-based auto-pause
      from Phase 5 is superseded by the explicit drawdown trigger.
  - source_section: "Phase 7 / Automated Weighting & Decay (Sharpe-based per-formula weighting)"
    source_bullet_ids: [101, 102, 103, 104, 105, 107]
    reason: |
      Phase 7 Sharpe-decay weighting is PIVOT-SUPERSEDED by the
      production-ensemble vol-scaled equal-weight rule per PIVOT Part E
      + CDP.20. Recent-performance weighting is mean-reverting noise at
      measurable timescales; dynamic weighting introduces correlation
      that defeats diversification. Monthly rebalance, weight = 1/N *
      (target_vol / realised_vol_of_pipeline). Resist dynamic ensembles.
  - source_section: "Phase 1 / formula_lineage Iceberg table + dual-env config split"
    source_bullet_ids: [3, 4]
    reason: |
      The Phase 1 formula_lineage table is PIVOT-SUPERSEDED by the
      SCD2 alpha_versions / pc_versions / exec_versions / strategies_versions
      Iceberg tables per PIVOT Part E + CDP.22. Single lineage table
      conflates component-level versioning with strategy-level composition;
      the four-table catalogue separates them with proper SCD2 semantics.
      The dual-env config split (config.company.yaml vs config.personal.yaml)
      is PIVOT-SUPERSEDED by EnvironmentConfig Pydantic (E.env.1) per
      CDP.6 environments-as-config-bundles.
  - source_section: "Phase Infra-Env / Multi-account staging+production model"
    source_bullet_ids: [109, 110, 115, 116, 120, 121]
    reason: |
      SCOPED TO THE PRODUCT CONFIG-PROMOTION AXIS ONLY (Decision 77). On the
      PRODUCT axis, the staging/production multi-account GitHub Environments
      model is PIVOT-RECLASSIFIED: per PIVOT Part G + CDP.7, single-account
      Pattern C (IAM + table-prefix isolation) is the correct posture for
      sole-dev; multi-account (Pattern A) is industrial overkill, and product
      phase advancement is promotion-as-config-change per CDP.6. CDP.6/CDP.7
      remain valid for the product axis.

      NOT RETIRED: the PLATFORM environment promotion train (sandbox -> SIT ->
      PROD as a gated deploy) is a DIFFERENT axis and is affirmed future-state.
      It lives in docs/INTENT-ci-cd-architecture.md section 6 and Decisions
      24/73, with the two-axis taxonomy in docs/contracts/environment-taxonomy.yaml.
      The platform also stays single-account until product live_full approaches
      real capital (the named PROD-account trigger). This retirement does not
      touch the platform axis.
  - source_section: "Phase 6 / Per-formula CloudWatch + Grafana surfaces"
    source_bullet_ids: [96, 97, 106]
    reason: |
      Per-formula CloudWatch metrics export + Grafana formula-performance
      dashboards + weight-distribution dashboards are PIVOT-SUPERSEDED.
      Per PIVOT Part E + CDP.20 vol-scaled equal-weight ensemble, the
      weight-distribution surface becomes a derived view over
      promotion_events + strategy_runs (Brinson attribution in L4.ops.2
      provides the canonical sizing surface). CloudWatch metrics are
      subsumed by LayerInvocation telemetry (L4.ops.1) emitted by every
      layer invocation; bespoke metrics emitter retired.
  - source_section: "Phase Infra-Platform / Copilot SDK migration P3 (parallel planning)"
    source_bullet_ids: []
    reason: |
      Phase Infra-Platform P3 (parallel multi-rec planning via SDK) is
      PIVOT-ORTHOGONAL but explicitly deferred per Decision 40 + PIVOT
      Part E "what NOT to do in v1." Decision 40's own migration trigger
      conditions are now moot (Copilot SDK path retired via Decision 116;
      Bedrock BYOK retired via CD.28, ratified by Decision 122); formal
      supersession of Decision 40 is pending, noted here so this citation
      is not left stale. Parallel execution is not a
      near-term priority - sequential processing on the autonomous
      executor VM at ~20 min per rec covers the open-rec queue in
      <3 calendar days. Parallelism becomes relevant only if throughput
      becomes the North Star bottleneck. Tracked as PLATFORM concern,
      not PRODUCT. source_bullet_ids intentionally empty - the catalogue
      classified Copilot SDK P3 entries as PIVOT-ORTHOGONAL not
      PIVOT-SUPERSEDED; this entry is structural acknowledgement that
      we declare P3 retired from PRODUCT roadmap regardless of
      catalogue classification.

out_of_product_scope:
  # Per Checkpoint 3 completeness F3 fix. PIVOT_ORTHOGONAL bullets from
  # Subagent C catalogue that are not PIVOT-superseded, not lifted to
  # PRODUCT tier_items, and not deferred to known_gaps. Each entry
  # carries an explicit disposition so a future reader knows why the
  # catalogued surface is not in tier_items[].
  - source_bullet_id: 6
    text: "config/README.md documenting environment configs"
    disposition: retained_as_doc_only
    note: |
      Documentation for the PRE-PIVOT dual-env split (config.company.yaml
      vs config.personal.yaml). The dual-env config is PIVOT-SUPERSEDED
      by EnvironmentConfig Pydantic (E.env.1); the README will be
      rewritten or retired as part of E.env.1 implementation.
  - source_bullet_id: 34
    text: "copilot-instructions.md modularisation (extract file router/gotchas/schema)"
    disposition: handed_to_platform
    note: |
      Repo-hygiene; falls under PLATFORM CD.4 + CD.20 (curated public
      portal). Not PRODUCT scope.
  - source_bullet_id: 35
    text: "Directory restructure aligning scripts/ src/ config/"
    disposition: handed_to_platform
    note: |
      Repo-hygiene; PLATFORM T-1.0 sweep + T0.14 Windows-assumption
      sweep cover similar refactors. Not PRODUCT scope.
  - source_bullet_id: 36
    text: "Workflow orphan cleanup (unused agents/prompts/workflows)"
    disposition: handed_to_platform
    note: |
      Repo-hygiene; PLATFORM T5.3 (legacy .github/prompts + .github/agents
      retirement) is the direct PLATFORM analog.
  - source_bullet_id: 37
    text: "Agent consolidation (merge overlapping agents)"
    disposition: handed_to_platform
    note: |
      Repo-hygiene; PLATFORM concern (agents are PLATFORM-tier under
      PLATFORM CD.4 + Decision 58 .agents canonicalisation).
  - source_bullet_id: 42
    text: ".agents canonical for interactive workflows; legacy .github/prompts compat-only"
    disposition: handed_to_platform
    note: |
      PLATFORM T5.3 covers this directly per Decision 58.
  - source_bullet_id: 86
    text: "Slack notifications for A/B test completion"
    disposition: obsolete_already
    note: |
      A/B testing framework is PIVOT-SUPERSEDED (retired_items Phase 4);
      Slack notification surface dies with it. Promotion events fire
      via promote_strategy() typed verb + promotion_events Iceberg
      table; ops portal handles operator notifications.
  - source_bullet_id: 91
    text: "Slack alerts when breaker opens"
    disposition: obsolete_already
    note: |
      Per-formula circuit breaker is PIVOT-RECLASSIFIED (retired_items
      Phase 5). Drawdown CB (L2.pc.7) fires via ops_recommendations
      (extended use per CDP.33) - no bespoke Slack channel needed.
  - source_bullet_id: 99
    text: "AWS Cost Explorer budgets terraform"
    disposition: handed_to_platform
    note: |
      Cost monitoring is PLATFORM concern - PLATFORM CD.6 + cost_projection
      block in ROADMAP-PLATFORM.yaml. Not PRODUCT scope. See KG.cost_controls
      for the catalogue traceability anchor.
  - source_bullet_id: 100
    text: "scripts/cost_report.py weekly email"
    disposition: handed_to_platform
    note: |
      Same as bullet 99. PLATFORM cost-tracking surface.
  - source_bullet_id: 127
    text: "copilot_instructions.md environment-strategy update"
    disposition: obsolete_already
    note: |
      Documentation for the PIVOT-SUPERSEDED multi-account env strategy;
      becomes obsolete when EnvironmentConfig (E.env.1) lands.
  - source_bullet_id: 128
    text: "docs/ENVIRONMENTS.md promotion workflow doc"
    disposition: obsolete_already
    note: |
      Same as bullet 127. The new promotion workflow is in promote_strategy()
      typed verb + promotion_funnel block in this YAML.
  - source_bullet_id: 129
    text: "GETTING_STARTED.md env-specific setup update"
    disposition: handed_to_platform
    note: |
      Repo-hygiene + onboarding surface. PLATFORM CD.20 curated public
      portal owns README.md / GETTING_STARTED.md scope.

open_questions:
  - id: OQ.1
    question: |
      RESOLVED 2026-05-24 by PLAN-cd25-platform-gap-sequencing. The six platform-gap
      sentinel entries (cd25-contract-ritual, t-1-12-product-contracts-schema-amendment,
      tca-aggregation, broker-secrets-manager, reconciliation-lambda,
      class-b-product-lambdas) were dispositioned in a single IMPLEMENTATION plan
      that resolved each sentinel to a concrete PLATFORM tier_item (CD.25, T-1.12,
      T3.5, T2.14, T1.14, T1.12 respectively). The known_platform_gaps[] block
      now reflects the resolved ids; cross_roadmap_depends_on edges and prose
      references point at the concrete PLATFORM ids.
    auditor_default_recommendation: |
      RESOLVED. Original recommendation: single follow-up REPORT-ONLY plan reviewing
      all six gaps with the user before any are filed as recs or PLATFORM tier_items.
      Implemented as PLAN-cd25-platform-gap-sequencing (IMPLEMENTATION rather than
      REPORT-ONLY because the INTENT had already been ratified upstream).
    consequences_if_resolved_differently: |
      RESOLVED. Original concern about per-gap routing producing bad sequencing
      did not materialise -- the single-plan resolution preserved coherent
      PLATFORM sequencing.
  - id: OQ.2
    question: |
      The scripts/roadmap/product_roadmap.py Pydantic schema does not yet exist.
      Without it, ROADMAP-PRODUCT.yaml cannot be validated by
      scripts/validate.py at presubmit. Should the schema be authored
      as a follow-up IMPLEMENTATION plan immediately, or deferred until
      the first PRODUCT tier_item executes?
    auditor_default_recommendation: |
      Author scripts/roadmap/product_roadmap.py + scripts/validate.py wiring as
      an immediate follow-up IMPLEMENTATION plan. Without it, this YAML
      drifts uncaught - the agent-first principle requires structural
      validation. Plan should reference the schema delta code in
      docs/audit-reports/wave-1-outputs/G.yaml::schema_delta_code.
    consequences_if_resolved_differently: |
      Deferring leaves the YAML as unenforced documentation. Subsequent
      edits can silently drop required fields (the extra='forbid'
      discipline only fires when the validator runs). At minimum, file
      a rec for the schema-extension follow-up.
  - id: OQ.3
    question: |
      Does the auditor's classification of operations_telemetry
      observability metrics (paper_vs_live_divergence,
      reconciliation_drift_rate, code_path_hash_divergence) need to
      become its own tier_item, or are they sufficiently captured by
      L4.ops.1 LayerInvocation infrastructure?
    auditor_default_recommendation: |
      Sufficient under L4.ops.1 - the drift-detector metrics are
      computed by jobs that consume LayerInvocation events. No
      additional tier_item needed in v1; revisit if drift detection
      proves to need dedicated infrastructure.
    consequences_if_resolved_differently: |
      If a dedicated tier_item is needed, propose L4.ops.6 in a
      follow-up plan referencing PIVOT Part G "diagnostic question"
      framing.
  - id: OQ.4
    question: |
      Should the MVP composition reference (mvp_v1@<hash>) be authored
      as docs/contracts/minimum-viable-v1.yaml immediately in a
      ratification predecessor plan, or wait until L1.alpha + L2.pc +
      L3.exec components exist as ratified versions?
    auditor_default_recommendation: |
      Wait until component contracts ratify. The composition references
      component versions that don't exist yet; authoring a composition
      pointing at nonexistent components is the cart-before-horse
      anti-pattern. Track docs/contracts/minimum-viable-v1.yaml as a
      late-stage MVP item (effectively MVP.1's contract_gates entry).
    consequences_if_resolved_differently: |
      Early authoring would create a "provisional" composition contract
      with no enforceable consumers. Acceptable if the user wants the
      MVP shape committed early as a north-star artefact; otherwise
      defer.
  - id: OQ.5
    question: |
      The same-code-path gate (G.same_code_path_verified_before_paper)
      uses code_path_verification_present_for() rather than
      code_path_hash() because the hash mechanism doesn't exist yet.
      Is the runtime-recording substrate (E.env.10) sufficient, or does
      a dedicated hash-emission tier_item need to land?
    auditor_default_recommendation: |
      Sufficient via E.env.10 + L4.ops.1 LayerInvocation events
      carrying code_path_hash field. No dedicated tier_item; revisit
      if hash-only gating proves brittle.
    consequences_if_resolved_differently: |
      If dedicated tier_item is needed, propose L4.ops.6 (code-path
      hash emission + divergence detector) in a follow-up plan.

known_platform_gaps:
  # All six gaps resolved 2026-05-24 by PLAN-cd25-platform-gap-sequencing. This block
  # is now a satisfied-gap audit trail; intended_platform_tier_item points at the live
  # PLATFORM id that absorbs each gap.
  - id: GAP-cd25-contract-ritual
    description: Pre-codegen contract ratification ceremony per CD.25.
    intended_platform_tier_item: CD.25
  - id: GAP-t-1-12-product-contracts-schema-amendment
    description: T-1.12 needs schema amendment to admit product contracts. Absorbed into T-1.12's own scope (INTENT v4 frames the schema amendment AT T-1.12).
    intended_platform_tier_item: T-1.12
  - id: GAP-tca-aggregation
    description: Transaction-cost-analysis aggregation Lambda for L3 telemetry.
    intended_platform_tier_item: T3.5
  - id: GAP-broker-secrets-manager
    description: Broker credentials in AWS Secrets Manager with rotation.
    intended_platform_tier_item: T2.14
  - id: GAP-reconciliation-lambda
    description: >-
      Daily broker-vs-cache reconciliation Lambda. PLATFORM:T1.14 (DuckLake append-only write mode)
      is the prerequisite kernel; the reconciliation Lambda is a consumer of the kernel, not its
      implementation. Gap remains open pending T1.14 completion and a successor reconciliation item.
    intended_platform_tier_item: T1.14
  - id: GAP-class-b-product-lambdas
    description: Class-B Lambda packaging for product-tier handlers. Absorbed by INTENT v4's T1.12 Class B ratification wave.
    intended_platform_tier_item: T1.12
known_gaps:
  # Items deliberately out of scope for this audit. Each gap is intentional
  # (PIVOT-DEFERRED or PIVOT-ORTHOGONAL); the rationale is captured here so
  # future readers don't re-discover and re-defer.
  - id: KG.options
    gap: Options trading (CDP.10 deferral)
    notes: |
      Options trading reshapes L2 from "aggregate weights" to "aggregate
      Greek exposures" and L3 from "single-leg orders" to "multi-leg
      with expiry state machine." Per PIVOT Part G + CDP.10 + cross-
      tier gate G.no_options_until_stock_system_live_and_stable.
      Hypothesis trigger: 6 months live_full uptime + 2 consecutive
      positive-Sharpe quarters AND a specific options strategy hypothesis
      with tested edge. The seven architectural extensions listed in
      PIVOT Part G (instrument schema with OCC ID; pricing model; Greeks
      at decision time; portfolio-level Greek aggregation; multi-leg
      orders; expiry state machine; assignment risk monitor) all
      represent post-MVP tier_items.
      open_design_questions:
        - Which use-case to target first (covered calls; cash-secured puts; tail hedging; directional speculation; vol trading)?
        - Greek aggregation tier (L2 extension) vs separate options L4?
        - Multi-leg execution model (single broker ticket per spread vs leg-by-leg)?
  - id: KG.multi_asset
    gap: Multi-asset generalisation (cross-market, FX, commodities, crypto)
    notes: |
      Per Subagent C catalogue Phase 8+ post-launch + PIVOT Part C
      reading list framing. Multi-asset generalisation requires factor
      models tuned to each asset class, cross-currency hedging, and
      market-microstructure adapters per venue. Out of scope until
      single-market (FTSE 100 equities) trading is profitable in
      live_full.
  - id: KG.alternative_data
    gap: Alternative data sources (Reddit sentiment, news NLP, satellite, options-flow) + data vendor expansion (Alpha Vantage, Polygon, Bloomberg)
    notes: |
      Per Subagent C Phase 8+ post-launch + Checkpoint 3 completeness F5
      fix. Alternative data integration adds L1 surfaces but does not
      change the four-layer architecture. Defer until equity-only L1
      (technicals + fundamentals + sentiment) proves alpha-positive in
      live_small.
      Data-vendor expansion (Alpha Vantage / Polygon / Bloomberg) is a
      separate concern in the same gap class: yfinance is the v1 provider
      per L0.1; vendor-redundancy + hourly-beyond-730-days coverage
      requires Alpha Vantage or Polygon when data quality / latency
      becomes binding. Reconsider when daily yfinance + 1-month-hourly
      backfill (L0.4) proves operationally insufficient.
  - id: KG.intraday_streaming
    gap: Intraday streaming data via Kinesis / WebSockets (PIVOT Part A defer)
    notes: |
      Per PIVOT Part A "what not to do" #6 + CDP.27. SQS + Lambda is
      sufficient at single-agent volume. Intraday streaming becomes
      relevant only with multiple consumers of the same stream.
      Reconsider when production ensemble scales beyond 5 pipelines
      or when sub-minute decision cadence is required.
  - id: KG.regulatory_compliance
    gap: Regulatory compliance reporting (MiFID II, Dodd-Frank)
    notes: |
      Per Subagent C Phase 8+ post-launch. Compliance is a function of
      capital scale and jurisdiction; out of scope at sole-dev scale.
      Revisit when capital exceeds professional-investor thresholds.
  - id: KG.formula_explainability
    gap: Formula explainability (SHAP values, decision attribution at the alpha level)
    notes: |
      Per Subagent C Phase 8+ post-launch. Captured loosely under
      L4.ops.2 (Brinson attribution); deeper SHAP-style explainability
      is a research direction not core MVP. Reconsider when alpha
      versioning catalogue (L1.alpha.2) holds 10+ alphas in production
      and per-alpha attribution becomes operationally interesting.
  - id: KG.external_marketplace
    gap: External formula marketplace integration
    notes: |
      Per Subagent C Phase 8+ post-launch. External-marketplace
      integration introduces an L1 supply chain that the existing
      AlphaModelVersion catalogue subsumes. No dedicated tier_item.
  - id: KG.recursive_optimiser_v2
    gap: Recursive optimiser for component selection (post-MVP)
    notes: |
      Per PIVOT Part E "what NOT to do in v1" + cross_tier_gate
      G.no_recursive_optimiser_before_mvp_complete. The optimiser that
      proposes new component versions (extending the existing PLATFORM
      autonomous-improvement substrate to product) is intentionally
      deferred. Build one strategy through the funnel first; add a
      second strategy alongside; THEN add the optimiser that proposes
      new versions across components.
  - id: KG.cd25_lifetime_pending
    gap: PLATFORM:CD.25 is the parent gate for many tier_items
    notes: |
      Per INTENT-pre-codegen-contract-ratification.md round-3 M7
      sequencing note: CD.25 is state:pending for the full project
      lifetime until the log-decision Lambda is live (PLATFORM T0.7b).
      This means many product items have a long-lived GAP dependency.
      Readers should expect to see the GAP active alongside fully-active
      contract YAMLs - this is the bootstrap clause operating as
      designed. Mitigation: PLATFORM CD.25 bootstrap_allowance: true
      permits contract authoring under pending CD.25 state, identically
      to how T-1 items completed under pending CD.1.

  # Items added per Checkpoint 3 completeness F6/F7 fix.
  - id: KG.multi_broker_capacity_scaling
    gap: Multi-broker capacity scaling (IBKR swap-in beyond Alpaca capacity)
    notes: |
      Per PIVOT Part C reference + Checkpoint 3 completeness F6 fix.
      L3.exec.4 specifies AlpacaBroker for both live-small and initial
      live-full; L3.exec.5 names IBKRBroker as the optional capacity
      scale-out path but is itself a tier_item (not a KG). This KG
      entry tracks the broader hypothesis trigger: when does multi-
      broker routing become a v2 concern? Trigger heuristics: (a)
      live_full notional > $500k AND fill quality degrades on Alpaca
      vs benchmark broker; (b) execution-policy thresholds (L3.exec.12)
      indicate spread / liquidity gating fires > 5% of orders in
      Alpaca routing; (c) regulatory access changes make IBKR preferable.
      Until then, capacity is a single-broker concern.
  - id: KG.code_path_hash_emission
    gap: Dedicated code-path-hash emission + divergence detector tier_item
    notes: |
      Per Checkpoint 3 completeness F6 + audit open_question OQ.5.
      Current discipline: code_path_verification_present_for() gate
      helper checks for a runtime-recorded code-path verification;
      L4.ops.1 LayerInvocation events carry the code_path_hash field;
      E.env.10 backtest engine integration emits the hash from inside
      the backtest run. No dedicated tier_item lands a hash-divergence
      DETECTOR (a job that reads LayerInvocation rows across env
      partitions and asserts equal hashes per strategy_version_id).
      Hypothesis trigger: when paper-vs-live divergence (evaluation_
      metric layer_4_operations_telemetry.paper_vs_live_divergence)
      consistently exceeds budget over 3 promotion cycles, file as
      L4.ops.6 follow-up tier_item.
  - id: KG.paper_vs_live_divergence_observer
    gap: Dedicated paper-vs-live divergence observer + alarm tier_item
    notes: |
      Per Checkpoint 3 completeness F6 + PIVOT Part E champion-challenger
      framework + Part G platform/product diagnostic question. The
      metric paper_vs_live_divergence is declared in evaluation_metrics.
      layer_4_operations_telemetry but no scheduled observer lands the
      alarm filing. Current discipline: drift events file recs via
      ops_recommendations (extended use per CDP.33). Dedicated tier_item
      L4.ops.7 (paper-vs-live observer Lambda or Fargate task) is
      deferred until E.env.11 (champion-challenger paper alongside
      live) lands and the metric is actually computed at scheduled
      cadence.
      DEFERRED: build_lambda.py --deploy + run_scheduled_agent.py --smoke-test (pending Decision 67 reversal)
  - id: KG.tca_bucket_schema
    gap: TCA bucket schema enumeration as Class A contract addendum
    notes: |
      Per Checkpoint 3 completeness F6 + PIVOT Part E implementation
      shortfall bucketing dimensions. L3.exec.10 names the bucket
      surface (time-of-day, spread quintile, ADV quintile, vol regime,
      side) but does not lock the bucket schema as a Class A contract
      addendum. tca-events.yaml docs/contracts/tca-events.yaml Class A
      contract MUST enumerate the bucket dimensions verbatim from PIVOT
      Part E when authored in follow-up plan (post CD.25 ratification).
      Without bucket schema lock, L2.pc.10 (cost-aware PC) and L3.exec.12
      (policy thresholds) consume inconsistent bucket definitions across
      time.
  - id: KG.tail_hedge_sleeve_surface
    gap: Tail-hedge sleeve as L2 behavioural-overlay surface
    notes: |
      Per Checkpoint 3 completeness F6 + PIVOT Part G + CDP.36. Tail-
      hedge sleeve (1-3% capital in long OTM puts) is named in CDP.36
      as behavioural-overlay-not-edge but has no tier_item, no contract
      surface, no constraint surface in L2.pc.5 or L2.pc.8. Defer with
      explicit hypothesis trigger: when MVP.5 (live_full) reaches 6
      months uptime + 2 consecutive positive-Sharpe quarters AND
      operator behaviour audit shows alpha-sizing-too-conservative,
      file follow-up plan adding L2.pc.13 tail-hedge sleeve surface
      with explicit Greeks-aware constraint set (touches options
      machinery; gated on KG.options resolution too).
  - id: KG.cost_controls
    gap: 'Cost monitoring + budget controls (current_state limitation: "No cost controls beyond budgets")'
    notes: |
      Per Checkpoint 3 completeness F7 + current_state PRODUCT.md
      limitation "No cost controls beyond budgets." Cost monitoring
      is delegated to PLATFORM (see out_of_product_scope source bullets
      99, 100). PLATFORM cost_projection block in ROADMAP-PLATFORM.yaml
      + PLATFORM CD.6 cover the substrate. PRODUCT-tier cost concerns
      surface only via per-trade transaction cost analysis (L3.exec.11
      TCA aggregation) which is a different surface from infrastructure
      cost monitoring. No PRODUCT tier_item is needed; this KG entry
      preserves the catalogue traceability anchor for the silent-drop
      finding F7 raised.
  - id: KG.multi_product_topology
    gap: This is the TRADING product roadmap -- one of N products hosted on the shared platform.
    notes: |
      Prospective products (reaper-tools, dbt-daywork) are not yet real; when they become real they
      would each own their own product roadmap. The platform operational data plane is shared and
      distinguished by project_id (one DuckLake ops lakehouse, N products as rows keyed by origin
      dimension).

      Cross-references: docs/INTENT-multi-product-platform.md (exploratory multi-product design record),
      platform KG.1 (product-agnostic platform roadmap framing), platform CD.32 (multi-product platform
      topology candidate decision with open decisions OD-1..OD-5). The trading product scope of this
      roadmap -- L0-L4, D.fast, D.lake, E.env, MVP tiers -- is unchanged by the multi-product framing.
