schema_version: 4
handoff_policy:
  full_validation_required_before_commit: true
  timeout_disposition: blocked
slug: "reader-projection-substrate"
intent: >-
  Build the substrate that makes reader-projection mismatches provable: a declared VERB_FIELDS
  table proven against the registry SQL, a fixture helper that forces test rows into the shape a
  verb actually emits, and a loud-failing query(). Non-breaking by construction -- strict access
  enforcement is deliberately sequenced AFTER the call-site repairs it would otherwise red.
plan_type: IMPLEMENTATION
verification_tier: V3
plan_path: docs/plans/PLAN-reader-projection-substrate.yaml
phase: >-
  Ad-hoc platform hardening on the ops read boundary (soft-warn exception category ad_hoc_rec),
  so the Tier Item Freshness Gate is skipped. Names no tier_item: defect-class groundwork on the
  Decision 84 I-3 named-verb surface, not roadmap-sequenced work.
scope:
  - file: src/common/ducklake_reader_client.py
    action: Modify
    purpose: >-
      Add VERB_FIELDS (declared projection per verb; the four SELECT * verbs carry an
      ALL_TABLE_COLUMNS sentinel rather than ~80 hand-copied names, so a routine field_semantics
      column add cannot drift them) and a literal registry-version pin; make
      query() raise instead of swallowing every exception and returning None. Deliberately does
      NOT add strict Row access -- see the sequencing note in context.
  - file: tests/fixtures/reader_rows.py
    action: Create
    purpose: >-
      verb_rows(verb, [...]) strips hand-authored dicts to the verb's declared projection, so a
      fixture cannot express a row shape the reader never emits. Lives in tests/fixtures/ because
      Decision 131 exempts that tree from the no-cross-test-import guard.
  - file: tests/common/ducklake_reader_client/test_verb_fields_parity.py
    action: Create
    purpose: >-
      Executes each NAMED_READS verb's SQL in in-memory DuckDB and asserts the column set equals
      its VERB_FIELDS entry; SELECT * verbs resolve via resolve_table_spec(table).ordered_columns
      (a tuple of (name, sql_type) pairs). Note rec_history selects from {hist} while its
      NamedRead.table is ops_recommendations -- the sentinel resolves correctly only because
      _order_columns is shared between the history and current tables.
      Also asserts the client's literal version pin equals NAMED_READS_VERSION at this commit.
  - file: tests/common/ducklake_reader_client/test_reader_rows_helper.py
    action: Create
    purpose: verb_rows stripping behaviour, including rejection of an unknown verb name.
  - file: tests/common/ducklake_reader_client/test_ducklake_reader.py
    action: Modify
    purpose: >-
      Three assertions require query() to return None on error (lines ~124, ~132, ~187); they
      must become pytest.raises now that query() raises. Declared here because
      validate_scope_boundary runs in both tiers and implement-scope-boundary.yaml forbids
      resolving an undeclared touched path by editing scope after the fact.
  - file: tests/convergence_health/test_escalate.py
    action: Modify
    purpose: >-
      Convert the hand-authored fixtures at lines ~19-22 (which carry source and status, a shape
      open_recs never emits) to verb_rows("open_recs", ...), and mark the lookup assertions
      xfail(strict=True) naming the repair plan. This turns the plan's own diagnosis into a
      standing, self-invalidating record instead of a green test over broken production code.
bundled_recommendations: []
infrastructure_dependencies: []
acceptance_criteria:
  - >-
    VERB_FIELDS declares a column tuple for every fixed-column NAMED_READS verb and the
    ALL_TABLE_COLUMNS sentinel for the four SELECT * verbs; a test proves each fixed-column entry
    equals the column set that verb's SQL actually produces in DuckDB, and resolves each sentinel
    through resolve_table_spec(table).ordered_columns at assertion time -- which yields
    (name, sql_type) PAIRS, so project it as [c for c, _ in spec.ordered_columns]. No SELECT *
    column list is hand-copied, so an ops_tables column add cannot drift the declaration.
  - >-
    The client's literal registry-version pin equals NAMED_READS_VERSION at this commit, asserted
    by test rather than by comment.
  - >-
    verb_rows(verb, rows) returns each row containing exactly the verb's declared columns --
    extra keys dropped, and an unknown verb name raises rather than passing through.
  - >-
    TestFindOpenConvergenceStaleRec's two fixture lists (five dicts, lines ~19-23 and ~29-32)
    build rows through
    verb_rows("open_recs", ...). EXACTLY ONE test is marked xfail(strict=True):
    test_returns_first_matching_rec. test_returns_none_when_no_match and
    test_returns_none_on_empty_list still pass after stripping and MUST NOT be marked, or
    strict=True reports XPASS and reds the step. The remaining ~12 hand-authored fixture sites in
    that file convert in the call-site repair plan, not here.
  - >-
    DuckLakeReader.query() raises on reader failure instead of returning None, and its three
    former return-None assertions are pytest.raises.
  - >-
    _READER_TRANSIENT_STATUS survives the edit and still equals scripts.ops_portal
    .reader_transient's set (Decision 155 clause 2).
  - >-
    NO strict-access behaviour ships in this plan: no Row subclass, no UnprojectedField, no
    legacy roster. Reading an unprojected field still returns None after this change.
  - >-
    Both triggered deploy channels conclude green: the automatic deploy-ducklake-lambdas run
    (fired by this file matching its src/common/ducklake_*.py glob) and the manually dispatched
    deploy-prod-lambdas run. No shell-executable verifier for a run conclusion exists here (the
    gh CLI is prohibited by AGENTS.md and the agent profile lacks lambda:GetFunctionConfiguration),
    so both conclusions are read via mcp__github__actions_get and recorded in the handoff report;
    the VP asserts the preconditions and the absence of any break-glass local deploy.
verification_plan:
  - step: 1
    phase: pre-deploy
    action: >-
      Every verb's VERB_FIELDS entry matches the column set its registered SQL actually produces,
      executed in in-memory DuckDB rather than compared to a hand-copied list.
    command: bin/venv-python -m pytest tests/common/ducklake_reader_client/test_verb_fields_parity.py::test_verb_fields_match_registry_sql -q
    expected: exit 0; all 12 NAMED_READS verbs covered, the four SELECT * verbs resolved via resolve_table_spec
    fix_if: A mismatch means VERB_FIELDS drifted from the registry -- correct the declaration, never relax the test.
    hermetic: false
    graduation: graduate
    graduation_check_id: verb-fields-match-registry-sql
  - step: 2
    phase: pre-deploy
    action: The client's literal version pin equals NAMED_READS_VERSION, so same-commit parity is asserted not assumed.
    command: bin/venv-python -m pytest tests/common/ducklake_reader_client/test_verb_fields_parity.py::test_version_pin_matches_registry -q
    expected: exit 0
    fix_if: >-
      A drifted pin means VERB_FIELDS was updated without re-checking the registry version, or
      the registry moved without updating the client.
    hermetic: false
    graduation: graduate
    graduation_check_id: verb-fields-version-pin-matches
  - step: 3
    phase: pre-deploy
    action: verb_rows strips a fixture dict to the verb's declared projection and rejects an unknown verb.
    command: bin/venv-python -m pytest tests/common/ducklake_reader_client/test_reader_rows_helper.py -q
    expected: exit 0; extra keys dropped, unknown verb raises
    fix_if: >-
      If extra keys survive, fixtures can still express shapes production never emits -- the
      mechanism by which this defect survived three prior repairs.
    hermetic: true
    graduation: graduate
    graduation_check_id: verb-rows-strips-to-projection
  - step: 4
    phase: pre-deploy
    action: >-
      The converted convergence fixtures reproduce production blindness: with open_recs-shaped
      rows the dedup lookup finds nothing, and the assertions are xfail(strict=True) so the test
      fails loudly the moment a repair makes it pass without being rewritten.
    command: bin/venv-python -m pytest tests/convergence_health/test_escalate.py -q -rx
    expected: exit 0 with `1 xfailed` reported (not XPASS, not a bare PASS)
    fix_if: >-
      XPASS means the lookup started matching without the test being updated -- rewrite the test
      against the repaired lookup rather than deleting the marker.
    hermetic: true
    graduation: graduate
    graduation_check_id: convergence-fixtures-verb-shaped-xfail
  - step: 5
    phase: pre-deploy
    action: >-
      query() raises on reader failure rather than returning None (Decision 55 loud-fail +
      Decision 84 I-3's closed boundary; NOT I-4, which is a write invariant), and
      _READER_TRANSIENT_STATUS still equals the ops_portal.reader_transient set.
    command: bin/venv-python -m pytest tests/common/ducklake_reader_client/test_ducklake_reader.py tests/ops_data_portal/test_reader_transient.py -q
    expected: exit 0; the three former return-None assertions now assert a raise
    fix_if: >-
      A surviving `except Exception: return None` in query() reintroduces the silent-degradation
      path; a changed _READER_TRANSIENT_STATUS breaks Decision 155 clause 2's equality assertion.
    hermetic: false
    graduation: graduate
    graduation_check_id: query-raises-transient-export-intact
  - step: 6
    phase: pre-deploy
    action: >-
      Prove the non-breaking claim mechanically: DuckLakeReader.query() has NO production call
      sites, so making it raise cannot red any live path.
    command: |
      if grep -rn '\.query(' --include='*.py' scripts/ src/ | grep -v 'def query'; then
        echo "FAIL - production .query() call site found"
        exit 1
      fi
      echo "OK - no production .query() call sites"
    expected: prints the OK line; every .query( occurrence is confined to tests/
    fix_if: >-
      A production call site means query() raising IS breaking -- bring that caller into scope
      with an explicit try/except rather than shipping a silent behaviour change.
    hermetic: false
    graduation: graduate
    graduation_check_id: reader-query-no-production-callers
  - step: 7
    phase: pre-deploy
    action: >-
      The xfail reasons cite a filed recommendation id, so the production defect the markers
      encode has a durable record rather than living only as test metadata.
    command: |
      grep -Eq 'xfail\(strict=True[^)]*rec-[0-9]+' tests/convergence_health/test_escalate.py && echo "OK - xfail reason cites a rec id"
    expected: prints `OK - xfail reason cites a rec id`
    fix_if: >-
      File the dedup-blindness rec first (execution step 1) and cite its id in the xfail reason;
      an xfail pointing at a non-existent 'repair plan' is an orphan.
    hermetic: false
    graduation: graduate
    graduation_check_id: convergence-xfail-cites-rec
  - step: 8
    phase: pre-deploy
    action: >-
      The per-file coverage mapping check passes for the modified client. (The 88.3 ratchet in
      config/coverage_baseline.yaml is enforced separately inside validate.py -- this command runs
      the per-file check, not that comparison.)
    command: bin/venv-python -m scripts.test_coverage_checker --check-coverage --file src/common/ducklake_reader_client.py
    expected: exit 0 with a PASS verdict (the checker emits PASS/FAIL and "all N source files at 100%", never a bare percentage to compare)
    fix_if: On FAIL, add assertions rather than lowering the baseline.
    hermetic: false
    graduation: waive
    graduation_waiver_reason: >-
      Coverage is already ratcheted by its own standing baseline gate inside validate.py;
      graduating a second per-file coverage check would duplicate that mechanism rather than
      guard any behaviour this plan introduces.
  - step: 9
    phase: pre-deploy
    action: Fast presubmit tier passes on the branch.
    command: bin/venv-python -m scripts.validate --pre
    expected: exit 0
    fix_if: Diagnose and fix on the branch; a --pre failure files no rec and gates nothing.
    hermetic: false
    graduation: not-applicable
  - step: 10
    phase: pre-deploy
    action: >-
      Full validation tier completes green before commit/PR, as handoff_policy declares
      (Decision 163 / rec-2965).
    command: bin/venv-python -m scripts.validate
    expected: exit 0
    fix_if: On timeout the disposition is `blocked` -- resume later and rerun from the start.
    hermetic: false
    graduation: not-applicable
  - step: 11
    phase: post-deploy
    action: >-
      Assert the two-channel deploy preconditions. This file matches BOTH
      deploy-ducklake-lambdas.yml's 'src/common/ducklake_*.py' glob (so merging fires the
      five-artifact DuckLake chain automatically -- byte-identical, since no ducklake manifest
      lists this file, but it must still go green and it IS ci-rca-watched) AND nothing in
      deploy-prod-lambdas.yml's paths filter (so the prod redeploy needs a manual dispatch). The
      implementing agent then reads both run conclusions via mcp__github__actions_get.
    command: |
      bin/venv-python -c "
      import yaml, pathlib, fnmatch
      g = yaml.safe_load(pathlib.Path('.github/workflows/deploy-ducklake-lambdas.yml').read_text())
      p = yaml.safe_load(pathlib.Path('.github/workflows/deploy-prod-lambdas.yml').read_text())
      gon = g[True] if True in g else g['on']
      pon = p[True] if True in p else p['on']
      f = 'src/common/ducklake_reader_client.py'
      assert any(fnmatch.fnmatch(f, x) for x in gon['push']['paths']), 'ducklake channel no longer auto-fires'
      assert not any(f.startswith(x.rstrip('*/')) for x in pon['push']['paths']), 'prod filter now covers this path'
      assert 'workflow_dispatch' in pon, 'no manual dispatch path on the prod channel'
      print('OK - ducklake auto-fires, prod needs manual dispatch')
      "
    expected: prints the OK line; both dispatched/triggered runs then conclude success
    fix_if: >-
      If the DuckLake run's reconcile-gate refuses (non-empty pending_reconcile on a
      reconcile_scope=ducklake table), that is a pre-existing warehouse condition, not this
      change -- resolve it before re-running; do NOT fall back to a local build_lambda --deploy
      (break-glass only, Decision 125 cl.3).
    hermetic: false
    graduation: not-applicable
  - step: 12
    phase: post-deploy
    action: No break-glass local deploy was used anywhere in this change.
    command: |
      if git diff origin/main --unified=0 -- 'src/*' 'scripts/*' 'tests/*' '.github/*' | grep -E '^\+.*build_lambda.*--deploy'; then
        echo "FAIL - break-glass deploy invocation added"
        exit 1
      fi
      echo "OK - no break-glass deploy invocation added"
    expected: prints the OK line
    fix_if: Remove the local deploy invocation; the governed channels are the only sanctioned route.
    hermetic: false
    graduation: not-applicable
test_obligations:
  - source: src/common/ducklake_reader_client.py
    behavior: query() raises on reader failure instead of returning None.
    verification_step: 5
    test_selector: tests/common/ducklake_reader_client/test_ducklake_reader.py::test_ducklake_reader_query_raises_on_error
    red_green_expectation: >-
      red before -- the pre-change assertion is `query(...) is None`, so the rewritten
      pytest.raises form fails until the method actually raises; green after.
  - source: src/common/ducklake_reader_client.py
    behavior: VERB_FIELDS declares the true projected column set for every registered verb.
    verification_step: 1
    test_selector: tests/common/ducklake_reader_client/test_verb_fields_parity.py::test_verb_fields_match_registry_sql
    red_green_expectation: red before (VERB_FIELDS does not exist -- ImportError at collection), green after.
  - source: src/common/ducklake_reader_client.py
    behavior: the literal registry-version pin tracks NAMED_READS_VERSION.
    verification_step: 2
    test_selector: tests/common/ducklake_reader_client/test_verb_fields_parity.py::test_version_pin_matches_registry
    red_green_expectation: red before (the pin does not exist -- ImportError at collection), green after.
  - source: tests/fixtures/reader_rows.py
    behavior: verb_rows() drops keys outside the named verb's declared projection.
    verification_step: 3
    test_selector: tests/common/ducklake_reader_client/test_reader_rows_helper.py::test_verb_rows_strips_extra_keys
    red_green_expectation: red before (the helper does not exist -- collection error), green after.
  - source: tests/fixtures/reader_rows.py
    behavior: verb_rows() raises on an unregistered verb name rather than passing rows through.
    verification_step: 3
    test_selector: tests/common/ducklake_reader_client/test_reader_rows_helper.py::test_verb_rows_rejects_unknown_verb
    red_green_expectation: red before (the helper does not exist -- collection error), green after.
constraints:
  - No rescue agents or workaround loops (Decision 55).
  - >-
    NO strict-access behaviour in this plan. No Row subclass, no UnprojectedField, no legacy
    roster, no ProjectionDrift assertion. Adding any of them here reds four hourly
    convergence-health.yml steps -- see the sequencing note in context.
  - >-
    NO change to src/common/ducklake_scd2_schema.py or any verb SQL -- packaged into five Lambda
    artifacts; VERB_FIELDS mirrors it client-side because data-pipeline's manifest excludes it.
  - >-
    Do NOT derive projections by parsing verb SQL at runtime -- fragile, and unresolvable for
    SELECT * verbs. The declaration plus the DuckDB parity test is the honest form.
  - >-
    Do NOT widen deploy-prod-lambdas.yml's paths filter here -- it is kept in sync with
    PROD_SOURCE_PATHSPECS (code_drift.py:261) and changing one without the other desyncs the
    drift oracle. Manual dispatch is the sanctioned route for this change.
  - Do not raise config/sloc_budgets.yaml -- projected ~370 SLOC stays under the 500 threshold.
context:
  - "decision-scout verdict: FLAGS_FOUND (134 of 134 triaged). CITE: Decision 84 (I-1/I-3/I-4),
    Decision 55, Decision 61, Decision 79, Decision 125+126, Decision 88, Decision 155,
    Decision 163, Decision 170, Decision 128+130, Decision 131, Decision 48, Decision 119."
  - "gates: decision-scout=FLAGS_FOUND after 1 round; plan-critique=REVISE round 1 (9 findings,
    all adopted; plan re-scoped and renamed from PLAN-projection-aware-reader-client),
    <verdict> after <N> round(s)"
  - >-
    CI-RCA DEFERRAL (Related-Work Check): rec-3293, rec-3292 open and unresolved; this plan meets
    none of the three related-work conditions. Deferred on explicit human direction at planning
    time -- both are being resolved concurrently by another agent.
  - >-
    SEQUENCING (the critique's central finding). Strict access must ship LAST, not first.
    scripts/convergence_health/code_drift.py imports _fetch_open_recs FROM escalate.py, so it
    reads open_recs rows without ever calling named() -- invisible to a grep census, and omitted
    from the earlier draft's roster. Under strictness, code_drift's two lookups plus escalate()
    plus budget_ingest's would raise, reddening four steps of the hourly convergence-health.yml
    (set -euo pipefail, no continue-on-error), and _budget_recs.py:129 would regress to
    duplicate-filing. Order: this substrate -> call-site repairs -> strictness with NO roster.
  - >-
    Enabling fact for the follow-on: named_read builds rows via dict(zip(col_names, row)), so
    unprojected == ABSENT KEY and projected-NULL == present key holding None. The wire already
    distinguishes them; the client discards it. No server change needed.
  - >-
    VERB_FIELDS is client-side because src/lambdas/data-pipeline/manifest.yaml `excludes:` keeps
    ducklake_scd2_schema.py out of the zip -- NAMED_READS and NAMED_READS_VERSION are both
    unimportable at Lambda runtime, hence a literal pin the parity test asserts.
  - >-
    Deferred to the strictness follow-on, do not solve here: a ProjectionDrift assertion on the
    four SELECT * verbs would fire on any routine ops_tables column add (context_v2_json and
    _contract_version were exactly such additions), including the portal's fetch-before-update
    path. Scope it to fixed-column verbs or make it a superset check.
  - >-
    Single-SoT follow-on (Decision 91): describe_named_reads() emits no field list, so
    VERB_FIELDS duplicates a server-owned contract. Adding `fields` there is the real fix;
    deploy-ducklake-lambdas.yml makes the five-artifact redeploy CI time, not toil -- re-weigh.
  - >-
    Recorded, not fixed here (execution step 10 files the rec): this path matches
    deploy-ducklake-lambdas.yml's and DUCKLAKE_SOURCE_PATHSPECS' ducklake_*.py glob while
    appearing in no ducklake manifest, and is absent from deploy-prod-lambdas.yml's filter and
    PROD_SOURCE_PATHSPECS despite being packaged into data-pipeline -- over-triggering one
    channel, under-triggering the other, with no prod_code_drift alarm. Separately,
    preflight/alerts.py calls named("budget_breach_recent"), registered NOWHERE server-side
    (live probe: HTTP 500 unknown-verb), surviving only via a broad except -- call-site follow-on.
pre_implementation_checklist:
  - Branch confirmed not on main
  - docs/PROJECT_CONTEXT.md read
  - only the plan-cited decision sections
  - All files in scope located and readable
  - Acceptance criteria understood and verifiable
execution_steps:
  - >-
    FIRST, file the dedup-blindness rec via scripts.ops_data_portal (file_rec): open_recs
    consumers filtering on unprojected source/status, citing scripts/convergence_health/
    escalate.py:57 and code_drift.py:89,:284. Its id is cited by the xfail reason below and
    checked by VP step 7, so nothing else can start until it exists.
  - >-
    tests/fixtures/reader_rows.py -- verb_rows(verb, rows) looks the verb up in the client's
    VERB_FIELDS, resolves the ALL_TABLE_COLUMNS sentinel through the table spec, drops keys
    outside that projection, and raises on an unregistered verb name.
  - >-
    src/common/ducklake_reader_client.py -- add VERB_FIELDS covering all 12 registered verbs
    (fixed-column tuples; ALL_TABLE_COLUMNS sentinel for rec_by_id, rec_history, decision_by_id,
    priority_queue_current) plus a literal _NAMED_READS_VERSION_PIN. Leave named() and
    current_state() returning plain dicts; leave _READER_TRANSIENT_STATUS and the retry loop
    untouched.
  - >-
    src/common/ducklake_reader_client.py -- make query() raise instead of catching Exception and
    returning None, and narrow its return annotation at ~line 319 from `list[dict] | None` to
    `list[dict]` now that the None branch is gone.
  - >-
    tests/common/ducklake_reader_client/test_verb_fields_parity.py -- COPY (do not import) the
    real DuckDB verb harness pattern from tests/test_session_preflight_cache_serving.py
    (_con/_load_recs/_run_verb, ~lines 65-85): validate_no_cross_test_imports forbids importing
    any module whose final dotted component starts with test_, and tests/fixtures/
    ducklake_fakes.py's FakeCon is a connection double that cannot execute SQL. Guard the import
    with `duckdb = pytest.importorskip("duckdb")` (precedent: tests/ops_data_portal/
    test_cli.py:14) so the module is skippable where duckdb is absent.
  - tests/common/ducklake_reader_client/test_reader_rows_helper.py -- verb_rows stripping and unknown-verb rejection.
  - >-
    tests/common/ducklake_reader_client/test_ducklake_reader.py -- rewrite the three
    query()-returns-None assertions (~124, ~132, ~187) as pytest.raises and rename
    test_ducklake_reader_query_returns_none_on_error to ..._query_raises_on_error. Leave the two
    success-path calls (~114, ~175) alone.
  - >-
    tests/convergence_health/test_escalate.py -- convert ONLY TestFindOpenConvergenceStaleRec's
    fixtures (~19-22, ~29-31) via verb_rows("open_recs", ...) and mark ONLY
    test_returns_first_matching_rec with pytest.mark.xfail(strict=True, reason="... rec-NNNN ...")
    -- the rec id must sit INSIDE the xfail(...) call itself, inline in the reason string, because
    VP step 7 anchors on that shape. Do NOT mark test_returns_none_when_no_match or
    test_returns_none_on_empty_list -- they still pass after stripping and would XPASS.
  - >-
    Execute Verification Plan -- run each step; loop until pass; on unrecoverable V3 failure stop
    and RCA (Decision 55).
  - >-
    File a second rec for the deploy-trigger/affected-set mismatch: this file matches
    deploy-ducklake-lambdas.yml's and DUCKLAKE_SOURCE_PATHSPECS' src/common/ducklake_*.py glob
    while appearing in NO ducklake manifest, and is absent from deploy-prod-lambdas.yml's filter
    and PROD_SOURCE_PATHSPECS despite being packaged into data-pipeline -- so it over-triggers
    one channel and under-triggers the other.
  - 'Report: what was implemented, verification results, and both deploy run conclusions'
work_areas: []
rollback: >-
  Revert the client commit and redeploy data-pipeline via deploy-prod-lambdas.yml
  workflow_dispatch. VERB_FIELDS is inert data and query() has no production call sites (only the
  three tests in scope), so reverting restores prior behaviour without touching any caller.
