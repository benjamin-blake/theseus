schema_version: 1
slug: "intent-migration-wave1-ducklake-consolidation"
intent: >-
  Advance platform tier T5.5 (INTENT-doc extract-and-retire, Decision 86) by retiring WAVE-1's
  last remaining document docs/INTENT-ducklake-consolidation.md, whose architecture graduated to
  Decision 84 (DuckLake single-backend ops store). Per the feasibility_graduated -> delete_pointer
  archetype, the doc's still-live forward content is preserved as live tier_items BEFORE deletion
  (the manifest's own drift_finding requires "the pending Phase 4 (telemetry on DuckLake) and
  rec-curator producer repoint must be live tier_items before delete"), then the prose is deleted
  and every live inbound reference is repointed to the surviving canonical surface. Completing this
  doc closes WAVE-1 and unblocks WAVE-2 per docs/intent-migration/MANIFEST.yaml wave gating.
plan_type: IMPLEMENTATION
verification_tier: V1
plan_path: docs/plans/PLAN-intent-migration-wave1-ducklake-consolidation.yaml
phase: >-
  Platform tier T5.5 (INTENT-* document extraction and retirement); WAVE-1 of
  docs/intent-migration/MANIFEST.yaml (feasibility_graduated -> delete_pointer). This is the
  third and final WAVE-1 doc, following bazel-feasibility (done) and ops-pipeline-consolidation (done).
scope:
  - file: docs/INTENT-ducklake-consolidation.md
    action: Delete
    purpose: >-
      Retire the DuckLake consolidation prose. Its ratified premises, problem statement, target
      end-state, and the I-1..I-4 invariants are fully captured in Decision 84. The Phase-3
      demolition forward content is carried by live tier_item T2.26; the Phase-4 telemetry forward
      content is carried by the new tier_item T2.36 (created in this plan) plus Decisions 95/96/97
      and the docs/contracts/storage-substrate.yaml telemetry_tables disposition. Prose deleted per
      delete_pointer archetype; recoverable in git history.
  - file: docs/ROADMAP-PLATFORM.yaml
    action: Modify
    purpose: >-
      Four edits. (a) CREATE new tier_item T2.36 "Phase 4: telemetry re-lands on DuckLake"
      (depends_on [T2.26, T2.18]) consolidating the doc's Phase-4 forward hooks and cross-linking
      Decisions 95/96/97 + T0.12.6 Class A telemetry contracts + storage-substrate telemetry_tables.
      This satisfies the manifest drift_finding's "live tier_item" bar and gives the scattered
      Phase-4 pointers a single repoint target. (b) Repoint the T2.26 progress_note's
      docs/INTENT-ducklake-consolidation.md path reference (~line 5559) to Decision 84 / T2.36.
      (c) Remove the `- docs/INTENT-ducklake-consolidation.md` line from T5.5 files_in_scope
      (~line 7077). (d) Append a T5.5 progress_note line recording 3 of 18 docs retired and WAVE-1
      complete.
  - file: docs/intent-migration/MANIFEST.yaml
    action: Modify
    purpose: >-
      Flip the ducklake-consolidation documents[] entry disposition_state from pending to done.
      Change ONLY that one field; leave content_blocks / drift_findings / primary_targets /
      related_decisions and the X7 cross_cutting_finding intact as the disposition record. MANIFEST
      self-references are c4-exempt and are NOT swept.
  - file: docs/DECISIONS.md
    action: Modify
    purpose: >-
      Finding X7: Decision 84's Related line (~line 909) cites docs/INTENT-ducklake-consolidation.md
      BY PATH as "(full program)" -- a circular dangling pointer once the doc is deleted. Repoint it
      to the surviving canonical targets: the roadmap carriers T2.26/T2.27/T2.28 (already named on
      that line) plus the new T2.36, and drop the deleted-doc path. Leave the line 864 slug mention
      ("ducklake-consolidation INTENT graduates to it") untouched -- it is a correct historical
      statement and contains no INTENT- path token.
  - file: docs/contracts/storage-substrate.yaml
    action: Modify
    purpose: >-
      Repoint four references to the deleted path: the header comment (~line 10, "For
      Iceberg-boundary tables pending T2.26 disposition: see docs/INTENT-ducklake-consolidation.md")
      and three `migration_ref:` fields. For ops_session_log and ops_execution_plans (Iceberg-boundary,
      pending) -> tier_item T2.26 (their live disposition carrier). For telemetry_tables (Phase-4
      pending) -> tier_item T2.36 plus Decisions 95/96/97 (the canonical 4-table model + partition +
      identity standards). Preserve each entry's existing disposition prose; only swap the dangling
      doc-path pointer for the live target.
  - file: docs/PROJECT_CONTEXT.md
    action: Modify
    purpose: >-
      Repoint the storage-backend section (~line 37) trailing "See docs/INTENT-ducklake-consolidation.md."
      pointer to Decision 84 (the ratified consolidation) -- the sentence already cites Decision 84;
      replace the dangling doc-path tail with the canonical Decision/tier_item reference.
  - file: AGENTS.md
    action: Modify
    purpose: >-
      Repoint the warehouse-as-source-of-truth section (~line 116) "Telemetry stays on its
      (dead, to-be-rebuilt) path until consolidation Phase 4 (docs/INTENT-ducklake-consolidation.md)"
      to cite Decision 84 Phase 4 / tier_item T2.36 instead of the deleted doc path. Minimal pointer
      swap; sentence meaning preserved. (This is a hygiene pointer in the source-of-truth section, not
      a semantic/merge-protocol change.)
  - file: .claude/skills/planning/SKILL.md
    action: Modify
    purpose: >-
      Repoint the telemetry-store note (~line 50) "telemetry re-lands on DuckLake (consolidation
      Phase 4, docs/INTENT-ducklake-consolidation.md)" to cite Decision 84 / tier_item T2.36 instead
      of the deleted doc path. Minimal pointer swap; guidance meaning preserved.
  - file: docs/INTENT-ops-decisions-graduation.md
    action: Modify
    purpose: >-
      Repoint the cross-INTENT pointer (~line 8) "DECISIONS.md decommission question. See
      docs/INTENT-ducklake-consolidation.md." to Decision 84. This doc is a SURVIVING INTENT doc
      (Wave 3, disposition_state still pending) -- leaving it pointing at a deleted sibling would
      strand the pointer. Change ONLY the dangling pointer; do not extract or alter any other content
      in this Wave-3 doc (out of scope for WAVE-1).
bundled_recommendations: []
infrastructure_dependencies: []
acceptance_criteria:
  - docs/INTENT-ducklake-consolidation.md no longer exists on disk.
  - >-
    A new tier_item T2.36 "Phase 4: telemetry re-lands on DuckLake" exists in
    docs/ROADMAP-PLATFORM.yaml with depends_on [T2.26, T2.18], carrying the doc's Phase-4 forward
    hooks (field_semantics telemetry tables; OpsWriter.emit -> writer-boundary; preflight telemetry
    health re-enable; OpsWriter staging outbox + VarChar coercion retirement; point
    ducklake_maintenance merge/GC at the production catalog before telemetry write volume lands) and
    cross-linking Decisions 95/96/97 + T0.12.6 Class A telemetry contracts.
  - >-
    Before deletion, T2.26 is confirmed to carry the Phase-3 demolition (legacy DDL drops,
    ops_compaction retirement, _current view drops, pgvector-sync script delete) AND the
    rec-curator / queue producer repoint -- so no still-live content is stranded by the delete
    (Decision 86 clause 2; manifest drift_finding "must be live tier_items before delete").
  - >-
    The MANIFEST ducklake-consolidation documents[] entry has disposition_state: done, and it is the
    only entry flipped to done in this plan; every other documents[] entry is unchanged.
  - >-
    The validate.py intent-doc-freeze guard (validate_intent_doc_freeze) produces no failure naming
    ducklake-consolidation when run against the post-change tree.
  - tier_item T5.5 files_in_scope no longer lists docs/INTENT-ducklake-consolidation.md.
  - >-
    Finding X7 closed: docs/DECISIONS.md Decision 84 Related line no longer contains the path
    docs/INTENT-ducklake-consolidation.md; it points at T2.26/T2.27/T2.28 + T2.36 instead.
  - >-
    Every live inbound reference is repointed: docs/contracts/storage-substrate.yaml,
    docs/PROJECT_CONTEXT.md, AGENTS.md, .claude/skills/planning/SKILL.md, and
    docs/INTENT-ops-decisions-graduation.md no longer reference the deleted doc path; each points at
    its surviving canonical target (Decision 84 / T2.26 / T2.36 / Decisions 95-97).
  - >-
    X6/X7 generic close: a fresh repo-wide `rg 'INTENT-ducklake-consolidation'` returns ZERO live
    references. The only permitted residual hits are c4-exempt or historical: docs/plans/* plan
    artefacts (historical) and docs/intent-migration/MANIFEST.yaml (c4-exempt self-reference). (The
    synthetic tests/test_validate.py fixture references only the bare slug "ducklake-consolidation"
    without the INTENT- prefix, so it does NOT match this path-prefixed sweep; it is left untouched
    by design.) No code/infra surface and no canonical doc (DECISIONS.md, ROADMAP-PLATFORM.yaml,
    PROJECT_CONTEXT.md, AGENTS.md, contracts/, skills/) contains the path or slug as a live pointer.
  - Full `bin/venv-python -m scripts.validate` exits 0 (credential-gated checks may report SKIPPED in degraded mode).
verification_plan:
  - step: 1
    phase: pre-deploy
    action: >-
      No-stranded-content gate -- confirm T2.26 carries the Phase-3 demolition + rec-curator/queue
      producer repoint BEFORE deleting the doc (Decision 86 clause 2; manifest drift_finding).
    command: >-
      grep -A60 '^  - id: T2.26$' docs/ROADMAP-PLATFORM.yaml | grep -Ei 'compaction|pgvector|producer|_current view|demolition'
    expected: >-
      Prints lines showing T2.26 owns ops_compaction retirement, pgvector-sync script deletion,
      legacy view/DDL teardown, and the queue/producer repoint. (T2.26 intent + exit_criteria c3/c5
      cover these.)
    fix_if: >-
      If T2.26 does NOT cover one of these, do NOT git rm yet -- add the missing demolition hook to
      T2.26 (or T2.36 for telemetry) first, so deletion strands no live content.
  - step: 2
    phase: pre-deploy
    action: Confirm the new tier_item T2.36 exists with the required dependency edges.
    command: >-
      grep -A4 '^  - id: T2.36$' docs/ROADMAP-PLATFORM.yaml | grep -E 'name:|depends_on:'
    expected: >-
      Prints the T2.36 name ("Phase 4: telemetry re-lands on DuckLake" or equivalent) and
      `depends_on: [T2.26, T2.18]`.
    fix_if: >-
      Missing or wrong edges -- add/fix the T2.36 block. Both T2.26 and T2.18 must already exist
      (they do) or the T-1.5 roadmap schema check will fail on an unresolved depends_on.
  - step: 3
    phase: pre-deploy
    action: Confirm the INTENT doc is deleted from the working tree.
    command: test ! -f docs/INTENT-ducklake-consolidation.md && echo DELETED || echo PRESENT
    expected: Prints DELETED.
    fix_if: >-
      Prints PRESENT -- ensure `git rm docs/INTENT-ducklake-consolidation.md` ran.
  - step: 4
    phase: pre-deploy
    action: Confirm the MANIFEST ducklake-consolidation entry is flipped to disposition_state done.
    command: >-
      grep -A20 '^  - id: ducklake-consolidation$' docs/intent-migration/MANIFEST.yaml | grep -m1 'disposition_state:'
    expected: "Prints a line whose value is `    disposition_state: done`."
    fix_if: >-
      Prints `    disposition_state: pending` (or nothing) -- flip ONLY the ducklake-consolidation
      entry's disposition_state to done; do not touch other entries.
  - step: 5
    phase: pre-deploy
    action: Run the real intent-doc-freeze guard; confirm no ducklake-consolidation false-positive.
    command: >-
      bin/venv-python -c "import scripts.validate as v; f=[]; v.validate_intent_doc_freeze(f); print('PASS' if not any('ducklake-consolidation' in x for x in f) else 'FAIL:'+str(f))"
    expected: Prints PASS.
    fix_if: >-
      Prints FAIL -- the guard flags ducklake-consolidation. Ensure BOTH the file is deleted AND the
      manifest entry is done (the two must land together; the guard derives its allowed set from
      disposition_state != done).
  - step: 6
    phase: pre-deploy
    action: Finding X7 -- confirm DECISIONS.md no longer dangles the deleted doc path.
    command: >-
      grep -n "INTENT-ducklake-consolidation.md" docs/DECISIONS.md; echo "grep_exit=$?"
    expected: No matching lines; trailing line reads grep_exit=1.
    fix_if: >-
      Any line prints -- the Decision 84 Related line still cites the deleted path. Repoint it to
      T2.26/T2.27/T2.28 + T2.36 and drop the path.
  - step: 7
    phase: pre-deploy
    action: Inbound-reference sweep -- confirm every live canonical surface is repointed.
    command: >-
      grep -rn "INTENT-ducklake-consolidation" docs/ROADMAP-PLATFORM.yaml docs/contracts/storage-substrate.yaml
      docs/PROJECT_CONTEXT.md AGENTS.md .claude/skills/planning/SKILL.md docs/INTENT-ops-decisions-graduation.md;
      echo "grep_exit=$?"
    expected: No matching lines; grep_exit=1.
    fix_if: >-
      Any line prints -- a live surface still dangles. Repoint per the scope table (T2.26 / T2.36 /
      Decision 84 / Decisions 95-97). Do not delete the host content, only swap the pointer.
  - step: 8
    phase: pre-deploy
    action: >-
      X6/X7 generic close -- fresh repo-wide sweep returns only c4-exempt / historical residual.
    command: >-
      grep -rl "INTENT-ducklake-consolidation" . --include=*.md --include=*.yaml --include=*.py | sort
    expected: >-
      Only these paths appear, all c4-exempt or historical: ./docs/intent-migration/MANIFEST.yaml
      (c4-exempt self-reference) and docs/plans/PLAN-*.{md,yaml} historical plan artefacts (e.g. this
      plan, PLAN-intent-migration-wave1-bazel-feasibility.yaml, PLAN-ratify-ops-class-a-contracts.yaml,
      PLAN-ratify-class-a-telemetry-contracts.yaml). NOTE: tests/test_validate.py does NOT appear --
      its fixture uses the bare slug "ducklake-consolidation" without the INTENT- prefix this grep
      matches. NO live canonical surface (DECISIONS.md, ROADMAP-PLATFORM.yaml, PROJECT_CONTEXT.md,
      AGENTS.md, docs/contracts/*, .claude/skills/*) appears.
    fix_if: >-
      A canonical surface appears -- repoint it (step 7). An UNEXPECTED new path appears -- investigate
      before editing; it may be a reference introduced after the 2026-06-30 plan-time inventory.
  - step: 9
    phase: pre-deploy
    action: Confirm the synthetic-fixture guard tests still pass (unaffected by the real manifest flip).
    command: bin/venv-python -m pytest tests/test_validate.py::TestIntentDocFreeze -q
    expected: All TestIntentDocFreeze tests pass.
    fix_if: >-
      A failure -- inspect it; the tests are synthetic and should be flip-independent. Do NOT edit
      the tests to mask a real regression (Decision 55). The fixture's
      {"id": "ducklake-consolidation", "disposition_state": "pending"} is intentionally synthetic
      test data and is NOT swept by this plan (same precedent as bazel-feasibility, already deleted).
  - step: 10
    phase: pre-deploy
    action: Run the authoritative presubmit (CI-equivalent gate).
    command: bin/venv-python -m scripts.validate
    expected: >-
      Exits 0; the "Intent doc freeze (Decision 86)" section reports no failures; the ROADMAP schema
      check passes (T2.36 depends_on resolves) and the MANIFEST schema check passes (credential-gated
      DQ checks may report SKIPPED).
    fix_if: >-
      Non-zero exit -- read the failing check. Likely causes: malformed YAML after the roadmap/manifest/
      contract edit, or a T2.36 schema violation (missing required field, unresolved depends_on). Fix
      the cited check; do not add or remove checks to make it pass.
constraints:
  - >-
    Scope is STRICTLY the nine files listed (one delete + eight modifies). Touch no other INTENT doc
    beyond the single pointer-repoint in docs/INTENT-ops-decisions-graduation.md -- do NOT extract,
    migrate, or otherwise alter that Wave-3 doc's content (that is Wave-3 work).
  - >-
    The new tier_item T2.36 CARRIES forward intent only (Decision 86 routing: forward intent ->
    tier_items). Do not duplicate Decision 95/96/97 rationale into it; cross-link them. T2.36 is the
    Phase-4 execution carrier that Wave-4's telemetry-system migration will later enrich, not a
    re-statement of the 4-table model.
  - >-
    Preserve every repointed host's surrounding prose -- these are pointer swaps, not content
    rewrites. Only the dangling docs/INTENT-ducklake-consolidation.md path is replaced with the live
    target.
  - >-
    Do NOT edit tests/test_validate.py. Its TestIntentDocFreeze fixture is synthetic and
    flip-independent (it still lists the already-deleted bazel-feasibility as pending by design).
  - No rescue agents or workaround loops (Decision 55). A failing guard or schema check is fixed at
    the source, never masked.
  - >-
    Do NOT edit logs/.recommendations-log.jsonl or logs/.decisions-index.jsonl. This plan files no
    recs and closes none.
  - >-
    All edits land in one branch commit so deletion + manifest flip + sweep are atomic (the guard
    requires file-deleted AND entry-done together).
context:
  - >-
    Governing authority: Decision 86 (INTENT prose docs retired; extract-and-retire supersedes
    CD.14's demote-and-keep). The validate.py intent-doc-freeze guard's allowed set is auto-derived
    from MANIFEST disposition_state != done, so flipping the entry to done + deleting the file keeps
    the guard green without a hand-edit.
  - >-
    Primary deletion pointer: Decision 84 (DuckLake single-backend ops store). The doc's ratified
    premises (P1-P4), six-point problem statement, target end-state, deployment protocol, and the
    I-1..I-4 invariants are all captured in Decision 84. Decision 84's Related line cites the doc BY
    PATH as "(full program)" -- finding X7 -- which the sweep repoints.
  - >-
    Content-block disposition:
    Block "Ratified premises + Problem statement + Target end-state + I-1..I-4 invariants" ->
    Decision 84 (already captured). Block "Phase 3-demolition (legacy-engine drops, ops_compaction retire,
    DynamoDB counters destroy, _current view drops, pgvector-sync script delete)" -> live
    tier_item T2.26 (verified at VP step 1). Block "Phase 4 -- telemetry on DuckLake (field_semantics
    tables; OpsWriter.emit -> writer-boundary; preflight health re-enable; staging outbox + VarChar
    coercion retire; maintenance cadence point GC/merge at prod catalog before telemetry volume)" ->
    NEW tier_item T2.36 + Decisions 95/96/97 + T0.12.6 contracts + storage-substrate telemetry_tables
    disposition + T2.18 (maintenance cadence). The rec-curator/queue producer repoint -> T2.26.
  - >-
    Why a new tier_item (T2.36) rather than relying on existing capture: the manifest's
    ducklake-consolidation drift_finding explicitly requires "the pending Phase 4 (telemetry on
    DuckLake) and rec-curator producer repoint must be live TIER_ITEMS before delete." rec-curator is
    carried by T2.26, but Phase-4 telemetry had NO tier_item (only Decisions 95/96/97 + a
    contract-index disposition block). T2.36 satisfies that literal bar and gives the scattered
    Phase-4 pointers (AGENTS.md, PROJECT_CONTEXT.md, SKILL.md, storage-substrate telemetry_tables) a
    single canonical repoint target. Decisions to cite per scout: Decision 86 + Decision 84 +
    Decisions 95/96/97.
  - >-
    Inbound-reference inventory (2026-06-30 plan-time rg):
    LIVE to repoint -- docs/DECISIONS.md Decision 84 Related (~909, X7); docs/contracts/
    storage-substrate.yaml (header comment ~10 + 3 migration_ref: ops_session_log, ops_execution_plans,
    telemetry_tables); docs/PROJECT_CONTEXT.md (~37); AGENTS.md (~116); .claude/skills/planning/
    SKILL.md (~50); docs/INTENT-ops-decisions-graduation.md (~8, cross-INTENT pointer);
    docs/ROADMAP-PLATFORM.yaml (T2.26 progress_note ~5559 + T5.5 files_in_scope ~7077).
    LEAVE (historical/exempt) -- docs/plans/PLAN-*.{md,yaml} artefacts (PLAN-ratify-ops-class-a-contracts.yaml,
    PLAN-intent-migration-wave1-bazel-feasibility.yaml, PLAN-ratify-class-a-telemetry-contracts.yaml,
    this plan); docs/DECISIONS.md:864 (slug mention, no INTENT- path token);
    docs/intent-migration/MANIFEST.yaml (c4-exempt; X7 finding + telemetry-system cross-ref kept as
    the disposition record); tests/test_validate.py:3660 (synthetic flip-independent fixture).
  - >-
    Decision-scout gate verdict: FLAGS_FOUND (two NOTE flags on Decision 86, both addressed in this
    plan). Flag 1 (X6 sweep exhaustiveness) -> VP step 8 closes X6/X7 generically with a fresh
    repo-wide rg, not just enumerated targets. Flag 2 (no stranded content) -> VP step 1 verifies
    T2.26 actually carries the Phase-3 demolition + rec-curator repoint before the git rm. CITE:
    Decision 86, Decision 84, Decisions 95/96/97. RELATED: Decision 88 (T2.18/T2.26 catalog-egress),
    Decision 78/81 (DuckLake substrate origin/runtime).
  - >-
    WAVE-1 gate "WAVE-0 complete" holds: Decision 86 is Decided and the validate.py intent-doc-freeze
    guard shipped 2026-06-12. WAVE-1 has NO cross_wave_gate. This is the last of WAVE-1's three docs
    (bazel-feasibility done 2026-06-18; ops-pipeline-consolidation done 2026-06-29); landing it flips
    WAVE-1 to complete and satisfies WAVE-2's gate.
  - >-
    Disposition fork resolved with the human (2026-06-30): scope = WAVE-1 closeout (ducklake only);
    disposition = extract residual into a live tier_item (T2.36) + T2.26, then delete now (rather than
    deferring the doc until Decision 84 Phase 4 lands). The "pending work survives as live tier_items"
    bar is the operative gate, and T2.26 (+ new T2.36) meets it.
pre_implementation_checklist:
  - Branch confirmed not on main (claude/intent-doc-extraction-retirement-5x6so6)
  - docs/INTENT-ducklake-consolidation.md read in full
  - Decision 84 read (primary deletion pointer confirmed); Decisions 95/96/97 confirmed as telemetry capture targets
  - T2.26 read and confirmed to carry Phase-3 demolition + rec-curator/queue producer repoint (VP step 1)
  - T2.18 confirmed to exist (T2.36 depends_on edge) and to own the maintenance cadence
  - All nine scope files located and readable
  - Acceptance criteria understood and verifiable
  - >-
    Inbound-reference inventory confirmed (LIVE to repoint: DECISIONS.md X7, storage-substrate.yaml x4,
    PROJECT_CONTEXT.md, AGENTS.md, planning/SKILL.md, INTENT-ops-decisions-graduation.md, ROADMAP x2;
    LEAVE: docs/plans/*, MANIFEST self-ref, synthetic test fixture)
execution_steps:
  - >-
    VERIFY FIRST (VP step 1): read tier_item T2.26 and confirm it carries the Phase-3 demolition
    (ops_compaction retirement, legacy DDL/view teardown, pgvector-sync script delete) and the
    rec-curator / queue producer repoint. If any is missing, add it to T2.26 before proceeding.
  - >-
    In docs/ROADMAP-PLATFORM.yaml, CREATE tier_item T2.36 "Phase 4: telemetry re-lands on DuckLake"
    (tier T2, depends_on [T2.26, T2.18], effort M, strategic false, status not_started or in_progress
    per schema). Body carries the Phase-4 forward hooks (field_semantics telemetry tables;
    OpsWriter.emit -> writer-boundary writes; preflight telemetry health re-enable against the reader;
    OpsWriter staging outbox + VarChar coercion retirement; point ducklake_maintenance merge/GC at the
    production catalog before telemetry write volume lands) with files_in_scope and exit_criteria, and
    cross-links Decisions 95/96/97 + T0.12.6 Class A telemetry contracts + storage-substrate
    telemetry_tables. Match the surrounding tier_item schema exactly (run validate after).
  - >-
    In docs/contracts/storage-substrate.yaml, repoint the header comment (~10) and the three
    migration_ref fields: ops_session_log + ops_execution_plans -> T2.26; telemetry_tables -> T2.36 +
    Decisions 95/96/97. Preserve each entry's disposition prose.
  - >-
    In docs/DECISIONS.md, repoint Decision 84's Related line (~909, finding X7): drop the
    docs/INTENT-ducklake-consolidation.md path and reference T2.26/T2.27/T2.28 + T2.36. Leave line 864
    untouched.
  - >-
    In docs/PROJECT_CONTEXT.md (~37), AGENTS.md (~116), and .claude/skills/planning/SKILL.md (~50),
    swap the dangling doc-path pointer for Decision 84 / tier_item T2.36. Pointer swaps only.
  - >-
    In docs/INTENT-ops-decisions-graduation.md (~8), repoint the cross-INTENT pointer to Decision 84.
    Change ONLY that pointer.
  - >-
    Delete docs/INTENT-ducklake-consolidation.md (`git rm docs/INTENT-ducklake-consolidation.md`).
  - >-
    In docs/intent-migration/MANIFEST.yaml, flip the ducklake-consolidation documents[] entry's
    disposition_state from pending to done. Change ONLY that one field.
  - >-
    In docs/ROADMAP-PLATFORM.yaml, remove the `- docs/INTENT-ducklake-consolidation.md` line from
    tier_item T5.5 files_in_scope (~7077); repoint the T2.26 progress_note path reference (~5559) to
    Decision 84 / T2.36; and append a T5.5 progress_note line (3 of 18 docs retired; WAVE-1 complete).
  - >-
    Execute the Verification Plan -- run each step in order; loop until all pass.
  - >-
    Report: the doc is deleted, T2.36 created, T2.26 confirmed carrying the demolition + producer
    repoint, the manifest entry is done, the guard passes, X7 closed, the repo-wide sweep returns only
    c4-exempt/historical residual, the synthetic guard tests pass, and the full presubmit is green.
work_areas: []
rollback: >-
  All edits land in one branch commit. To roll back: restore docs/INTENT-ducklake-consolidation.md
  from git history, re-flip the MANIFEST entry to pending, remove tier_item T2.36, and restore the
  original docs/INTENT-ducklake-consolidation.md path references in DECISIONS.md (X7),
  storage-substrate.yaml, PROJECT_CONTEXT.md, AGENTS.md, planning/SKILL.md,
  INTENT-ops-decisions-graduation.md, and ROADMAP-PLATFORM.yaml (T2.26 progress_note + T5.5
  files_in_scope). `git revert` of the single commit is the clean path.
