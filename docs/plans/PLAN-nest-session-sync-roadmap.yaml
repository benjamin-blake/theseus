schema_version: 1
slug: "nest-session-sync-roadmap"
intent: >-
  Nest three grandfathered scripts/ prefix families -- session_* (3), sync_* (3), and the
  roadmap/plan tooling group (6) -- from the scripts/ root into scripts/session/, scripts/sync/,
  and scripts/roadmap/ subpackages (git mv + __init__.py) with a single same-commit repo-wide
  reference rewrite and no import shims. Phase 2 of 3 of audit finding RS-01: converts the scripts/
  root's largest remaining predictability gap into the proven scripts/ci_rca//checks//executor/
  subpackage pattern so agents locate session, sync, and roadmap code by home, not by scanning flat
  filenames. rec-164 Phase A territory.
plan_type: IMPLEMENTATION
verification_tier: V2
plan_path: docs/plans/PLAN-nest-session-sync-roadmap.yaml
phase: >-
  Repository-structure restructure Phase P4b (audit finding RS-01 [medium], phase 2 of 3, from
  audits/repository-structure-cb02572.yaml RS-01 + proposed_target_tree scripts/{session,sync,roadmap}/
  nodes). Runs AFTER P1 (RS-04 file-router registry + validate_placement link-validity check), P2
  (RS-02/03 stray consolidation), P3 (RS-05 context-locality scripts/CLAUDE.md carries the
  root-vs-subpackage rule), and P4a (nest ci_rca_* -> scripts/ci_rca/, the proven pattern) -- all
  merged at origin/main f119383. Platform axis; RS-01 is planned-insufficient via open rec-164.
  IMPLEMENTATION type (STRATEGIC frozen + >5-file heuristic suspended, Decision 67). rec-164 does
  NOT close here -- it closes at RS-01 phase 3.
scope:
  # ---- session_* family: strip the session_ prefix -> scripts/session/{preflight,postflight,metrics}.py
  - file: scripts/session_preflight.py
    action: Delete
    purpose: git mv to scripts/session/preflight.py. Facade over scripts/preflight/ (non-moving). Cross-family imports of platform_roadmap/product_roadmap (roadmap family) + sync_ops (sync family) rewritten. NO own ROOT anchor (uses _common.ROOT).
  - file: scripts/session/preflight.py
    action: Create
    purpose: >-
      Receives session_preflight via git mv. Rewrite `from scripts import platform_roadmap` ->
      `from scripts.roadmap import platform_roadmap`; `from scripts import product_roadmap as
      product_roadmap_module` -> `from scripts.roadmap import product_roadmap as product_roadmap_module`;
      the three `from scripts.sync_ops import ...` -> `from scripts.sync.ops import ...`; docstring
      `-m scripts.session_preflight` -> `-m scripts.session.preflight`. No ROOT edit (imports _common.ROOT).
  - file: scripts/session_postflight.py
    action: Delete
    purpose: git mv to scripts/session/postflight.py. Facade over scripts/postflight/ (non-moving). Has the sys.path bootstrap ROOT anchor + a file-path self-reinvocation.
  - file: scripts/session/postflight.py
    action: Create
    purpose: >-
      Receives session_postflight via git mv. FIX line 46 sys.path.insert(Path(__file__).resolve().parent.parent)
      -> parent.parent.parent (bootstrap must still reach repo root one level deeper). Rewrite the line ~250
      self-reinvocation `_common._run([_common.PYTHON, "scripts/session_postflight.py", "--pre-commit-sanity"])`
      -> "scripts/session/postflight.py". Rewrite the docstring usage lines + comment self-refs.
  - file: scripts/session_metrics.py
    action: Delete
    purpose: git mv to scripts/session/metrics.py. Has ROOT=Path(__file__).parent.parent anchor. Invoked by PATH from scripts/postflight/housekeeping.py.
  - file: scripts/session/metrics.py
    action: Create
    purpose: Receives session_metrics via git mv. FIX ROOT parent.parent -> parent.parent.parent (anchors METRICS_LOG, PREFLIGHT_REPORT under logs/).
  # ---- sync_* family: strip the sync_ prefix -> scripts/sync/{ops,recommendations,ducklake_version}.py
  - file: scripts/sync_ops.py
    action: Delete
    purpose: git mv to scripts/sync/ops.py. ROOT anchor _REPO_ROOT=parent.parent. Highest external fan-out (126 test refs). No moving-module imports of its own.
  - file: scripts/sync/ops.py
    action: Create
    purpose: Receives sync_ops via git mv. FIX _REPO_ROOT parent.parent -> parent.parent.parent (anchors _LOGS_DIR, _OUTBOX_DIR).
  - file: scripts/sync_recommendations.py
    action: Delete
    purpose: git mv to scripts/sync/recommendations.py. ROOT anchor _REPO_ROOT=resolve().parent.parent.
  - file: scripts/sync/recommendations.py
    action: Create
    purpose: Receives sync_recommendations via git mv. FIX _REPO_ROOT parent.parent -> parent.parent.parent (anchors _LOCAL_RECS_FILE under logs/).
  - file: scripts/sync_ducklake_version.py
    action: Delete
    purpose: git mv to scripts/sync/ducklake_version.py. ROOT anchor=resolve().parent.parent. Carries a "generated by scripts/sync_ducklake_version.py" provenance string + docstring -m refs.
  - file: scripts/sync/ducklake_version.py
    action: Create
    purpose: >-
      Receives sync_ducklake_version via git mv. FIX ROOT parent.parent -> parent.parent.parent (anchors
      REQUIREMENTS_PATH at repo root). Rewrite docstring `-m scripts.sync_ducklake_version` -> nested and
      the provenance string "generated by scripts/sync_ducklake_version.py" -> "scripts/sync/ducklake_version.py".
  # ---- roadmap/plan family: KEEP names (no shared prefix) -> scripts/roadmap/<same_name>.py
  - file: scripts/platform_roadmap.py
    action: Delete
    purpose: git mv to scripts/roadmap/platform_roadmap.py. Imports OUT-OF-SCOPE siblings platform_roadmap_{gate_rules,models,state} (stay at root -- NOT rewritten). No ROOT anchor.
  - file: scripts/roadmap/platform_roadmap.py
    action: Create
    purpose: >-
      Receives platform_roadmap via git mv (name kept). Its `from scripts.platform_roadmap_gate_rules/
      _models/_state import ...` are LEFT unchanged (those siblings remain at scripts/ root, out of phase-2
      scope; \b-anchored sweep does not touch them). No ROOT edit (no __file__ anchor).
  - file: scripts/product_roadmap.py
    action: Delete
    purpose: git mv to scripts/roadmap/product_roadmap.py. Intra-family imports product_roadmap_schema + platform_roadmap. No ROOT anchor.
  - file: scripts/roadmap/product_roadmap.py
    action: Create
    purpose: >-
      Receives product_roadmap via git mv. Rewrite `from scripts.product_roadmap_schema import ...` ->
      `from scripts.roadmap.product_roadmap_schema import ...` and the function-local
      `from scripts.platform_roadmap import load as load_platform` -> `from scripts.roadmap.platform_roadmap import ...`.
  - file: scripts/product_roadmap_schema.py
    action: Delete
    purpose: git mv to scripts/roadmap/product_roadmap_schema.py. Clean -- no intra-family import, no ROOT anchor.
  - file: scripts/roadmap/product_roadmap_schema.py
    action: Create
    purpose: Receives product_roadmap_schema via git mv (pure move; no reference or ROOT edit).
  - file: scripts/plan_document.py
    action: Delete
    purpose: git mv to scripts/roadmap/plan_document.py. Load-bearing (validate.py's validate_plan_documents + validate_vp_replay import it). ROOT anchor at line 145.
  - file: scripts/roadmap/plan_document.py
    action: Create
    purpose: Receives plan_document via git mv (name kept). FIX line 145 Path(__file__).resolve().parent.parent / "docs" / "plans" -> parent.parent.parent (default plans_root must still resolve docs/plans).
  - file: scripts/plan_audit.py
    action: Delete
    purpose: git mv to scripts/roadmap/plan_audit.py. ROOT anchor + intra-family import of find_plan + sys.path.insert(ROOT).
  - file: scripts/roadmap/plan_audit.py
    action: Create
    purpose: >-
      Receives plan_audit via git mv. FIX ROOT parent.parent -> parent.parent.parent (anchors JSONL_LOG,
      RECS_LOG, and the sys.path.insert(ROOT)). Rewrite `from scripts.find_plan import find_plan_file` ->
      `from scripts.roadmap.find_plan import find_plan_file`. Rewrite the plan_document docstring ref.
  - file: scripts/find_plan.py
    action: Delete
    purpose: git mv to scripts/roadmap/find_plan.py. Invoked BOTH by path (scripts/find_plan.py) AND module (-m scripts.find_plan). ROOT anchor at line 22.
  - file: scripts/roadmap/find_plan.py
    action: Create
    purpose: Receives find_plan via git mv (name kept). FIX line 22 ROOT=resolve().parent.parent -> parent.parent.parent (anchors all PLAN-{slug}.yaml resolution). Rewrite the two docstring path refs.
  # ---- new package markers (docstring-only; coverage-excluded)
  - file: scripts/session/__init__.py
    action: Create
    purpose: Package marker with one-line docstring so scripts.session is importable.
  - file: scripts/sync/__init__.py
    action: Create
    purpose: Package marker with one-line docstring so scripts.sync is importable.
  - file: scripts/roadmap/__init__.py
    action: Create
    purpose: Package marker with one-line docstring so scripts.roadmap is importable.
  # ---- non-moving scripts/ importers + coverage map (rewrite references only; files do not move)
  - file: scripts/test_coverage_checker.py
    action: Modify
    purpose: >-
      Extend map_source_to_test with three len==3 nested-package branches AFTER the ci_rca branch:
      session/<stem> -> tests/test_session_<stem>.py; sync/<stem> -> tests/test_sync_<stem>.py;
      roadmap/<stem> -> tests/test_<stem>.py (names kept). Also update the docstring mapping list.
      Self-excluded from its own coverage gate (name starts with test_).
  - file: scripts/validate.py
    action: Modify
    purpose: >-
      Rewrite any direct reference to the moved roadmap modules if present at module scope (the three
      registered roadmap checks are imported via scripts.checks.roadmap.*, unaffected). Verify no
      residual `scripts.plan_document`/`scripts.platform_roadmap`/`scripts.product_roadmap` token; include
      only if the sweep flags one.
  - file: scripts/checks/roadmap/validate_plan_documents.py
    action: Modify
    purpose: Rewrite `from scripts.plan_document import validate_paths` -> `from scripts.roadmap.plan_document import validate_paths` (load-bearing -- validate.py plan-schema gate).
  - file: scripts/checks/roadmap/validate_platform_roadmap.py
    action: Modify
    purpose: Rewrite `from scripts.platform_roadmap import ExitCriterion, load` -> `from scripts.roadmap.platform_roadmap import ExitCriterion, load`.
  - file: scripts/checks/roadmap/validate_product_roadmap.py
    action: Modify
    purpose: Rewrite `from scripts.product_roadmap import load` -> `from scripts.roadmap.product_roadmap import load`.
  - file: scripts/checks/roadmap/validate_candidate_decision_ratification.py
    action: Modify
    purpose: Rewrite `from scripts.platform_roadmap import load` -> `from scripts.roadmap.platform_roadmap import load`.
  - file: scripts/checks/verification/validate_vp_replay.py
    action: Modify
    purpose: Rewrite `from scripts.plan_document import load` + the two docstring/error-string `scripts.plan_document` mentions -> scripts.roadmap.plan_document.
  - file: scripts/checks/misc/validate_ducklake_version_lockstep.py
    action: Modify
    purpose: Rewrite `import scripts.sync_ducklake_version as _sdv` -> `import scripts.sync.ducklake_version as _sdv` and the `-m scripts.sync_ducklake_version` CLI hint string -> nested.
  - file: scripts/checks/ops_governance/validate_outbox_staleness.py
    action: Modify
    purpose: Rewrite the `python -m scripts.sync_ops sync` CLI hint string -> `-m scripts.sync.ops sync`.
  - file: scripts/checks/ops_governance/validate_decisions_local_writes.py
    action: Modify
    purpose: Rewrite the docstring path ref scripts/sync_ops.py -> scripts/sync/ops.py.
  - file: scripts/checks/ops_governance/validate_rec_write_paths.py
    action: Modify
    purpose: Rewrite the docstring path ref scripts/sync_recommendations.py -> scripts/sync/recommendations.py.
  - file: scripts/executor/batch.py
    action: Modify
    purpose: Rewrite `from scripts.sync_ops import drain as drain_outbox` -> `from scripts.sync.ops import drain as drain_outbox`.
  - file: scripts/ops_data_portal.py
    action: Modify
    purpose: Rewrite the two function-local `from scripts.sync_ops import ...` -> `from scripts.sync.ops import ...`.
  - file: scripts/ops_portal/cache.py
    action: Modify
    purpose: Rewrite the two function-local `from scripts.sync_ops import ...` -> `from scripts.sync.ops import ...`.
  - file: scripts/ops_portal/decisions.py
    action: Modify
    purpose: Rewrite the two function-local `from scripts.sync_ops import ...` -> `from scripts.sync.ops import ...`.
  - file: scripts/migrate_ops_data.py
    action: Modify
    purpose: Rewrite `from scripts.sync_ops import ...` and `from scripts.sync_recommendations import ...` -> nested forms.
  - file: scripts/lambda_manifest.py
    action: Modify
    purpose: Rewrite the docstring "Parallel to scripts/platform_roadmap.py" -> scripts/roadmap/platform_roadmap.py (cosmetic; live source docstring).
  - file: scripts/verifiers/outbox_health.py
    action: Modify
    purpose: Rewrite the "scripts/sync_ops.py" string-literal path -> "scripts/sync/ops.py".
  - file: scripts/preflight/__init__.py
    action: Modify
    purpose: Rewrite the docstring facade ref scripts/session_preflight.py -> scripts/session/preflight.py.
  - file: scripts/preflight/_common.py
    action: Modify
    purpose: Rewrite the docstring `scripts.session_preflight` ref -> scripts.session.preflight.
  - file: scripts/preflight/correlation.py
    action: Modify
    purpose: Rewrite the docstring scripts/session_preflight.py ref -> scripts/session/preflight.py.
  - file: scripts/postflight/__init__.py
    action: Modify
    purpose: Rewrite the docstring facade ref scripts/session_postflight.py -> scripts/session/postflight.py.
  - file: scripts/postflight/_common.py
    action: Modify
    purpose: Rewrite `from scripts.find_plan import find_plan_file` -> `from scripts.roadmap.find_plan import find_plan_file` and the docstring scripts.session_postflight ref.
  - file: scripts/postflight/housekeeping.py
    action: Modify
    purpose: >-
      Rewrite the two subprocess PATH invocations `[_common.PYTHON, "scripts/session_metrics.py"]` ->
      "scripts/session/metrics.py" and `[_common.PYTHON, "scripts/plan_audit.py"]` -> "scripts/roadmap/plan_audit.py",
      plus the docstring facade ref.
  - file: scripts/postflight/remote.py
    action: Modify
    purpose: Rewrite the docstring facade ref scripts/session_postflight.py -> scripts/session/postflight.py.
  # ---- config (live, some existence-adjacent)
  - file: config/agent/verification_registry/registry.yaml
    action: Modify
    purpose: Rewrite guard_target scripts/plan_document.py -> scripts/roadmap/plan_document.py and the four scripts/session_preflight.py guard_target/path entries -> scripts/session/preflight.py.
  - file: config/agent/validate/iam_runner_manifest.yaml
    action: Modify
    purpose: Rewrite the three iam_runner_manifest source entries (scripts/sync_recommendations.py) -> scripts/sync/recommendations.py.
  - file: requirements.txt
    action: Modify
    purpose: >-
      Rewrite the duckdb floor line's inline provenance comment path scripts/sync_ducklake_version.py ->
      scripts/sync/ducklake_version.py (matches the module's rewritten _FLOOR_COMMENT). requirements.txt is
      NOT Lambda-bundled (compute_affected_artifacts=={}) and the lockstep gate compares only the duckdb
      version prefix, not the comment -- safe. The config/lambda/ducklake/version.yaml path in the same
      comment is a config file (not moving) and is LEFT as-is. NOTE: config/lambda/ducklake/version.yaml is
      deliberately NOT in scope (see the version.yaml carve-out constraint) to keep this V2.
  - file: config/README.md
    action: Modify
    purpose: Rewrite scripts/session_metrics.py, scripts/plan_audit.py, scripts/session_preflight.py path refs -> nested (portal-projection README; live surface).
  # ---- machine-readable discovery index (MANDATORY -- validate_placement is a live CI gate)
  - file: docs/contracts/file-router.yaml
    action: Modify
    purpose: >-
      Rewrite the 4 existing rows whose targets would go dead (session-preflight -> scripts/session/preflight.py,
      session-postflight -> scripts/session/postflight.py, session-metrics -> scripts/session/metrics.py,
      plan-execution-audit -> scripts/roadmap/plan_audit.py) and APPEND 3 package discovery routes
      (session-code -> [scripts/session/], sync-code -> [scripts/sync/], roadmap-code -> [scripts/roadmap/]).
  - file: docs/ARCHITECTURE-WORKFLOW.md
    action: Modify
    purpose: Rewrite the live path refs scripts/plan_audit.py, scripts/session_metrics.py, scripts/sync_ops.py -> nested forms (allowlisted governance doc; live surface).
  - file: docs/GETTING_STARTED.md
    action: Modify
    purpose: Rewrite the onboarding verify hint `-m scripts.session_preflight` -> `-m scripts.session.preflight` (allowlisted governance doc; live surface).
  - file: docs/contracts/candidate-decision-ratification.yaml
    action: Modify
    purpose: Rewrite the three `scripts/platform_roadmap.py` location/detail semantics refs -> scripts/roadmap/platform_roadmap.py (live machine-readable contract).
  - file: docs/contracts/exit-criteria-ledger.yaml
    action: Modify
    purpose: Rewrite `scripts/platform_roadmap.py::ExitCriterion` and `scripts/plan_document.py` refs -> scripts/roadmap/platform_roadmap.py / scripts/roadmap/plan_document.py (live contract).
  - file: docs/contracts/session-id.yaml
    action: Modify
    purpose: Rewrite `scripts/session_preflight.py` and `scripts/session_postflight.py::close_telemetry_session()` refs -> scripts/session/preflight.py / scripts/session/postflight.py (live contract).
  - file: docs/runbooks/ducklake-catalog-operations.md
    action: Modify
    purpose: Rewrite `scripts/sync_ducklake_version.py` and `-m scripts.sync_ducklake_version` refs -> scripts/sync/ducklake_version.py / -m scripts.sync.ducklake_version (live operational runbook).
  - file: docs/ROADMAP-PLATFORM.yaml
    action: Modify
    purpose: >-
      Cosmetic sweep of ~41 live path/module refs (files_in_scope + prose) scripts/<moving>.py /
      scripts.<moving> -> nested forms. Verified NOT existence-validated by any CI check (no files_in_scope
      existence gate), so non-blocking -- swept per the live-surface policy (P4a precedent). The pathless
      `# new` annotations and the out-of-scope platform_roadmap_{gate_rules,models,state} siblings stay.
  - file: docs/ROADMAP-PRODUCT.yaml
    action: Modify
    purpose: Cosmetic sweep of the 4 live refs (product_roadmap / plan tooling) -> nested forms (live-surface policy; not existence-gated).
  - file: docs/ROADMAP-SEMANTO.yaml
    action: Modify
    purpose: Cosmetic sweep of the 1 live ref -> nested form (live-surface policy; not existence-gated).
  # ---- .claude/ command + skill surfaces (MAKE-OR-BREAK: /plan + /implement depend on these post-merge)
  - file: .claude/commands/plan.md
    action: Modify
    purpose: Rewrite `-m scripts.session_preflight` (x3), `-m scripts.session_postflight` (x1), `-m scripts.platform_roadmap` (x1) -> nested module forms.
  - file: .claude/commands/implement.md
    action: Modify
    purpose: Rewrite `-m scripts.session_preflight` (x2), `-m scripts.session_postflight` (x2), and the PATH invocations `scripts/find_plan.py` (x2) -> scripts/roadmap/find_plan.py.
  - file: .claude/commands/orient.md
    action: Modify
    purpose: Rewrite `-m scripts.session_preflight` and the `scripts.platform_roadmap.load()` import + `-m scripts.platform_roadmap` refs (incl the runnable one-liner) -> scripts.roadmap.platform_roadmap.
  - file: .claude/commands/audit.md
    action: Modify
    purpose: Rewrite `-m scripts.session_preflight` (x2) and `-m scripts.session_postflight` (x1) -> nested module forms.
  - file: .claude/skills/planning/SKILL.md
    action: Modify
    purpose: Rewrite `-m scripts.sync_ops` (x2), `scripts/plan_document.py` (x1 path), `-m scripts.plan_document` (x1 runnable) -> nested forms.
  - file: .claude/skills/implement/SKILL.md
    action: Modify
    purpose: Rewrite `scripts/plan_document.py` + `scripts/find_plan.py` path refs, `-m scripts.sync_ops`, `-m scripts.session_preflight`, `-m scripts.platform_roadmap` -> nested forms.
  - file: .claude/skills/code-review/SKILL.md
    action: Modify
    purpose: Rewrite the three `-m scripts.find_plan` module invocations -> `-m scripts.roadmap.find_plan`.
  - file: .claude/skills/orient/SKILL.md
    action: Modify
    purpose: Rewrite `scripts.platform_roadmap.load()` + the two `-m scripts.platform_roadmap` banned-entrypoint refs -> scripts.roadmap.platform_roadmap.
  - file: .claude/skills/plan-critique/SKILL.md
    action: Modify
    purpose: Rewrite the `scripts/plan_document.py` schema-owner path ref -> scripts/roadmap/plan_document.py.
  - file: .claude/skills/audit-prompt/SKILL.md
    action: Modify
    purpose: Rewrite the pinned cache-gen command `-m scripts.session_preflight --roadmap-detail full` -> `-m scripts.session.preflight --roadmap-detail full`.
  # ---- remaining live surfaces
  - file: AGENTS.md
    action: Modify
    purpose: >-
      Rewrite the anchored `python -m scripts.sync_ops sync` (warehouse section) -> `-m scripts.sync.ops sync`.
      Also refresh the two bare CLI shorthands in the same section ("Do not call `sync_ops`", "via `sync_ops
      pull`") for accuracy -> `sync.ops`. Live ambient doc; keep the warehouse section coherent.
  - file: .github/instructions/executor-supervisor-workflow.instructions.md
    action: Modify
    purpose: Rewrite the PATH invocation `python scripts/session_postflight.py --auto ...` -> scripts/session/postflight.py (tracked instruction file; executor frozen but swept for correctness).
  - file: bin/setup-cloud-env.sh
    action: Modify
    purpose: Rewrite the verify hint `-m scripts.session_preflight` -> `-m scripts.session.preflight`.
  - file: EVALUATION-PROMPTS.yaml
    action: Modify
    purpose: Rewrite the `scripts/plan_document.py` reference -> scripts/roadmap/plan_document.py.
  # ---- tests (imports, from-scripts-import, @patch string targets, subprocess -m, path literals)
  - file: tests/test_session_preflight.py
    action: Modify
    purpose: Rewrite scripts.session_preflight imports + @patch targets + cross-family scripts.sync_ops / scripts.platform_roadmap / scripts.product_roadmap targets -> nested forms.
  - file: tests/test_session_preflight_cache_serving.py
    action: Modify
    purpose: Rewrite scripts.session_preflight + any moved-module @patch/string refs -> nested forms.
  - file: tests/test_session_preflight_contracts.py
    action: Modify
    purpose: Rewrite scripts.session_preflight refs -> scripts.session.preflight.
  - file: tests/test_session_preflight_decomposition.py
    action: Modify
    purpose: Rewrite scripts.session_preflight refs -> scripts.session.preflight.
  - file: tests/test_session_preflight_platform_roadmap.py
    action: Modify
    purpose: Rewrite scripts.session_preflight + `from scripts import product_roadmap as product_roadmap_module` -> `from scripts.roadmap import product_roadmap as product_roadmap_module` and platform_roadmap refs.
  - file: tests/test_session_preflight_product_roadmap.py
    action: Modify
    purpose: Rewrite scripts.session_preflight + product_roadmap refs -> nested forms.
  - file: tests/test_session_postflight.py
    action: Modify
    purpose: Rewrite scripts.session_postflight imports + @patch targets + the "scripts/session_postflight.py" self-reinvocation string literal -> scripts/session/postflight.py.
  - file: tests/test_session_postflight_decomposition.py
    action: Modify
    purpose: Rewrite scripts.session_postflight refs -> scripts.session.postflight.
  - file: tests/test_session_metrics.py
    action: Modify
    purpose: Rewrite the scripts.session_metrics ref -> scripts.session.metrics.
  - file: tests/test_sync_ops.py
    action: Modify
    purpose: >-
      Rewrite ~126 refs: `import scripts.sync_ops as X` -> `import scripts.sync.ops as X`; the 16
      `from scripts import sync_ops` -> `from scripts.sync import ops as sync_ops` (alias to preserve the
      bound name); all @patch("scripts.sync_ops...") string targets -> scripts.sync.ops.
  - file: tests/test_sync_recommendations.py
    action: Modify
    purpose: Rewrite scripts.sync_recommendations imports + @patch targets -> scripts.sync.recommendations.
  - file: tests/test_sync_recommendations_decisions.py
    action: Modify
    purpose: Rewrite scripts.sync_recommendations refs -> scripts.sync.recommendations.
  - file: tests/test_sync_ducklake_version.py
    action: Modify
    purpose: Rewrite scripts.sync_ducklake_version imports + refs -> scripts.sync.ducklake_version.
  - file: tests/test_platform_roadmap.py
    action: Modify
    purpose: Rewrite `import scripts.platform_roadmap as facade` + refs -> scripts.roadmap.platform_roadmap.
  - file: tests/test_platform_roadmap_gate_rules.py
    action: Modify
    purpose: Rewrite any scripts.platform_roadmap import (the facade) -> scripts.roadmap.platform_roadmap. Leave scripts.platform_roadmap_gate_rules (out-of-scope sibling, stays at root) UNTOUCHED.
  - file: tests/test_platform_roadmap_models.py
    action: Modify
    purpose: Rewrite any scripts.platform_roadmap facade import -> scripts.roadmap.platform_roadmap; leave scripts.platform_roadmap_models untouched.
  - file: tests/test_platform_roadmap_state.py
    action: Modify
    purpose: Rewrite any scripts.platform_roadmap facade import -> scripts.roadmap.platform_roadmap; leave scripts.platform_roadmap_state untouched.
  - file: tests/test_product_roadmap.py
    action: Modify
    purpose: Rewrite scripts.product_roadmap imports + refs -> scripts.roadmap.product_roadmap.
  - file: tests/test_product_roadmap_schema.py
    action: Modify
    purpose: Rewrite scripts.product_roadmap_schema refs -> scripts.roadmap.product_roadmap_schema.
  - file: tests/test_product_roadmap_state.py
    action: Modify
    purpose: Rewrite scripts.product_roadmap facade refs -> scripts.roadmap.product_roadmap.
  - file: tests/test_plan_document.py
    action: Modify
    purpose: Rewrite scripts.plan_document imports/refs -> scripts.roadmap.plan_document.
  - file: tests/test_plan_audit.py
    action: Modify
    purpose: Rewrite scripts.plan_audit + scripts.find_plan refs -> scripts.roadmap.plan_audit / scripts.roadmap.find_plan.
  - file: tests/test_find_plan.py
    action: Modify
    purpose: Rewrite scripts.find_plan imports + any scripts/find_plan.py path literal -> scripts.roadmap.find_plan / scripts/roadmap/find_plan.py.
  - file: tests/test_validate.py
    action: Modify
    purpose: Rewrite the five `import scripts.sync_ducklake_version as ...` in the lockstep-check tests -> scripts.sync.ducklake_version.
  - file: tests/test_ops_data_portal.py
    action: Modify
    purpose: Rewrite scripts.sync_ops @patch/import refs -> scripts.sync.ops (leave any ci_rca_evidence_dispute rec-source strings untouched).
  - file: tests/test_ops_portal_incremental_cache.py
    action: Modify
    purpose: Rewrite `from scripts import sync_ops` -> `from scripts.sync import ops as sync_ops` and any scripts.sync_ops refs -> scripts.sync.ops.
  - file: tests/test_migrate_ops_data.py
    action: Modify
    purpose: Rewrite scripts.sync_ops / scripts.sync_recommendations refs -> nested forms.
  - file: tests/test_classify_automatable.py
    action: Modify
    purpose: Rewrite any scripts.sync_ops / roadmap module ref surfaced by the sweep -> nested form.
  - file: tests/test_implement_skill_decomposition_hints.py
    action: Modify
    purpose: Rewrite any moved-module path/module ref surfaced by the sweep -> nested form (skill-hint assertion file).
  - file: tests/test_ci_rca_back_validate.py
    action: Modify
    purpose: Rewrite the moved-module ref (sync_ops or find_plan) surfaced by the sweep -> nested form. Leave ci_rca refs (already nested in P4a) untouched.
bundled_recommendations: []
infrastructure_dependencies: []
acceptance_criteria:
  - >-
    None of the 12 moved families' files remain DIRECTLY under scripts/: session_{preflight,postflight,
    metrics} live under scripts/session/, sync_{ops,recommendations,ducklake_version} under scripts/sync/,
    and platform_roadmap/product_roadmap/product_roadmap_schema/plan_document/plan_audit/find_plan under
    scripts/roadmap/ -- each package with an __init__.py. The out-of-scope siblings platform_roadmap_{gate_rules,
    models,state}.py remain at scripts/ root, byte-unchanged versus origin/main.
  - >-
    Prefix rule applied consistently: session_*/sync_* STRIP the family prefix (session.preflight,
    sync.ops, ...); the roadmap family KEEPS full names (scripts.roadmap.platform_roadmap, ...). Every
    `from scripts import sync_ops` binding is preserved via `from scripts.sync import ops as sync_ops`.
  - >-
    The self-referential workflow entrypoints resolve post-move: `bin/venv-python -m scripts.session.preflight`
    runs (the /plan + /implement + /audit + /orient Step-1 entrypoint), `bin/venv-python scripts/roadmap/find_plan.py`
    resolves a PLAN path (the /implement Step-2 resolver), and `scripts/session/postflight.py --pre-commit-sanity`
    self-reinvokes correctly. Every .claude/ command + skill reference is rewritten (no flat form survives there).
  - >-
    The 8 ROOT-anchoring modules resolve repo-root paths post-move (parent.parent -> parent.parent.parent):
    scripts.roadmap.find_plan.ROOT, scripts.roadmap.plan_audit.ROOT, and scripts.session.metrics.ROOT each
    contain scripts/validate.py; scripts.sync.ops._REPO_ROOT, scripts.sync.recommendations._REPO_ROOT, and
    scripts.sync.ducklake_version.ROOT each resolve the repo root; scripts.roadmap.plan_document default
    plans_root resolves <repo>/docs/plans; and scripts/session/postflight.py sys.path bootstrap reaches root.
  - >-
    Zero flat references remain in LIVE surfaces: a git grep for scripts[./](session_preflight|session_postflight|
    session_metrics|sync_ops|sync_recommendations|sync_ducklake_version|platform_roadmap|product_roadmap|
    product_roadmap_schema|plan_document|plan_audit|find_plan) AND `from scripts import (session_*|sync_*|...)`,
    excluding docs/plans/, audits/, logs/, docs/DECISIONS*.md, docs/CHANGELOG.md, docs/SESSION_LOG*.md,
    docs/INTENT-*.md, the EXECUTED docs/audit-prompts/*.md, and config/lambda/ducklake/version.yaml
    (append-only/grandfathered history + the Decision-79 Lambda carve-out), returns nothing.
  - >-
    docs/contracts/file-router.yaml: the 4 rewritten rows resolve to the new nested paths and the 3 new
    package routes (session-code/sync-code/roadmap-code) resolve; validate_placement is green (no dead target,
    no duplicate topic).
  - >-
    scripts/test_coverage_checker.py map_source_to_test resolves each moved module to its kept-in-place test:
    session/<stem> -> test_session_<stem>.py, sync/<stem> -> test_sync_<stem>.py, roadmap/<stem> ->
    test_<stem>.py (coverage-existence enforcement preserved; test files keep their names).
  - >-
    FULL validate (bin/venv-python -m scripts.validate, not --pre) and FULL pytest (bin/venv-python -m pytest)
    are both green, including import-linter (scripts is a root_package so the new subpackages are auto-covered)
    and validate_placement; compute_affected_artifacts over the ACTUAL changed files returns {} (V2 -- no Lambda
    step), which holds because config/lambda/ducklake/version.yaml (the only manifest-tracked file the sweep
    would otherwise touch) is deliberately left unedited (Decision 79 carve-out).
  - rec-164 remains OPEN (it closes at RS-01 phase 3, not here); no Resolves trailer.
verification_plan:
  - step: 1
    phase: pre-deploy
    hermetic: false
    action: Validate this PLAN document against the PlanDocument schema.
    command: bin/venv-python -m scripts.roadmap.plan_document docs/plans/PLAN-nest-session-sync-roadmap.yaml
    expected: Reports the plan validates against the PlanDocument schema (no errors). (Post-move the validator is scripts.roadmap.plan_document; pre-move use scripts.plan_document.)
    fix_if: A schema error names the offending field; correct it (extra keys forbidden; every VP step needs a non-empty command; unique step ids).
  - step: 2
    phase: pre-deploy
    hermetic: false
    action: Confirm all 12 modules moved into their packages with __init__.py markers and no flat original remains directly under scripts/.
    command: >-
      set -e; for p in session/preflight session/postflight session/metrics sync/ops sync/recommendations sync/ducklake_version roadmap/platform_roadmap roadmap/product_roadmap roadmap/product_roadmap_schema roadmap/plan_document roadmap/plan_audit roadmap/find_plan; do test -f "scripts/$p.py" || { echo "MISSING scripts/$p.py"; exit 1; }; done; for i in session sync roadmap; do test -f "scripts/$i/__init__.py" || { echo "MISSING scripts/$i/__init__.py"; exit 1; }; done; for old in session_preflight session_postflight session_metrics sync_ops sync_recommendations sync_ducklake_version platform_roadmap product_roadmap product_roadmap_schema plan_document plan_audit find_plan; do test ! -e "scripts/$old.py" || { echo "STILL FLAT scripts/$old.py"; exit 1; }; done; echo MOVED_OK
    expected: Prints MOVED_OK (12 modules under their packages, 3 __init__.py present, zero flat originals).
    fix_if: A MISSING/STILL FLAT line names the offender; complete the git mv (or add the __init__.py) and re-run.
  - step: 3
    phase: pre-deploy
    hermetic: false
    action: Stale-reference sweep (Closure Obligation) -- zero flat references (dotted, path, or from-scripts-import) survive in LIVE surfaces; append-only/grandfathered history is intentionally excluded.
    command: >-
      ALT="product_roadmap_schema|sync_ducklake_version|session_preflight|session_postflight|sync_recommendations|platform_roadmap|session_metrics|product_roadmap|plan_document|plan_audit|find_plan|sync_ops"; MATCHES=$(git grep -nE "scripts[./]($ALT)\b|from scripts import ($ALT)\b" -- ':(exclude)docs/plans/*' ':(exclude)audits/*' ':(exclude)logs/*' ':(exclude)docs/DECISIONS.md' ':(exclude)docs/DECISIONS_ARCHIVE.md' ':(exclude)docs/CHANGELOG.md' ':(exclude)docs/SESSION_LOG*.md' ':(exclude)docs/INTENT-*.md' ':(exclude)docs/audit-prompts/*' ':(exclude)config/lambda/ducklake/version.yaml' || true); if [ -z "$MATCHES" ]; then echo SWEEP_CLEAN; else echo "STALE_REFS:"; echo "$MATCHES"; fi
    expected: >-
      Prints SWEEP_CLEAN. (NB `scripts.platform_roadmap` inside scripts.platform_roadmap_gate_rules/_models/_state
      is NOT matched -- the \b after platform_roadmap fails before the `_` -- so the out-of-scope siblings
      correctly do not appear.)
    fix_if: STALE_REFS lists file:line -- apply the anchored rewrite to that live file (unless it is genuinely append-only, in which case widen the exclude pathspec, not the rewrite).
  - step: 4
    phase: pre-deploy
    hermetic: false
    action: ROOT-anchor correctness -- the 8 depth-sensitive modules resolve the repo root post-move (the move-sensitivity fix the import rewrite does NOT provide).
    command: >-
      bin/venv-python -c "import scripts.roadmap.find_plan as fp, scripts.roadmap.plan_audit as pa, scripts.roadmap.plan_document as pd, scripts.session.metrics as sm, scripts.sync.ops as so, scripts.sync.recommendations as sr, scripts.sync.ducklake_version as sd; from pathlib import Path; assert (fp.ROOT/'scripts'/'validate.py').exists(), fp.ROOT; assert (pa.ROOT/'scripts'/'validate.py').exists(), pa.ROOT; assert (sm.ROOT/'scripts'/'validate.py').exists(), sm.ROOT; assert (so._REPO_ROOT/'scripts'/'validate.py').exists(), so._REPO_ROOT; assert (sr._REPO_ROOT/'scripts'/'validate.py').exists(), sr._REPO_ROOT; assert (sd.ROOT/'requirements.txt').exists(), sd.ROOT; import inspect; print('ROOT_ANCHORS_OK')"
    expected: Prints ROOT_ANCHORS_OK (every anchored ROOT/_REPO_ROOT contains scripts/validate.py or requirements.txt). plan_document + session/postflight anchors are exercised in steps 6/10.
    fix_if: An AssertionError shows a path missing scripts/ -- that module still has parent.parent (2 levels); change it to parent.parent.parent (3 levels).
  - step: 5
    phase: pre-deploy
    hermetic: false
    action: DO-NOT-TOUCH invariants -- the out-of-scope roadmap siblings are byte-unchanged and still importable (proves the \b-anchored sweep did not over-reach the platform_roadmap substring).
    command: >-
      CHANGED=$(git diff --name-only origin/main -- scripts/platform_roadmap_gate_rules.py scripts/platform_roadmap_models.py scripts/platform_roadmap_state.py); if [ -n "$CHANGED" ]; then echo "UNEXPECTED_CHANGE:"; echo "$CHANGED"; exit 1; fi; bin/venv-python -c "import scripts.platform_roadmap_gate_rules, scripts.platform_roadmap_models, scripts.platform_roadmap_state; print('SIBLINGS_OK')"
    expected: Prints SIBLINGS_OK (no diff on the three siblings; they still import from scripts/ root).
    fix_if: UNEXPECTED_CHANGE names a sibling the sweep wrongly touched -- revert it (git checkout origin/main -- <file>); the anchored regex must never rewrite these.
  - step: 6
    phase: pre-deploy
    hermetic: false
    action: >-
      Self-referential workflow-entrypoint smoke -- the three moved entrypoints the REMAINING restructure
      phases depend on resolve end-to-end: session.preflight runs, find_plan resolves a plan by path, and the
      postflight facade + plan_document default-root work.
    command: >-
      bin/venv-python -m scripts.session.preflight --health >/dev/null 2>&1; echo "preflight rc=$?"; bin/venv-python scripts/roadmap/find_plan.py docs/plans/PLAN-nest-session-sync-roadmap.yaml; bin/venv-python -c "import scripts.session.postflight as sp, scripts.roadmap.plan_document as pd; from pathlib import Path; r=Path(sp.__file__).resolve().parents[2]; assert (r/'scripts'/'validate.py').exists(), r; assert pd.main.__defaults__ is not None; print('ENTRYPOINTS_OK')"
    expected: >-
      preflight rc is 0 or 1 (runs, not an ImportError); find_plan prints the plan path (not NOT_FOUND / a
      traceback); ENTRYPOINTS_OK printed (postflight repo-root reachable two levels up; plan_document importable).
    fix_if: An ImportError/ModuleNotFoundError or NOT_FOUND means a cross-family import, the sys.path bootstrap depth, or the find_plan ROOT was not fixed -- resolve per steps 3/4 and re-run.
  - step: 7
    phase: pre-deploy
    hermetic: false
    action: Coverage-map preservation -- map_source_to_test resolves every moved module to its kept-in-place test file.
    command: >-
      bin/venv-python -c "from scripts.test_coverage_checker import map_source_to_test as m; from pathlib import Path; import sys; cases={'scripts/session/preflight.py':'tests/test_session_preflight.py','scripts/session/postflight.py':'tests/test_session_postflight.py','scripts/session/metrics.py':'tests/test_session_metrics.py','scripts/sync/ops.py':'tests/test_sync_ops.py','scripts/sync/recommendations.py':'tests/test_sync_recommendations.py','scripts/sync/ducklake_version.py':'tests/test_sync_ducklake_version.py','scripts/roadmap/platform_roadmap.py':'tests/test_platform_roadmap.py','scripts/roadmap/product_roadmap.py':'tests/test_product_roadmap.py','scripts/roadmap/product_roadmap_schema.py':'tests/test_product_roadmap_schema.py','scripts/roadmap/plan_document.py':'tests/test_plan_document.py','scripts/roadmap/plan_audit.py':'tests/test_plan_audit.py','scripts/roadmap/find_plan.py':'tests/test_find_plan.py'}; bad=[s for s,t in cases.items() if str(m(Path(s))).replace(chr(92),'/').endswith(t)==False]; print('MAP_OK' if not bad else 'MAP_FAIL: '+str(bad)); sys.exit(0 if not bad else 1)"
    expected: Prints MAP_OK (all 12 nested modules map to their existing test files).
    fix_if: MAP_FAIL lists the offenders; add/fix the session/sync/roadmap len==3 branch in map_source_to_test.
  - step: 8
    phase: pre-deploy
    hermetic: false
    action: Discovery-index + placement -- validate_placement is green (4 rewritten rows resolve, 3 new package routes resolve, no duplicate topic).
    command: >-
      bin/venv-python -c "from scripts.checks.hygiene.validate_placement import validate_placement as v; f=[]; v(f); print('PLACEMENT_OK' if not f else 'PLACEMENT_FAIL: '+'; '.join(f))"
    expected: Prints PLACEMENT_OK (scripts/session|sync|roadmap tracked and resolve; session-code/sync-code/roadmap-code topics unique; no dead target).
    fix_if: PLACEMENT_FAIL names the issue -- a dead target means a file-router row still points at a flat path or git add did not stage the new package; a duplicate topic means a new route name collides.
  - step: 9
    phase: pre-deploy
    hermetic: false
    action: >-
      Lambda non-regression -- confirm zero Lambda artifacts are affected: compute_affected_artifacts over the
      ACTUAL changed files is {}, AND the manifest-tracked config/lambda/ducklake/version.yaml is byte-unchanged
      (the deliberate carve-out that keeps this V2, per the Decision 79 scout flag).
    command: >-
      bin/venv-python -c "import subprocess; from scripts.lambda_manifest import compute_affected_artifacts as c; changed=[l for l in subprocess.run(['git','diff','--name-only','origin/main'],capture_output=True,text=True).stdout.splitlines() if l.strip()]; r=c(changed); vy='config/lambda/ducklake/version.yaml'; print('CHANGED_N='+str(len(changed))); print('LAMBDA_NONE' if r=={} else 'LAMBDA_AFFECTED: '+str(r)); print('VERSION_YAML_UNTOUCHED' if vy not in changed else 'VERSION_YAML_EDITED(triggers V3)')"
    expected: Prints LAMBDA_NONE and VERSION_YAML_UNTOUCHED (no changed file is Lambda-packaged; version.yaml deliberately not edited -> V2 holds, no build/deploy/smoke-test).
    fix_if: LAMBDA_AFFECTED or VERSION_YAML_EDITED means a manifest-tracked file was edited -- revert that edit (rehome the reference to a non-bundled surface) to keep V2, or re-classify to V3 with per-Lambda deploy for the named artifacts (needs AWS; escalate).
  - step: 10
    phase: pre-deploy
    hermetic: false
    action: >-
      Functional -- run the moved families' primary + satellite + cross-cutting tests, which exercise real
      imports, all @patch string targets, the sync_ops from-import aliasing, the find_plan path resolver, and
      the postflight self-reinvocation subprocess.
    command: >-
      bin/venv-python -m pytest tests/test_session_preflight.py tests/test_session_preflight_cache_serving.py tests/test_session_preflight_contracts.py tests/test_session_preflight_decomposition.py tests/test_session_preflight_platform_roadmap.py tests/test_session_preflight_product_roadmap.py tests/test_session_postflight.py tests/test_session_postflight_decomposition.py tests/test_session_metrics.py tests/test_sync_ops.py tests/test_sync_recommendations.py tests/test_sync_recommendations_decisions.py tests/test_sync_ducklake_version.py tests/test_platform_roadmap.py tests/test_platform_roadmap_gate_rules.py tests/test_platform_roadmap_models.py tests/test_platform_roadmap_state.py tests/test_product_roadmap.py tests/test_product_roadmap_schema.py tests/test_product_roadmap_state.py tests/test_plan_document.py tests/test_plan_audit.py tests/test_find_plan.py tests/test_validate.py tests/test_ops_data_portal.py tests/test_ops_portal_incremental_cache.py tests/test_migrate_ops_data.py -q
    expected: All listed test modules pass (imports, patches, from-import aliasing, path resolver, and subprocess entrypoints all resolve against the nested packages).
    fix_if: A failing test names the unresolved import/patch target; finish the anchored rewrite (imports + every @patch string + from-scripts-import alias) in that test file or the moved module.
  - step: 11
    phase: pre-deploy
    hermetic: false
    action: Authoritative gate 1 -- FULL validate (not --pre). Runs import-linter, validate_placement, validate_plan_documents/vp_replay/platform_roadmap/product_roadmap, the coverage checker, prompt-compliance, and the whole registered sequence.
    command: bin/venv-python -m scripts.validate
    expected: Exits 0 -- all checks pass, including import-linter (no new cycle/contract violation) and validate_placement, and the roadmap checks that now import scripts.roadmap.*.
    fix_if: A named check fails; forward-fix on-branch (Decision 55 -- never disable a check). If a roadmap check errors on import, its `from scripts.<name>` was not repointed to scripts.roadmap.<name>.
  - step: 12
    phase: pre-deploy
    hermetic: false
    action: Authoritative gate 2 -- FULL pytest across the whole suite (catches any missed @patch target or import in cross-cutting tests beyond the step-10 subset).
    command: bin/venv-python -m pytest -q
    expected: The full suite passes (0 failures).
    fix_if: A failure names the test/module; if an unresolved patch target in a cross-cutting test, finish the anchored rewrite there; if a moved module's runtime path broke, re-check the ROOT fix (step 4).
constraints:
  - IMPLEMENTATION plan type; no recommendations bundled or filed (orchestrator constraint; STRATEGIC + >5-file heuristic suspended per Decision 67 -- authored as one larger IMPLEMENTATION plan).
  - >-
    Do NOT close rec-164 -- it owns the whole RS-01 territory and closes only at RS-01 phase 3. This phase
    moves THREE families (session, sync, roadmap) and leaves the remaining grandfathered families (llm_*,
    ops_*) untouched. No Resolves: trailer.
  - >-
    No import shims, re-export stubs, or compatibility deferred-imports (audit Q6 answer -- the import-linter
    contracts exist precisely to prevent that pattern). The move is a hard same-commit cutover.
  - >-
    Per-name rename mapping (NOT a single family-prefix substitution): session_* and sync_* STRIP the family
    prefix (session_preflight->session.preflight, sync_ops->sync.ops, etc.); the roadmap family KEEPS full
    names (platform_roadmap->roadmap.platform_roadmap, find_plan->roadmap.find_plan, etc.). Build the sweep as
    a re.sub over an explicit per-name mapping (dotted `scripts.NAME`, path `scripts/NAME.py`, AND
    `from scripts import NAME`), names sorted longest-first, each pattern \b-anchored.
  - >-
    Binding preservation: `from scripts import sync_ops` (17 sites, mostly tests/test_sync_ops.py) MUST become
    `from scripts.sync import ops as sync_ops` -- the `as sync_ops` alias keeps the bound name so downstream
    `sync_ops.X` / @patch refs still resolve. The roadmap family keeps its name so `from scripts import
    platform_roadmap` -> `from scripts.roadmap import platform_roadmap` needs NO alias (a redundant
    `as platform_roadmap` would trip ruff PLC0414).
  - >-
    The 8 depth-sensitive modules MUST have Path(__file__)...parent.parent changed to parent.parent.parent:
    session/postflight.py (sys.path bootstrap), session/metrics.py, sync/ops.py, sync/recommendations.py,
    sync/ducklake_version.py, roadmap/plan_document.py (default plans_root), roadmap/plan_audit.py, and
    roadmap/find_plan.py. session/preflight.py, roadmap/platform_roadmap.py, roadmap/product_roadmap.py, and
    roadmap/product_roadmap_schema.py have NO __file__ anchor -- do NOT add one. This is a mandatory
    correctness edit the import rewrite does not provide.
  - >-
    DO-NOT-TOUCH: the out-of-scope siblings scripts/platform_roadmap_gate_rules.py, scripts/platform_roadmap_models.py,
    scripts/platform_roadmap_state.py stay at scripts/ root (not in phase-2 scope). The moved roadmap/platform_roadmap.py
    imports them via absolute `from scripts.platform_roadmap_gate_rules import ...` -- LEAVE those imports unchanged.
    The \b boundary already excludes them from the sweep (the `_` after platform_roadmap defeats \b).
  - >-
    Sweep LIVE surfaces only. Append-only / grandfathered history is LEFT untouched: docs/plans/ (PLAN-*,
    archive/, reports/), audits/ (incl. audits/repository-structure-cb02572.yaml), logs/, docs/DECISIONS.md,
    docs/DECISIONS_ARCHIVE.md, docs/CHANGELOG.md, docs/SESSION_LOG*.md, the grandfathered docs/INTENT-*.md, and
    the EXECUTED docs/audit-prompts/*.md (composed-and-run audit artifacts, P4a precedent). Conversely, these
    ARE live surfaces and ARE swept: the 3 ROADMAP-*.yaml (cosmetic files_in_scope/prose), docs/GETTING_STARTED.md,
    the 3 live docs/contracts/*.yaml (candidate-decision-ratification, exit-criteria-ledger, session-id), and
    docs/runbooks/ducklake-catalog-operations.md.
  - >-
    file-router.yaml (docs/contracts/) is a live CI gate via validate_placement: the 4 rows targeting
    scripts/session_preflight.py / session_postflight.py / session_metrics.py / plan_audit.py MUST be repointed
    to the new nested paths in this same commit or validate_placement fails on dead targets. Stage the new
    packages (git add) BEFORE running validate so its `git ls-files` snapshot sees them tracked.
  - >-
    version.yaml Lambda carve-out (Decision 79 / decision-scout WARN, RESOLVED): config/lambda/ducklake/version.yaml
    is tracked in FOUR active DuckLake manifests (ducklake_catalog_dr/maintenance/reader/writer), so ANY edit to it
    makes compute_affected_artifacts non-empty and escalates this plan to V3 (build+deploy+smoke-test, needs AWS).
    Its header "TO BUMP" comment `-m scripts.sync_ducklake_version` is therefore LEFT stale (deliberate carve-out,
    excluded from the VP3 sweep) to keep this a clean V2 refactor. The provenance path is instead corrected on the
    NON-bundled surfaces: the module's _FLOOR_COMMENT (scripts/sync/ducklake_version.py) and the requirements.txt
    duckdb-floor comment. Do NOT edit version.yaml. A future DuckLake-Lambda deploy can piggyback the comment fix.
  - Test files are NOT renamed. tests/test_<old_flat_name>.py stay put; only their imports / @patch targets / from-imports / path literals are rewritten. The coverage-map extension keeps them resolvable.
  - Gates are FULL validate (not --pre) AND FULL pytest, both green before the orchestrator pushes.
  - No emojis; ASCII hyphens only; ruff line length 127; type hints; async for I/O (AGENTS.md style). After editing a test file keep its imports in ONE block (tests/CLAUDE.md -- ruff format drops symbols from a second same-module block); verify with `ruff check` after the sweep.
  - "Public-repo content boundary (Decision 101): every touched file is engineering code/config/docs -- no account IDs, ARNs, ExternalIds, credentials, or trading alpha."
  - No rescue agents or workaround loops (Decision 55). Forward-fix on-branch only. Never edit or commit on main; work on the harness claude/ branch (AGENTS.md Git-ops).
context:
  - >-
    Decision-scout verdict + CITE list (verbatim decision ids): FLAGS_FOUND (84 of 84 decisions triaged). ONE
    WARN raised and RESOLVED in this plan: Decision 79 (+CD.16/CD.24, per-Lambda deploy gating) -- the scout
    caught that config/lambda/ducklake/version.yaml is tracked in 4 active DuckLake manifests, so editing its
    bump-comment would make compute_affected_artifacts non-empty and force V3; RESOLVED by adopting the scout's
    primary recommendation (drop version.yaml from scope; correct the provenance on the non-bundled module +
    requirements.txt surfaces instead), preserving V2. CITE: Decision 104 (validate.py check registry -> the
    test_coverage_checker map extension + roadmap check-module rewrites); Decision 48 (Verification Tier -> V2,
    intact after the version.yaml carve-out); Decision 79 + CD.16/CD.24 (per-Lambda deploy gating -> the
    version.yaml carve-out keeps compute_affected_artifacts=={}); Decision 84 (DuckLake sole ops backend -> the
    sync_ops move + AGENTS.md `-m scripts.sync.ops` rewrite; nesting does not touch the closed reader/writer
    boundary); Decision 85 (PlanDocument schema; names plan_document/find_plan/plan_audit descriptively, not as
    immutable path pins); Decision 86 (machine-parseable homes -> file-router rows + package routes; no prose
    docs added); Decision 80 (import-linter -- scout VERIFIED none of the 12 modules in any of the 4 contracts;
    scripts is a root_package so nested packages auto-covered); Decision 102 (SLOC ratchet -- scout VERIFIED no
    moving module in config/sloc_budgets.yaml; git mv budget-neutral); Decision 67 (STRATEGIC freeze + heuristic
    suspension -> single IMPLEMENTATION plan); Decision 73 (two-tier CI -- validate_placement/coverage-existence
    are full-tier-only, so the FULL-validate acceptance is the pre-merge signal). RELATED (scout-verified):
    Decision 99 (DuckDB/DuckLake version SSOT -- sync_ducklake_version is its helper; keep the lockstep gate
    green), Decision 124 (ops_data_portal facade no-cycles contract -- scout verified the sync_* move names
    nothing in its layers), Decision 105 (candidate-decision ratification guard targets DECISIONS.md HEADERS not
    body module paths -- so leaving append-only DECISIONS.md untouched breaks no gate), Decisions 103/114/93
    (reference session_preflight/platform_roadmap descriptively, not as path pins), Decision 55 (no-shim
    same-commit forward-fix consistent), Decision 101 (public-repo boundary -- all ~75 touched files are
    engineering; compliant).
  - "gates: decision-scout=FLAGS_FOUND (1 WARN, Decision 79, resolved via version.yaml carve-out); plan-critique=PROCEED after 2 round(s)"
  - >-
    CRITICAL FINDING (design) -- ROOT move-sensitivity: 8 of the 12 modules compute repo root as
    Path(__file__)...parent.parent, correct only at flat depth. Nested one level deeper (scripts/<pkg>/<name>.py)
    the same expression resolves to scripts/, breaking logs/ paths (session.metrics, sync.ops, sync.recommendations),
    requirements.txt (sync.ducklake_version), docs/plans default root (roadmap.plan_document), the find_plan
    resolver + plan_audit logs + its sys.path.insert (roadmap.find_plan, roadmap.plan_audit), and the postflight
    sys.path bootstrap. Fix = parent.parent -> parent.parent.parent in those 8. session.preflight uses _common.ROOT
    (imported from the non-moving scripts/preflight/ package) so it needs NO fix; platform_roadmap/product_roadmap/
    product_roadmap_schema have no __file__ anchor. VP step 4 + 6 assert the anchors resolve.
  - >-
    CRITICAL FINDING (design) -- from-scripts-import binding + three reference forms: modules are referenced as
    (a) dotted scripts.NAME (import/-m/from-import/@patch strings), (b) path scripts/NAME.py, AND (c)
    `from scripts import NAME`. Form (c) binds the bare name, so for the prefix-stripped families the rewrite must
    alias (`from scripts.sync import ops as sync_ops`) to avoid breaking downstream sync_ops.X references (126
    refs in tests/test_sync_ops.py alone). A naive P4a-style two-pattern (dotted+path) sweep would MISS form (c)
    entirely and, done wrong, break bindings. This is the key delta from P4a (whose ci_rca family had no
    from-scripts-import consumers).
  - >-
    CRITICAL FINDING (design) -- self-referential hazard: session_preflight/postflight ARE the /plan + /implement
    + /audit + /orient Step-1/Step-2 tooling, and find_plan is /implement's plan resolver. After this merges the
    REMAINING phases (P4c/P5/P6) run /plan and /implement, so EVERY .claude/ command + skill + AGENTS.md + hook
    reference must be rewritten same-commit or those workflows break and the restructure stalls. The .md/skill
    surfaces are NOT exercised by validate/pytest -- VP step 3 (git-grep sweep) + step 6 (entrypoint smoke) are
    the guardrails. Enumerated live .claude/ consumers (verified at f119383): commands plan/implement/orient/audit;
    skills planning/implement/code-review/orient/plan-critique/audit-prompt. NOTE the session-start HOOKS
    (.claude/hooks/*) do NOT invoke these modules (session_start_sync_deps.sh calls bin/sync-deps.sh, not
    scripts.sync_ops) -- the orchestration hint's "hooks" ref does not materialize; the live callers are the
    commands/skills.
  - >-
    Self-reinvocation: scripts/session_postflight.py (~line 250) does `_common._run([_common.PYTHON,
    "scripts/session_postflight.py", "--pre-commit-sanity"])` -- a file-path self-reinvocation. Rewrite that
    literal to scripts/session/postflight.py AND fix the line-46 sys.path.insert depth, or run_close breaks.
    postflight/housekeeping.py separately PATH-invokes scripts/session_metrics.py and scripts/plan_audit.py --
    both rewritten. (scripts/postflight/_common.py ROOT is already parent.parent.parent -- it is one level nested
    already and does NOT move.)
  - >-
    Coverage is EXISTENCE-only for scripts/ modules -- verified this session. pyproject.toml [tool.coverage.run]
    source=["src"] means `--cov=scripts/X` collects no data, so check_per_file_coverage hits its
    "coverage.py unavailable or file not tracked" informational-continue path and appends NO 100%-coverage error
    (confirmed by running the real checker on scripts/ci_rca/evidence.py [P4a control], platform_roadmap,
    session_preflight, find_plan, sync_ops -- all returned []). So the ONLY coverage requirement is that
    map_source_to_test resolves the moved module to an existing test file: the 3 nested-package branches in
    scripts/test_coverage_checker.py (VP step 7). This is why P4a passed and why per-file coverage is not a gate here.
  - >-
    Scoping boundary (noted): the task scope is exactly the 6 roadmap files; the platform_roadmap DECOMPOSITION
    siblings platform_roadmap_{gate_rules,models,state}.py are NOT in scope and STAY at scripts/ root (a residual
    3-file platform_roadmap_* group). The moved roadmap/platform_roadmap.py imports them via absolute path
    (unaffected). This is functionally correct; a future cleanup phase may consolidate them. Not a blocker --
    acceptance ("none of the moved families' files remain directly under scripts/") targets the 6 named files.
  - >-
    Lambda / import-linter / SLOC / file-router verified at f119383: compute_affected_artifacts over the 12
    moved modules + edited importers returns {} (none is Lambda-packaged; grep of src/lambdas/*/manifest.yaml
    confirms) -> V2, NO deploy gate. The ONE manifest-tracked file the sweep would otherwise touch --
    config/lambda/ducklake/version.yaml (in 4 DuckLake manifests) -- is deliberately EXCLUDED (Decision 79
    carve-out; scout WARN); the provenance-comment fix is rehomed to the non-bundled module _FLOOR_COMMENT +
    requirements.txt (both compute_affected_artifacts=={}). .importlinter names no moving module (scripts is a root_package so
    scripts/session|sync|roadmap/ are auto-covered -- excluding .importlinter is correct, not a gap).
    config/sloc_budgets.yaml registers no moving module and git mv is SLOC-neutral (no --update-sloc-budgets).
    docs/contracts/file-router.yaml has 4 rows to repoint (existence-checked) + 3 package routes to add. NO
    .github/workflows/*.yml references any moving module (unlike P4a's ci-rca.yml) and docs/PROJECT_CONTEXT.md
    has no refs.
  - >-
    ci_rca HARD BLOCK status: preflight at f119383 reports ci_rca_unresolved_recs == [] (no active ci-rca
    CI-failure rec) -- no HARD BLOCK fires. Any residual ci_rca CI-failure investigation on branch
    codex/investigate-ci-failure-root-cause is under SEPARATE human ownership and DEFERRED per the orchestrator
    directive; it is DISTINCT from the roadmap/session/sync CODE families this phase moves. Recorded per
    Related-Work/deferral discipline.
  - >-
    Sequencing/dependency: RS-01 depends on RS-04 (P1: file-router.yaml + validate_placement link-validity) and
    follows P4a (the proven single-family precedent, PLAN-nest-ci-rca-family.yaml). The safety net is live, so a
    missed file-router reference fails CI loudly. scripts/CLAUDE.md (P3) already names session_*, sync_*, and
    roadmap/plan_* as grandfathered families that migrate 'one family per PR with a same-commit reference rewrite'
    -- this plan realizes three of them in one commit (necessary because session.preflight cross-imports both the
    sync and roadmap families; splitting would create broken intermediate states).
  - >-
    Main Divergence Assessment (base-agnostic -- main advances continuously; final alignment is deferred to the
    commit-flow auto-rebase, orchestrator-owned). This plan was authored/rebased on the P4a lineage (branched at
    f119383) and rebased once onto origin/main at 5bfa1fe, which absorbed #538 (Decision 125,
    lambda-deploy-channel-defaults): a BENIGN overlap touching 3 in-scope files -- AGENTS.md,
    .claude/skills/planning/SKILL.md, config/agent/verification_registry/registry.yaml -- with NO new moving-module
    references and in DIFFERENT sections than this plan's rewrite targets; the global re.sub sweep is robust to the
    line shifts and does NOT touch the 2 Decision-125 existence guards (patterns `deploy channel (Decision 125)` /
    `Deploy channel by artifact class (Decision 125)` contain no scripts. token, so the sweep cannot break them).
    Main has SINCE advanced past 5bfa1fe (e.g. #537 landed, independently assessed BENIGN: zero moving-module
    references, zero new existence guards, all touched in-scope files remain sweep-covered) and will keep moving.
    This plan deliberately pins NO exact base: any residual divergence at merge time is absorbed by the mandatory
    commit-flow auto-rebase (AGENTS.md Git-ops) FOLLOWED BY VP step 3's post-rebase reference sweep, which re-runs
    the global re.sub against whatever main is at that time and fails loudly on any missed reference. No scope file
    carries a semantic conflict with any landed commit. open_recommendations surfaced (none bundled per orchestrator constraint).
pre_implementation_checklist:
  - Branch confirmed not on main (claude/repo-restructure-audit-hm1gti)
  - AGENTS.md + scripts/CLAUDE.md (root-vs-subpackage rule; session_*/sync_*/roadmap grandfathered families) + tests/CLAUDE.md (namespace-migration discipline; single import block; @patch retargeting) read
  - Audit RS-01 + proposed_target_tree scripts/{session,sync,roadmap}/ nodes + Q6 hazard sequencing read from audits/repository-structure-cb02572.yaml
  - P4a precedent read (docs/plans/PLAN-nest-ci-rca-family.yaml -- the proven git mv + anchored-sweep + ROOT-fix + coverage-map + file-router pattern)
  - All 12 moving modules read; the 8 ROOT anchors, the from-scripts-import sites, the postflight self-reinvocation, the intra-roadmap imports (product_roadmap->schema/platform, plan_audit->find_plan), and the cross-family imports (session.preflight->sync.ops/roadmap.*) enumerated
  - "Reference surface enumerated at f119383: LIVE rewrites across scripts/ importers + config/ + docs/contracts/file-router.yaml + .claude/ (commands+skills) + AGENTS.md + .github/instructions + bin/ + EVALUATION-PROMPTS.yaml + docs/ARCHITECTURE-WORKFLOW.md + ~30 test files; append-only/grandfathered history classified and LEFT"
  - "Coverage confirmed EXISTENCE-only for scripts/ (source=['src']); per-file 100% not a gate; only map_source_to_test resolution matters"
  - "Lambda overlap re-verified: compute_affected_artifacts == {} (V2, no deploy gate); no .importlinter/sloc_budgets entry"
  - Acceptance criteria understood and verifiable
execution_steps:
  - |
    Create the three packages with one-line docstring markers (coverage-excluded), e.g.:
      scripts/session/__init__.py  -> """scripts.session -- preflight/postflight/metrics session automation subpackage (RS-01 / rec-164 Phase A)."""
      scripts/sync/__init__.py     -> """scripts.sync -- ops/recommendations/ducklake_version cache-sync subpackage (RS-01 / rec-164 Phase A)."""
      scripts/roadmap/__init__.py  -> """scripts.roadmap -- roadmap + plan-artefact tooling subpackage (RS-01 / rec-164 Phase A)."""
  - |
    Move the 12 modules with git mv (preserves history):
      for m in preflight postflight metrics; do git mv "scripts/session_$m.py" "scripts/session/$m.py"; done
      for m in ops recommendations ducklake_version; do git mv "scripts/sync_$m.py" "scripts/sync/$m.py"; done
      for m in platform_roadmap product_roadmap product_roadmap_schema plan_document plan_audit find_plan; do git mv "scripts/$m.py" "scripts/roadmap/$m.py"; done
  - |
    Apply the anchored reference rewrite with a Python re.sub sweep (NOT plain sed -- needs \b + per-name mapping).
    Build a RENAME dict (old flat name -> new dotted suffix): session_preflight->session.preflight,
    session_postflight->session.postflight, session_metrics->session.metrics, sync_ops->sync.ops,
    sync_recommendations->sync.recommendations, sync_ducklake_version->sync.ducklake_version, and the 6 roadmap
    names -> roadmap.<same_name>. Apply THREE \b-anchored patterns over the EXPLICIT live-file list (the Scope
    Modify/Create rows -- moved modules at their NEW paths, non-moving importers, config, file-router,
    ARCHITECTURE-WORKFLOW, .claude/ commands+skills, AGENTS.md, .github/instructions, bin, EVALUATION-PROMPTS,
    and the ~30 test files) and NO other file:
      1. r"\bscripts\.(NAMES)\b"          -> "scripts." + RENAME[name]                (dotted: -m, from-import target, import, @patch)
      2. r"\bscripts/(NAMES)\.py\b"        -> "scripts/" + RENAME[name].replace(".","/") + ".py"   (path form)
      3. r"\bfrom scripts import (NAMES)\b" -> for roadmap: "from scripts.roadmap import NAME";
                                              for session/sync: "from scripts.<pkg> import <short> as NAME"  (alias to preserve binding)
    NAMES = the 12 names joined by | sorted longest-first. DO NOT include platform_roadmap_gate_rules/_models/_state,
    config/ci_rca_taxonomy.yaml-style non-modules, or the bare AGENTS.md "sync_ops" prose (handle those two bare
    mentions by hand). After the sweep run `ruff check --fix` on the edited Python to catch F401/PLC0414.
  - |
    Fix the 8 ROOT depths (the sweep does NOT do this). Change the single ROOT/_REPO_ROOT/sys.path definition line
    from Path(__file__)[.resolve()].parent.parent to ...parent.parent.parent in:
      scripts/session/postflight.py (line ~46 sys.path.insert), scripts/session/metrics.py, scripts/sync/ops.py,
      scripts/sync/recommendations.py, scripts/sync/ducklake_version.py, scripts/roadmap/plan_document.py (line ~145
      default plans_root expression), scripts/roadmap/plan_audit.py, scripts/roadmap/find_plan.py.
    Also rewrite the scripts/session/postflight.py self-reinvocation literal "scripts/session_postflight.py" ->
    "scripts/session/postflight.py" (the sweep's path pattern covers it if the file is in the list) and the
    sync/ducklake_version.py "generated by ..." provenance string.
  - |
    Extend scripts/test_coverage_checker.py map_source_to_test with three len==3 branches AFTER the ci_rca branch
    and BEFORE the len==2 branch:
      elif parts[0]=="scripts" and len(parts)==3 and parts[1]=="session": return ROOT/"tests"/f"test_session_{rel.stem}.py"
      elif parts[0]=="scripts" and len(parts)==3 and parts[1]=="sync":    return ROOT/"tests"/f"test_sync_{rel.stem}.py"
      elif parts[0]=="scripts" and len(parts)==3 and parts[1]=="roadmap": return ROOT/"tests"/f"test_{rel.stem}.py"
    Add the three mappings to the docstring. Do NOT add a general len==3 rule (would break sibling subpackages).
  - |
    Update docs/contracts/file-router.yaml: repoint the 4 existing rows (session-preflight, session-postflight,
    session-metrics, plan-execution-audit) to the new nested targets, and APPEND 3 package routes to the routes: list:
      - {topic: session-code,  targets: [scripts/session/],  description: "Session automation subpackage: preflight/postflight/metrics (RS-01)."}
      - {topic: sync-code,     targets: [scripts/sync/],     description: "Cache-sync subpackage: ops/recommendations/ducklake_version (RS-01)."}
      - {topic: roadmap-code,  targets: [scripts/roadmap/],  description: "Roadmap + plan tooling: platform/product_roadmap(_schema), plan_document, plan_audit, find_plan (RS-01)."}
  - "Handle the two bare AGENTS.md prose mentions by hand (warehouse section): `Do not call sync_ops` and `via sync_ops pull` -> `sync.ops` for accuracy; the anchored `-m scripts.sync_ops sync` is covered by the sweep."
  - "Stage everything with `git add -A` (git mv already staged the moves; this stages the __init__.py files, the sweep edits, the ROOT fixes, the coverage-map branches, and the file-router rows) so validate_placement's `git ls-files` snapshot sees scripts/session|sync|roadmap/ as tracked."
  - "Execute the Verification Plan -- run steps 1-12 in order; loop until all pass. Gates 11 (FULL validate) and 12 (FULL pytest) are authoritative; step 6 (entrypoint smoke) de-risks the self-referential hazard. On any failure, forward-fix on-branch (Decision 55); never disable a check or add a re-export shim."
  - "Report: the 12 modules moved into 3 packages + 3 __init__.py, the anchored+from-import rewrite (files touched), the 8 ROOT fixes, the postflight self-reinvocation fix, the coverage-map branches, the 4 file-router row repoints + 3 package routes, the DO-NOT-TOUCH sibling invariants held, compute_affected_artifacts=={}, and the full VP results (each step verdict incl. entrypoint smoke + FULL validate + FULL pytest green). Confirm rec-164 left OPEN."
work_areas: []
rollback: >-
  All changes are git mv + surgical text edits + three new __init__.py + one additive coverage-map branch set +
  file-router row repoints/additions, all on the branch; revert with `git checkout -- . && git clean -fd
  scripts/session scripts/sync scripts/roadmap` before commit, or `git revert <sha>` after. No infrastructure, no
  deploy, no data migration, no Lambda artifact touched (compute_affected_artifacts == {}) -- rollback is a plain
  git operation with zero external side effects. rec-164 is untouched, so no rec-state rollback is needed.
