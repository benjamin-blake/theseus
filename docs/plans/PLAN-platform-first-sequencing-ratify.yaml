# Generated by /plan for audit findings SEQ-01/SEQ-03/SEQ-04 (platform-first-sequencing-f4dec93).
schema_version: 1
slug: platform-first-sequencing-ratify
intent: Ratify the platform-first sequencing of the sole operator's build capacity as a conscious, reversal-conditioned Decision (Decision 132) rather than an inherited premise -- converting the system's most consequential standing allocation into a recorded, observably-reversible governance choice so the self-improvement loop's capacity bet is auditable and time-boxed (audit SEQ-01/03/04).
plan_type: IMPLEMENTATION
verification_tier: V1
plan_path: docs/plans/PLAN-platform-first-sequencing-ratify.yaml
phase: 'Platform governance (ad-hoc, user-explicit) -- Decision-93 platform-MVP lineage; docs-only, no tier_item. ROADMAP-PRODUCT.yaml stays status: draft.'
scope:
- file: docs/DECISIONS.md
  action: Modify
  purpose: Insert Decision 132 (full drafted text in execution_steps) at the top of the newest-first decisions list, before '## Decision 131:'. Add bracketed inline annotations to Decision 93's two 'per the platform-first directive' lines (currently ~:2049 and ~:2053) pointing at Decision 132. Decision 93's substance is NOT rewritten.
- file: docs/ROADMAP-PLATFORM.yaml
  action: Modify
  purpose: 'Re-point T2.6''s circular reactivation clause (~:4940) to name Decision 132 condition (c) alpha-readiness + (a); sweep-re-point T3.5''s circular ''product-axis work resumes'' note (~:7240) to name Decision 132 condition (a). No tier_item status flips; CD.3 stays state: pending.'
bundled_recommendations: []
infrastructure_dependencies: []
acceptance_criteria:
- 'SEQ-01: docs/DECISIONS.md contains a ''## Decision 132:'' header ratifying platform-first sequencing of build capacity, naming the interleaved manual/paper-loop alternative as EXAMINED, with >= 1 observable reversal condition.'
- 'SEQ-01 stanza contract: exactly one ''```yaml reversal-conditions'' fenced stanza sits under Decision 132''s ''**Reversal conditions:**'' prose marker; it yaml.safe_loads to a single mapping with decision==132, top-level review_by==2026-09-30 (NOT a 4th conditions[] row), conditions ids [platform-mvp-closes, alpha-readiness, sustained-slip], and sustained-slip.params.consecutive==3.'
- 'SEQ-03: the Decision text states (a) a manual paper loop does not feed the Decision-95 agent-telemetry chain (category error; paper data lands in ABSENT product tables strategy_runs/tca_events/fills) and (b) the operator-unit costing (~20-40h setup; ~2-5h/week over >= 13 weeks; >= 1 quarter calendar), citing audits/platform-first-sequencing-f4dec93.yaml.'
- 'SEQ-01 annotation: Decision 93''s two ''per the platform-first directive'' lines each carry a bracketed pointer to Decision 132; Decision 93''s substance is otherwise unchanged.'
- 'SEQ-04: T2.6''s reactivation clause in ROADMAP-PLATFORM.yaml no longer says ''when product formula-discovery work begins'' and names Decision 132''s alpha-readiness condition; the swept T3.5 note no longer says ''when product-axis work resumes'' and names Decision 132 condition (a); CD.3''s disposition (DEFERRED, gated on the named conditions) is stated in Decision 132.'
- 'docs/ROADMAP-PRODUCT.yaml is unchanged (absent from the diff; stays status: draft).'
- 'ETL: parse_decisions_md captures no stanza-distinctive token into Decision 132''s problem/decision_text/context fields (stanza invisible to --backfill-decisions-md); post-merge an ops_decisions dec-132 row is present via the operator backfill.'
- 'Gates green: plan_document schema PASS; ROADMAP-PLATFORM.yaml RoadmapDocument load PASS (Decision 93 no-live-dep invariant intact); validate.py --pre exits 0.'
verification_plan:
- step: 1
  phase: pre-deploy
  action: 'SEQ-01: Decision 132 header exists at the top of DECISIONS.md.'
  command: grep -n '^## Decision 132:' docs/DECISIONS.md
  expected: 'One match: the Decision 132 header line.'
  fix_if: No match -> header missing or misnumbered; re-check the next-free-number pre-check.
- step: 2
  phase: pre-deploy
  action: 'SEQ-01 stanza contract: exactly one well-formed reversal-conditions stanza, decision==132, review_by 2026-09-30, three condition ids, sustained-slip params.consecutive==3.'
  command: bin/venv-python -c "import re, yaml, pathlib; t = pathlib.Path('docs/DECISIONS.md').read_text(encoding='utf-8'); f = chr(96)*3; pat = '^' + re.escape(f + 'yaml reversal-conditions') + chr(10) + '(.*?)^' + re.escape(f); blocks = re.findall(pat, t, re.DOTALL | re.MULTILINE); assert len(blocks) == 1, 'expected exactly 1 stanza, got ' + str(len(blocks)); d = yaml.safe_load(blocks[0]); assert d['decision'] == 132, d['decision']; assert str(d['review_by']) == '2026-09-30', d['review_by']; ids = [c['id'] for c in d['conditions']]; assert ids == ['platform-mvp-closes', 'alpha-readiness', 'sustained-slip'], ids; assert [c for c in d['conditions'] if c['id'] == 'sustained-slip'][0]['params']['consecutive'] == 3; print('STANZA OK', d['decision'], str(d['review_by']), ids)"
  expected: 'Prints: STANZA OK 132 2026-09-30 [''platform-mvp-closes'', ''alpha-readiness'', ''sustained-slip'']'
  fix_if: AssertionError or block-count != 1 -> fence not at column 0, decision key != 132, review_by moved into conditions, or a malformed/duplicate stanza.
- step: 3
  phase: pre-deploy
  action: 'SEQ-01: the interleaved paper-loop alternative is named as examined AND a reversal-conditions prose marker is present.'
  command: grep -q 'Examined alternative (the interleaved manual/paper L1+L3 loop)' docs/DECISIONS.md && grep -q '\*\*Reversal conditions:\*\*' docs/DECISIONS.md && echo SEQ01_PROSE_OK
  expected: Prints SEQ01_PROSE_OK.
  fix_if: Either grep fails -> the examined-alternative section or the reversal-conditions marker is missing.
- step: 4
  phase: pre-deploy
  action: 'SEQ-03: telemetry category-error statement + operator-unit costing + audit-yaml citation present in the Decision text.'
  command: grep -q 'CATEGORY ERROR' docs/DECISIONS.md && grep -q 'does not feed the Decision-95 agent-telemetry chain' docs/DECISIONS.md && grep -q 'strategy_runs, tca_events, fills' docs/DECISIONS.md && grep -q '20-40 hours' docs/DECISIONS.md && grep -q '2-5 hours/week' docs/DECISIONS.md && grep -q '>= 13 weeks' docs/DECISIONS.md && grep -q 'audits/platform-first-sequencing-f4dec93.yaml' docs/DECISIONS.md && echo SEQ03_OK
  expected: Prints SEQ03_OK.
  fix_if: Any grep fails -> the SEQ-03 category statement, operator-unit costing, or audit citation is missing from Decision 132.
- step: 5
  phase: pre-deploy
  action: 'SEQ-04: T2.6 reactivation clause re-pointed (no circular phrase; names Decision 132 alpha-readiness) and CD.3 disposition stated.'
  command: bin/venv-python -c "import yaml; d = yaml.safe_load(open('docs/ROADMAP-PLATFORM.yaml')); t26 = [i for i in d['tier_items'] if i.get('id') == 'T2.6'][0]; pn = t26['progress_note']; assert 'product formula-discovery work begins' not in pn, 'circular T2.6 clause still present'; assert 'Decision 132' in pn and 'alpha-readiness' in pn, 'T2.6 does not name Decision 132 alpha-readiness'; import pathlib; dec = pathlib.Path('docs/DECISIONS.md').read_text(encoding='utf-8'); assert 'CD.3 (compute node for PySR formula discovery) is DEFERRED' in dec, 'CD.3 disposition not stated'; print('SEQ04 T2.6 re-point + CD.3 disposition OK')"
  expected: 'Prints: SEQ04 T2.6 re-point + CD.3 disposition OK'
  fix_if: AssertionError -> T2.6 still says 'when product formula-discovery work begins', or does not name Decision 132 alpha-readiness, or the CD.3 DEFERRED disposition is not stated in the Decision.
- step: 6
  phase: pre-deploy
  action: 'SEQ-04 sweep: no circular product-reactivation note remains in ROADMAP-PLATFORM.yaml (T2.6 + T3.5 both re-pointed).'
  command: '! grep -nE ''when (the )?product[A-Za-z0-9 _-]*work (begins|resumes)|product-axis work resumes'' docs/ROADMAP-PLATFORM.yaml'
  expected: No output, exit 0 (zero circular reactivation notes remain).
  fix_if: A line prints -> a circular 'when product...work begins/resumes' note was missed; re-point it to a named Decision 132 condition.
- step: 7
  phase: pre-deploy
  action: 'SEQ-01 annotation: Decision 93''s two platform-first lines now point at Decision 132.'
  command: test $(grep -c 'ratified with reversal conditions by Decision 132' docs/DECISIONS.md) -ge 2 && echo ANNOT_OK
  expected: Prints ANNOT_OK (both Decision 93 annotations landed).
  fix_if: Count < 2 -> one or both Decision 93 annotations are missing.
- step: 8
  phase: pre-deploy
  action: 'Constraint: docs/ROADMAP-PRODUCT.yaml is untouched.'
  command: git diff --stat -- docs/ROADMAP-PRODUCT.yaml
  expected: 'Empty output (ROADMAP-PRODUCT.yaml is absent from the diff; stays status: draft).'
  fix_if: Any diff line -> the product roadmap was edited; revert it (out of scope).
- step: 9
  phase: pre-deploy
  action: 'ETL non-interference: the stanza is invisible to --backfill-decisions-md field extraction.'
  command: 'bin/venv-python -c "import pathlib; from scripts.decisions_md import parse_decisions_md; d = next(r for r in parse_decisions_md([pathlib.Path(''docs/DECISIONS.md'')]) if r[''decision_id''] == 132); toks = [''reversal-conditions'',''review_by'',''platform-mvp-closes'',''on_trigger'',''consecutive: 3'',''predicate: null'',''kind: repo_state'']; blob = d[''problem''] + d[''decision_text''] + d[''context'']; leak = [x for x in toks if x in blob]; assert not leak, ''ETL captured stanza content: '' + str(leak); print(''ETL-NON-INTERFERENCE OK; related='', sorted(set(d[''related_decisions''])))"'
  expected: Prints ETL-NON-INTERFERENCE OK and the related-decision id list.
  fix_if: AssertionError -> a stanza token leaked into an extracted field; ensure '**Reversal conditions:**' is a line-leading marker after '**Rationale:**' and the stanza body has no line-leading '**' and no bare '---'.
- step: 10
  phase: pre-deploy
  action: Plan artefact validates against the PlanDocument schema.
  command: bin/venv-python -m scripts.roadmap.plan_document docs/plans/PLAN-platform-first-sequencing-ratify.yaml
  expected: 'PASS: docs/plans/PLAN-platform-first-sequencing-ratify.yaml validates against PlanDocument schema.'
  fix_if: FAIL -> a schema violation (unknown key, missing required field, slug/path mismatch).
- step: 11
  phase: pre-deploy
  action: Decision 93 no-live-dep invariant intact after the ROADMAP edits (decision-scout NOTE-flag mitigation) + full fast gate.
  command: bin/venv-python -m scripts.roadmap.platform_roadmap docs/ROADMAP-PLATFORM.yaml && bin/venv-python -m scripts.validate --pre
  expected: platform_roadmap prints PASS (RoadmapDocument load, no-live-dep model_validator holds); validate --pre exits 0.
  fix_if: platform_roadmap FAIL -> a status flip or live->deferred edge slipped in (revert to prose-only reactivation-trigger edits). validate --pre non-zero -> address the reported check.
- step: 12
  phase: post-deploy
  action: '[operator, post-merge] Sync ops_decisions from DECISIONS.md so the dec-132 row (and the amended dec-093 row) land in the warehouse. Requires the agent_platform writer profile.'
  command: bin/venv-python -m scripts.ops_data_portal --backfill-decisions-md
  expected: Backfill succeeds; ops_decisions gains dec-132 and refreshes dec-093 (SEQ-01 'ops_decisions row present post-merge').
  fix_if: Writer profile unavailable -> re-run after `aws sts get-caller-identity --profile agent_platform`; the operator runs this, not the implement PR.
constraints:
- 'Docs-only governance ratification: NO code, NO infra, NO Lambda, NO build-capacity reallocation. Only docs/DECISIONS.md and docs/ROADMAP-PLATFORM.yaml may be modified.'
- 'docs/ROADMAP-PRODUCT.yaml is NOT edited and stays status: draft (VP step 8 proves it is absent from the diff). docs/plans/* are historical artifacts -- never edited by the sweep.'
- 'bundled_recommendations is empty: this ratification is delivered as a Decision + roadmap edits, never via a recommendation; no rec is the delivery vehicle, and no audit finding is filed via file_rec.'
- 'Decision 93''s substance is immutable: its two platform-first lines are ANNOTATED with a bracketed pointer to Decision 132, never reworded or deleted. The circular ''until product work begins'' end-language persists inside Decision 93 under the annotation; de-circularization of the operative reactivation triggers happens only in ROADMAP-PLATFORM.yaml (T2.6, T3.5) where the triggers actually live.'
- 'CD.3 stays state: pending and is NOT flipped to ratified; no ratified_as/filed_via is added to any candidate_decision, and no tier_item status is flipped (/plan never flips statuses). This keeps the Decision-105 ratified-CD shape / R1-R3 guard (validate_candidate_decision_ratification) OUT of scope (decision-scout NOTE).'
- 'Reversal-conditions stanza contract (a future --pre check + the SEQ-02 monitor will enforce; no consumer exists today): exactly one stanza per Decision; fence opener EXACTLY ''```yaml reversal-conditions'' at column 0 and closer ''```'' at column 0; body is ONE yaml.safe_load mapping; decision: MUST equal the header number (132); the calendar review is the top-level review_by ONLY (never a 4th conditions[] row); kind in {manual, repo_state}; repo_state + predicate: null means ''machine-checkable later, manual now'' with thresholds pre-recorded in params; no bare ''---'' line and no line-leading ''**'' inside the block.'
- ASCII only; plain hyphens; ASCII '->' arrows; NO em dashes or Unicode arrows anywhere in the Decision text or roadmap edits (Windows console encoding). Verified pure-ASCII at planning time.
- 'No rescue agents or workaround loops (Decision 55): on an unrecoverable VP failure, stop and RCA; never inline-patch around a gate.'
- 'No new standing prose-architecture doc (Decision 86): rationale lives inside Decision 132; the machine-parseable stanza is agent-first-consistent.'
context:
- Actions audit findings SEQ-01 (high), SEQ-03 (medium), SEQ-04 (medium) from audits/platform-first-sequencing-f4dec93.yaml (exec summary .md). Disposition Q5=ratify_with_reversal_conditions is SETTLED by the human -- interleave-vs-sequence is NOT re-litigated; only the condition values were open and are all resolved.
- 'Pre-resolved by the human (overseer as proxy): review_by=2026-09-30; sustained-slip N=3; CD.3=DEFERRED gated on the named conditions (no stand-up date, no substitute substrate); filed as user-explicit ad-hoc governance (precedent Decision 93), NOT a new tier_item; plan_type IMPLEMENTATION (STRATEGIC suspended, Decision 67/CD.17).'
- 'DECISIONS.md is newest-first: Decision 131 is at the file top (line ~5); Decision 132 inserts ABOVE it. Max live Decision header confirmed == 131 at planning time (132 is the next free number; DECISIONS.md is the numbering authority, Decision 84). If a concurrent PR claims 132, shift to the next free number in the header, the stanza decision: key, and the Warehouse ID together.'
- Line numbers cited here (Decision 93 ~:2049/:2053; T2.6 ~:4940; T3.5 ~:7240) are as-of the branch tree at planning time and have DRIFTED from the audit's f4dec93 anchors (:1311/:1315/:4741) -- locate every edit by the quoted substring, never by line number.
- 'ETL non-interference VERIFIED at planning time against the drafted text: scripts.decisions_md.parse_decisions_md extracts only Status/Problem/Decision/Rationale/Related/Date and terminates each section at the next line-leading ''**word'' marker, so the ''**Reversal conditions:**'' block (prose + stanza) is captured into NO ops_decisions field (zero stanza-distinctive tokens in problem/decision_text/context; related_decisions parsed = [40,67,75,78,84,86,87,93,95,96,97,101,105,108,126]).'
- 'All VP steps are hermetic: false by design. validate_vp_replay re-executes only (pre-deploy AND hermetic) steps of a diff-added PLAN against the PR tree; this plan''s VP asserts POST-implementation state (Decision 132 present, T2.6 re-pointed) which does NOT exist when the plan-doc PR merges, so marking any step hermetic would fail replay. Excluded-as-not-hermetic is the correct posture.'
- 'ci-rca deferral (planning-skill HARD-BLOCK escape): rec-2710 (Critical; ''Unit test hits real AWS: missing mock for find_recent_ci_rca_rec_by_fingerprint'' in tests/test_ops_data_portal.py) is open, surfaced SOFT/LIKELY-RESOLVED by preflight (a recent main commit appears to have fixed it; back-validation shows recurrence vs prior rec-2707). This docs-only governance plan is UNRELATED (different file, no shared Decision, different failure category = pytest-mock/real-AWS-leak); its triage/closure is /orient''s responsibility and is deferred out of scope here -- it does not block this ratification.'
- 'Preflight: telemetry-store not migrated (Decision 84 Phase 4, expected-degraded; open/close-session known-degraded per Decision 84, non-blocking). Ops sync pulled 1520 rows. No uncommitted-change or main-divergence blocker at planning time.'
- 'Decision-scout verdict + CITE list (verbatim decision ids): FLAGS_FOUND (91 of 91 decisions triaged; complete read). Novel ratification confirmed -- no existing Decision ratifies the frame (Decision 93 only cites it as given) and nothing contradicts ratifying it. CITE = Decision 93, 95, 75, 84, 105, 67 (/CD.17), 86; RELATED = 96, 97, 78, 87, 126, 40, 108, 101 (all folded into the Decision 132 Related line). Single NOTE flag: Decision 93 no-live-dep invariant on the ROADMAP-PLATFORM.yaml edits -- mitigated (reactivation-trigger prose only; no status flips / no depends_on edges; VP step 11 re-runs platform_roadmap load()).'
- 'gates: decision-scout=FLAGS_FOUND (1 round; NOTE accepted with mitigation); plan-critique=PROCEED (fresh-context gate, no REVISE findings; independently verified next-free-number==132, both T2.6/T3.5 roadmap edit anchors unique, the stanza contract matches VP step 2, the hermetic:false posture against validate_vp_replay source, and ETL non-interference against the parse_decisions_md section-termination regex; ran VP step 10 plan_document schema == PASS).'
pre_implementation_checklist:
- Branch confirmed not on main (harness claude/* branch).
- docs/PROJECT_CONTEXT.md read.
- 'docs/DECISIONS.md numbering re-confirmed: max live header == 131 (Decision 132 is free).'
- audits/platform-first-sequencing-f4dec93.{yaml,md} read (SEQ-01/03/04 are the spec).
- All in-scope files (docs/DECISIONS.md, docs/ROADMAP-PLATFORM.yaml) located; edit anchors located by quoted substring (line numbers have drifted).
- Acceptance criteria understood and each maps to a VP step.
execution_steps:
- 'Pre-check: confirm on the harness branch (not main). Re-run `grep -oE ''^## Decision [0-9]+'' docs/DECISIONS.md | grep -oE ''[0-9]+'' | sort -n | tail -1` and confirm it returns 131 (so 132 is free). If a concurrent PR claimed 132, shift this entire Decision to the next free number in the header AND the stanza `decision:` key AND the Warehouse ID together, then re-run the VP.'
- 'docs/DECISIONS.md -- INSERT the full Decision 132 text (the large verbatim block in the NEXT execution_steps entry) at the TOP of the decisions list: immediately after the preamble line ''This document tracks ... as the system evolves.'' and its trailing blank line, and BEFORE ''## Decision 131:''. Keep one blank line on each side. The block already ends with the ''---'' inter-decision separator. Transcribe EXACTLY, at column 0: if you are reading the raw plan YAML, strip the literal-block indentation so the ''## Decision 132:'' header, the ''```yaml reversal-conditions'' fence opener, and the ''```'' closer all sit at column 0.'
- |
  ## Decision 132: Platform-first sequencing of build capacity (ratified with reversal conditions) (Decided)

  **Status:** Decided
  **Date:** 2026-07-16
  **Warehouse ID:** dec-132 (keyed on the decision number; synced to ops_decisions via `ops_data_portal --backfill-decisions-md` post-merge, per Decision 84)

  **Problem:**
  All of the sole operator's build capacity flows to the platform plane under a "platform-first" directive that, until now, existed nowhere as a ratified decision. The audit `audits/platform-first-sequencing-f4dec93.yaml` (executive summary `audits/platform-first-sequencing-f4dec93.md`) census found 41 citations of the frame and ZERO ratify-class artifacts: the frame was cited as a given by Decision 93 ("per the platform-first directive", twice) and recorded only as a user steer in one plan's scoping fields (`docs/plans/PLAN-platform-mvp-boundary.yaml:17`, restated `:146`, ~2026-06-20). None of the live Decision headers ratified plane sequencing; no alternative was recorded as examined; the only end-condition language was circular ("until product work begins" / "revisited when the product roadmap is activated" / T2.6 "when product formula-discovery work begins"). This is the system's single most consequential standing allocation carried as an inherited premise while materially smaller choices (file formats, provider routing, size ceilings) carry full ratification + reversal machinery (SEQ-01).

  **Decision:**
  Ratify platform-first sequencing of build capacity as a CONSCIOUS, reversal-conditioned choice: while the platform-MVP boundary (Decision 93) is open, the sole operator's build capacity is sequenced to the platform plane rather than interleaved with a manual product/paper trading loop. This is a governance-record change ONLY -- it reallocates no build capacity, changes no code or infra, and leaves `docs/ROADMAP-PRODUCT.yaml` at `status: draft`. The allocation is held for a recorded reason (below), not by inheritance, and is bounded by the observable reversal/review conditions in the stanza below -- it is never silently renewed.

  **Frame provenance (conscious choice, not agent inertia):**
  The frame is a conscious human steer, dated ~2026-06-20 in `docs/plans/PLAN-platform-mvp-boundary.yaml:17` (restated `:146`) as "the user's 'complete the full platform first' directive", and cited-as-given in Decision 93 (the two "per the platform-first directive" lines). The pivot transcript settles the ARCHITECTURAL platform/product plane SEPARATION (contracts, sibling roadmaps, validation lenses) but never weighs build-capacity TIMING between the planes -- so the sequencing rationale was genuinely absent from the record, not merely unindexed. The stronger "frame-locked" reading (Decision 75) was examined and REJECTED: the operator consciously restated the steer in a dated artifact and the owned /audit chain re-asked the question, so Decision 75's "silently constrains, nobody re-asks" limb does not hold. This Decision is the Decision-75-mandated conversion of an inherited, cited-as-given premise into a conscious, reversal-conditioned choice.

  **Examined alternative (the interleaved manual/paper L1+L3 loop):**
  The alternative -- interleaving platform work with a manual paper-trading loop today -- is FEASIBLE (human operations bypass every absent code surface: the operator is the broker adapter, order lifecycle, and reconciliation loop, over a production-grade data plane) but HIGH-COST in the binding resource (the sole operator's attention). Costed in operator units (audit Q2): one-time setup ~20-40 hours (paper brokerage account, daily signal-extraction path, sizing rule, pre-registered promotion criteria, logging/reconciliation runbook); recurring ~2-5 hours/week of market-hours-coupled, interrupt-driven attention sustained >= 13 weeks; calendar >= one quarter before promotion-grade evidence. The load-bearing qualifier: an honest ALPHA-premise test needs a discovered-formula artifact, and the discovery substrate is parked (CD.3 pending, T2.6 deferred_post_mvp) -- without a formula artifact the loop exercises operations plumbing, not the edge.

  **Corrected value model (SEQ-03 -- telemetry category separation):**
  The interleave's strongest stated payoff -- "real trading telemetry feeding the T2.36/T3.x chain" -- is a CATEGORY ERROR. The Decision-95 telemetry chain (T2.36 relands the agent-telemetry write path; T3.20 captures agent turns) is the session-rooted AGENT trace/observation model; a manual paper loop does not feed the Decision-95 agent-telemetry chain (it emits none of it). Paper-trading data would land in PRODUCT tables (strategy_runs, tca_events, fills) that `current_state` lists as ABSENT (the Decision-78 / Decision-95-97 ops/telemetry vs product-table split makes this literal) -- so "real telemetry sooner" does not hold, and building those product tables early IS the resequencing decision, circularly. Net (audit Q3): on the corrected value model the interleave's headline payoff evaporates, the near-term platform critical path (T2.26 -> T2.36 -> T3.2 -> T3.3) is dual-use substrate the product's evaluation funnel needs later, and reversal stays operationally ~free -- so sequencing is favored, CONDITIONALLY on the triggers below. Full costing and category analysis: `audits/platform-first-sequencing-f4dec93.yaml` (Q2/Q3, findings SEQ-03/SEQ-04).

  **CD.3 / T2.6 disposition (SEQ-04 -- alpha-premise test path):**
  CD.3 (compute node for PySR formula discovery) is DEFERRED, gated on the named reversal conditions below -- specifically alpha-readiness (c) and platform-MVP close (a). No compute-node stand-up date is committed and no substitute discovery substrate is named; the PySR discovery substrate and T2.6 stay parked. CD.3's re-entry trigger BECOMES condition (c): a candidate PySR formula artifact passing the research bar. This de-circularizes the prior note (T2.6 reactivating "when product formula-discovery work begins", an event nothing defined): T2.6's reactivation clause and Decision 93's product-edge language are re-pointed at the named conditions in this same change. This keeps CD.3 DEFERRED (not ratified), so the Decision-105 candidate-decision ratified-shape / R1-R3 guard is not engaged.

  **Rationale:**
  The audit's core result is an asymmetry the ratify-with-conditions disposition uniquely banks: the frame is, on present evidence, the right allocation held for the wrong reason (by inheritance rather than by record). The affirmative case survives adversarial tracing (operator attention is the binding resource; the near-term platform critical path is dual-use; the interleave's payoffs shrink under scrutiny; reversal stays operationally free), AND the gap case survives (zero ratify-class artifacts, no trigger, a convention sitting unused -- 24 "Reversal conditions:" stanzas elsewhere). `resequence_interleaved` is rejected on the corrected value model; `ratify_as_is` is rejected because the bet compounds monthly against an untested load-bearing premise (NS-D); `insufficient_evidence` is rejected -- once the telemetry category error is removed the evidence discriminates. Filed as user-explicit ad-hoc governance (precedent: Decision 93 / PLAN-platform-mvp-boundary; a fresh forward Decision per the Decision 87 / Decision 126 pattern), not a new tier_item. Reversal is cheap by construction: the frame binds via one /orient scope line and ROADMAP-PRODUCT.yaml's `status: draft`, so nothing is foreclosed by sequencing longer.

  **Reversal conditions:**
  This sequencing is re-decided, never silently renewed, when any of the following fires: (a) the platform MVP (Decision 93 boundary) closes -- at which point a product-activation decision becomes mandatory, not optional; (b) the hard calendar review date 2026-09-30 is reached without the platform MVP having closed -- the sequencing is re-decided via /plan, not renewed by default; (c) alpha-readiness -- a candidate PySR formula artifact passing the research bar exists, collapsing the paper loop's marginal cost and re-opening the interleave question; or (d) sustained slip -- 3 consecutive orient/triage cycles show the platform-MVP date receding. Condition (b) is the top-level `review_by` in the stanza below (single source; a re-decision is a one-line diff); (a)/(c)/(d) are the enumerated conditions. On any trigger, re-decide via /plan and update or re-arm the stanza.

  ```yaml reversal-conditions
  decision: 132
  review_by: 2026-09-30
  on_trigger: "re-decide sequencing via /plan; update or re-arm this stanza; never silently renew"
  conditions:
    - id: platform-mvp-closes
      kind: manual
      description: "Platform MVP (Decision 93 boundary) closes -> a product-activation decision becomes mandatory, not optional"
    - id: alpha-readiness
      kind: repo_state
      predicate: null   # manual until a formula-artifact predicate is registered (CD.3/T2.6 disposition)
      params: {}
      description: "a candidate PySR formula artifact passing the research bar exists -> paper-loop marginal cost collapses; re-open the interleave question"
    - id: sustained-slip
      kind: repo_state
      predicate: null   # manual until orient/triage cycles snapshot the MVP date
      params: {consecutive: 3}
      description: "3 consecutive orient/triage cycles show the platform-MVP date receding"
  ```

  **Related:** Decision 93 (Platform-MVP boundary + deferred_post_mvp lifecycle -- the frame this Decision reversal-conditions; its two "per the platform-first directive" lines are the annotation targets; also the user-explicit ad-hoc-governance filing precedent and the no-live-dep invariant the ROADMAP edits respect), Decision 75 (frame-lock anti-pattern -- this Decision is its mandated conscious-choice conversion), Decision 95 (session-rooted agent-telemetry trace/observation model -- the SEQ-03 category boundary), Decision 96 + Decision 97 (telemetry temporal + identity standards completing the Decision-95 trio), Decision 78 (DuckLake adoption / product-table vs ops-telemetry split that makes "absent PRODUCT tables" literal), Decision 105 (reversal-condition-drafting-as-a-plan-step convention; CD.3 stays DEFERRED so the ratified-CD guard is not engaged), Decision 84 (Single Portal Invariant / DECISIONS.md numbering authority + backfill ETL), Decision 86 (rationale routes into this Decision, not a new prose doc), Decision 67 (STRATEGIC-plan freeze / CD.17 -- plan_type is IMPLEMENTATION), Decision 87 + Decision 126 (fresh forward-Decision precedent), Decision 108 (reversal-conditions section precedent the machine-parseable stanza extends), Decision 40 (recorded-then-forgotten triggers -- the cautionary failure mode a follow-on condition-monitor prices), Decision 101 (platform-first external-brand posture -- same lineage).

  ---
- 'docs/DECISIONS.md -- annotate Decision 93''s FIRST platform-first line (the ''PLATFORM-INTERNAL scoping:'' paragraph, ~:2049). Find ''remain dormant until product work begins, per the platform-first directive.'' and insert, immediately after ''per the platform-first directive'' (before the period), the bracketed annotation: '' [ratified with reversal conditions by Decision 132 (2026-07-16); the circular "until product work begins" / "product roadmap is activated" end-condition is superseded by Decision 132''s named conditions]''. Do NOT delete or reword Decision 93''s existing text.'
- 'docs/DECISIONS.md -- annotate Decision 93''s SECOND platform-first line (the T2.9 ''Items parked'' bullet, ~:2053). Find ''product edge E.env.3 left dormant per platform-first directive.'' and insert, immediately after ''per platform-first directive'' (before the period), the bracketed annotation: '' [ratified with reversal conditions by Decision 132]''. Do NOT reword the bullet.'
- 'docs/ROADMAP-PLATFORM.yaml -- re-point T2.6''s reactivation clause (tier_item id: T2.6, progress_note, ~:4940). Replace the final sentence ''Reactivate -> not_started when product formula-discovery work begins.'' with: ''Per Decision 132, CD.3''s disposition is DEFERRED (gated on the named reversal conditions, not a committed date). Reactivate -> not_started when Decision 132 reversal condition (c) alpha-readiness fires (a candidate PySR formula artifact passing the research bar exists), or when the platform-MVP boundary (Decision 93) closes per Decision 132 condition (a).'' Keep the rest of the progress_note. Do NOT change T2.6.status (stays deferred_post_mvp) or CD.3.state (stays pending).'
- 'docs/ROADMAP-PLATFORM.yaml -- sweep re-point of T3.5 (tier_item id: T3.5, progress_note, ~:7240). Replace ''Reactivate when product-axis work resumes (the cross-roadmap edge from ROADMAP-PRODUCT stays dormant per Decision 93).'' with: ''Reactivate when Decision 132 reversal condition (a) fires (the platform-MVP boundary closes -> a product-activation decision becomes mandatory); the cross-roadmap edge from ROADMAP-PRODUCT stays dormant per Decision 93 and Decision 132 until then.'' Keep the rest of the progress_note. No status change.'
- Execute the Verification Plan -- run each pre-deploy step (1-11) in order; loop until all pass. Do NOT edit docs/ROADMAP-PRODUCT.yaml. On any unrecoverable failure, stop and RCA (Decision 55); do not inline-patch around a failing gate.
- '[operator, post-merge -- NOT part of the implement PR] After squash-merge to main, an operator runs VP step 12: `bin/venv-python -m scripts.ops_data_portal --backfill-decisions-md` (agent_platform writer profile) to sync the new dec-132 row and refresh the amended dec-093 row into ops_decisions.'
- 'Report: Decision 132 landed at the DECISIONS.md top; Decision 93 annotated (2 lines); T2.6 + T3.5 reactivation clauses re-pointed; ROADMAP-PRODUCT.yaml untouched; VP results (steps 1-11 green).'
rollback: 'Pure docs revert: `git revert` the implement commit (removes Decision 132, the Decision 93 annotations, and the T2.6/T3.5 re-points). If already backfilled, the ops_decisions dec-132 row is superseded on the next --backfill-decisions-md run from the reverted DECISIONS.md (append-only SCD2; no manual warehouse delete). No code/infra to unwind.'
