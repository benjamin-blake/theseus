# DEP-05 / T2.50 / Decision 144: path-scoped code-owner review requirement over the
# repo-side deploy-gate surface (the apply guard, the terraform bootstrap/github roots,
# and the terraform-*/reconcile workflows). Enforced by the main-protection ruleset's
# require_code_owner_review = true (terraform/github/repo.tf); admin-bypassable per
# Decision 83 -- benjamin-blake is the sole collaborator and the retained bypass actor,
# so this is a deliberate, GitHub-audit-logged bypass boundary, not a hard block against
# the admin identity (that hard block is the deferred rec-2827). Routine PRs that do not
# touch these paths are unaffected -- required_approving_review_count stays 0.

/.github/workflows/terraform-*.yml   @benjamin-blake
/.github/workflows/reconcile.yml     @benjamin-blake
/scripts/terraform_apply_guard.py    @benjamin-blake
/terraform/bootstrap/                @benjamin-blake
/terraform/github/                   @benjamin-blake
/.github/CODEOWNERS                  @benjamin-blake
